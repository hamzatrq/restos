# T-01-12 fix round — review findings + planner rulings (July 2026)

Adversarial review of `885063b..b8d5036`: verdict **BLOCKING** (F1+F2, F3 rides along). Clean elsewhere: 19 §5 checkpoint mechanism (4-layer monotonicity, forgery blast radius bounded by the independent LAN cursor), per-origin contiguity under the org lock, auth seam as designed, drain-loop termination/starvation, sim-cloud fidelity incl. the byte-guarded XP transcript, zero smuggling.

- **F1 (HIGH, ruled):** identity-mismatch quarantines from a **relay-capable** session fill **NO stream**. The law-6 fill was ratified under pusher==author; a relayed mismatch's `lamport_seq` belongs to the ORIGIN's numbering, so filling the hub's own slot at that number displaces the hub's genuine event: watermark advances, plain ack, `store.advanceTo`, and the real event dies as `lamport_conflict` — durable merged-log loss. Nothing can wedge by not filling: the garbage was never in the hub's outbox. Law6's plain-session pin survives untouched. Row still stored verbatim (01-F37).
- **F2 (MED, ruled):** quarantine-row `device_id` attribution = the stream semantics. Identity-mismatch rows (no stream filled, per F1) attribute to `session.deviceId` — the only authenticated identity; content-class quarantines of identity-valid envelopes stay ORIGIN-attributed and origin-slot-filling (DEC-SYNC-005, T-01-11 Auditor counts per-origin). No existing pin asserts row device_id — free to fix.
- **F3 (MED, ruled):** the follower's `advanceTo(message.acked_watermark)` must never throw in the transport handler: ignore-and-count acks beyond own high water (forged-peer DoS + the wiped-device DR rejoin both hit this). Clamp/ignore, never crash.
- **F4 (ruled):** a demoted hub must stop relaying — the `relayRequested` latch clears on hub→follower transition (mechanism implementer's, outcome pinned: followers never relay, DEC-SYNC-006).
- **F5 (ruled):** drop the `forwardedCloudAck` one-shot latch — re-forward on heartbeat like `replayWindowTo`; receiver is idempotent. No single lost LAN frame may stall a checkpoint.
- **F6 (ruled):** document, don't build: origin-existence/registry check is T-01-09 — one line at the auth seam + DEC-SYNC-009's open-dependency column.
- **F7 (accepted as designed):** restart-from-zero re-relay is documented and dedupe-safe; note the adopted-`global_seq` sidecar as the future skip signal at the seam comment.
