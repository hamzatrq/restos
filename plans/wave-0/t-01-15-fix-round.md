# T-01-15 fix round — adversarial review findings + planner rulings (July 2026)

Two read-only reviews of `feat/t-01-15-merge-engine` (test-faithfulness; engine correctness). Suites 72/72 + 220/220 green throughout — **every finding below is one the green suite structurally cannot see.** Loop: oracle session authors the new red pins first; implementer fixes second; both cite this file.

## Engine findings (reviewer 2)

- **F1 (P0)** `merge.ts:663` — `retentionDrop` mixed-key crash. `validateDrop` runs over ALL keys before ANY drop, so `["order:O1","line:O1:L1"]` passes validation, deletes O1, then crashes on the line key **mid-transaction**: SQLite rolls back, the in-memory lattice does not — engine/DB divergence, then fragment overwrite. Key-order dependent. `c00d45c` removed a live guard under coverage pressure ("trim dead branches" was wrong for this branch). **Ruling: restore guards; coverage gate is satisfied by real tests for those branches, never by deleting the branches.**
- **F2 (P0)** post-drop straggler resurrection. Nothing remembers a dropped key in-session; a weeks-late `order.created` under a new envelope id rebuilds a fragment entity → a settled, dropped order **reappears as open on the floor**, retired table chips resurrected. Breaks projection-as-function-of-(delivered set, retention scope). **Ruling: restore session-scoped dropped-key memory; a straggler for a dropped key is ledger-retained, never folded, never projected. Oracle pins exact semantics incl. the counter treatment.**
- **F3 (P1)** `cap_violated` regresses 1→0 when a later divergent member disputes the key — matrix row 39 ("monotone absorbing, join = max") vs Addendum-A ("totals can decrease on dispute") genuinely conflict. **Ruling: BOTH hold — totals are non-monotone; the FLAG is a latch. `cap_violated` becomes latched fold state (once violated, stays), not a per-projection derivation. Matrix erratum note; oracle pins dispute-after-violation.**
- **F4 (P1)** `uncovered_addition`: a snapshot-less close (only `order_id` required) yields ceiling 0 → every closed order with billable lines raises the exception — "no attestation" conflated with "attested zero". Also `c00d45c` dropped `Number.isInteger`: `billed_paisa: Infinity` suppresses the exception in-session but stores `null` → **session vs reopen divergence**; negative snapshots fire spuriously. **Ruling: snapshot-less close asserts NO ceiling (skipped, not zero); non-integer/negative snapshots are ignored-with-anomaly; guard restored. Oracle pins all three.**
- **F5 (P1)** no exhaustiveness guard on `foldIn`'s switch; key derivation hardcoded to `payload.order_id`. First registry growth (availability/table.state are already named as coming) compiles, folds nothing, **and still increments `events_folded`** — the honesty counter overcounts. **Ruling: assertNever on the switch (registry growth must fail compile); key derivation stays order-keyed with an explicit comment until the sidecar generalisation task.**
- **F6 (P1)** engine mutations sit OUTSIDE the SQLite tx: a mid-tx I/O failure rolls back rows but not the lattice → phantom fold state with no invalidation. F1 is the reachable instance. **Ruling: reorder (SQL first, engine after success) or refold-on-tx-failure; implementer proposes, oracle reviews the shape.**
- **F7 (P1, spec gap)** a legal-but-inconsistent edge whose `preds` name a terminal edge retires the TERMINAL head — `['served']` projects away to `['picked_up']`; one inconsistent emitter un-serves a line fleet-wide. 01-F35's spirit violated; matrix silent. **Ruling (conservative, per 01-F35): only an ADOPTION edge may retire a terminal head. A non-adoption edge naming a terminal pred lands, fires `inconsistent_predecessor`, and the terminal head survives. Matrix erratum; oracle pins.**
- **F8 (P2)** `parseDropKey` silently mis-parses `"line:O1"`; tolerated loose `payment_id` can seed a false dispute across client-version skew (whole-payload immutability — **ruling: exclude superseded-tolerated fields from the immutable-intent comparison**); stale `canonical.ts` comment referencing deleted `replay.ts`; `snapshot()` bypasses `scoped_rebuilds` (informational).

**Clean, explicitly:** ordering-metadata audit (zero seq/lamport/device-id reads; the one id-order comparison feeds only the C1-excluded time columns — the documented §6.2(e) residual); sidecar-only adoption on both seams; the whole hunted money-plane list; adoption/≼-max/MVR/anomaly-priority vs matrix; `payloadHash` canonical; SQL all prepared statements; 01-F17 no-throw appends; foldStats honest on current types; oracle files untouched post-`dbca3e2`; parked drain `waiting_for`-indexed.

## Test-suite findings (reviewer 1) — verdict: no violations
- **R1 (process)** the oracle's 33-entry enumeration lives only in an agent report + commit messages. **Ruling: this file + the enumeration land in-repo (done); the oracle fix-round session post-hoc ratifies the ~18 re-expressed S slots.**
- **R2 (low)** entry-7 re-expression computes its expectation with the implementation's own `payloadHash` — self-referential; mitigated by merge-schema's independent pin. Oracle: substitute an independent hash.
- **R3 (low)** entry-8 pins an oracle-reserved area (line-value MVR rendering). Oracle: ratify or replace with its own pin (the implementer's §4 proposal is on the table).
- **R4 (low)** stale titles at `spike-scenarios.test.ts:60-61,338` still claim "≡ refold()" — titles are conformance-mapping surface. Oracle: retitle.
- **R5 (info)** X1b system-level supersession re-script deferred — schedule with the harness follow-up.

## Implementer proposals awaiting oracle pins (from the T-01-15 report §4)
`inconsistent_predecessor` trigger (both-edges-present, first pred, one per edge, never on illegal); `line_value_conflict` rendering (min-payloadHash member wholesale + order-level exception); `order.settlement_closed` snapshot fields (`billed_paisa` ceiling — amended by F4's ruling above). `CONTESTED_LINE_BILLABLE = true` is an implementer pick on a §5.4 constant with no matrix default — **founder should ratify with F4.**
