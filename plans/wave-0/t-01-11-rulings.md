# T-01-11 oracle-round rulings (binding on the implementer)

1. **Chain leg IN** (oracle contradiction 1 — the planner's brief was factually wrong; T-01-10 is an ancestor @ 52f020a). `auditor-audit-chain.test.ts` stands; 01-F5's promoted text makes the cross-check a T-01-11 requirement.
2. **Engine access = additive PURE export**: the T-01-15 fold engine is pure TS over `@restos/domain`; export it from `@restos/sync-client` WITHOUT dragging `better-sqlite3` into the gateway runtime (subpath entry or dedicated index — implementer proposes mechanism; protected-path review covers it). `@restos/sync-client` becomes a runtime dependency of `services/sync-gateway`.
3. **Coverage law ratified** (obligation = slots [0..max(ack, max merged lamport)]; attributed rows COVER, never EXTEND; masking residual accepted, fill-credit column recorded as follow-up candidate, not owed).
4. 01-F30's domain-placement clause = senior-review checkpoint (oracle correctly did not pin a signature; implementer proposes per T-01-15 precedent).
5. Open rulings 2–5 stay unpinned as reported (consistent with the pending founder constants). AGENTS.md repo-state staleness noted — updated at kernel-ready close, not mid-stack.
