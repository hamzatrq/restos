# T-01-08 fix round — review READY, one ruled finding + recorded notes

**F-1 (ruled):** step 3.5's Σ of merged priors can exceed 2^53−1 (refunds merged while parent unprovable), and the pinned domain fn rightly throws — today that RangeError aborts the whole push: no merge, no quarantine, no slot fill, origin outbox wedges forever (the DEC-SYNC-005 failure class). **Ruling:** an unrepresentable prior-Σ is a PROVABLE violation, not an unprovable case — if Σ(priors) alone exceeds 2^53−1 paisa it necessarily exceeds any schema-valid `payment_amount_paisa` (≤ 2^53−1), so remainder < 0 by pure magnitude. Gateway catches the domain fn's RangeError at the step-3.5 call site and quarantines `invariant_violation` (slot-filled, noticed, never wedges). The domain surface stays as pinned (throws on unsafe args); the gateway's catch carries the magnitude argument as a comment. Micro fix round: oracle red pin first (Σ-overflow construction), then the catch.

**Recorded, no action this round:** F-2 — mark-on-send's residual at-most-once window at the transport buffer is DEC-SYNC-008's accepted posture (exactly-once ack deferred); caveat noted here against the row's "at-least-once" wording. F-3 — duplicate live notice may carry a newer reason than the durable row (first-stored-wins correct; observability nit). F-5 — LAN notice re-forward unpruned (consistent with heartbeat re-send design; ack/prune post-Wave-0). F-4 — merged-attempt-key refunds pass uncapped BY DESIGN (01-F31 first-wins; 26 §7 row 2 is Auditor territory).
