# Wave 0 — Kernel spike task plan (owning spec: 01; exit criteria: 01 §8)

Sessions are sized to 1–4 FRs (24-F4). Acceptance tests per task are authored by a **different session** from spec text + this plan's contracts only (24 §3 step 2). Deleted after Wave 0 per plans/README.

### T-01-01  Domain foundation: ids, money brands, envelope, canonical states, event registry
- **FRs:** 01-F3 (envelope ordering fields), 01-F4 (typed event registry — unknown type is an error), 01-F31 (settlement_attempt_id on payment emissions), 01-F35 (terminal-state monotonicity as a pure function). Conventions: 00 §6 (UUIDv7, integer paisas/mg/ml, envelope shape), 01 §4 canonical line states.
- **Files touchable:** `packages/domain/src/**` (implementation session); `packages/domain/src/__acceptance__/**` (test session only).
- **Check:** `pnpm --filter @restos/domain test` + root `pnpm verify`.
- **DoD rung:** D1* (tests green; formal conformance derivation lands with T-01-00).
- **API contract (binding for the test session):**
  - `newId(): string` — UUIDv7, lexicographically time-ordered.
  - Branded ints: `paisa(n)`, `mg(n)`, `ml(n)`, `units(n)` — throw on non-integers/NaN/±Infinity; `addPaisa/subPaisa/sumPaisa` exact integer arithmetic.
  - `EventEnvelope` zod schema + `parseEnvelope(v)` — fields per 00 §6: `id, org_id, branch_id, device_id, actor_user_id (nullable), lamport_seq (int ≥0), device_created_at (epoch ms int), server_received_at (int|null), type, schema_version (int ≥1), payload, refs (string[])`.
  - `ORDER_LINE_STATES`, `TERMINAL_LINE_STATES`; `applyLineState(current, next) → { state, applied, anomaly? }` — legal transitions per 01 §4 (`placed→confirmed→in_prep→ready→served` | `ready→picked_up→delivered`; `voided/cancelled` exits from any non-terminal); terminal states never regress (anomaly `terminal_regression`, 01-F35); illegal jumps → anomaly `illegal_transition`, not applied.
  - `eventRegistry` seeded with `order.created`, `order.line_state_changed`, `payment.recorded`, `payment.refunded` payload schemas (01 §4, 01-F29/F31 fields: refunded references payment id, method `cash_out|raast_reversal_ref|khata_credit`; recorded carries `settlement_attempt_id`); `parseEvent(envelope)` → typed event; unknown type → `UnknownEventTypeError` (01-F4).
- **Assumptions stated:** `settled` is money-side, not a line state (01 §4); negative amounts rejected at the brand level — corrections are separate refund events, not negative payments (01-F33); registry starts with 4 seed events, the full catalog lands with its consuming modules (surgical minimum).

### T-01-00  Harness: conformance derivation tool + `verify:01`
- Vitest reporter → `conformance/01.yml` status derivation (24 §11.1); `verify:01` script. After T-01-01 (needs real test results to derive).

### T-01-02  sync-protocol wire messages (PROTOCOL.md → zod schemas + codec round-trip property tests, 20 §2.3)
- **Status: T-01-01/T-01-00/T-01-02/T-01-03/T-01-04/T-01-05 DONE** (T-01-05 landed after a fix round closing two review blockers: session-cursor LAN paging, inbound-life-only hub suspicion; deterministic session ids; wallClock shipped). T-01-07 DONE (gateway landed after fix round: in-batch dedupe, persisted-slot uniformity, storage_reject via savepoint scopes, ack guard, per-connection serialization; golden fixtures corrected per 20 §2.7). Next: T-01-04b + T-01-05b perf, then T-01-06 exit run; then T-01-08+ per wave-0 remainder survey.
- **FRs:** 01-F8 (push/ack watermark semantics), 01-F9 (catchup range fetch), 01-F37 (quarantine notice), 01-F39/01-F40 (device classes on `hello`; slices are sender-enforced — the protocol carries class, never client-declared slices). Contract-fixture law: 20 §2.7.
- **Files touchable:** `packages/sync-protocol/src/**` (impl session); `packages/sync-protocol/src/__acceptance__/**` incl. `fixtures/` (test session only); `packages/domain/src/**` for the DEVICE_CLASSES addition below (impl session; protected-path review applies).
- **Check:** `pnpm --filter @restos/sync-protocol test` + `pnpm verify:01`.
- **API contract (binding for the test session):**
  - Added to `@restos/domain` (cite 01-F39): `DEVICE_CLASSES = ["counter_electron","counter_rn","kitchen","manager","waiter","rider"] as const`; `type DeviceClass`; `HUB_ELIGIBLE_CLASSES = ["counter_electron","counter_rn","kitchen"] as const` (a strict subset of DEVICE_CLASSES, in hub-priority order per HUB-ELECTION.md).
  - `@restos/sync-protocol` exports: `PROTOCOL_VERSION = 1`; `MESSAGE_KINDS` (the 11 kinds per PROTOCOL.md table: hello, hello_ack, push, push_ack, event_batch, catchup_request, catchup_response, quarantine_notice, purge_command, ping, pong); per-kind zod schemas with the PROTOCOL.md bodies (`push.events` = domain `EventEnvelope[]`; `event_batch` events additionally allow optional integer `global_seq ≥ 0`); `type ProtocolMessage` (discriminated union on `kind`); `parseMessage(value)` — throws `UnknownMessageKindError` on unknown kind, zod error on bad body or `v !== 1`; `encodeMessage(msg): string` (JSON); `decodeMessage(text): ProtocolMessage`; law: `decodeMessage(encodeMessage(m))` deep-equals `m` for every valid message (fast-check property).
  - Golden fixtures (20 §2.7): 3+ canonical message JSON files checked in under `__acceptance__/fixtures/`; tests decode each fixture and re-encode to a semantically equal message — the wire contract cannot drift silently.
- **Assumptions stated:** watermarks/global_seq are non-negative safe integers; `push_ack.acked_watermark` may lag the pushed watermark (partial ack is legal — outbox advances only to the ack, 19 §5); `purge_command.scope` is `"all"` at v1 (finer scopes when doc 22 erasure lands).
### T-01-03  sync-client device store + outbox core (the canonical durable queue, 18 §4) + kill-test seed
- **FRs:** 01-F2 (persist-before-ack — plug-pull/kill safe), 01-F3 device half (`lamport_seq` monotonic, persisted, gap-free; cloud `global_seq` assignment is gateway work), 01-F8 outbox half (drain in lamport order, advance only to acked watermark, idempotent append/ack; the network push itself lands with T-01-05/gateway), 01-F11 (sync status queryable). Constraints, not closed here: 01-F1 (store API exposes no update/delete), 01-F4 (append validates against the domain registry), 18 §4 canonical-queue laws (never-lose, crash-resume, idempotent-drain), 19 §5 write-checkpoint, 20 §2.6 kill-tests.
- **Files touchable:** `packages/sync-client/src/**` (impl session; protected path); `packages/sync-client/src/__acceptance__/**` (test session only); `packages/sync-client/package.json` + lockfile (deps/wiring: better-sqlite3 pinned per 18 §4, vitest, fast-check — wiring may land with the test session so the red run executes).
- **Check:** `pnpm --filter @restos/sync-client test` + `pnpm verify:01` + root `pnpm verify`.
- **DoD rung:** D1 (in-suite kill seed; the nightly randomized harness and physical rig are later 20 §2.6 rungs).
- **API contract (binding for the test session):**
  - `openStore({ path, identity }): DeviceStore` — `path` = SQLite file path or `":memory:"`; `identity = { org_id, branch_id, device_id }`. SQLite WAL via better-sqlite3 (node/Electron adapter; the RN adapter arrives with the RN app — the adapter seam stays internal until a second consumer exists, 24-F23).
  - `store.append(input): EventEnvelope` — `input` = envelope minus `lamport_seq`/`server_received_at`; the store assigns the next gap-free `lamport_seq` (starting at 0) and `server_received_at: null`, validates through the domain registry (unknown type / invalid payload throws, nothing persisted, 01-F4), requires `input` org/branch/device ids to equal the store identity (mismatch throws), and persists durably **before** returning — a returned envelope IS the confirmed event (01-F2). Re-append of an already-stored event `id` with identical content returns the stored envelope unchanged and assigns nothing (idempotent — crash-retry safe, 18 §4); a re-append whose content diverges from the stored row (any field beyond the two store-assigned ones) throws with nothing changed — silent divergence between caller belief and ledger truth is the 18 §4 loud-failure case (adversarial-review finding).
  - `store.nextBatch(max): EventEnvelope[]` — oldest unacked own events, `lamport_seq` order (the push feed, 01-F8).
  - `store.advanceTo(watermark): void` — write-checkpoint (19 §5): acks all events with `lamport_seq ≤ watermark`. Partial acks legal; idempotent; a watermark below the current checkpoint is a no-op; a watermark above `own_high_water` throws `AckBeyondAppendedError` (loud failure, 18 §4 — an impossible ack means protocol corruption). The watermark must be a non-negative integer: NaN/negative/fractional values throw with the checkpoint unchanged (same corruption class — adversarial-review finding, checkpoint must never silently regress); on an empty store every watermark is beyond high water and throws.
  - `store.readOwnEvents(fromLamport?): EventEnvelope[]` — inclusive from `fromLamport` (default 0), lamport order; acked events remain readable: the events table is the device's ledger copy (01 §5); compaction is later work.
  - `store.status(): { queue_depth, own_high_water, acked_watermark, last_global_seq }` (01-F11) — `own_high_water`/`acked_watermark`/`last_global_seq` are `null` until first append/ack/pull; `store.setLastGlobalSeq(n)` records the pull position (consumed by T-01-05).
  - `store.close(): void`. No API method updates or deletes an event row (01-F1).
  - **Kill-seed law (20 §2.6, D1 form):** events confirmed before an abrupt handle abandon (no `close()`) all survive `openStore` reopen with no lamport gap, and the unacked tail re-drains identically; at `append` return the event is already visible to an independent second handle on the same file (proves synchronous persistence, not buffering). The true SIGKILL child harness is the nightly 20 §2.6 rung — a follow-on harness task, since a bare-node child needs a TS-execution runtime decision that is infra, not oracle.
- **Assumptions stated:** lamport starts at 0 (envelope permits ≥ 0; consistent with T-01-02 watermark-zero tests); acked events retained, not deleted — simpler delete-on-ack alternative rejected because the events table is the ledger copy (01 §5); loud `AckBeyondAppendedError` over silent clamping (18 §4 loud-terminal-failure); `PRAGMA synchronous=FULL` accepted (00 §5.2 plug-pull law outranks write throughput at this volume); store identity fixed at open — one device, one store; multiple handles on one file are legal for reads (WAL), single writer is the usage contract (enforcement not required at v1).
### T-01-04  Folds v1 per FOLDS.md (`open_orders`, `kitchen_queue`) + fold determinism/commutativity properties (01-N1, 01-F34)
- **FRs:** 01-F6 (materialized state tables by folding events, deterministic + replayable — closed via the fold engine and the two FOLDS.md v1 folds; the four remaining registry folds are later tasks citing 01-F6 as a constraint), 01-F10 (park events awaiting an unseen parent, drain on arrival, never crash or drop), 01-F34 (folds commutative + idempotent over concurrently-received events; `global_seq` tiebreak for order-sensitive fields, converge on ack), 01-N1 (replay determinism, property-tested per 20 §2.3). **Constraints, not closed here:** 01-F35 (already green from T-01-01 — every line transition MUST route through `applyLineState`; regression-guarded here, not re-closed), 01-F1/F2/F3 (store laws, T-01-03 — the `global_seq` sidecar below exists so no event row is ever updated), 01-F4 (ingest validates through the domain registry), 01-F16 (concurrent line adds merge — both stand), 01-F18/F20 (LWW list unchanged; `table_id` resolution is the 01-F34 tiebreak, not a new LWW entity), 01-F29/F30 (money conservation — v1 `settled` is the stated approximation below; refund-remainder enforcement and quarantine are hub/cloud work, 01-F37), 03-F19/F24 (order-level "ready" stays a fold of line states — `lines_ready/lines_total` feed it; doc 03 owns display/bump/exit semantics).
- **Files touchable:** `packages/sync-client/src/**` excluding `__acceptance__/` (impl session; protected path); `packages/sync-client/src/__acceptance__/**` (test session only); `packages/domain/src/registry.ts` + `packages/domain/src/index.ts` re-exports, **only** for the registry additions below (impl session; protected-path senior review — same pattern as T-01-02's DEVICE_CLASSES line; all added types are already canonical in 01 §4, so no spec PR is required); `packages/sync-client/package.json` + vitest/coverage config + lockfile (test wiring may land with the test session so the red run executes).
- **Check:** `pnpm --filter @restos/sync-client test` + `pnpm verify:01` + root `pnpm verify`. Folds are in the 20 §2.2 mandatory 100%-branch-coverage set: coverage gate on `src/folds/**` wired with the test session. Property failures commit their seed as a regression case (20 §2.3).
- **DoD rung:** D1 (properties + directed cases in-suite; the seeded virtual-scheduler simulation is the 20 §2.4 rung and lands with T-01-05/T-01-06).
- **Depends on:** T-01-03 store landed (impl in flight), `packages/sync-client/FOLDS.md` (the binding 24-F8 fold-registry artifact), T-01-01 registry/states.
- **Registry additions (binding — `packages/domain`, all types canonical in 01 §4; loose objects, required fields are law, extras pass through per 00 §6):**
  - `order.confirmed`: `{ order_id: string≥1 }`.
  - `order.line_added`: `{ order_id: string≥1, line_id: string≥1, item_id: string≥1, qty: int > 0 (integer units, 00 §6), unit_price_paisa: int ≥ 0 }` — price is snapshotted at line-add and never re-derived (01-F18).
  - `order.table_assigned`: `{ order_id: string≥1, table_id: string≥1 }`.
  - `kot.printed`: `{ order_id: string≥1 }`.
  - `order.created` gains optional declared fields `order_type: string≥1 (optional)`, `table_id: string≥1 (optional)` — additive under `schema_version` 1 (00 §6). Nothing else in `domain` changes.
  - Deferred (their columns tolerate absence, see semantics): `void/comp/discount.recorded`, `order.line_removed` — they land with the module that emits them (doc 02 wave); FOLDS.md's `consumes` column remains the target contract.
- **API contract (binding for the test session):**
  - **Placement:** folds live in `packages/sync-client` and are always-on — `openStore({ path, identity })` (unchanged signature) creates/maintains the fold tables in the same SQLite file and guarantees, on return, state tables ≡ `refold()` (fold state is durable with the ledger and self-heals on reopen). Apps never register folds (FOLDS.md registration rule, 18 §6).
  - **Canonical replay order (the determinism anchor):** fold state is defined as the fold of the stored event **set** replayed in the total order `key(e) = (global_seq(e) ?? +∞, device_created_at, device_id, lamport_seq)`, lexicographic. Arrival order never matters (01-N1). When every competitor carries a `global_seq`, this is exactly cloud order (01-F34); before assignment the tail key is the deterministic provisional order and every device holding the same set agrees. Incremental maintenance is an implementation choice; the law is equivalence with canonical replay.
  - **New store surface (additive to T-01-03; nothing existing changes shape):**
    - `store.ingest(envelope: EventEnvelopeT, opts?: { global_seq?: int ≥ 0 }): { stored: boolean }` — the branch-stream entry point (mesh/cloud delivery wires in at T-01-05; tests call it directly). Validates via domain `parseEvent` — unknown type/invalid payload throws, nothing persisted (01-F4). `org_id`/`branch_id` must equal the store identity (mismatch throws). An envelope `id` already stored (own or peer) with no `opts.global_seq` → `{ stored: false }`, zero state change (idempotence); a duplicate-id ingest that CARRIES `opts.global_seq` adopts the cloud ordering exactly as `assignGlobalSeq(id, global_seq)` would — idempotent on the same value, divergent value or already-held seq throws, and the call returns `{ stored: false }` (the envelope itself was not newly stored; only the ordering was adopted) — so the LAN-first-then-cloud-catchup path converges (01-F34; adversarial-review finding: silently dropping the carried seq left same-information devices divergent). An own-`device_id` envelope not already stored throws (own events enter only via `append` — loud, 18 §4). Persists to the new `peer_events` table (`id PK, device_id, lamport_seq, envelope; UNIQUE(device_id, lamport_seq)` — a (device, lamport) collision with a different id throws: lamport is gap-free monotonic per device, so a collision is corruption; cloud order lives ONLY in the `global_seq` sidecar — a mirror column on `peer_events` was cut in review as write-only dead data that silently goes stale) and applies folds in the same transaction.
    - `store.append(input)` — unchanged contract from T-01-03, now also applies folds in the same transaction as the insert. Append NEVER fails or blocks for fold reasons (a sale is never blocked, 01-F17): an appended event with an unmet dependency persists to the ledger and parks at the fold layer.
    - `store.assignGlobalSeq(event_id: string, global_seq: int ≥ 0): void` — records cloud ordering for an already-stored own or peer event in a sidecar table (`global_seq` map keyed by event id; envelope rows are never updated, 01-F1). Same value again = idempotent no-op; a different value for the same event, an unknown `event_id`, or a `global_seq` already held by another event throws (protocol corruption — loud, 18 §4). After it returns, state tables ≡ `refold()` (this is how "devices converge to cloud ordering on ack" is observable).
    - `store.openOrders(): OpenOrderRow[]` (sorted by `order_id` asc), `store.kitchenQueue(): KitchenQueueRow[]` (sorted by `order_id` asc), `store.parked(): ParkedRow[]` (sorted by `event_id` asc), `store.refold(): void` (drop + rebuild all fold tables from `events` ∪ `peer_events` + sidecar — the device-side seed of the 20 §4.2 refold-and-diff pattern).
  - **`open_orders` state (SQLite `orders(order_id PK, channel, order_type, table_id, confirmed_at, settled, json_lines)` — exactly FOLDS.md):**
    - `order.created` → row `{ order_id, channel, order_type: payload.order_type ?? NULL, table_id: payload.table_id ?? NULL, confirmed_at: NULL, settled: 0, json_lines: {} }`. A second `order.created` for the same `order_id`: first in canonical order wins, later ones are logged no-ops.
    - `order.confirmed` → `confirmed_at = device_created_at` of the canonically-first confirm; later confirms are logged no-ops.
    - `order.line_added` → line `{ item_id, qty, unit_price_paisa, state: "placed", anomalies: {} }` under `line_id`; duplicate `line_id`: first in canonical order wins. Lines are never removed at v1 (`line_removed` deferred; post-KOT removal is `void.recorded` per 01 §4†).
    - `order.line_state_changed` → for each distinct `line_id` in `line_ids` (duplicates within one event are deduped in the fold loop — an applied event must never self-flag an anomaly; adversarial-review finding), `applyLineState(current, payload.state)`; non-applied results write `anomalies[envelope.id] = "terminal_regression" | "illegal_transition"` on that line (retained + flagged, 01-F35; the ledger row is untouched).
    - `order.table_assigned` → `table_id` = payload of the canonically-**last** assignment (the 01-F34 order-sensitive field: once all competitors carry `global_seq`, highest `global_seq` wins on every device).
    - `payment.recorded` / `payment.refunded` → `settled = 1` iff `billed_effective > 0 ∧ Σ payments.amount_paisa − Σ refunds.amount_paisa == billed_effective`, where `billed_effective = Σ qty × unit_price_paisa` over lines with state ∉ {voided, cancelled}, and a refund attributes to the order of its parent payment. `settled` is derived, recomputed deterministically from the set (v1 approximation of 01-F30 — void/comp/discount value terms are 0 until their event types land; refund-remainder enforcement (01-F29) is hub/cloud quarantine work, 01-F37 — both listed above as constraints-not-closed).
    - `json_lines` and `envelope_json` are canonical JSON (object keys sorted at every depth **by UTF-16 code unit** — pinned so a future non-JS refold-and-diff implementation cannot byte-mismatch, 20 §4.2 — no insignificant whitespace) — determinism assertions compare byte-for-byte.
  - **`kitchen_queue` state (SQLite `queue(order_id PK, confirm_at, channel, age_basis, lines_ready, lines_total)` — exactly FOLDS.md):**
    - A row exists iff a canonically-applied `order.confirmed` exists for the order. `confirm_at = device_created_at` of the canonically-first confirm; `channel` from the order's `order.created` (the fold therefore also consumes `order.created`/`order.line_added` — FOLDS.md's consumes list is read as the trigger list; drift flagged for a FOLDS.md correction).
    - `age_basis = device_created_at` of the canonically-first `kot.printed` for the order, else `confirm_at` (kitchen age runs from ticket print when one exists; doc 03 owns timing refinements).
    - `lines_total` = count of lines with state ∉ {voided, cancelled}; `lines_ready` = count with state ∈ {ready, served, picked_up, delivered} (feeds 03-F19/F24's order-ready-is-a-fold).
    - No row exit at v1 — consumers filter; bump/served-exit semantics are doc 03 work.
  - **Parking (01-F10; SQLite `parked(event_id PK, waiting_for, envelope_json)` — exactly FOLDS.md):**
    - Dependencies come from typed payloads (not raw `refs[]`, which stays advisory at v1): every consumed type except `order.created` requires its `order_id` resolved (an applied `order.created` with that payload id); `order.line_state_changed` additionally requires every `line_id` in `line_ids` resolved (an applied `order.line_added`); `payment.refunded` requires `payload.payment_id` resolved (a stored-and-applied `payment.recorded` **event id**).
    - An event with an unmet dependency is persisted to the ledger normally but parks: one `parked` row, `waiting_for` = the first unmet id (deterministic: order id, then lowest missing line id, then payment event id), and it affects NO state table while parked.
    - Drain: when an applied event resolves an id (`order.created` resolves its order id, `order.line_added` its line id, `payment.recorded` its envelope id), parked rows waiting on that id re-attempt in canonical order; still-unmet dependencies re-park under the new `waiting_for`; cascades (refund → payment → order) run to fixpoint. Duplicate ingest of a parked event's id is a no-op (PK).
    - Nothing is ever dropped: every stored envelope of a consumed type is at all times either applied or parked. Rolling-window parked-age anomaly → fleet health is later doc-15 wiring; `store.parked()` is the v1 observability.
  - **Laws (each is an acceptance test; FR id in the test name per 20 §4.1):**
    1. **Determinism (01-N1):** for any registry-valid generated event set (N devices, per-device gap-free lamport) and ANY two delivery interleavings that respect each device's lamport order (own events via `append`, peers via `ingest`), final `orders`/`queue`/`parked` tables are identical, `json_lines` byte-identical (fast-check property; named generator).
    2. **Commutativity (01-F34):** swapping two adjacent deliveries from different devices yields identical final tables (property).
    3. **Idempotence (01-F34):** re-delivering any already-stored events (same ids, any subset, any order) changes nothing — tables byte-identical before/after (property).
    4. **Refold equivalence (01-F6):** after any delivery sequence, `refold()` reproduces the incrementally-maintained tables exactly (property; the Auditor pattern, 20 §4.2).
    5. **Terminal monotonicity (01-F35, regression guard):** a line in a terminal state receiving a later non-terminal `line_state_changed` keeps its state, gains exactly one `anomalies[event_id] = "terminal_regression"` entry, and the ignored event remains readable in the ledger.
    6. **Parking (01-F10):** child-before-parent delivery → parked row present, state tables unaffected; parent arrival → parked row gone, final state identical to parent-first delivery; the refund→payment→order chain delivered fully reversed drains to fixpoint; no consumed event is ever dropped (applied ∪ parked = stored).
    7. **Tiebreak/convergence (01-F34):** two concurrent `order.table_assigned` from different devices with no `global_seq` resolve identically on every delivery order via the provisional key; after `assignGlobalSeq` on both such that cloud order reverses the provisional winner, `table_id` is the higher-`global_seq` value and tables ≡ `refold()`.
    8. **Durability (extends T-01-03's kill-seed):** after abrupt handle abandon (no `close()`), `openStore` on the same path yields state tables ≡ `refold()` of the surviving ledger — confirmed folds are never ahead of or behind the confirmed ledger.
- **Assumptions stated (24 §3b — interpretation chosen / simpler alternative considered):**
  - **Registry gap:** FOLDS.md's consumed types don't all exist in the T-01-01 seed registry; chosen: minimal additive `domain` registry additions (4 types + 2 optional fields, all already canonical in 01 §4 — T-01-04 is their first consuming module, exactly T-01-01's stated plan). Simpler alternative — fold only the 4 seed types — rejected: `kitchen_queue` would be vacuous (its entry event wouldn't exist) and `json_lines` would carry no line content.
  - **Canonical order key** `(global_seq ?? +∞, device_created_at, device_id, lamport_seq)`: simpler alternative — apply in arrival order with parking only — rejected: `applyLineState` is deliberately not order-insensitive (illegal jumps don't apply), so arrival-order folding cannot satisfy 01-N1. Caveat: a device whose clock regresses mid-order can misorder its own events in the canonical tail (01-N2 flags health, never blocks; anomalies record, nothing crashes) — flagged for DECISIONS.md, not silently solved.
  - **`kitchen_queue` also consumes `order.created`/`order.line_added`:** its `channel`/`lines_total` columns are unpopulatable otherwise; FOLDS.md's consumes column is read as the trigger list and the artifact should be corrected in review.
  - **`age_basis` = first `kot.printed` else `confirm_at`:** simpler alternative — always `confirm_at`, `kot.printed` feeds nothing — named; chosen reading is why FOLDS.md lists `kot.printed` at all; doc 03 refines.
  - **`settled` v1** is the exact-cover equality over non-voided/cancelled lines; void/comp/discount value terms and refund-remainder enforcement arrive with their event types / the money-fold wave — until then a partially-refunded or comped order simply stays unsettled (visible, never wrong-positive).
  - **No state-table row deletion** at v1 (open_orders filters on `settled`; queue exit is doc 03) — folds stay append-derived.
  - **`global_seq` sidecar table** over a column on `events` — an UPDATE on an event row is exactly what 01-F1 bans.
  - **`ingest` is the test-visible seam** for peer events; LAN/cloud delivery wires into it at T-01-05 — no transport code here.
  - **Property generators live in `__acceptance__`** at v1; promotion to `packages/testing` named generators (20 §2.3) is scheduled consolidation with the sim-scheduler task, not a drive-by.
### T-01-05  LAN mesh session + hub election per HUB-ELECTION.md, over injected transport + deterministic sim scheduler seed (20 §2.4)
- **FRs:** 01-F13 (deterministic hub election, star relay, re-election on hub loss < 10 s) — **closed at the logic level**: proven under virtual time in the 20 §2.4 sim; the wall-clock demonstration on real radio is T-01-06. 01-F12 (LAN discovery + direct exchange with WAN down) — **partial**: the discovery *abstraction* (peer visibility driving the mesh) and event exchange over an injected transport are closed; real mDNS and manual-IP adapters are T-01-06 (the sim proves logic, not radio). 01-F15 (fast-path propagation) — **partial**: the propagation *logic* (append → immediate drain → hub fan-out; exactly-once per event id; per-origin lamport order at every receiver) is closed; the physical < 1 s p95 on shop-grade Wi-Fi (00 §5.3) is T-01-06/D3. Constraints honored but **not closed** here: 01-F2 (hub ingest persists before ack), 01-F8 (event-id dedupe everywhere), 01-F14 (branch-stream ingest + serving joiners lands; rolling-window retention/compaction stays open), 01-F27 (LAN auth is a stub), 01-F37 (invalid ingest skipped + counted; quarantine machinery is a later task), 01-F40 (slice filtering deferred — sim runs hub-eligible classes only), 20 §2.4 (this task is the *seed* of the harness, not the nightly thousands-of-seeds rung).
- **Files touchable:** `packages/sync-client/src/**` (impl session; protected path; **lands only after T-01-03 merges** — additive store extension per contract below, no rework of T-01-03 surfaces); `packages/sync-client/src/__acceptance__/**` (test session only); `packages/sync-protocol/src/transport.ts` (impl session; protected path; **additive seam types only — no wire message or fixture changes**, rationale in assumptions); `packages/testing/src/**` (sim scheduler seed, owning spec 20 §2.4 + §6-Q1); `packages/testing/package.json`, `packages/sync-client/package.json` + lockfile (dep wiring: `@restos/testing` → dep on `@restos/sync-protocol`; `@restos/sync-client` → devDep on `@restos/testing`).
- **Check:** `pnpm --filter @restos/testing test` + `pnpm --filter @restos/sync-client test` + `pnpm verify:01` + root `pnpm verify`.
- **DoD rung:** D1 (in-suite deterministic sim scenarios; the nightly long-seed corpus, chaos E2E, and mutation gates are the D2 rungs — later harness tasks build them on this seed).
- **Depends on:** T-01-02 (wire messages — consumed from `@restos/sync-protocol`, never redeclared), T-01-03 (device store + outbox — the mesh drains `nextBatch` and ingests into the same store), T-01-00 (`verify:01`). **Not** T-01-04: convergence here is asserted as ledger set-equality + per-origin order, not fold identity (fold-identity convergence is T-01-06's exit run, with FOLDS.md folds).
- **API contract (binding for the test session):**
  - **(a) Transport seam** — added to `@restos/sync-protocol` (`src/transport.ts`; consumed by both the mesh and the sim, never redeclared):
    - `type PeerInfo = { device_id: string; device_class: DeviceClass }` (`DeviceClass` from `@restos/domain`).
    - `interface Clock { now(): number; setTimeout(fn: () => void, ms: number): TimerId; clearTimeout(id: TimerId): void }` — the mesh takes all time from here; a trivial wall-clock adapter ships with this task, the sim provides the virtual one.
    - `interface TransportHandlers { onPeerVisible(peer: PeerInfo): void; onPeerLost(device_id: string): void; onMessage(from: string, message: ProtocolMessage): void }`.
    - `interface MeshTransport { start(handlers: TransportHandlers): void; stop(): void; send(to: string, message: ProtocolMessage): void }` — `send` is fire-and-forget; delivery is not guaranteed (the mesh's correctness must not depend on it — id-dedupe + re-push absorb loss). Discovery announcements carry `PeerInfo` (this is how HUB-ELECTION.md's "broadcast `ping{class, device_id}`" is realized — mDNS TXT/manual config at the real adapter); the wire `ping{t}`/`pong{t}` of PROTOCOL.md is the session heartbeat, not discovery.
  - **(b) Election — pure function** (`packages/sync-client`, per 01 §8 "hub election runs inside sync-client"): `electHub(peers: readonly PeerInfo[]): string | null` — input includes self; returns the winning `device_id` or `null` if no hub-eligible peer is visible. Laws (HUB-ELECTION.md, all property-tested): rank = index in `HUB_ELIGIBLE_CLASSES` (from `@restos/domain`, hub-priority order — lower index wins); tie → lexicographically lowest `device_id`; classes outside `HUB_ELIGIBLE_CLASSES` never win; **permutation-invariant** (`electHub(shuffle(p)) === electHub(p)`) — every device computes the same winner from the same visible set, no consensus rounds.
  - **(c) Mesh session state machine** (`packages/sync-client`): `createMeshSession({ store, transport, clock, device_class, token }): MeshSession` with `start()`, `stop()`, `notifyAppended()` (host-app fast-path trigger: drain outbox now, 01-F15), and `status(): { state: "solo"|"follower"|"candidate"|"hub", hub_id: string|null, peers: PeerInfo[], last_push_ack: number|null }` (01-F11 mesh half).
    - Exported constants (HUB-ELECTION.md, binding): `HEARTBEAT_INTERVAL_MS = 2_000`; `HEARTBEAT_MISSED_LIMIT = 3`; `HUB_LOSS_TIMEOUT_MS = 6_000` (derived); `REELECTION_BUDGET_MS = 10_000` (01-F13 assertion budget — virtual ms here, wall-clock in T-01-06).
    - Transitions: empty peer set → `solo` if hub-eligible (acts as hub for later joiners), else `follower` with `hub_id: null` (waits). Any peer-set change → recompute `electHub(visible ∪ self)`: winner = self → `hub`; winner = other → `follower` (connect: `hello` → `hello_ack{hub:true}`). Hub loss (no wire `ping` for `HUB_LOSS_TIMEOUT_MS`) → eligible devices enter `candidate` (the no-hub recompute window; exits on adopting the new result), non-eligible → `follower`/null. Loss-to-new-hub-connected < `REELECTION_BUDGET_MS` virtual on every device.
    - Hub duties: heartbeat wire `ping{t}` to each connected follower every `HEARTBEAT_INTERVAL_MS` (follower replies `pong`; 3 unanswered → hub drops that follower). On follower `push`: `store.ingestBatch(events)` (persists before ack, 01-F2), reply `push_ack{acked_watermark}`, fan the new events as `event_batch` to every *other* connected follower. On follower `hello`: `hello_ack{ session_id, hub: true, resume_from }` where `resume_from` = next `lamport_seq` the hub expects from that device (highest contiguously held + 1; 0 if none), then replay its stored branch window to the joiner as `event_batch` (id-dedupe absorbs redundancy — LAN catchup does not use `catchup_request`, whose cursor is cloud `global_seq`; see assumptions).
    - Follower duties: on connect and on `notifyAppended`: `store.nextBatch(max)` → `push{events, watermark}` from `resume_from` onward; track `last_push_ack` from `push_ack`. **The hub ack never calls `store.advanceTo`** — the outbox watermark is the *cloud* write-checkpoint (19 §5; HUB-ELECTION.md keeps followers' own cloud fallback); the LAN push cursor is session-local and volatile (worst case re-push; id-dedupe absorbs). Ingest received `event_batch` via `store.ingestBatch`.
    - Store extension (additive to the T-01-03 contract, same file): `store.ingestBatch(events: EventEnvelope[]): { appended: number; deduped: number; rejected: number }` — branch-stream ingest: envelope + registry validation reusing T-01-03's append validation (failures are skipped and counted, never thrown — full 01-F37 quarantine is a later task), dedupe by event `id` (01-F8), no lamport assignment, no outbox rows, persisted before return (01-F2). `store.readAllEvents(): EventEnvelope[]` — own + ingested, for window replay to joiners (01-F14 half) and sim ledger assertions.
  - **(d) Sim scheduler seed** (`@restos/testing`, 20 §2.4 / §6-Q1 dedicated harness — fast-check drives generation, this scheduler owns time): `createSim({ seed: number }): Sim` with `sim.clock: Clock` (virtual — the same seam interface the mesh consumes), `sim.lan: SimLan`, `sim.run()` / `sim.runFor(virtualMs)` / `sim.runToQuiescence({ maxVirtualMs }): boolean`, `sim.now()`, `sim.trace(): readonly TraceEntry[]` (ordered record of timer fires + deliveries, for ordering assertions).
    - `interface SimLan { attach(peer: PeerInfo): MeshTransport; partition(...groups: string[][]): void; heal(): void; disconnect(device_id: string): void; reconnect(device_id: string): void; policy(p: { latency?: [minMs, maxMs]; dropRate?: number; duplicateRate?: number }): void }` — `partition` splits both visibility (`onPeerLost` fires across the cut) and delivery (messages across the cut drop); `disconnect` models a device vanishing (crash/leave).
    - Determinism laws (tested in `@restos/testing` itself): single-threaded virtual time; timers and deliveries execute in (time, schedule-seq) order; all delay/drop/duplicate draws come from the seeded RNG only (no `Math.random`, no `Date.now` inside sim or mesh code); **same seed + same script ⇒ deep-equal trace**. The bus round-trips every message through `encodeMessage`/`decodeMessage` (T-01-02 codec exercised on every hop).
    - **Acceptance scenarios this contract must support (binding targets):** **S1 election convergence** — N devices attach at staggered virtual times; at quiescence every `status().hub_id` equals `electHub(all peers)` and topology is a star on the hub. **S2 re-election under hub loss** — `disconnect(hub)` at t; all remaining devices converge on the same new hub in < 10 000 virtual ms. **S3 partition split-brain then heal** — partition into two groups; each side with an eligible device elects its own hub (safe by design, HUB-ELECTION.md); events append on both sides and propagate within each side; `heal()` → one hub again and every device's `readAllEvents()` id-set equals the union of all appended events, exactly once per id, per-origin lamport order preserved. **S4 fast-path propagation ordering** — under seeded `policy({ latency, dropRate, duplicateRate })`, an appended event reaches every connected device exactly once with per-origin lamport order intact at each receiver (trace-asserted push → ack → fan-out; the sim asserts logic and ordering, not radio latency).
- **Explicitly NOT this task (still owned by T-01-06 + later tasks):** real mDNS discovery + manual-IP fallback adapter and the real WebSocket LAN transport (01 §9-Q1 decision — bias plain WebSocket — is made *in the spike*, T-01-06); wall-clock 01-F15 p95 < 1 s and 01-F13 < 10 s on real hardware; the 01 §8 exit run (3 devices Electron + 2 RN, virtual printer, WAN cut mid-rush-replay, plug-pull mid-print, fold-identity convergence with T-01-04 folds); cloud uplink and hub-as-preferred-uplink (needs `services/sync-gateway`); LAN auth validation (01-F27) and slice enforcement for scoped classes (01-F40); quarantine machinery (01-F37); retention window/compaction (01-F14 remainder); the nightly randomized-seed corpus and chaos E2E (20 §2.4–2.5 D2 rungs).
- **Assumptions stated:** (1) **Transport injected, sim-only at this rung** — simpler alternative (build on real localhost WebSockets and test against them) rejected: 20 §2.4 mandates seeded reproducibility and real sockets are nondeterministic; 19 §7 prices the sim harness in as the cost of building custom. (2) **Seam types live in `sync-protocol`** — `PeerInfo`/`Clock`/`MeshTransport` are consumed by both `sync-client` (mesh) and `testing` (sim); placing them in `sync-client` creates a dev-dependency cycle; the simpler alternative (structurally-identical duplicate types in `testing`) rejected as silent-drift risk against declare-once (protected-path review covers the addition; wire messages and fixtures untouched). (3) **HUB-ELECTION.md's `ping{class, device_id}` broadcast is discovery-layer**, realized as `PeerInfo` announcements; wire `ping{t}` stays the PROTOCOL.md heartbeat — neither artifact is edited. (4) **`candidate` is a transient recompute window**, not a Raft-style campaign (election is a pure function; no terms/votes) — simpler 3-state alternative rejected because HUB-ELECTION.md names four states and is binding. (5) **Hub ack ≠ outbox ack**: `advanceTo` remains reserved for cloud-persistence ack (19 §5); volatile LAN cursor + re-push + id-dedupe is the simpler-and-correct choice over per-hub durable cursors. (6) **LAN join catchup = full-window `event_batch` replay + id-dedupe** — `catchup_request` keys on `global_seq`, which LAN-only events don't have yet (01-F3: cloud-assigned); a per-device-lamport catchup cursor would need a PROTOCOL.md spec PR and isn't needed at spike scale. (7) **`hello_ack.resume_from` on LAN = next expected `lamport_seq`** for the connecting device (PROTOCOL.md doesn't pin the LAN meaning; flagged for a one-line clarification PR). (8) **Sim uses hub-eligible classes only**; scoped classes (waiter LAN member, rider cloud-only) join with the slice-enforcement task. (9) **Convergence = ledger set-equality + per-origin order**, not fold identity (T-01-04/T-01-06 own folds). (10) Building the dedicated scheduler resolves 20 §6-Q1 per its stated bias — record the resolution in specs/20 (or DECISIONS.md) when this lands.
- **Reconciliation with T-01-04 (planner note):** `ingestBatch` is the batch seam over T-01-04's per-envelope `ingest` (per-event validate → skip-and-count instead of throw; same persistence + fold application in-transaction); `readAllEvents` reads `events` ∪ `peer_events`. Sequencing is T-01-03 → T-01-04 → T-01-05: the mesh builds on landed fold-task store surfaces even though its convergence assertions are ledger-level, not fold-level.
- **Fix-round amendments (adversarial review; each is binding contract text):**
  1. **LAN push paging (blocker):** the follower's LAN drain pages **own events from the session cursor** — `readOwnEvents(from)` capped at `PUSH_BATCH_MAX` — never the cloud outbox page (`nextBatch` pages from the cloud checkpoint; once the LAN cursor is ≥ 500 past it the windows are disjoint and propagation stalls forever, verified at the WAN-down-rush scenario 01-F15 exists for).
  2. **Election liveness (blocker):** a computed winner that fails to `hello_ack` within `HELLO_TIMEOUT_MS = 4_000` (new exported constant) becomes **suspect** exactly as a lost connected hub does; suspicion clears **only on inbound traffic from the suspect device — never by timer expiry** (timer-expiring suspicion re-adopts a corpse forever, verified). With every eligible peer suspect, the highest-ranked non-suspect eligible device (self included) wins; a suspect that shows life re-enters candidacy on the next peer-set recompute.
  3. **Determinism (20 §2.4 law):** no wall-clock or crypto randomness anywhere in mesh code — `session_id` is deterministically composed from `(hub device_id, follower device_id, clock.now(), per-session counter)`; same seed + same script ⇒ deep-equal traces INCLUDING mesh sessions.
  4. The contracted **wall-clock `Clock` adapter ships** (`wallClock` export from `sync-client`: `{ now: Date.now, setTimeout, clearTimeout }` behind the seam — T-01-06 needs it).
  5. `stop()` clears the visibility map (ghost peers from a stopped period must not survive into a restart and win elections).
  6. `store.identity: StoreIdentity` (defensive copy) is a **binding additive T-01-03 surface** (forced by `createMeshSession`'s contracted signature — the session derives self-identity from the store).
  7. `sim.run()` is an unbounded drain — with self-rescheduling mesh timers it never returns; sim-driven mesh tests use `runFor`/`runToQuiescence` (documented, not a defect).
  8. **Known contract-inherent gap (not fixed here):** followers never relay third-party events, so a departed origin's events cannot cross a healed partition until cloud catchup — DEC-SYNC-006; T-01-06 must include the scenario so the cloud path's coverage of it is demonstrated, not assumed. Perf posture (full-window re-fan each heartbeat, O(ledger) scans in the push path) is scheduled as T-01-05b alongside T-01-04b before T-01-06.
### T-01-05b  Mesh hot-path perf (DEFERRED past T-01-06 — rationale below)
- Review-measured debt in the 01-F15 fast path: full-window `event_batch` re-fan to every follower each heartbeat (load-bearing for loss recovery — replacing it needs a targeted-retry design, not deletion: S4 under drops depends on it), O(ledger) `contiguousHigh`/id-set scans per push. Laws already pinned by the S1–S4 + fix-round oracle; impl swaps mechanisms, suite gates.
- **Deferred rationale:** unlike T-01-04b, this does NOT gate T-01-06 — the mesh scenarios run 3 devices (small ledgers), so the re-fan cost is invisible at sim scale; it bites only at rush-ledger size on real hardware, which is the D3 rig, not the virtual-time sim gate. The efficient re-fan is also partly wire-blocked (a follower branch-high-water on `pong` would unlock targeted retry — a PROTOCOL.md spec-review event). Schedule with the wire decision at W0-EXIT consolidation (24-F16), not before T-01-06.
- **Audit-1 surfaced (fold into this task):** both drain paths use `store.readOwnEvents(from).slice(0, PUSH_BATCH_MAX)` — an UNLIMITED read then a JS slice (follower drainPush from fix-round-1, hub notifyAppended from the audit-1 K-04 fix). O(1) in realistic operation (notifyAppended fires per append) but O(N) per call when a large backlog is fanned in one shot (the synthetic X8 case). That, plus the T-01-04b incremental engine falling back to full recompute on the `global_seq`-adoption path (which origin-inclusive fan-out hits on every event), is the remaining architectural O(N^2). **CORRECTION (measured on a quiet machine):** an earlier note here claimed X8 ran ~57 s and TIMED OUT under coverage, and prescribed scoping `test:coverage`. Both readings were contaminated by concurrent agent load. Re-measured: the X7+X8 file runs ~23-24 s (stable over two runs) and the FULL `test:coverage` run PASSES in ~47 s at 100% branches (82/82). No gate is broken and no coverage scoping is needed. The O(N^2) debt above is still real and still owned here — it is a headroom/scaling concern, not a current failure.

### T-01-04b  Incremental fold maintenance (scheduled perf work — MUST land before T-01-06)
- T-01-04 shipped the contract-permitted recompute-per-write (full canonical replay in every mutating transaction — the minimal correct v1). Adversarial review measured 0.8→13.5 ms/append from N=100→2000 (:memory:, dev laptop): quadratic cumulative, inside the UI-ack path (01-F2) and the LAN fast-path (01-F15) — a foreseeable exit-run blocker on 2 GB tablets at rush-day ledger sizes (01-N3). Law is already pinned: incremental state must stay ≡ `refold()` (the T-01-04 refold-equivalence property is the oracle; no new tests needed, the existing suite gates the rewrite). Plan when dispatching: oracle unchanged, impl swaps recompute for incremental application with parking-aware invalidation.

### T-01-06  Spike exit run — 01 §8 criteria per DEC-TEST-002 (accepted): deterministic sim leg + real-process smoke leg against the landed gateway on Testcontainers Postgres, **both binding pass/fail gates**, with the D3 wall-clock remainder named
- **FRs (honesty ledger — closed vs demonstrated):** 01-F9 **device half closed**: the cloud session consumes `hello_ack`/`event_batch` fan-out and paged `catchup_request`/`catchup_response` range fetch end-to-end (the served half closed at T-01-07; the org-scope reference-data clause stays **open** — catalog wave). 01-F12 (LAN discovery + direct exchange with WAN down) — **partially closed**: real WebSocket-over-LAN transport + manual-IP peer directory close the "exchange directly while WAN is down" clause over a real socket; the mDNS clause stays **open** (per-platform native work, 18 §16-Q2 — app wave). 01-F14 — **serving half closed**: a joiner attached mid-run is served the full branch window by whoever is hub; rolling-window retention/compaction stays **open** (needs the 00 §7 config layer). 01-F15 — **closed at the sim rung**: virtual-time p95 under a seeded adversarial policy; physical p95 on shop-grade Wi-Fi is **demonstrated-only** here (loopback wall-clock printed as evidence, never gated) and binds at D3 (00 §4 rig) + the H-01-D chaos rung (20 §2.5). 01-N1 — **closed at system level**: fold-identity convergence across 3 devices ≡ `refold()` ≡ cloud-order replay of the merged stream, on top of T-01-04's package properties. 01-F37 **device-notification half demonstrated**: `quarantine_notice` reaches the origin's cloud session and surfaces in `status()`, and the ack advances over the quarantined slot so the outbox never wedges (DEC-SYNC-005, device half); the resolution/correction flow and fleet-health surfacing stay **open** (T-01-08 / doc 15). 01-F38 **exercised, not closed**: the DEC-SYNC-006 departed-origin shape is demonstrated — LAN heal alone provably does NOT converge the departed origin's events; cloud catchup does (evidence for the founder's protocol call; the row stays `proposed`). Regression-extended, **not re-closed**: 01-F2 (kill-seed law through folds + mesh + a real SIGKILL + the real gateway's persist-before-ack), 01-F3/F8 (the outbox write-checkpoint finally driven by a real `push_ack` path, 19 §5), 01-F11 (status fed by live cloud values: last push ack, last pull position), 01-F13 (re-election demonstrated under process death; logic closed in T-01-05), 01-F34 (converge-to-cloud-order demonstrated system-wide against BOTH the sim-cloud and the landed gateway). Constraints honored, not closed: 01-F27 (dev-token stub, T-01-09), 01-F40 (identity `sliceFilter`, Wave 1), 01-N3 (storage budget is rig territory), 01-N4 (Wave-1 staging).
- **Decision — what is real vs simulated (DEC-TEST-002, accepted: sim leg + real-process smoke leg are co-binding gates; only physical wall-clock is deferred to D3):** 01 §8 names "3 devices (Electron + 2 RN)" and "shop-grade Wi-Fi"; 20 §1 places the scripted exit criteria in the *containerized* sim-branch (Toxiproxy chaos — that full rung is H-01-D); D3 owns physical wall-clock. No app hosts exist in Wave 0, so this task stages the criteria honestly:
  1. **Deterministic sim leg (the assertion vehicle, binding; Docker-free):** 3 sync-client instances under the landed `@restos/testing` sim (virtual time), device classes `counter_electron`/`counter_rn`/`kitchen` — "Electron + 2 RN" read as host classes, which also exercises the full 01-F13 priority order. All invariant gates (X1–X9) assert here, reproducible from a pinned seed.
  2. **Real-core leg (binding; lives in the gateway workspace, whose suite already mandates Docker):** the landed `createGateway` on Testcontainers Postgres is driven two ways — (XP) transcript parity: the sim-cloud double's committed directed transcript replays message-for-message against the real core, pinning the double to the real contract (20 §2.7); (X10) real-process smoke: 3 `tsx` children with real SQLite + real WebSockets over loopback against the real `buildServer`, real SIGKILL mid-print, WAN cut by closing the listener. X10 asserts set-equality + fold digests; wall-clock numbers print as *evidence, not gates*. Green X10 resolves 01 §9-Q1 (plain WebSocket).
  3. **Sim-cloud vs real-core-in-process (decided: hybrid, double retained):** the sim leg keeps a protocol double because the landed sim drains synchronously in virtual time (20 §2.4 determinism law) while `conn.handle()` is async over real Postgres I/O — interleaving them deterministically would mean reworking the landed scheduler (rework of a landed surface) and coupling the crown-jewel harness to Docker, killing the thousands-of-runs economics 20 §1's in-process row exists for. The clocks do compose (`sim.clock` satisfies the gateway's `{ now() }`) — but composition solves timestamps, not I/O scheduling, so it does not rescue a full real-core sim leg. Honesty is preserved by pinning the double to the real core via XP: because the double is retained, its transcript lands as a gateway-consumed golden fixture (20 §2.7); if the double is ever dropped, the fixture goes with it.
  4. **Remains simulated / deferred:** device hosts (Node stands in for Electron main and RN; host legs + op-sqlite adapter land with the app wave), radio (loopback ≠ Wi-Fi → D3 + H-01-D Toxiproxy), the printer (job-recording stub, not `packages/escpos`/03-F4), wall-clock time in the sim leg.
- **Files touchable:** `packages/sync-client/src/**` excluding `__acceptance__/` (impl session; protected path; **additive only**: `cloud-session.ts`, `transport-ws.ts`, index re-exports — zero edits to landed `device-store.ts`/`mesh-session.ts`/`folds/**`; a defect a scenario exposes is a fix-round on the owning task, never silent rework here); `packages/sync-client/src/__acceptance__/spike/**` (test session — X1–X9; this suite stays Docker-free); `packages/sync-protocol/src/transport.ts` (impl session; protected; **additive `CloudTransport` seam types only** — no wire-message or fixture changes; same declare-once rationale as T-01-05's seam); `packages/sync-protocol/src/__acceptance__/fixtures/transcripts/` (test session **adds** the XP transcript fixture; editing any existing fixture is a spec-review event, 20 §2.7); `packages/testing/src/**` (sim-cloud double, rush-replay generator, virtual printer — harness impl session; determinism self-tests test-session); `services/sync-gateway/src/__acceptance__/spike/**` (test session — XP + X10 harness + child entry script; gateway impl `src/**` is NOT touched); `packages/sync-client/package.json` (`ws` + `@types/ws` — `ws` is 18 §14-registry-listed), `services/sync-gateway/package.json` (devDeps `@restos/sync-client`, `@restos/testing`, `tsx` — registry-listed), lockfile; spec one-liners per 00 §8 + `pnpm docs:lint`: `specs/01-kernel-sync.md` §9.1 (resolved: WebSocket-over-LAN), `specs/20-testing-correctness.md` §6.1 (resolved: dedicated harness built, T-01-05), `packages/sync-protocol/PROTOCOL.md` (protected; two clarifying lines recording landed behavior: `hello_ack.resume_from` = next expected lamport on both planes; `catchup_request.from_global_seq` = exclusive "last received" cursor, `global_seq` starts at 1) — the T-01-05/T-01-07 debts assigned to "whichever merges second".
- **Check:** `pnpm --filter @restos/testing test` + `pnpm --filter @restos/sync-client test` + `pnpm --filter @restos/sync-gateway test` + `pnpm verify:01` + root `pnpm verify`. Docker prerequisite unchanged from T-01-07 (gateway workspace only; loud failure when absent, never a skip). Evidence in the closing message (24 §3 step 6): captured X1–X10 output — event counts, per-device fold digests, virtual p95, re-election times, XP parity verdict, and X10's wall-clock propagation table. This is the founder-facing 24-F19 checkpoint artifact and the 19 §6 spike verdict: pass = custom engine confirmed; a miss counts as iteration 1 of 2 before the Ditto ramp.
- **DoD rung:** D1 (all legs in-suite). Completes the kernel spike at the staged rigs; D2 (H-01-D containerized chaos E2E with Toxiproxy — where 20 §1 runs the 01 §8 script in full, nightly seed corpus, mutation gates) and D3 (physical plug-pull, shop-grade Wi-Fi p95, 01-N3) are named follow-ons — the 01 §8 wall-clock clause is NOT claimed by this task.
- **Depends on:** T-01-04 + T-01-05 implementations (incl. their fix-round amendments), **T-01-04b landed** (incremental folds; T-01-05b deferred, re-fan cost invisible at 3-device sim scale), T-01-07 (+ fix round — the real-core leg drives it), T-01-02, T-01-00.
- **T-01-06a (sim leg) status — LANDED, two adversarial reviews clean (no blockers).** X1–X9 + cloud session + sim-cloud double green (sync-client 133/133, testing 49/49); 01-F9 device-half + 01-F38 closed, 01-N1 system-level demonstrated. Review findings carried forward (none block): **(S1/perf)** X8's 1600-event backlog is still O(N²) ~20 s — T-01-04b's incremental fast path does NOT cover the `global_seq`-adoption reorder, which origin-inclusive fan-out hits on every event; the earlier "T-01-04b makes X8 tractable" rationale was wrong (it is tractable under a raised 60 s test timeout, correctness untouched, X6 gate is virtual-time — but not fast). Scheduled as a T-01-04b follow-up (adoption-path incremental maintenance) in the W0-EXIT consolidation. **(N1/coverage)** X1's "converges to cloud order" is vacuous for reordering (rush generator is single-writer, no `order.table_assigned`) — the reorder mechanism is unit-tested (T-01-04 law 7) and probe-verified system-side, but a spike scenario with cross-device `table_assigned` + LAN-first-then-cloud-reversal is owed for genuine system coverage (added as X1b before spike sign-off). **(N3/T-01-05 note)** hub `notifyAppended` still fans via `nextBatch` (cloud-checkpoint page), not `readOwnEvents`; a hub with >500 own backlog leans on the heartbeat re-fan — no invariant breaks; folded into T-01-05b. **(N2/foot-gun)** bare root `npx vitest` uses 5000 ms and times out X8 — the real gate (`pnpm --filter`/turbo) uses the package's 60 s config.
- **T-01-06b (real-core leg) status — LANDED, two adversarial reviews clean (no blockers); X10 verified 10/10 non-flaky (2 impl + 5 review + 3 main-session).** Real WS transport adapters (`transport-ws.ts`: hub ws-server + follower dialers realizing the MeshTransport seam; cloud ws-client realizing CloudTransport, both consuming the codec, zero wire changes), a `tsx` child device entrypoint, XP transcript-parity (the committed `spike-cloud-contract.json` pins the sim-cloud double to the real `createGateway`, full-stream deep-equal modulo session_id/server_received_at, global_seq exact), and X10 real-process smoke (3 children, real SQLite, real loopback WS to the real `buildServer` on Testcontainers PG, real SIGKILL-mid-print + WAN cut + respawn + resume). Asserts id-set equality, per-origin order, fold-digest identity, gap-free lamport on the killed device, every event global_seq'd, `acked_watermark == own_high_water` on all 3, quarantine_notice at the poisoned origin. Wall-clock is evidence-only (DEC-TEST-002). Non-blocking notes carried forward: **(perf debt is the ONLY substantive item across both spike legs — X8 O(N²), already recorded above)**; **(kill-timing)** X10's SIGKILL lands at an idle point (the held print event is not yet appended) — a faithful print-then-record process-death test (4 confirmed events survive gap-free, no phantom event), with the randomized in-flight-write injection owned by the nightly H-01-A SIGKILL harness; **(LAN validation)** the real LAN adapter passes inbound frames without `parseMessage` (raw JSON.parse), asymmetric with the cloud adapter + the sim bus — no X10 invariant is affected (trusted-LAN rung, LAN auth is a stub), folded into the real-transport hardening at T-01-09/W0-EXIT so every hop round-trips the codec like the sim did. Severance line if the session hits the 24-F4 horizon: T-01-06a = sim leg (contracts a–e, X1–X9) is a coherent session alone; T-01-06b = real-core leg (contract f, XP, X10, spec one-liners) splits cleanly with the same check.
- **API contract (binding for the test session):**
  - **(a) `CloudTransport` seam** — added to `@restos/sync-protocol` `src/transport.ts` (consumed by the cloud session, the sim-cloud, and the real WS adapter; declared once): `interface CloudTransport { start(handlers: { onUp(): void; onDown(): void; onMessage(message: ProtocolMessage): void }): void; stop(): void; send(message: ProtocolMessage): void }`. `send` is fire-and-forget while up; sends while down are dropped (re-push on `onUp` + id-dedupe absorb, same law as the LAN seam).
  - **(b) Cloud session** (`packages/sync-client`, new `cloud-session.ts`): `createCloudSession({ store, transport, clock, device_class, token }): CloudSession` with `start()`, `stop()`, `notifyAppended()`, `status(): { connected: boolean; last_push_ack: number | null; last_global_seq: number | null; quarantined: readonly { event_id: string; reason: string }[] }` (01-F11 cloud half; quarantined list is session-local — durable notice handling is T-01-08). Duties, matched to the LANDED gateway: on `onUp` → `hello` composed exactly as the mesh's (`last_global_seq: status().last_global_seq ?? 0`, `own_high_water: status().own_high_water ?? 0`, `branch_id` from `store.identity`, token); on `hello_ack{resume_from}` → drain own tail via `store.nextBatch(CLOUD_PUSH_BATCH_MAX = 500)` (the cloud outbox pages from the write-checkpoint — correct here, unlike the LAN cursor of T-01-05 fix-round 1) and issue `catchup_request{ from_global_seq: status().last_global_seq ?? 0 }` — the cursor is **exclusive** and cloud `global_seq` starts at **1**, so `0` means "send everything"; loop pages via `next_from` while `complete === false`. On `push_ack{acked_watermark}` → `store.advanceTo(acked_watermark)` — the cloud ack IS the outbox write-checkpoint (19 §5; below-checkpoint acks no-op, beyond-high-water throws loud per T-01-03); then if `nextBatch` is nonempty, push the next page (chained paging drains a >500 backlog). No ack arrives when nothing was contiguously persisted (gateway fix-round 4) — the session simply re-pushes on the next trigger. On `event_batch` / `catchup_response` → per wire event, split the two cloud stamps: `const { global_seq, ...envelope } = e`; `store.ingest(envelope, global_seq === undefined ? undefined : { global_seq })` — own events return via origin-inclusive fan-out and take the landed duplicate-id adoption path; per-event failures are counted, never thrown (01-F37 posture); then `setLastGlobalSeq(max seen)`, monotonically. On `quarantine_notice` → append to `status().quarantined`. **Every device runs its own cloud session** — the DEC-SYNC-004 interim (HUB-ELECTION.md's follower fallback, and the gateway's `device_mismatch` enforcement makes proxying impossible anyway); hub-as-preferred-uplink stays blocked on that row.
  - **(c) Sim-cloud double** (`@restos/testing`; speaks only PROTOCOL.md messages; mirrors the LANDED gateway laws): `createSimCloud({ sim }): SimCloud` with `transportFor(device_id): CloudTransport` (per-device sessions, `hello`-first), `cut()` / `heal()` (all links) and `cutFor(device_id)` / `healFor(device_id)` (fire `onDown`/`onUp`; in-flight messages across a cut drop, mirroring `SimLan`), `state(): { events: number; last_global_seq: number | null }`, `mergedStream(): readonly (EventEnvelopeT & { server_received_at: number; global_seq: number })[]`, `transcript(): readonly { direction: "in" | "out"; device_id: string; message: ProtocolMessage }[]`. Mirrored laws (each is what XP pins): `global_seq` dense **from 1** in arrival order; dedupe by id; stop-at-gap contiguity with per-slot fill; `hello_ack.resume_from = acked_watermark + 1` (0 when none); **no `push_ack` when nothing is contiguously persisted**; fan-out after "commit" as one `event_batch` **including the origin**, both cloud stamps merged into each wire event; catchup = exclusive cursor, ascending, pages of 500 (mirrors `CATCHUP_PAGE_SIZE`; XP catches drift), `next_from` echoes the cursor on an empty page; **`storage_reject` mirror rule**: any envelope containing U+0000 in a string quarantines verbatim (slot fills — DEC-SYNC-005; `quarantine_notice{event_id, reason}` to the pusher; no `global_seq` consumed). Not mirrored (stays gateway-only, tested at T-01-07): identity/schema/divergence quarantine classes, auth. WAN links use a **fixed** scripted virtual latency (default 50 ms) — no RNG (the sim's seeded randomness stays LAN-side); WAN is modeled reliable-or-down (it is TCP: loss manifests as `onDown`, never silent drop while up — assumption 11).
  - **(d) Rush-replay generator** (`@restos/testing`; the 00 §4 rush-simulation seed): `generateRushScript({ seed, devices, orders }): RushStep[]` — deterministic, seeded; interleaved order lifecycles (`order.created → order.confirmed → order.line_added×n → kot.printed → order.line_state_changed… → payment.recorded`; registry types only — `order.created` carries the registry-required `channel`; integer paisas) as steps `{ at_virtual_ms, device_id, action }` where `action` is an append input, a print request, or a fault-hook slot. Law (self-test): same seed ⇒ deep-equal script. One canonical seed pinned as a committed constant; scale ≥ 300 orders across 3 devices on a rush-shaped arrival curve (harness parameters, stated — 01-N4's 50 orders/min is a cloud NFR, not this gate).
  - **(e) Virtual printer stub** (`@restos/testing`): `createVirtualPrinter({ sim }): VirtualPrinter` — `jobs(): readonly { device_id, order_id, completed: boolean, at: number }[]`, `holdNext(): void` (freeze the next job mid-transfer — the deterministic kill point), `release(): void`. **Print model (binding):** driver sends job → printer acks completion → device appends `kot.printed`. A crash between printer-ack and append re-issues the job on restart: a duplicate *print* is possible and flagged in the run log; a phantom `kot.printed` (no completed job) is impossible; a lost confirmed order is impossible. NOT `packages/escpos`, NOT the 03-F4 spooler — this stub exists only to make "plug-pull mid-print" a ledger-durability assertion.
  - **(f) Real WS adapters** (`packages/sync-client` `src/transport-ws.ts`, over `ws`): `createWsLanTransport({ self: PeerInfo, listen_port, peers: { device_id, host, port }[] }): MeshTransport` — discovery = the **manual-IP static peer directory** (the 01-F12 fallback promoted to the spike's real discovery; visible = connectable, lost = closed/heartbeat-missed; mDNS explicitly NOT this task) — and `createWsCloudTransport({ url, clock, reconnect_ms = 1_000 }): CloudTransport` — client of the gateway's `/sync` route; `onDown` on socket close/error, timer-based reconnect through the injected `clock` (`wallClock` in X10). Node/Electron-main hosts both roles at this rung; RN gets the platform WebSocket client at app wave.
  - **(g) Exit scenarios (acceptance tests; FR ids in test names, 20 §4.1; X1–X9 sim leg in `sync-client/src/__acceptance__/spike/`, XP + X10 in `sync-gateway/src/__acceptance__/spike/`):**
    - **X1 rush baseline (01-N1/01-F34):** 3 devices + sim-cloud, canonical seed, run to quiescence → every appended event exactly once on every device (id-set equality with the union of appends), per-origin lamport order at each receiver, every event carries `global_seq`, each device's `acked_watermark == own_high_water`, `orders`/`queue`/`parked` tables byte-identical across devices, each ≡ its own `refold()`, and ≡ a fresh reference store (same org/branch, distinct device id) ingesting `mergedStream()` with its seqs in `global_seq` order — device state ≡ cloud-order replay.
    - **X2 WAN cut mid-rush (01-F8/19 §5):** `cut()` mid-script → LAN-only operation continues (new orders reach all 3 devices; virtual p95 during the cut < 1 000 ms), `heal()` near end → push/catchup resume → full X1 assertions.
    - **X3 partition + rejoin, doubly degraded (01-F17/01-F38):** WAN cut AND LAN partitioned `{A} | {B, C}` mid-rush; both sides keep appending (a sale is never blocked); each side elects its own hub (safe by design); heal LAN then WAN → zero lost, zero duplicated, one hub, fold identity ≡ `refold()` ≡ cloud-order replay.
    - **X4 plug-pull mid-print (01-F2):** `holdNext()` on the hub's KOT job, abrupt store abandon + mesh death at the hold point → reopen/rejoin → every pre-kill confirmed event survives gap-free (kill-seed law), tables ≡ `refold()` (T-01-04 law 8), no `kot.printed` for the held job, re-issued job flagged duplicate-print-only, mesh reconverges to X1 assertions.
    - **X5 hub death + cold joiner (01-F13/01-F14):** kill the hub → remaining devices converge on the same new hub < 10 000 virtual ms, zero loss; a cold joiner attached after the kill is served the full branch window by the NEW hub.
    - **X6 propagation p95 (01-F15):** per-event time-to-last-connected-device over the full rush; p95 < 1 000 virtual ms under the seeded adversarial LAN policy (latency [5, 150] ms, drop 1%, duplicate 0.5% — harness parameters, stated). Proves one push→fan-out round with no polling/batching stalls; cannot prove radio (D3).
    - **X7 departed-origin heal (DEC-SYNC-006/01-F38):** WAN up for B only (`cutFor(A)`, `cutFor(C)`); LAN partitioned `{A, B} | {C}`. B appends (events reach A via LAN and the cloud via B's session), then B departs both planes (`disconnect` + `cutFor(B)`). LAN heals A↔C → run to LAN quiescence → **assert C does NOT hold B's events** (followers push own events only — the documented gap is real, not assumed). `healFor(C)`/`healFor(A)` → catchup delivers B's events with `global_seq` → set-union + fold identity across A and C ≡ cloud-order replay. The run log names this the DEC-SYNC-006 evidence.
    - **X8 >500 backlog drain (T-01-05 fix-round 1 at full scale; 01-F15/01-F8/01-F9):** WAN cut from t0; device A appends 1 600 events (> 3× both page constants) during LAN-connected rush → full LAN propagation via multi-page session-cursor drain; WAN heal → chained cloud push pages to `acked_watermark == own_high_water`; device C, held LAN-disconnected with a cold store, attaches WAN-only and pulls the branch through ≥ 2 catchup pages (`complete` false→true, `next_from` chaining across the 500 boundary).
    - **X9 quarantine surfacing on-device (01-F37/DEC-SYNC-005):** one scripted event whose payload carries U+0000 (registry-valid); WAN up → sim-cloud quarantines it `storage_reject`, the notice lands in the origin's `status().quarantined`, the ack advances OVER the poisoned slot (`acked_watermark` reaches `own_high_water`; outbox drains; later sales unaffected, 01-F17). The run log records the honest divergence: the origin's local fold contains the order, `mergedStream()` does not — the correction flow that reconciles this is T-01-08+, not claimed here.
    - **XP transcript parity (20 §2.7; real-core leg):** a small directed exchange (hello → pushes incl. a dedupe re-push, one U+0000 event, one catchup) recorded from the sim-cloud is committed as `fixtures/transcripts/spike-cloud-contract.json`; the gateway suite replays its device-side messages through the real `createGateway` (Testcontainers PG, scripted clock) and asserts the outbound stream deep-equals the transcript modulo `session_id` and `server_received_at` only — `global_seq` values must match exactly (dense from 1, arrival order). This test is what licenses the sim leg's double.
    - **X10 real-process smoke (01 §9-Q1; real-core leg):** 3 `tsx` children (real SQLite files, `createWsLanTransport` over loopback, `createWsCloudTransport` → the real `buildServer` on an ephemeral port → Testcontainers Postgres; dev-tokens = the Wave-0 base64url claims shape) run a reduced script including one U+0000 event (real `storage_reject` end-to-end). Parent closes the gateway listener mid-run (WAN cut; LAN continues), SIGKILLs the hub child mid-print (printer hold coordinated over IPC), respawns the child, re-listens on the same port with the same Postgres (gateway sessions reset — cloud sessions re-hello and resume: the reconnect contract exercised). Parent collects ledgers + fold digests over IPC → id-set equality, per-origin order, digest identity, gap-free lamport on the killed device, every event `global_seq`'d, `acked_watermark == own_high_water` on all 3, `quarantine_notice` observed at the poisoned origin. Wall-clock propagation table printed (evidence only). Green X10 = 01 §9.1 resolved: plain WebSocket.
- **Assumptions stated (24 §3b — interpretation chosen / simpler alternative named):**
  1. **"Electron + 2 RN" read as device classes on Node hosts**; real host runtimes are app-wave + D3. Waiting for the apps was rejected: 19 §6 needs the verdict now and Wave 0 owns zero app code.
  2. **Sim leg and real-process smoke leg are co-binding gates (DEC-TEST-002, accepted); only physical wall-clock p95 is deferred to D3.** The smoke leg's correctness assertions (set-equality, per-origin order, fold-digest identity, gap-free lamport, quarantine_notice observed) gate pass/fail exactly like the sim; only its printed wall-clock numbers are evidence-not-gate. The 24-F19 run evidence is the 19 §6 Ditto-ramp pass/fail artifact. Full containerized Toxiproxy at this rung rejected: that is H-01-D's named D2 rung.
  3. **Sim-cloud double retained, pinned to the real core by XP** (full justification in the Decision block). Simpler alternatives rejected: LAN-only exit run (WAN-cut criteria vacuous, 01-F34 undemonstrable); full real-core sim leg (async Postgres I/O cannot be scheduled deterministically by the landed sync drain; Docker-couples the 20 §2.4 harness).
  4. **Per-device cloud sessions** (DEC-SYNC-004 interim; HUB-ELECTION.md follower fallback; gateway `device_mismatch` enforces no-proxy). Hub-as-preferred-uplink stays blocked on the DEC row — nothing here forecloses it.
  5. **Print-then-record** (`kot.printed` only on printer ack): the ledger must not record a print that may never have happened; duplicate physical prints on crash-recovery are acceptable and flagged (00 §5.2 protects confirmed facts, not side-effect uniqueness); real spooler semantics are doc 03.
  6. **Virtual p95 asserts protocol logic, not radio** — the binding wall-clock number is D3 + H-01-D; printing loopback numbers as evidence avoids a meaningless green gate.
  7. **`ws` in `sync-client`** (18 §14-listed): Electron main hosts both roles; RN needs only the client role (platform WebSocket) at app wave — no native module forced now.
  8. **Manual-IP directory is the spike's discovery**; mDNS deferred (18 §16-Q2 — the app wave decides the Android native-module shape, exactly as that question anticipates).
  9. **`tsx` children** resolve T-01-03's deferred kill-harness runtime decision; the nightly 20 §2.6 SIGKILL harness inherits it.
  10. **One canonical seed pinned; corpus deferred** to the nightly 20 §2.4 rung.
  11. **WAN modeled reliable-or-down** (TCP/WebSocket: loss = disconnect, never silent in-connection drop); fixed virtual WAN latency, no RNG on WAN links — keeps the double honest without inventing a loss model the transport cannot exhibit.
  12. **No cloud-side app-level heartbeat at this rung** — socket close/error drives `onDown`; keep-alive/backoff tuning is later work (the wire `ping`/`pong` exists and the gateway answers it; unused by the session at v1).
  13. **Quarantine surfacing = session-local status list**; durable notice redelivery and the operator resolution flow are T-01-08 (stated so X9/X10 are not mistaken for closing 01-F37).
  14. **Real-core legs live in the gateway workspace** — the Docker mandate stays where T-01-07 already put it, and `pnpm --filter @restos/sync-client test` stays fast and Docker-free (20 §1 in-process economics). Simpler alternative (everything in sync-client's suite) rejected for exactly that coupling; a new top-level e2e workspace rejected as a scaffold change this task does not own.
  15. **Spec one-liners bundled here** (01 §9.1, 20 §6.1, PROTOCOL.md ×2): all record already-landed decisions whose debts were assigned to "whichever merges second" — T-01-05 and T-01-07 both merged without them; splitting them to a W0-EXIT docs task is the named simpler alternative if review prefers a smaller diff.
- **Explicitly NOT this task:** real mDNS; Electron/RN host integration + op-sqlite adapter; gateway impl changes of any kind (fix-rounds on T-01-07 if a defect surfaces); H-01-D containerized chaos E2E + Toxiproxy (20 §2.5) — where the 01 §8 script runs in full per 20 §1; nightly seed corpus + SIGKILL harness + mutation gates (20 §2.4/2.6); N−1 matrix (20 §2.7 nightly half); D3 rig protocol (physical plug-pull, Wi-Fi p95, 01-N3); retention/compaction (01-F14 remainder); slice enforcement (01-F40) and LAN/real auth (01-F27, T-01-09); quarantine resolution flow + durable notices (T-01-08); hub-proxied cloud uplink (DEC-SYNC-004 accepted: per-device sessions is the shipped design, hub-proxy deferred); org-scope reference data in the stream (catalog wave); Auditor v1 (DEC-TEST-003 / T-01-11).

### T-01-07  services/sync-gateway core — cloud merge, global_seq, push/ack, catchup, fan-out (the cloud half of the kernel)

- **FRs:**
  - **Closed:** 01-F3 cloud half (cloud stamps `server_received_at` + a per-org global sequence on merge; the device half closed at T-01-03/T-01-04). 01-F8 server half (push ingest in lamport order, ack = per-device high-water mark, event-id dedupe, idempotent retries; the device outbox half closed at T-01-03).
  - **Partial:** 01-F9 (the *served* side closes: branch-stream fan-out to connected sessions + `catchup_request`/`catchup_response` range paging; the device-side cloud session that consumes it, and org-scope reference-data in the stream, land with the chaos-E2E/catalog waves — named below). 01-F7 (the merged org log lands as the substrate read models rebuild from; no read model is built here — they arrive with their owning modules). 01-F37 (quarantine *storage* verbatim + exclusion from merge/fan-out/catchup + `quarantine_notice` to the pushing session close here; the invariant-validation pipeline, durable notice-on-next-contact, fleet-health surfacing (doc 15), and the operator resolution flow are T-01-08 / doc 14/15 — explicitly deferred).
  - **Constraints honored, not closed:** 01-F1 (no update/delete path in any gateway API or SQL; event-table migrations additive-only, 18 §4), 01-F2 cloud side (`push_ack` is emitted only after commit — persist-before-ack), 01-F4 server boundary (every pushed event registry-parses before merge; the fuller server-side validation pipeline is T-01-08), 01-F27 (token *shape* validation only — real verification is T-01-09; stub boundary below), 01-F40 (no slice filtering: fan-out passes a named filter seam that is identity at v1; slice predicates are Wave 1), 01-N4 (load is a Wave-1 staging assertion, 20 §2.10), DEC-SYNC-004 (per-device sessions only; a push containing another device's events is rejected to quarantine — no on-behalf/proxy semantics exist or are added here).
- **Files touchable:** `services/sync-gateway/src/**` excluding `__acceptance__/` (impl session; protected path 20 §4.4 — senior review); `services/sync-gateway/src/__acceptance__/**` (test session only); `services/sync-gateway/drizzle/**` (generated migrations — append-only, never edited after application, 18 §4); `services/sync-gateway/package.json`, `drizzle.config.ts`, vitest config + root lockfile (deps: `fastify`, `@fastify/websocket`, `drizzle-orm`, `drizzle-kit` (dev), `postgres`, `pino`; dev: `vitest`, `testcontainers`, `fast-check`, `@restos/domain`, `@restos/sync-protocol` — all already in the 18 §14 registry; test wiring may land with the test session so the red run executes); `packages/config/src/**` for the ~15-line `defineEnv` factory only (18 §5 mandates env via the `packages/config` factory; this task is its first consumer — same first-consumer pattern as T-01-02's DEVICE_CLASSES); `scripts/verify-module.mjs` for **exactly two changes**: add `"@restos/sync-gateway"` to `MODULE_WORKSPACES["01"]`, and fix the workspace→dir resolution (line 45 hardcodes `replace("@restos/", "packages/")` — sync-gateway lives under `services/`; resolve via an explicit dir map or pnpm workspace lookup). `packages/sync-protocol/src/__acceptance__/fixtures/` — test session may **add** fixture files (e.g. `catchup_request.json`, `push_ack.json`); editing an existing fixture is a spec-review event (20 §2.7), not this task.
- **Check:** `pnpm --filter @restos/sync-gateway test` + `pnpm verify:01` + root `pnpm verify`. **Environment prerequisite: local Docker** (Testcontainers Postgres — see testing approach; suite must fail loudly with a clear message when Docker is absent, not silently skip).
- **DoD rung:** D1 (acceptance suite green against real Postgres in-suite; real-socket serving, chaos, and wall-clock numbers are H-01-D's D2 rung; 01-N4 load is Wave 1).
- **Depends on:** T-01-02 (wire messages — consumed from `@restos/sync-protocol`, never redeclared), T-01-01 (domain registry — `parseEvent` at the merge boundary), T-01-00 (`verify:01`). NOT on T-01-04/05/06: the gateway merges and serves envelopes; it folds nothing. If T-01-06's sim-cloud transcripts have landed by dispatch, the oracle consumes them as additional fixtures; otherwise the T-01-02 fixture set + oracle-added fixtures are the 20 §2.7 baseline.

- **Postgres data contract (binding — Drizzle, Postgres schema `kernel`, migrations under `services/sync-gateway/drizzle/`; sync-gateway is the sole writer of all four tables, 18 §4):**
  - `kernel.events` — `id text PK`, `org_id text`, `branch_id text`, `device_id text`, `lamport_seq bigint`, `global_seq bigint`, `server_received_at bigint`, `envelope jsonb` (the full envelope verbatim as received; the two cloud-stamped values live in their columns and are merged into the envelope at serve time). Constraints: `UNIQUE(org_id, global_seq)`, `UNIQUE(org_id, device_id, lamport_seq)` (the T-01-03 lamport-collision-is-corruption law, cloud side), index `(org_id, branch_id, global_seq)` (catchup paging). Retained forever (01 §5); **no partitioning at v1** (assumption below). No UPDATE or DELETE statement exists anywhere in the package for this table (01-F1).
  - `kernel.org_sequences` — `org_id text PK`, `next_global_seq bigint`. The per-org counter row, locked `FOR UPDATE` inside the merge transaction and held to commit — this serialization is what makes catchup paging unable to skip a not-yet-visible lower seq (law 4).
  - `kernel.device_watermarks` — `(org_id, device_id) PK`, `acked_watermark bigint`. Updated in the same transaction as the insert; the source of `hello_ack.resume_from`.
  - `kernel.quarantine` — `id text PK` (gateway-assigned `newId()`), `org_id`, `branch_id`, `device_id` (all from the session), `claimed_event_id text`, `reason text`, `envelope jsonb` (verbatim), `received_at bigint`; `UNIQUE(org_id, claimed_event_id)` — re-quarantine of the same claimed id is an idempotent no-op (first stored wins). Quarantined events get **no** `global_seq`, never enter `kernel.events`, never fan out, never appear in catchup (01-F37 "excluded from folds", cloud reading).
- **API contract (binding for the test session — module surface, not HTTP routes; the acceptance suite drives this surface in-process):**
  - `createGateway({ db, clock }): Gateway` — `db` = Drizzle instance over the `postgres` driver; `clock: { now(): number }` injected (18 §4 — `new Date()` banned in domain logic; `server_received_at` comes from `clock.now()`). `gateway.close(): Promise<void>`.
  - `gateway.connect(sink: (message: ProtocolMessage) => void): GatewayConnection` — one connection per device socket (DEC-SYNC-004: per-device sessions; no proxy/multiplex). `conn.handle(message: ProtocolMessage): Promise<void>` processes one inbound message; every outbound message (acks, batches, notices, pong) is delivered as a decoded `ProtocolMessage` through the sink of the addressed connection. `conn.close(): void` deregisters from fan-out. Wire codec (`encodeMessage`/`decodeMessage`) is the socket adapter's job, not the core's.
  - **Session (`hello`) — Wave-0 stub boundary:** the first message on a connection MUST be `hello`; anything else, a second `hello`, or a server→device kind arriving inbound (`hello_ack`, `push_ack`, `event_batch`, `catchup_response`, `quarantine_notice`, `purge_command`) throws `ProtocolViolationError` (adapter closes the socket — no error wire kind exists in the closed PROTOCOL.md set; assumption below). Token: `verifyDeviceToken(token) → { org_id, branch_id, device_id } | null` is the **named auth seam**; at Wave 0 it accepts exactly the unsigned base64url-JSON dev-token shape (claims `{ org_id, branch_id, device_id }`) — malformed shape or claims mismatching `hello.device_id`/`hello.branch_id` → `AuthRejectedError`, no session. T-01-09 swaps the seam's internals for `jose` verification + device-registry/revocation checks with the same claims contract; nothing else changes (that is the whole point of the seam). `hello` has no `org_id` field — the token claims are how the session learns its org (see assumptions). Reply: `hello_ack { session_id: newId(), hub: false, resume_from }` where `resume_from` = the device's `acked_watermark + 1` (0 when no watermark row — next `lamport_seq` the cloud expects). The session joins the (org, branch) fan-out set on `hello_ack`.
  - **Push merge (per `push { events, watermark }`, processed in array order):** per event —
    1. **Identity checks (authz class):** `org_id`/`branch_id`/`device_id` must equal the session's; mismatch → quarantine, reasons `org_mismatch | branch_mismatch | device_mismatch` (device mismatch is the DEC-SYNC-004 no-proxy enforcement).
    2. **Registry parse (01-F4):** `parseEvent` from `@restos/domain`; unknown type or invalid payload → quarantine, reason `schema_invalid` (stored verbatim — an N+1 client's new type loses nothing, 00 §6).
    3. **Dedupe (01-F8):** `id` already in `kernel.events` with identical envelope content (ignoring the two cloud-stamped values) → skip, counts toward the watermark. Same `id`, divergent content → quarantine, reason `id_content_divergence` (never overwrite — 01-F1; loud, never silent — 18 §4). A new id at an already-occupied `(org, device, lamport)` slot → quarantine, reason `lamport_conflict`.
    4. **Contiguity:** events must extend the device's stored lamport sequence without a gap. At the first gap, processing of the remaining batch stops; nothing beyond the gap is stored (stop-at-gap — assumption below). The device re-pushes from `acked_watermark + 1`; dedupe absorbs overlap.
    5. **Merge (01-F3):** surviving events get `server_received_at = clock.now()` and `global_seq` = next value from the org's counter row, assigned in array order inside **one transaction** with the inserts and the watermark update.
    - `acked_watermark` = highest lamport that is contiguously **persisted** for the device, where persisted = merged into `kernel.events` **or** stored in `kernel.quarantine` — a quarantined event is durably held, so the ack advances over it and the device outbox never wedges on a poisoned event (assumption below; 01-F17 never-blocked spirit). `push_ack { acked_watermark }` is delivered **only after commit** (01-F2 cloud side — law 3).
    - **Fan-out (01-F9 serve half):** after commit, the merged (non-quarantined) events go out as one `event_batch` (envelopes + `global_seq`) to **every** session in the (org, branch) fan-out set, **including the origin** — that is how a device learns its own events' `global_seq` and converges to cloud ordering (01-F34 / T-01-04 `assignGlobalSeq`). Fan-out passes through `sliceFilter(session, events)` — identity at v1, the named 01-F40 seam. Quarantines additionally produce `quarantine_notice { event_id, reason }` to the pushing session in the same `handle()` call (durable redelivery when the origin is offline at notice time is T-01-08).
  - **Catchup (01-F9):** `catchup_request { from_global_seq }` (session required) → `catchup_response { events, complete, next_from }`: the session's **branch** stream, events with `global_seq > from_global_seq` (exclusive cursor — matches `hello.last_global_seq` / `store.setLastGlobalSeq` "last received" semantics), ascending, at most `CATCHUP_PAGE_SIZE = 500` (exported binding constant); `next_from` = the highest `global_seq` in the page (= `from_global_seq` when the page is empty) so the response feeds the next request unchanged; `complete` = no further branch events beyond the page at read time.
  - `ping { t }` → `pong { t }` on the same connection. `purge_command` is never emitted at Wave 0 (revocation is T-01-09+).
  - **Error taxonomy (18 §3/§5):** `ProtocolViolationError`, `AuthRejectedError` extend a `GatewayError` base; quarantine reasons are the closed string set `schema_invalid | org_mismatch | branch_mismatch | device_mismatch | id_content_divergence | lamport_conflict` (invariant-class reasons arrive with T-01-08).
  - **Thin Fastify adapter (in scope, not acceptance-gated):** `src/server.ts` — Fastify + `@fastify/websocket` (18 §5): socket frame → `decodeMessage` → `conn.handle`; sink → `encodeMessage` → `socket.send`; decode/handle errors → log (pino) + socket close. Env (`DATABASE_URL`, `PORT`) via the `packages/config` `defineEnv` factory (crash at boot on invalid env, 18 §5). One boot smoke test at most; real-socket behavior is verified at H-01-D (the first 20 §4.5 reality gate), not here.
- **Laws (each is an acceptance test; FR id in the test name per 20 §4.1):**
  1. **Global sequence (01-F3):** merged events carry `server_received_at` from the injected clock and a strictly monotonic, dense (gap-free) per-org `global_seq` in merge order; two orgs' sequences are independent; a rolled-back merge consumes nothing (counter update rolls back with it).
  2. **Idempotent push (01-F8):** re-pushing any prefix or the whole batch produces the same `acked_watermark`, zero new rows, zero re-fan-out of already-merged events; dedupe is by event `id`; divergent content under a known id quarantines and never mutates the stored row (01-F1).
  3. **Persist-before-ack (01-F2 cloud):** at the moment `push_ack` reaches the sink, every acked event is readable through an **independent** DB connection (the sink itself queries and finds the rows — commit precedes ack, observably).
  4. **Catchup pages never skip and never duplicate (01-F9):** paging from 0 with concurrent merges in flight yields every branch event exactly once in `global_seq` order across pages; `complete`/`next_from` behave per contract; page cap enforced; events of other branches and other orgs never appear (00 §5.4 isolation).
  5. **Fan-out (01-F9/01-F34):** an event pushed by device A reaches every connected same-branch session including A, exactly once, as `event_batch` with `global_seq` present; a session on another branch or org receives nothing; a closed connection is removed from the set (no send after close).
  6. **Quarantine (01-F37):** a registry-invalid event → verbatim `kernel.quarantine` row + `quarantine_notice` on the pushing session + absent from `kernel.events`/fan-out/catchup + **the watermark still advances over it** (the outbox never wedges); re-push of the same bad event is an idempotent no-op; each identity-mismatch reason class is exercised.
  7. **Stop-at-gap (01-F8/01-F3 device-half constraint):** a push whose lamports jump past the expected next stores and acks only the contiguous prefix; a later contiguous re-push completes the sequence; `kernel.events` is lamport-gap-free per device modulo quarantine rows.
  8. **Golden fixtures (20 §2.7):** every fixture under `packages/sync-protocol/src/__acceptance__/fixtures/` (read via repo-relative fs path) decodes with `decodeMessage` and drives the gateway: `hello.json` opens a session, `push.json` merges and acks, `event_batch.json`/`quarantine_notice.json` byte-shape-match what the gateway emits (modulo instance values); every gateway-emitted message round-trips `encodeMessage → decodeMessage` — client and gateway consume the same contract and cannot drift apart silently.
- **Testing approach (stated per 24 §3b):** **Testcontainers Postgres** — one real container per suite run (started in a global setup, Drizzle migrations applied programmatically at start, so migrations themselves are exercised every run); per-test isolation via fresh `org_id`s per test (org scoping is the natural partition — no TRUNCATE choreography). Rationale: 20 §1 CI row and 18 §12 **ban mocked infra in service tests**, and `testcontainers` is already in the 18 §14 registry; GitHub Actions ubuntu runners ship Docker, so the suite runs in existing CI with no remote infrastructure. Simpler alternatives named and rejected: `pg-mem` (in-process fake — exactly the banned mocked-DB class, and its SQL surface diverges from real Postgres precisely where this task lives: row locks, unique-violation semantics); `embedded-postgres` npm binaries (not in the 18 §14 registry; would need the §15 process for a test-only convenience Testcontainers already covers).
- **Explicitly NOT this task:** real-WebSocket serving as a verified behavior (H-01-D chaos E2E — the adapter ships but is boot-smoked only); device-side cloud session consuming `hello_ack`/`event_batch`/catchup (rides the chaos-E2E/client task with T-01-06's scenarios); invariant validation (01-F29 refund remainder), durable quarantine-notice redelivery, quarantine query/surfacing (T-01-08); real token verification, device registry, revocation, `purge_command` emission (T-01-09); slice filtering (01-F40, Wave 1); hub-as-cloud-uplink (DEC-SYNC-004 accepted: per-device sessions is the shipped design; hub-proxied uplink deferred, nothing here forecloses it); org-scope reference data in the stream (catalog wave); partition management, retention/archival (DEC-SYNC-003); read models (owning modules); Redis/multi-instance fan-out (Wave-1 scale); 01-N4 load assertions (Wave-1 staging).
- **Assumptions stated (24 §3b — interpretation chosen / simpler alternative named):**
  1. **Partitioning deferred.** 01 §5 says "partitioned by org + month"; v1 is a plain table with the three indexes. Partitioning now means per-org-per-month DDL automation (a partition-management job) at zero fleet size — speculative machinery (24-F23). The later conversion is a physical reorganization (attach-as-partition / table swap), not a schema break, scheduled with the Wave-1 scale task; recorded for W0-EXIT. Alternative (partition from day one) named and rejected as above.
  2. **Per-org counter row over `BIGSERIAL`.** Chosen: dense per-org sequence, assignment serialized under the row lock held to commit — which is precisely what makes law 4 (catchup never skips) provable; a shared sequence yields per-org gaps *and* the commit-visibility race (seq N+1 visible before N), forcing snapshot guards in catchup. Cost: merges serialize per org — accepted at Wave-0 volume (01-N4 binds at Wave-1 staging). 01-F3 does not require gap-free globally; density is a choice for auditability, not a claimed law of the FR.
  3. **Watermark advances over quarantined events** because they are durably stored (01-F37 storage); the alternative (ack only merged events) wedges the device outbox forever on one poisoned event — the anti-pattern 01-F17's never-blocked principle exists to prevent. Consequence: the Auditor's per-device lamport-gap check must treat quarantine rows as filling their slots — noted for T-01-11.
  4. **Stop-at-gap push processing** over store-beyond-gap: keeps `kernel.events` contiguous per device with a stateless contiguity rule; the device re-pushes from the ack anyway (19 §5), and id-dedupe absorbs the overlap. Store-beyond-gap is harmless but buys nothing and complicates the watermark computation.
  5. **Fan-out includes the origin session** — this is the delivery path by which a device learns its own events' `global_seq` (T-01-04's `assignGlobalSeq`; 01-F34 "converge to cloud ordering on ack"): `push_ack` carries only the watermark, so without origin fan-out a device needs a catchup round-trip to converge. Alternative (exclude origin) named and rejected as slower convergence for zero saved complexity (id-dedupe makes the duplicate delivery free).
  6. **Catchup cursor is exclusive** (`from_global_seq` = highest seq already held): matches `hello.last_global_seq` ("last received") and T-01-03's `store.setLastGlobalSeq` semantics; `next_from` feeds the next request unchanged. PROTOCOL.md doesn't pin inclusivity — flagged for a one-line clarification PR at W0-EXIT (same class as the `resume_from` LAN note from T-01-05).
  7. **Dev-token stub carries org claims.** `hello` has no `org_id` field, and the session must know its org before any push (catchup + fan-out registration are org/branch-scoped) — so the Wave-0 stub token is unsigned base64url JSON claims `{ org_id, branch_id, device_id }` behind the `verifyDeviceToken` seam; T-01-09 replaces the internals with `jose` + registry/revocation, same claims out. Alternative (trust `hello` fields, learn org from the first pushed envelope) rejected: leaves the session org-less for catchup and inverts the 18 §5 server-side-validation posture. The stub is validation of *shape and consistency* only — stated plainly so nobody mistakes it for auth (DEC rows T-01-09/W1).
  8. **Acceptance tests drive the core module surface in-process; transport is a seam.** Real sockets in the acceptance suite would be nondeterministic and duplicate H-01-D's rung — 20 §1 places real-process scenarios in the containerized sim-branch. The Fastify adapter still ships (18 §5 names the stack; H-01-D needs a bootable service) but thin and boot-smoked.
  9. **In-memory session registry; single gateway instance at Wave 0.** Multi-instance fan-out needs a pub/sub backplane (Redis is registry-allowed) — that is Wave-1 scale work ("scales separately from api", 18 §2); building it now is 24-F23 speculative flexibility. Named as a seam, not built.
  10. **No error wire kind:** PROTOCOL.md's message set is closed; protocol violations log + close the connection (WS close code at the adapter). Adding an `error` kind would be a PROTOCOL.md spec-review event — not owed at Wave 0.
  11. **`text` columns for ids**, not `uuid`: the envelope schema types ids as non-empty strings (T-01-01), not format-validated UUIDs; the storage layer must not tighten the wire contract (a foreign-format id would otherwise fail at the DB instead of at validation, bypassing quarantine).
  12. **`envelope jsonb` stored verbatim as received**, cloud-stamped values in their own columns and merged at serve time — the stored payload is exactly what the device signed off on (bytes-level fidelity for the future 01-F5 hash chain), and no row update ever back-fills anything (01-F1).
- **Fix-round amendments (adversarial review; each is binding contract text):**
  1. **In-batch dedupe (blocker):** the step-3 dedupe view includes events merged earlier in the SAME transaction — a repeated id within one push dedupes (identical content → skip and count toward the watermark; divergent → `id_content_divergence`), never reaching a duplicate-PK crash that aborts sibling events and wedges the outbox (verified: re-push of the same batch could never succeed).
  2. **Uniform persisted-slot rule (blocker; DEC-SYNC-005):** EVERY quarantine class fills its lamport slot for the watermark computation — `id_content_divergence` included (the shipped code filled identity/schema classes only; a divergent event beyond the frontier wedged everything after it).
  3. **`storage_reject` joins the closed reason set**, and `kernel.quarantine.envelope` becomes `text` (verbatim JSON string): bytes Postgres cannot faithfully hold in `jsonb` (e.g. ` ` in any string — passes Zod, aborts the insert) quarantine as `storage_reject` instead of aborting the merge; per-event savepoint isolation makes any single event's storage failure quarantine that event and never abort siblings or suppress the ack. `kernel.events` keeps `jsonb` (only storable events reach it, by construction).
  4. **No `push_ack` when nothing is contiguously persisted** for the device (`through < 0` — empty push, or gap-at-start on a fresh device): an ack of 0 would claim slot 0 is held; mirrors the LAN hub's guard (mesh-session sends acks only at `acked ≥ 0`).
  5. **`handle()` serializes per connection** — a frame never begins processing before the previous frame settles (kills the double-`hello` TOCTOU that could register one connection in two orgs' fan-out sets, 00 §5.4; the shipped ws adapter dispatches frames un-awaited, so the race was production-reachable).
  6. `@restos/domain` + `@restos/sync-protocol` are **runtime `dependencies`** (parseEvent/codec sit on production paths; the drafted dev classification would break a pruned deploy). The `packages/config` `exports` entry is ratified as binding (mechanically required; form identical to sibling packages).
  7. **Golden-fixture corrections (20 §2.7 spec-review, planner-approved):** `push.json`'s `order.created` payload gains the registry-required `channel`; `hello.json`'s token becomes the Wave-0 dev-token shape (unsigned base64url claims). Both landed by the gateway's test-owning session; law 8 then drives every fixture verbatim (its in-memory substitutions are removed). The T-01-02 codec suite must stay green — fixtures remain valid wire messages.

### T-01-10  Audit hash-chain emitter + verification helper (closes 01-F5) — the unscheduled kernel gap

- **FRs:** 01-F5 (audit events — `audit.*`, per 01 §4 admin family — are ordinary kernel events, hash-chained per device: each carries the hash of the device's previous audit event). **Constraints honored, not closed here:** 01-F1/F2/F3 (the chain is stamped inside the T-01-03 append transaction — no event row is ever updated; the chain field is stamped once, at first append, atomically with the durable insert), 01-F4 (audit payloads validate through the domain registry like every other type), 01-F6/F34 (audit events fold to nothing — they carry no order/line/money state; they must not perturb `open_orders`/`kitchen_queue`/`parked`), 01 §7 ("audit hash-chaining … not configurable, ever" — this task implements that platform law; it is not a policy choice). **Depends on / consumed by:** the Auditor (T-01-11, DEC-TEST-003) calls the `verifyAuditChain` helper this task exports over the merged cloud log — the Auditor cannot check a chain nobody writes, which is why 01-F5 is scheduled *before* T-01-11 (wave-0-scope.yml `pending: 01-F5 by T-01-10`).
- **Wave-0 basis:** 01-F5 is kernel ledger law and 01 §7 pins audit hash-chaining as non-configurable platform law; it is currently `unmapped` in `conformance/01.yml` and appears in no landed task — a genuine scheduling hole (survey contradiction C4, ratified: DEC-TEST-003 schedules the emitter here). This is a small task (1 FR).

- **Files touchable:**
  - `packages/domain/src/registry.ts` (+ `packages/domain/src/index.ts` re-exports) — the `audit.*` payload schemas, the `AUDIT_EVENT_TYPES` set / `isAuditEvent(type)` predicate, and the two pure chain primitives `auditEventHash(envelope)` + `verifyAuditChain(events)` (impl session; **protected path — `packages/domain` is SACRED (18 §2), senior review mandatory; schemas declared once here, never redeclared**). All added types live under the existing `audit.*` catalog wildcard (01 §4), so **no new FR** — the payload schemas + the hashing/verify helpers are new domain *surface* implementing already-binding law (01-F5 + 01 §7). New event-type/config additions require a spec PR first (commandment 2 / new-event-type rule) — see **Spec-PR precondition** below; the code lands only after it merges.
  - `packages/domain/src/canonical.ts` (new; impl session) — the single canonical-JSON serializer (keys sorted by UTF-16 code unit at every depth, no insignificant whitespace) pulled to a declared-once home; `folds/replay.ts`'s `canonicalJson` already implements exactly this rule but lives in `sync-client` and cannot be imported by `domain` (dependency direction). Domain owning the serializer is the correct declare-once placement; the `sync-client` copy converging onto it is scheduled consolidation (W0-CONS), **not** a drive-by here (24 §3b surgical-diffs).
  - `packages/domain/package.json` + root lockfile — add the hashing dependency (see hash decision; after the 18 §14 registry PR).
  - `packages/sync-client/src/device-store.ts` (+ its `SCHEMA`) — the per-device audit-chain HEAD table, the append-time stamp for audit-typed events, and the peer-audit passthrough in `ingest` (impl session; **protected path (20 §4.4), senior review**). Additive only: zero change to the non-audit append/ingest/fold paths, zero change to `nextBatch`/`advanceTo`/`status` shapes.
  - `packages/domain/src/__acceptance__/**`, `packages/sync-client/src/__acceptance__/**` (test session only).
- **Check:** `pnpm --filter @restos/domain test` + `pnpm --filter @restos/sync-client test` + `pnpm verify:01` + root `pnpm verify`. 01-F5 flips `unmapped → green` in `conformance/01.yml` (the derivation is automatic — the test titles carry `01-F5`).
- **DoD rung:** D1 (chain-emit + chain-verify properties and directed cases in-suite; the cloud-side continuous chain check over the merged Postgres log is T-01-11's Auditor rung, which *consumes* `verifyAuditChain`).
- **Status — LANDED, two adversarial reviews clean on the crypto (cross-plane hash stability, byte-level forge-resistance, in-sequence tamper/reorder/drop all detected, fold-inertness verified).** domain 42/42, sync-client 142/142, root verify exit 0, verify:01 27 green / 0 red (01-F5 closed). Carried forward: **(tail-protection — the load-bearing one, now a T-01-11 requirement + spec text)** a bare prev-hash chain does NOT detect truncation/mutation of the *last* events or a forged tip — `verifyAuditChain` alone returns ok; this is closed by the Auditor (T-01-11) cross-checking each device's chain against the merged cloud log (which independently holds every audit event), now written into 01-F5. **(re-append guard ordering — note)** re-appending a stored audit id with a caller-supplied `prev_audit_hash` is a silent no-op (dedupe short-circuits before the forge-guard) rather than the throw Law 5 literally states — safe (the retry is reconstructed from the stored link and compared), a guard-ordering deviation to tidy at consolidation. **(fold-inertness coverage — note)** inertness holds by construction (audit events are filtered out of the fold inputs entirely) but the fold *property* generator wasn't extended with audit events + equal-timestamp cases; fold into the 24-F13 property campaign. **(governance fix applied)** DEC-AUDIT-001 was accepted but not promoted into spec 01 §4/§5 — now promoted (01-F5 concretized); the one-token oracle typecheck defect (a strict-mode index access) was fixed by the test session (no assertion changed).
- **Depends on:** T-01-01 (registry/envelope/states), T-01-03 (device store + append transaction — the stamp rides its transaction), T-01-04 (fold engine — audit events must fold to no-op; the existing fold properties regression-guard this). Independent of the gateway (T-01-07 stores `envelope` jsonb verbatim, so `prev_audit_hash` in the payload survives merge/fan-out/catchup with zero gateway change) and of T-01-08.

- **Design decisions (each stated with the simpler alternative named, 24 §3b):**

  1. **Which events are audit events — the `audit.*` family (5 concrete subtypes).** 01-F5's list (login, drawer open, reprint, threshold override, settings change) maps to `audit.login`, `audit.drawer_opened`, `audit.reprint`, `audit.threshold_override`, `audit.settings_changed`. The 01 §4 catalog already sanctions `audit.*` as a canonical admin-family wildcard, so these concrete subtypes are canonical — the task pins them (no new FR). `AUDIT_EVENT_TYPES` is the closed set of these five; `isAuditEvent(type)` tests membership.
     - *Simpler alternative named & rejected:* treat "audit events" as a cross-cutting *classification* over existing operational types (chain `cash.drawer_opened`, `receipt.reprint_requested`, `config.changed`, …). Rejected: it forces the chain rule into many modules' event types (spreading platform law, 01 §7), and it entangles the tamper-evident audit trail with operational fold state (those events carry order/money payloads the chain must not depend on). A dedicated `audit.*` channel keeps the chain a single, self-contained, fold-inert concern. **Flag for the spec PR / DECISIONS:** whether an operational `cash.drawer_opened` *also* emits a paired `audit.drawer_opened` is doc-02/POS wiring, not kernel — named as out of scope so nobody reads T-01-10 as closing the drawer-audit UX.

  2. **Where the prev-hash lives — in the audit-event PAYLOAD (`prev_audit_hash: string | null`), NOT the envelope.** Decisive technical reason grounded in the landed shapes: `EventEnvelope = z.object({…})` (packages/domain/src/envelope.ts) is a *strict* Zod object — `parseEnvelope` **strips any unknown key**, and `WireEnvelope`/`parseMessage` (sync-protocol) likewise. An envelope-level `audit_prev_hash` would therefore be silently dropped on parse/encode unless added to the sacred all-event `EventEnvelope` schema (and `WireEnvelope`, and every consumer). By contrast `payload` is `z.unknown()` and is preserved verbatim, and the `audit.*` registry schemas are `looseObject` (extras pass through) — so a payload field rides through append → codec → gateway `jsonb` verbatim → fan-out → catchup → fold with zero change to any shared surface. Adding a field to the envelope would be additive under 00 §6 but pollutes the schema of every one of the ~50 event types for a concern touching 5.
     - *Simpler alternative named & rejected:* an optional envelope field `audit_prev_hash`. Rejected for the strip-on-parse hazard + editing the protected `EventEnvelope`/`WireEnvelope` for a 5-type concern; payload placement is both safer and more surgical.

  3. **Hash function — SHA-256 (hex) over the canonical JSON of the envelope with `server_received_at` OMITTED, via `@noble/hashes/sha2`.**
     - **What is hashed:** `auditEventHash(env) = sha256_hex( canonicalize( { …env, server_received_at: <key deleted> } ) )`. `server_received_at` is **excluded** because it is `null` on-device at emit and an integer after cloud merge — a hash covering it would differ device-side vs cloud-side and break cross-plane verification (the device stamps `prev_audit_hash` from the on-device hash; the Auditor recomputes cloud-side and must get the identical value). `global_seq` is naturally excluded (it is not an `EventEnvelope` field — `parseEnvelope` strips it; it lives in the wire extension / sidecar only). Everything else — `id`, `device_id`, `lamport_seq`, `device_created_at`, `type`, `schema_version`, `payload` (including this event's own `prev_audit_hash`), `refs`, `actor_user_id`, `org_id`, `branch_id` — is stable and covered.
     - **Algorithm choice `@noble/hashes` (pure-JS, synchronous, cross-runtime).** Rationale: `packages/domain` is consumed by the React-Native app in a later wave; `node:crypto` is unavailable there, and Web Crypto's `subtle.digest` is **async** — unusable inside the synchronous `append` transaction (01-F2 persist-before-ack is a sync return) and the synchronous fold path. `@noble/hashes` is pure-JS, sync, audited, and computes identically on Node/Electron/RN/browser — which is exactly what "hash-chaining is platform law, computed identically on every host" (01 §7) requires.
       - *Simpler alternative named & rejected:* `node:crypto.createHash("sha256")`. Rejected: breaks on RN and pins the platform-law hash to a Node-only API. **Precondition:** `@noble/hashes` must be added to the 18 §14 dependency registry (a small spec PR to doc 18 — see Spec-PR precondition).
     - The canonical serialization rule is **byte-pinned** (UTF-16-code-unit key sort at every depth, no whitespace) for the same reason FOLDS.md pins `json_lines` (20 §4.2): a future non-JS refold/verify implementation must byte-agree.

  4. **Per-device audit-chain HEAD — a dedicated single-row table `audit_chain` in the device SQLite, maintained atomically with the audit-event insert.** Schema (added to `device-store.ts` `SCHEMA`): `CREATE TABLE audit_chain (id INTEGER PRIMARY KEY CHECK (id = 0), head_hash TEXT, head_event_id TEXT) STRICT;` seeded `(0, NULL, NULL)` — mirroring the existing single-row `sync_state` pattern. It holds **only this device's own chain HEAD** (the store is single-device); it exists so the store can stamp the *next* own audit event's `prev_audit_hash` in O(1) without scanning + type-filtering the ledger. Peer devices' chains are **not** tracked locally — they are verified by the Auditor from the merged log (T-01-11).
     - *Simpler alternatives named:* (a) add `head_hash`/`head_event_id` columns to `sync_state` — rejected: mixes the tamper-evident audit head with the sync write-checkpoint (two unrelated concerns; muddies both); a dedicated table keeps the chain self-contained and the verify story clear. (b) derive HEAD on demand by scanning own events for the highest-lamport `audit.*` and re-hashing — rejected: O(ledger) per audit append and needs type-filtering over `envelope` JSON (type is not a column).

  5. **How `append()` stamps the chain — for audit-typed events only, store-owned like `lamport_seq`.** In `appendTx` (device-store.ts), the existing dedupe-first ordering is preserved:
     - The `input.payload` of an audit event **MUST NOT** contain `prev_audit_hash` — the chain position is store-owned platform law (01 §7), not caller-controlled. A caller-supplied `prev_audit_hash` throws (loud, 18 §4), nothing persisted.
     - On **first** append of the id (non-duplicate path): read `audit_chain.head_hash` (NULL ⇒ this device's first audit event ⇒ `prev_audit_hash = null`); set `payload.prev_audit_hash = head`; build + `parseEvent` the envelope (the `audit.*` schema requires `prev_audit_hash` present as `string | null`, so validation passes post-injection); `insertEvent.run(...)`; then `UPDATE audit_chain SET head_hash = auditEventHash(envelope), head_event_id = envelope.id WHERE id = 0` — **all inside the one existing append transaction**, so the chain HEAD is atomic with the durable ledger row (01-F2/F3). Non-audit append is byte-for-byte unchanged.
     - On **re-append of a stored id** (idempotent crash-retry, 18 §4): the existing `byId.get(input.id)` short-circuit runs *before* any stamping, so it returns the stored envelope unchanged and moves the HEAD **not at all** (idempotent). The divergence check reconstructs the retry envelope using the **stored** event's `payload.prev_audit_hash` (never the live HEAD) so an identical retry compares equal and divergent content still throws — the T-01-03 §01-F8 law extended to cover the store-owned chain field.
     - **Fold inertness:** `applyFold({ envelope, global_seq: null })` still runs (unchanged), but `audit.*` is not a consumed fold type (the fold engine's `attempt` has no `audit.*` case) — it applies to no order, parks nothing, touches no state table. The existing fold determinism/refold-equivalence properties (T-01-04 laws 1–8) regression-guard this with an audit event interleaved.
     - *Peer path (`ingest`)*: a peer's `audit.*` event already carries `prev_audit_hash` in its payload (stamped by its origin device); `ingest` persists it to `peer_events` verbatim and does **not** touch the local `audit_chain` HEAD (that head is this device's own chain only). No stamping, no chain maintenance for peers on-device.

  6. **The verification contract for the Auditor (T-01-11) — `verifyAuditChain`.** Pure, in `packages/domain`, so the device store, property tests, and the cloud Auditor all consume one rule (18 §2 declare-once):
     - `verifyAuditChain(events: readonly EventEnvelopeT[]): { ok: true } | { ok: false; broken_at: string; expected_prev: string | null; found_prev: string | null }`.
     - **Precondition (caller's job, documented):** `events` is exactly one device's `audit.*` events in ascending `lamport_seq` order (the Auditor filters `type ∈ AUDIT_EVENT_TYPES` for one `device_id` and orders by lamport from the merged log). The helper does not sort or filter — it validates the linkage of the sequence it is given.
     - **Rule:** walking the sequence, the first event's `payload.prev_audit_hash` MUST be `null`; each subsequent event's `payload.prev_audit_hash` MUST equal `auditEventHash(previous)`. First mismatch ⇒ `{ ok: false, broken_at: <event id>, expected_prev, found_prev }`. An unbroken chain over ≥ 0 events ⇒ `{ ok: true }` (empty ⇒ ok).

- **Registry additions (binding — `packages/domain`; all under the `audit.*` catalog wildcard, 01 §4):**
  - Five `looseObject` payload schemas keyed `audit.login | audit.drawer_opened | audit.reprint | audit.threshold_override | audit.settings_changed`, each requiring exactly `prev_audit_hash: z.union([z.string().min(1), z.null()])` and passing extras through (business fields — actor context, the overridden threshold value, the changed setting key — are additive and land with their emitting modules: doc 05 threshold override, doc 14/15 settings; the envelope's `actor_user_id` already carries "who"). Minimum-that-closes-01-F5 (24 §3b): the chain field is the whole payload contract at v1.
  - `AUDIT_EVENT_TYPES` (the five keys) + `isAuditEvent(type: string): boolean`.
  - `auditEventHash(envelope: EventEnvelopeT): string` and `verifyAuditChain(...)` (contracts above), re-exported from `index.ts`.

- **API contract (binding for the test session):**
  - **`@restos/domain`** exports `AUDIT_EVENT_TYPES`, `isAuditEvent`, `auditEventHash`, `verifyAuditChain`, and the five registry schemas (reachable through `parseEvent`). `parseEvent` on an `audit.login` envelope with `payload.prev_audit_hash: null` succeeds; with the key absent → parse failure (01-F4); with a non-string/non-null value → parse failure.
  - **`@restos/sync-client` `DeviceStore` (additive to T-01-03/04):**
    - `append(input)` — unchanged signature; for `isAuditEvent(input.type)`: stamps `prev_audit_hash` per decision 5. Caller supplying `prev_audit_hash` in an audit payload throws, nothing persisted. Returned envelope carries the store-stamped `prev_audit_hash`.
    - `auditChainHead(): { hash: string; event_id: string } | null` — the current own-device audit HEAD (null before the first own audit append). The one new public surface (observability + the test session's chain-advance assertion).
    - Every existing surface (`ingest`, `ingestBatch`, `nextBatch`, `advanceTo`, `readOwnEvents`, `readAllEvents`, `openOrders`, `kitchenQueue`, `parked`, `refold`, `status`, `assignGlobalSeq`, `setLastGlobalSeq`, `close`) unchanged in shape and behavior; audit events flow through `readOwnEvents`/`readAllEvents`/`nextBatch` like any own event (they push to the cloud and fan over LAN normally).

- **Laws (each an acceptance test; FR id in the test name per 20 §4.1):**
  1. **First audit event links to null (01-F5):** the device's first-ever `audit.*` append returns an envelope with `payload.prev_audit_hash === null`; `auditChainHead()` becomes `{ hash: auditEventHash(env), event_id: env.id }`.
  2. **Each links to the prior (01-F5):** a run of N audit appends (interleaved with ordinary order/payment appends that must NOT advance the chain) yields, for each audit event k>0, `payload.prev_audit_hash === auditEventHash(audit_{k-1})`; non-audit appends between them leave `auditChainHead()` unchanged.
  3. **`verifyAuditChain` accepts a real chain, pinpoints a break (01-F5):** the device's own audit events in lamport order verify `{ ok: true }`; mutating any middle event's stored `prev_audit_hash`, or reordering, or dropping the middle event, yields `{ ok: false, broken_at }` at the first bad link (property over generated chains).
  4. **Cross-plane hash stability (01-F5/01-F3):** `auditEventHash` of an envelope with `server_received_at: null` equals `auditEventHash` of the same envelope with `server_received_at` set to any integer — the stamped `null` and the cloud-merged integer hash identically (this is what lets the Auditor verify the cloud copy against device-stamped links).
  5. **Caller cannot forge chain position (01-F5/01 §7):** `append` of an audit event whose payload already contains `prev_audit_hash` throws and persists nothing; the chain HEAD is unmoved.
  6. **Idempotent re-append preserves the chain (01-F8 extension):** re-appending a stored audit id with identical content returns the stored envelope unchanged and does not advance HEAD; divergent content throws; HEAD unmoved in both cases.
  7. **Audit events are fold-inert (01-F6/F34 regression):** a delivery sequence containing `audit.*` events yields the same `orders`/`queue`/`parked` tables (byte-identical `json_lines`) as the same sequence with the audit events removed; `refold()` equivalence holds (the T-01-04 properties, extended with audit events in the generator).
  8. **Peer audit events carry their origin link, unstamped locally (01-F5):** an `ingest`ed peer `audit.*` event keeps the `prev_audit_hash` its origin device set (verbatim in `peer_events`), and does not touch this device's `auditChainHead()`.

- **Spec-PR precondition (commandment 2 / new-event-type rule; 00 §8 template):** a spec PR to **`specs/01-kernel-sync.md` §4 + §5** and **`specs/18` §14** must land *before* the code:
  - 01 §4/§5: pin the five concrete `audit.*` subtypes under the existing wildcard; record that the per-device chain link lives in the **payload** as `prev_audit_hash: string | null`; record the canonical hash rule (SHA-256 over canonical-JSON of the envelope minus `server_received_at`). None are new FRs — 01-F5 + 01 §7 already bind the behavior; these are the concretizations the code needs (never renumber/delete existing IDs, 00 §8).
  - 18 §14: add `@noble/hashes` to the dependency registry (the §15 registry-addition process).
  - `pnpm docs:lint` before finishing (the PostToolUse hook runs it on spec edits).

- **Explicitly NOT this task:** the cloud-side continuous audit-chain check over the merged Postgres log (T-01-11 Auditor — *consumes* `verifyAuditChain`); the business-field payloads of each audit subtype (their emitting modules: doc 05 threshold override, doc 14/15 settings/reprint — additive under the `looseObject` schemas); pairing operational events (`cash.drawer_opened`) with audit events (doc-02/POS wiring); the `sync-client` `canonicalJson` → `domain` `canonical` consolidation (W0-CONS); any gateway change (envelope `jsonb` is verbatim — the payload chain field survives untouched).

- **Candidate DECISIONS.md rows the founder should ratify:**
  - **DEC-AUDIT-001 — Audit hash-chain rule.** Ratify: chain link in the audit-event *payload* (`prev_audit_hash`), SHA-256-hex over canonical-JSON-minus-`server_received_at`, `@noble/hashes` (pure-JS, cross-runtime, sync) as the platform-law hash. Authority once promoted: 01-F5 / 01 §7. (Not strictly a cross-cutting decision — it's a 01 spec pin — but the hash-algorithm/serialization choice is platform law worth an explicit sign-off, and RN portability drove it.)
  - **DEC-AUDIT-002 — audit.* vs operational-event relationship.** Ratify the dedicated `audit.*` channel (5 subtypes) as the tamper-evident trail, separate from operational events, and defer "does `cash.drawer_opened` also emit `audit.drawer_opened`" to the POS wave (doc 02). Prevents a later double-modeling drift.

### T-01-08  Gateway validation + quarantine completion — invariant checks, durable notices, quarantine query

- **FRs (honesty ledger — what deepens vs what closes):**
  - **Enforcement-deepening on already-green rows (this is the point of the task, not a new-FR closure):** 01-F29 (the *fold-enforced / violators-quarantine* clause: `payment.refunded` amount ≤ the un-refunded remainder — currently green only via the domain payload *shape* + the device-fold "refund unsettles" test; the **server-side quarantine of an over-refund** is unclosed and lands here). 01-F37 (the **invariant-class** quarantine — currently green for schema/identity classes only; the new `invariant_violation` reason class closes the "failing … invariant validation … is quarantined" clause). 01-F30 (conservation — the **executable-in-`packages/domain`** refund-remainder invariant lands here and the gateway enforces the fold-free part; full settle-time conservation stays the Auditor's refold job, T-01-11, named below).
  - **New durable surfaces (T-01-07 explicitly deferred these):** durable `quarantine_notice` redelivery when the origin is offline at notice time (T-01-07 sent notices live-only, in-`handle()`); quarantine query/surfacing seam for fleet health (doc-15 seam — the query lands here, the dashboard is doc 15).
  - **Constraints honored, not closed:** 01-F1 (no update/delete of `kernel.events`; the notice outbox and quarantine table are the only new writes — additive migrations, 18 §4), 01-F2 (notice-outbox row commits in the merge transaction with the quarantine row — persist-before-notify), 01-F17 (a sale is never blocked: an invariant quarantine *fills its lamport slot* exactly like every other class — DEC-SYNC-005 — the ack advances, the outbox never wedges), 01-F40 (fan-out `sliceFilter` still identity), DEC-SYNC-004 (per-device sessions; the notice outbox is keyed by the origin device).
- **Wave-0 basis:** 01-F37 is kernel law; T-01-04/05/07 all explicitly punted "quarantine machinery / invariant validation is a later task" and named this as that task; the `quarantine_notice` wire message landed in T-01-02 and the quarantine *storage* + reason taxonomy landed in T-01-07. wave-0-scope.yml keeps 01-F29/F30/F37 `in_scope: closed` (shape/schema level) — T-01-08 hardens the enforcement sub-clauses; it does not flip a `pending` FR (that honesty matters: 01-F5→T-01-10, 01-F7→T-01-11, 01-F38→T-01-06 are the only pending rows).

- **Files touchable:**
  - `services/sync-gateway/src/gateway.ts` (impl session; **protected path 20 §4.4, senior review**) — the invariant-check seam in the merge loop, the notice-outbox insert (in-transaction) + the on-`hello` notice drain. Additive: the merge pipeline's existing steps 1–5 and the fan-out/catchup paths keep their shape; the invariant check is a new step 3.5, the notice outbox is a new post-quarantine write + a new hello-time drain.
  - `services/sync-gateway/src/errors.ts` (impl session; protected) — add `invariant_violation` to the closed `QuarantineReason` union (the taxonomy extension T-01-07 reserved: "invariant-class reasons arrive with T-01-08").
  - `services/sync-gateway/src/schema.ts` + `services/sync-gateway/drizzle/**` (impl session; protected; **migrations are append-only, never edited after application, 18 §4**) — the new `kernel.quarantine_notices` outbox table.
  - `services/sync-gateway/src/quarantine-query.ts` (new; impl session) + `services/sync-gateway/src/index.ts` re-exports — the fleet-health read seam.
  - `packages/domain/src/money.ts` (or new `packages/domain/src/invariants.ts`) + `index.ts` re-export (impl session; **protected path — `packages/domain` SACRED, senior review**) — the one executable invariant this task's consumer needs, per 01-F30's mandate that conservation invariants are "executable in `packages/domain`".
  - `services/sync-gateway/src/__acceptance__/**` (test session only); `packages/sync-protocol/src/__acceptance__/fixtures/` — test session may **add** a fixture (editing an existing one is a 20 §2.7 spec-review event, not this task).
- **Check:** `pnpm --filter @restos/sync-gateway test` + `pnpm --filter @restos/domain test` + `pnpm verify:01` + root `pnpm verify`. **Environment prerequisite: local Docker** (Testcontainers Postgres — same as T-01-07; the suite fails loudly when Docker is absent, never silently skips; 18 §12 bans mocked infra).
- **DoD rung:** D1 (acceptance suite green against real Postgres in-suite; the continuous-invariant cloud sweep and settle-time conservation are the Auditor's D-rung, T-01-11).
- **Depends on:** T-01-07 (gateway core — merge pipeline, quarantine table, session/fan-out; this task extends it additively, no rework of landed surfaces), T-01-02 (`quarantine_notice` wire message), T-01-01 (registry `parseEvent`). NOT on T-01-04/05: the gateway merges envelopes, it folds nothing — the refund-remainder check is a targeted read against `kernel.events`, not a fold.

- **Design decisions (each with the simpler alternative named, 24 §3b):**

  1. **Where invariant validation runs — inside the merge pipeline (`handlePush`), as a named seam, step 3.5 (after registry-parse + dedupe, before the merge insert).** The seam: `checkInvariants(tx, session, envelope): Promise<QuarantineReason | null>` — returns `"invariant_violation"` (quarantine) or `null` (proceed). At v1 it implements exactly one concrete rule (refund-remainder, decision 2); it returns `null` for every non-refund type. A violation calls the existing `quarantine(envelope, "invariant_violation")` helper — which already **fills the lamport slot** (fix-round amendment 2 / DEC-SYNC-005), so the ack advances and the outbox never wedges (01-F17). Sequencing note: the check runs only for events that survived identity + registry + dedupe + the `lamport_seq === through + 1` contiguity gate, i.e. events about to merge — so it never fires on a stop-at-gap-skipped or already-deduped event.
     - *Simpler alternative named & rejected:* a separate post-merge async validator (a job that scans merged events and retro-quarantines). Rejected: 01-F37 says validation happens *at* the merge boundary ("at hub or cloud"), and retro-quarantining an already-fanned-out, already-`global_seq`'d event violates append-only merge semantics (you cannot un-merge). Inline-at-merge is the only place a violator never enters `kernel.events`.

  2. **How refund-remainder (01-F29) is checked server-side — a targeted read against `kernel.events`, no fold.** For a surviving `payment.refunded` event with `payload.payment_id = P` and `payload.amount_paisa = A`:
     - Read the parent: `SELECT envelope FROM kernel.events WHERE org_id = $org AND id = P AND (envelope->>'type') = 'payment.recorded'` (inside the merge tx). **If the parent is not (yet) merged → return `null` (do not quarantine).** An unprovable violation is not a violation (a sale is never blocked, 01-F17; the out-of-order refund merges, the device fold parks it, the Auditor's refold catches any true inconsistency later — 01-F37's "surfaced to fleet health"). This is the honest fold-free boundary: the gateway quarantines only a **provable** over-refund.
     - Read prior refunds already merged against P: `SELECT envelope FROM kernel.events WHERE org_id = $org AND (envelope->>'type') = 'payment.refunded' AND (envelope->'payload'->>'payment_id') = P`. `priorTotal = Σ amount_paisa`.
     - Add refunds earlier **in this same batch** against P (the in-tx accumulator, mirroring the step-3 in-batch dedupe view — two refunds for one payment in one push must see each other).
     - Violation iff `A > parent.amount_paisa − priorTotal`, computed via the domain invariant (decision 4). Concurrency across devices/pushes is already safe: the org-counter `FOR UPDATE` lock serializes merges, so the second concurrent refund sees the first committed (assumption noted).
     - *Simpler alternative named & rejected:* build a cloud money read-model / projection (payments-and-refunds-per-order) and check against it. Rejected as speculative (24-F23): 01-F7 read models are per-module and land with their owning waves; a targeted indexed read against `kernel.events` (the `envelope` jsonb is GIN-able, and payment volume per order is tiny) closes the FR without a new projection to maintain and rebuild.

  3. **Durable notice redelivery — a `kernel.quarantine_notices` outbox, keyed by origin device, drained on next `hello`.** Table: `(id text PK, org_id, branch_id, device_id, claimed_event_id, reason, created_at bigint, delivered_at bigint NULL)`, `UNIQUE(org_id, claimed_event_id)` (idempotent with the quarantine row — one notice per claimed id, first wins). Flow:
     - **On quarantine (any class, in the merge tx):** `INSERT … ON CONFLICT (org_id, claimed_event_id) DO NOTHING` with `delivered_at = NULL`, committed atomically with the quarantine row (persist-before-notify, 01-F2). The existing post-commit **live** `quarantine_notice` send to `record.sink` stays (fast path) — and, because the origin *is* the connected pusher at push time, that live send succeeds; on success the row is marked `delivered_at = now`. (Marking runs post-commit best-effort; a crash before marking simply leaves the row undelivered → redelivered on next hello. At-least-once, never lost.)
     - **On `hello` (session open, after `hello_ack`):** `SELECT … WHERE org_id = $org AND device_id = $device AND delivered_at IS NULL ORDER BY created_at` → send each as a `quarantine_notice`, then `UPDATE … SET delivered_at = now`. This is the "origin offline at notice time" path: a notice enqueued while the device had no open session is delivered on its next connect.
     - *Simpler alternative named & rejected:* a `quarantine_notice_ack` wire message for true exactly-once. Rejected: adding a wire kind is a PROTOCOL.md spec-review event (closed message set) and Wave-0 doesn't need it — mark-on-send + redeliver-if-undelivered is at-least-once, and the device's `status().quarantined` is a set the UI dedupes by `event_id` (the cloud-session already appends by event_id; belt-and-suspenders device-side dedupe is a named follow-up, not required here). Recorded as a candidate DEC row so the founder can choose the stronger guarantee if wanted.

  4. **The executable invariant lives in `packages/domain` (01-F30 mandate), minimal.** Add `refundRemainderExceeded({ payment_amount_paisa, prior_refunds_total_paisa, this_refund_paisa }): boolean` (or `unRefundedRemainder(...) : Paisa`) — a pure integer-paisa function (branded types, `subPaisa`/`sumPaisa` already exist). The gateway supplies the three numbers from its `kernel.events` reads; the *rule* is declared once in domain and is the same function the Auditor (T-01-11) and 01-F30's property tests call.
     - *Simpler alternative named & rejected:* also add the full order-conservation equation (`Σ payments − Σ refunds = billed − voids − comps − discounts`) to domain now. Rejected as speculative (24 §3b minimum-code): its only consumer is the Auditor's settle-time conservation sweep (T-01-11, which refolds cloud-side); it needs the full order fold, which the gateway does not compute. It lands with T-01-11, declared once in domain then. T-01-08 adds only the invariant its live consumer (the gateway) needs.

  5. **Quarantine query seam for fleet health (doc-15 seam).** `listQuarantine(db, { org_id, branch_id?, device_id?, limit? }): Promise<QuarantineEntry[]>` in `quarantine-query.ts`, returning the verbatim quarantine rows (`claimed_event_id, device_id, reason, received_at, envelope` — envelope parsed from the `text` column) ordered by `received_at desc`, page-capped. A read-only projection over `kernel.quarantine`; **the fleet-health dashboard, alerting, and the operator resolution/correction flow are doc 14/15 — explicitly out of scope** (named). Exported from `index.ts` for the doc-15 consumer.

- **Postgres data contract (binding — additive to T-01-07's four tables; migrations under `services/sync-gateway/drizzle/`, append-only, sole-writer sync-gateway, 18 §4):**
  - `kernel.quarantine_notices` — `id text PK`, `org_id text`, `branch_id text`, `device_id text`, `claimed_event_id text`, `reason text`, `created_at bigint`, `delivered_at bigint` (nullable). `UNIQUE(org_id, claimed_event_id)`; index `(org_id, device_id, delivered_at)` (the hello-drain query). No UPDATE of any column except `delivered_at` (the outbox is not the ledger — this is delivery bookkeeping, not event history, so 01-F1's no-update law does not reach it; stated explicitly).
  - `kernel.events` / `kernel.org_sequences` / `kernel.device_watermarks` / `kernel.quarantine` — **unchanged** (T-01-07 contract).

- **API contract (binding for the test session):**
  - `QuarantineReason` gains `invariant_violation` (closed set now: `schema_invalid | org_mismatch | branch_mismatch | device_mismatch | id_content_divergence | lamport_conflict | storage_reject | invariant_violation`).
  - `handlePush` behavior additions (everything else per T-01-07):
    - A surviving `payment.refunded` whose amount exceeds the parent payment's un-refunded remainder (parent merged; remainder computed over prior-merged + in-batch-earlier refunds) → quarantine `invariant_violation`, verbatim row in `kernel.quarantine`, notice enqueued + sent, the lamport slot filled (ack advances over it, DEC-SYNC-005), the event **absent** from `kernel.events`/fan-out/catchup.
    - A `payment.refunded` whose parent is **not** merged → merges normally (no quarantine): the gateway does not block an unprovable case.
    - Every quarantine (all classes) writes a `kernel.quarantine_notices` row atomically with the quarantine row and (for the connected pusher) delivers live + marks delivered.
  - `hello` behavior addition: after `hello_ack`, undelivered notices for `(org, device)` are drained as `quarantine_notice` messages through the sink, then marked delivered.
  - `listQuarantine(db, filter)` — the doc-15 read seam (shape above), exported from `@restos/sync-gateway`.
  - `@restos/domain` exports `refundRemainderExceeded` / `unRefundedRemainder` (pure, integer-paisa).

- **Laws (each an acceptance test; FR id in the test name per 20 §4.1; Testcontainers Postgres, fresh-org isolation per T-01-07):**
  1. **Over-refund quarantines (01-F29/01-F37):** merge `payment.recorded {amount: 1000}` then `payment.refunded {amount: 1200}` against it → the refund is quarantined `invariant_violation`, stored verbatim, absent from `kernel.events`/fan-out/catchup; a notice reaches the pusher; the ack advances over the slot; a following contiguous event in the same push still merges (01-F17 outbox-never-wedges).
  2. **Cumulative remainder (01-F29):** two refunds of 600 and 600 against a 1000 payment → the first merges, the second quarantines `invariant_violation`; two refunds of 400 and 600 → both merge (exact cover); the in-batch case (both refunds in one push) resolves identically to the two-push case.
  3. **Unprovable refund is not blocked (01-F29/01-F17):** a `payment.refunded` whose `payment_id` parent is not merged (out-of-order arrival) merges normally, is fanned out, gets a `global_seq` — no quarantine (the gateway blocks only provable violations).
  4. **Durable notice redelivery (01-F37):** enqueue a quarantine for a device with **no open session** (push then close, or a notice row with `delivered_at NULL` and no live session) → on that device's next `hello`, exactly the undelivered `quarantine_notice`(s) are delivered after `hello_ack`, then not re-delivered on a subsequent `hello`; the notice survives a gateway `close()`/rebuild (the row is durable).
  5. **Notice idempotency (01-F37):** re-pushing the same bad event does not create a second notice row (`UNIQUE(org, claimed_event_id)`), and does not re-deliver a delivered notice.
  6. **Quarantine query isolation (01-F37/00 §5.4):** `listQuarantine` returns one org's rows only (another org reusing the same `branch_id` string never leaks), filters by branch/device, page-caps, and returns the verbatim envelope.
  7. **Domain invariant is the shared rule (01-F30):** `refundRemainderExceeded`/`unRefundedRemainder` unit-tested on integer-paisa boundaries (exact cover is not a violation; one paisa over is); the gateway's decision is exactly this function's output (no re-implemented arithmetic at the call site).

- **Explicitly NOT this task (named deferrals):** full settle-time conservation enforcement (01-F30 `Σ payments − Σ refunds = billed − voids − comps − discounts`) — needs the order fold; the Auditor refolds cloud-side and enforces it (T-01-11), with its domain conservation function landing there; the operator **resolution / correction-event flow** for a quarantined event (01-F37 "explicit operator action producing correction events") — doc 14/15 UI; the **fleet-health dashboard / alerting** over `listQuarantine` — doc 15 (only the read seam lands here); hub-side (LAN) invariant validation (01-F27/F40 LAN auth is a stub; the LAN hub still skip-and-counts, T-01-05); a `quarantine_notice_ack` wire message / exactly-once notices (PROTOCOL.md spec-review — candidate DEC); void/comp/discount value terms (their event types land with doc 02).

- **Candidate DECISIONS.md rows the founder should ratify:**
  - **DEC-SYNC-007 — Invariant validation at the merge boundary vs the Auditor.** Ratify the split: the gateway enforces only *fold-free, provable* invariants at merge (v1: refund-remainder, 01-F29) with a new `invariant_violation` quarantine class; *fold-requiring* invariants (settle-time conservation, 01-F30) are the Auditor's refold job (T-01-11). Authority once promoted: 01-F37 / 01-F30.
  - **DEC-SYNC-008 — Quarantine-notice delivery guarantee.** Ratify at-least-once via a durable notice outbox (mark-on-send + redeliver-undelivered-on-hello), and defer exactly-once (a `quarantine_notice_ack` wire kind) as a post-Wave-0 PROTOCOL.md addition unless the founder wants the stronger guarantee now. Authority: 01-F37 / PROTOCOL.md.
