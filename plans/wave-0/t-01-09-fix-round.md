# T-01-09 review READY — F1 ruled, F2 rides, F3 recorded

**F1 (ruled, must land before T-01-11):** two-part fix. (a) **Dedupe-before-origin-gate** in the relay push path: an event already in `kernel.events` acks through regardless of the origin's current registry state — its identity was authoritative at merge time; kills the merged-AND-quarantined contradiction (scenario B) and lets a crash-replayed prefix advance the hub cursor. (b) **Hub-side volatile suppression**: on receiving a `quarantine_notice` with reason `origin_unregistered|origin_revoked`, the hub stops relaying that origin for the life of the session (fresh session retries once → re-noticed → re-suppressed; bounded, not livelock). No new wire kind. **F2 (rides):** `DEVICE_TOKEN_SECRET` validator rejects < 32 bytes. **F3 (recorded):** the mid-session-eviction DEC candidate must name the fan-out confidentiality leak — a revoked device's open session passively receives all branch events until disconnect. F4–F8 recorded as follow-ups with the token-rotation item.
