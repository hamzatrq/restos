# 03 — Kitchen & Fulfillment: Printing Service, Pass Screen, KDS, Timing Pipeline

**Module spec — Draft 1, July 2026** · Parent: `00-platform-overview.md` (conventions §5–§7 inherited), `01-kernel-sync.md`. Seed detail: `restaurant-os.md` Appendix G (printing) and Appendix B (KOT). Wave 1 (printing + pass screen); KDS station routing ships with the T3 mesh (Wave 4).

## 1. Purpose & scope

Everything between order confirm and food leaving the kitchen:

- the **printing service** — KOTs, receipts, cash-drawer kick, on every tier;
- the **pass screen** (T2) — **OPTIONAL, and a 22-inch panel where present** (`27-F11e/F11f`, founder ruling July 2026; the earlier "one cheap Android tablet" is SUPERSEDED — a 10" tablet holds ~1.5 tickets at 1.5 m and more pixels change nothing, because only physical height buys capacity). Paper is the primary kitchen surface; most deployments never see glass. Shows the branch queue with aging and line-level assembly state;
- the **KDS** (T3) — per-station screens with routing and bump;
- the **timing pipeline** — aging timers from day one, and silent training of per-item prep-time estimates from ready-marks.

Users: chefs, pass staff, and whichever role owns the ready signal (03-F24). Devices: ESC/POS thermal printers (58/80 mm) and 2–3 GB Android tablets; the print service runs inside the counter POS host process (doc 02).

## 2. Position in platform

- **Depends on:** kernel (doc 01) for the order stream and fast-path LAN propagation; `packages/escpos`.
- **References:** doc 02 (confirm boundary, receipt jobs, T1 auto-advance), doc 04 (waiter ready-notifications; waiter-on-pickup ownership), doc 05 (late-order alarms consume the aging thresholds defined here; print-failure alarms), docs 04/06/13 (consumers of published ETAs — ETA *display* belongs to them, never here), doc 15 (printer/device fleet health).
- **Events consumed:** `order.created / confirmed / line_added / note_added / line_state_changed`, `availability.changed`, receipt job requests (doc 02).
- **Events emitted:** `kot.printed / reprint_requested / print_failed`, `order.line_state_changed`, `availability.changed` (pass-screen toggle), `cash.drawer_opened` (kick execution on POS request), extensions `printer.status_changed`, `eta.estimates_published` (§5).

## 3. Functional requirements

**Printing service**
- 03-F1 ESC/POS output, 58 mm and 80 mm, over USB, Bluetooth SPP/BLE, and TCP port 9100, via `packages/escpos` (encoder + pluggable transports).
- 03-F2 Routing by category: one `order.confirmed` fans out to N KOTs by category→printer rules (grill printer vs Chinese printer vs tandoor), with per-printer copy count.
- 03-F3 KOT layout:
  - order number + table/channel in large type, timestamp;
  - one line per item with qty/variant/modifiers;
  - item notes visually emphasized;
  - reprints carry a "REPRINT" band;
  - course grouping (starters/mains) optional, off by default.
- 03-F4 Durable spooler: every print job is persisted (SQLite, WAL) with an explicit state machine (`queued → transmitting → stalled? → printed | failed`; **`stalled` added July 2026 — see 03-F41**) before the first transmit attempt; a crash or power loss mid-print resumes or reprints the job on restart — never drops it. Retry with backoff (default 3 attempts over 30 s) on transport failure. (Instance of the canonical durable-local-queue pattern, 18 §4 — one implementation shared with the sync outbox 01-F8 and fiscal queue 16-F11.)
- 03-F5 **Silent KOT failure is forbidden.** When retries exhaust:
  - the host device raises a loud alert — full-screen banner + repeating sound — naming the printer and order ("KOT #142 did not print — grill printer offline"), repeating until acknowledged;
  - acknowledgment is logged (`audit.*`);
  - `kot.print_failed` is emitted (consumed by doc 05 alarms).
  Testable: kill a printer mid-rush; the alert shows within 45 s of confirm.
- 03-F6 One-tap reroute: from the failure alert, the operator can resend the failed job to any other registered printer; the reroute is logged.
- 03-F7 Reprint and KOT-void always logged with actor + reason (`kot.reprint_requested`; post-KOT void per 01 §4 requires approver).
- 03-F8 Text prints via printer fonts (English + numerals — 00 §5.6); the bitmap raster path serves logos, QR codes, and **user-content fields containing non-Latin text** (customer names, addresses, order notes may arrive in Urdu script — 00 §5.6 user-content rule): such fields render to bitmap per-field, never dropped or transliterated. Numerals Western.
  **This is a physical law, not a preference (verified July 2026 against the code-page tables, not inferred):** no ESC/POS code page can print Urdu. **CP1256 carries 14 of 15 Urdu letters and 0 of the 144 shaped glyph forms**; **CP864 carries 72 of 144 shaped forms and 0 of the 15 Urdu letters.** Arabic-script text is positional — each letter takes an initial/medial/final/isolated form — so a code page holding letters but no shaped forms cannot render connected script, and one holding shaped forms but not Urdu's letters cannot render Urdu. There is no font selection, no printer model and no firmware that changes this. **Any proposal to print Urdu as text is therefore wrong by construction; raster is the only path** — and **Wave 1 does not walk it (founder ruling July 2026).** No Wave-1 input path can put non-Latin text on a ticket: item names are back-office authored English (`00 §5.6`), order notes are not wired (`C7` is unbuilt and `27-F6` bans non-numeric typing for operational roles), and customer names and addresses arrive only with the storefront (`06`), WhatsApp (`07`) and phone orders (`C18`) — none of which is Wave 1. Rendering Urdu needs a font **and** a shaping engine, because the script is positional, which is this FR's own argument; until one is chosen the encoder **REFUSES a non-Latin user field** (`raster_font_unavailable`) rather than emitting a raster with no legible glyphs. That refusal is `03-F34` applied honestly — "a hard refusal to print, never a silent degradation" — and it is chosen because the assertions available at this layer (a raster was emitted, nothing was transliterated, the field changed the document) are **all satisfied by a correctly-sized blank**, which would ship a ticket that looks right and says nothing. `00 §5.6` is unchanged and still binds: user content is never transliterated or rejected for its script, and the first real consumer (`06`/`07`) is where the font question must be answered rather than deferred again — which also makes the unresolved question of `27-F61` (what Nastaliq actually costs at 203 dpi) unavoidable rather than optional.
- 03-F9 Cash drawer kick via receipt-printer RJ11. The POS emits `cash.drawer_opened` (with reason) as the authoritative record; this service executes the kick. Drawer opens without a sale still require the event first — no kick without a ledger entry.
- 03-F10 Printer test harness — scripted suite runnable from device settings and on the office rig (00 §4):
  - charset page and logo/QR bitmap page;
  - cut and drawer kick;
  - 9100 and Bluetooth reconnect;
  - 200-job soak;
  - **paper-out behaviour: pull the roll mid-job and assert the spooler reports `stalled` (03-F41) via `DLE EOT 4` (03-F40), then assert reloading prints the job EXACTLY ONCE;**
  - **head-to-cutter distance** (an unpublished per-model constant, observed 20–30 mm) and **`GS B` solid-fill fidelity**, both rig-calibrated per model into the capability record;
  - results feed the maintained compatibility list (Black Copper BC-58U/85AC + generic Chinese printers are the baseline set). **Note the BC-58U has NO auto-cutter** — a manual tear bar, i.e. a human action and a mis-tear vector per ticket; it stays a compatibility target, never a recommendation.
- 03-F11 `printer.status_changed` (extension) emitted on online/offline transitions per registered printer — feeds doc 05 alarms and doc 15 fleet health.
- 03-F12 Receipts print through the same spooler and durability rules; receipt events are owned by doc 02.

**Pass screen (T2)**
- 03-F13 One tablet at the pass shows the branch order queue — all channels, channel-tagged — **strictly chronological by confirm time**. Card contents: order number, channel badge, table, age, line summary.
- 03-F14 Aging colors on each card: neutral → amber at X min → red at Y min.
  - X/Y are org-configurable per order type (defaults: dine-in 10/20, delivery 15/25);
  - timer basis is `order.confirmed`, so a failed print never hides a late order.
- 03-F15 Line-level assembly view per order: "2 of 3 items ready, waiting on naan" — folded from `order.line_state_changed` across stations.
- 03-F16 Ready-marking: per line and whole-order, one tap → `order.line_state_changed` to `ready` with actor. Item availability is also toggleable from the pass (01-F22).
- 03-F17 An order leaves the queue when all its lines reach a terminal service state — `served`, or `picked_up` for delivery (canonical vocabulary, 01 §4); a recall strip keeps the last 20 cleared orders one tap away (wrong-bump recovery).

**KDS (T3)**
- 03-F18 Per-station screens: a station map (station → categories/items, mirroring or refining printer routing) filters each screen to its own lines only. **Amended July 2026 (`03-F50`): the item→station mapping is CATALOG data, not layer-2 config** — the catalog is the device-facing reference-data channel and no org-config plane exists. A device knows its station identity (layer 3: "this screen is grill").
  **MOVED TO WAVE 1 (founder ruling, July 2026) — it is the single largest low-literacy lever in the product.** Station routing is what turns a 300-item visual-discrimination problem into a ~20-item one — and a ~20-item set is within reach of sight-vocabulary recognition, where 300 is not, for a cook who cannot read. Every other accessibility measure in `27 §2b` operates *within* a ticket; this one decides how much a cook has to discriminate at all. The non-reader is a real user, so routing is not a refinement. It applies to **printers as well as screens** (`03-F1`), so it is not gated on the pass screen existing — which is what makes it deliverable in Wave 1 alongside printing.
- 03-F19 Bump: the station marks its lines done in one tap → `order.line_state_changed` (`ready`, station-scoped) for those lines. Un-bump within 2 min is allowed and logged (a new state event, never an edit).
- 03-F20 Cross-station assembly: the pass screen aggregates line states from all stations via `order.line_state_changed` (line-level state routing, 01 §4) — the pass sees the whole order; stations see only their part.
- 03-F21 A station screen shows its own open-line count and nothing about other stations' load — visibility without cross-station pressure games.
- 03-F22 KDS may run alongside printers (screen + paper) or replace them per station — layer-2 choice. **Amended August 2026 (`03-F51`): the choice is a named per-station fulfilment route — `paper` · `screen` · `both` — and a station routed `screen` never spools a job at all. Printer absence is a CONFIGURATION, not a failure.**

**Sequencing law (settled)**
- 03-F23 Sequencing is **visibility only**. The system never dictates cook order: no auto-prioritization, no reordering of the queue, no "cook this next" prompts — at any tier, ever. Chronological order + aging color is the entire sequencing UI; the chef decides.

**Ready-signal ownership**
- 03-F24 Who marks `ready` is a role assignment at layer 2 (00 §7): chef (KDS bump), pass person (pass screen), counter (POS, 02-F33), or waiter-on-pickup (doc 04). The emitted event is identical regardless of owner; every surface capable of ready-marking respects the assignment (others render read-only). **Canonical (01 §4): station bump = those lines `ready`; order-level ready = the fold of all lines ready; an owner's "order ready" mark simply marks all remaining lines at once. There is no separate order-ready state.**

**Timing pipeline (stages defined here; ETA display belongs to docs 04/06/13)**
- 03-F25 **Stage 1 — aging timers, day one:** timers from `order.confirmed` on every queue surface (pass, KDS, POS T1 panel, manager console). No learning required; this alone is the Wave 1 deliverable.
- 03-F26 **Stage 2 — silent sample capture:** every ready-mark yields a prep-time sample derived purely from the ledger (no staff input, automation law 00 §5.8). Fields:
  - `item_id`, `variant_id`, `station`, `branch_id`;
  - `duration` (confirm → ready, per line);
  - load context: `open_lines_at_confirm`, `orders_confirmed_trailing_15min`, `daypart`;
  - `segment`: `rush` when trailing-15-min confirms ≥ threshold (default 8, layer-2 adjustable), else `quiet`.
  T1 branches produce no ready-marks (02-F31), so they honestly produce no samples.
- 03-F27 **Stage 3 — estimation:** a cloud job (BullMQ; nightly full + hourly incremental) computes per `item × segment × branch`: median and p80 prep time over a rolling 60-day window, outlier-trimmed.
- 03-F28 **Stage 4 — confidence gate:** an estimate is published only when its cell passes both conditions (defaults; platform-layer adjustable):
  - ≥ 30 samples in the rolling window;
  - p80 ≤ 2× median (spread sanity).
  Below the gate, consumers get "no estimate" — never a guess (concept law 6).
- 03-F29 **Stage 5 — publication:** gated estimates ship as versioned reference data via `eta.estimates_published` (extension) on the normal sync channel (01 §8).
  - The order-level quote rule is defined once, here, so every consumer quotes identically: max of line-item p80s + assembly buffer (default 2 min).
  - Consumers and display surfaces: waiter capture (doc 04), storefront/WhatsApp quoting (docs 06/07), analyst metrics (doc 13).

**Printed-document model (03 owns the renderer; 14 owns only the editing surface — AGENTS.md routing, July 2026)**

- 03-F30 **Two layers, strictly separated.** A **`DocumentSpec`** is vendor-authored, versioned, shipped as code under CODEOWNERS: an ordered list of typed blocks, one spec per document type. A **`DocumentProfile`** is org/branch config — a flat `slot_id → value` map, Zod-schemad and `config.changed`-audited (14-F2) — which **cannot express position, order, font or structure**. It can only fill holes the spec declared and flip toggles the spec declared. Render is a pure function: `render(Spec@v, Profile, Data, PrinterCaps, FiscalBlock?) → blocks → bytes`.
  - Rationale, in order of weight: (a) a slot space is **property-testable in full** — the entire customisation space can be generated and asserted over, which no surveyed vendor can do; (b) an owner can lose a *slot*, never a *layout*, which shrinks migration by an order of magnitude; (c) `00 §7`'s "presets, not knobs" — a slot value inside a vendor layout is a preset with a hole, a template is free-form configuration. **No POS vendor surveyed ships a WYSIWYG thermal editor**; even the best "visual editor" only reorders predefined blocks.
  - **Purity is a testable law:** identical `(spec, profile, data, caps)` must produce byte-identical output on Electron and React Native. A shipped competitor emits different tickets for the same order on two of its own devices.
- 03-F31 **Document types are first-class entities, never a flag on a printer.** Each declares its own data contract, spec, invariants and render-mode policy: `kot`, `receipt`, `bill` (**unblocked — DEC-TAX-001 closed by founder ruling July 2026: ships everywhere, no Sindh special-casing**), `refund_slip`, `shift_close_slip`, `rider_settlement_slip`, `day_summary`, `test_page`. Structural differences live in the TYPE, not in config: price presence, default font scale, station filtering, one-item-vs-whole-order, course/seat grouping, modifier emphasis, void marker, max-lines-per-chit, reprint marker.
  - The evidence for type-not-knob: owner demand for kitchen-ticket font size is genuinely **bidirectional** — chits want larger, cup labels want smaller, both correct. A knob resolves that wrongly for one of them forever.
- 03-F32 **Type invariants override configuration.** A `kot` renders **no money token** under any profile; a fiscal block cannot be suppressed under any profile. This is enforced structurally — the profile schema has no slot id addressing them — not by a runtime check on a value the owner supplied. The deepest POS in the market has **no price option anywhere** in its kitchen-printer configuration: prices are simply not in the chit data model.
- 03-F33 **Region model.** Blocks carry exactly one region: `HEAD_LOCKED → HEAD_OWNER → BODY → TOTALS → FISCAL_LOCKED → FOOT_OWNER → TAIL_LOCKED`. Owner content is legal only **outside** the regulated block, which is what every strict-fiscal jurisdiction already does. `FISCAL_LOCKED` blocks are **not in the `DocumentSpec` at all** — they are injected at render by the certified authority adapter (16-F23), which declares the block **and its position**, because some authorities mandate field sets only and others mandate order.
- 03-F34 **Enforce at render; validate at save only for feedback.** Before bytes reach the spooler: assert every adapter-declared mandatory block is present, that the QR's computed physical size meets the adapter's declared minimum for the target dpi, and that no owner slot rendered inside a locked region. Failure is a **hard refusal to print plus an S1 band** (27-F11d), never a silent degradation. Save-time linting must never be able to block saving — a shipped competitor's linter left merchants unable to save **the vendor's own default template**, so *"the shipped default always validates and always saves"* is a named test.
- 03-F35 **The fiscal QR is always rasterised, never the native ESC/POS QR command.** Cheap printers report no QR capability and **fail silently** — printing nothing, or printing the raw payload as text. For a QR whose absence is an offence that can seal the premises, a silent no-op is the worst available failure mode. Size is computed from `dpi`; treat 7×7 mm as a legal floor and render 18–25 mm (FBR's own technical spec asks ~0.7–1.0 inch, ~2.5× the SRO figure). **The fiscal invoice number is an opaque token** — never parsed, reconstructed or shape-validated, because FBR's own documents give three different formats.
- 03-F50 **A sellable item's station is CATALOG data, not layer-2 config (founder ruling July 2026; amends `03-F18`).** `03-F18` called the station map "layer-2 config", and taken literally that requires an org-config model, a store, a distribution path to devices and a `config.changed` payload schema — **none of which exists**, while the thing actually needed is one field per item on a channel that is already built, versioned, tombstoned and delivering (`01-F52`..`01-F56`). So `station` joins `kitchen_name` and the per-channel visibility flags (`14-F5`) on the catalog entry, and a device resolves item → station locally from the catalog it already holds.
  - **Inheritance, because a kitchen routes categories and not dishes.** `03-F18`'s map is "station → categories/items", and the category form is how a kitchen actually thinks (all breads to the tandoor). An entry with no `station` **inherits its parent's** through the `01-F21` chain; an item that belongs elsewhere than its category overrides it. So the common case is a handful of values on categories, not one per dish.
  - **An unrouted item is not a dropped item.** Where no station resolves anywhere up the chain, the line prints on the **default station's** ticket rather than vanishing — `01-F17`'s spirit on the kitchen side: a sale that completed must reach someone who can cook it, and a line silently absent from every ticket is the one failure the paper cannot reveal. `03-F34`'s refusal covers documents that cannot be rendered, never lines that cannot be routed.
  - What stays layer-2 config: aging thresholds (`03-F14`), ready-signal ownership (`03-F24`), KDS-vs-printer per station, and printer assignment itself (`§7` layer 3). Those are genuinely org policy, they are not per-item, and none is needed by Wave 1's KOT.
- 03-F49 **A document type declares its MINIMUM COLUMNS, and below it the document is refused, never squeezed (founder ruling July 2026).** `03-F36` made 32-column rendering a build-time gate while `27-F57` held that 32 columns breaks the quantity-adjacent-to-name pairing — two floors that cannot both bind. Resolved the way `03-F31` already resolves everything else: **structural differences live in the TYPE.** Each `DocumentSpec` declares `min_columns`; a printer whose capability record (`§7` layer 3) reports fewer triggers `03-F34`'s existing path — **hard refusal to print plus an S1 band**, never silent degradation. `kot` declares **42**; `receipt` and `bill` declare **32**, because a price column genuinely can degrade through `03-F36`'s declared order and a comprehension pairing cannot.
  - **The consequence is a purchasing fact, and it is stated rather than hidden:** a 58 mm printer cannot print kitchen tickets. It can still print receipts and bills. Doc 14's printer setup must say so at assignment time, not at 20:40 on a Friday when the refusal fires.
  - **Why refusal and not a wrap.** `27-F58` makes vertical alignment the thing modifiers and notes hang off, and a wrapped item line destroys it — so degrading the KOT to fit narrow paper spends the exact property the ticket exists to deliver. `03-F44` makes this cheap to be strict about: paper is never the record, so a refused KOT costs a reprint on the right printer and never state.
  - Related correction: `27-F57` cited *"`03-F30`'s 80 mm floor"*. `03-F30` has no such floor — it governs the Spec/Profile split only. This FR is the floor, expressed in columns like every other layout figure (`§7`), because millimetres are used only where a regulator specifies them (`03-F35`).
- 03-F36 **Every DocumentSpec must render correctly at its declared `min_columns` (`03-F49`; 32 for every type that declares 32)** — a build-time test, not a review convention. Degradation is declared per block, in order: full `left | right` → the block's declared `short` form → omit the label and keep the number → wrap to two rows. **Banned:** absolute dot positioning (an `x=384` offset is mid-line at 576 dots and off-paper at 384), and space-as-layout (it makes a document permanently unreflowable).
- 03-F37 **Reprint markers are mandatory per type, in a locked region.** 03-F3 gives the KOT a REPRINT band; the receipt currently logs the event (02-F16) and prints nothing. Reprints are already a named fraud vector — the paper must say so.
- 03-F38 **Long item names are a CATALOG problem, not a template one.** Add a short `kitchen_name` to the 01-F21 catalog chain, editable in back office, falling back to the display name. This is the most-requested workaround in competitor communities and it is a one-field change rather than a template feature.
- 03-F39 **`max_lines_per_chit` is declarative pagination policy**, because a template cannot know its own output length. Owner-settable per printer.

**Device mechanics (these are the failure modes the renderer cannot see — all three would otherwise ship)**

- 03-F40 **Paper-out is detected with the real-time `DLE EOT 4`, never `GS r`.** On the entire TM-T88 family and its peers (T88IV–VII, T82II, T70II, T90, L90) the paper-end sensor takes the printer **offline, and it then does not execute `GS r` at all** — so a health check built on `GS r` reports **"paper present" forever** and a paper-out becomes a silent KOT failure. Real-time commands are answered while offline by design. The two commands also use **incompatible bit layouts for the same sensor** (`DLE EOT 4`: bits 2,3 near-end / 5,6 out; `GS r 1`: bits 0,1 / 2,3), so `packages/escpos` must never decode one with the other's map. A **near-end** sensor is not universal (the TM-T88VII lists paper-end + cover-open only) — model-gate the feature from the 03-F10 capability record rather than assuming it. Cap outstanding real-time queries at **4**.
- 03-F41 **A stalled printer is holding the job, not dropping it — so `03-F4`'s retry must not fire.** `ESC c 4`: on paper-end the printer *"stops after the current printing completes"*, goes offline and **holds** until the roll is replaced; on most deployed models the sensor cannot even be disabled. Therefore the spooler distinguishes **`transmitting, printer stalled`** from **`failed`**, and **a stall never counts toward the 3-attempt budget and never re-transmits.** A timeout that flips a stall to `failed` and retries **double-prints the instant the roll is loaded** — a duplicate KOT is a real kitchen error, not a cosmetic one. Recovery from a recoverable/cutter error is `DLE ENQ n=2`. The cheap-printer wedge that needs a power cycle is the degenerate case, not the model.
- 03-F42 **A document is rendered whole, buffered, and transmitted as one unit.** With cut reservation, *"if data is interrupted for two seconds or more, the printer automatically feeds to the reserved cut position and cuts"* — so **a chunked or streaming renderer that stalls >2 s mid-ticket gets its ticket cut in half.** No I/O wait may be interleaved inside a document. (Reservations are also cleared by reset, power-off, buffer clear or a feed-button press.) Related: a real-time byte sequence occurring **inside raster data** is executed as a command and corrupts the image unless disabled via `GS ( D`.
- 03-F43 **Printers are sited at the pass or on a wall, never at the range** — an installation requirement with a spec consequence. Above **45 °C every deployed printer is out of its operating envelope**, and at **60 °C thermal paper self-blackens spontaneously** (manufacturer-stated; the ~100 °C figure quoted by secondary sources is the melt regime and is **wrong for a kitchen**). Prolonged exposure above **40 °C** degrades the media. Star Micronics explicitly recommends **impact** printers for hot kitchens; **we decline that trade** — impact costs the raster path (so no Urdu, no icons), reliable solid-fill inversion, and **14× the print time** (a 10-line KOT at ~2,130 ms vs ~150 ms). A station genuinely too hot for a printer gets a **screen or a runner**, not an impact printer.
- 03-F44 **Paper is never a record; the ledger is (01-F1).** A thermal image is destroyed by cooking oil, alcohol/hand sanitiser, esters and ketones, **human sweat**, PVC or plasticiser contact, and adhesive tape — and is **developed by a fingernail** (frictional heat). Heat plus humidity together is many times worse than either alone. No flow may require a printed document to be readable later to recover state; plan printed image life at **5 years** for the 48 GSM stock actually sold in Pakistan, and unprinted roll shelf life at **2 years**.
- 03-F45 **Consumables are procured by metres and GSM, and checked on receipt.** The international "80×80" convention **does not exist as a SKU in Pakistan**, and the market is ~exclusively **48 GSM** (≈52 µm; µm ≈ GSM × 1.09). Short rolls are a live risk — the same nominal SKU ranges **2.6× in price** on one marketplace in one day, and a listing sells *"80 mm × 40 **Yards**"* (a 8.5% shortfall hidden in the unit). A **vernier caliper is a sufficient goods-inwards test**: at 0.80 packing on a 12 mm core a genuine roll measures ≈ 59 mm OD at 40 m, 65 at 50 m, 74 at 65 m. Paper prices rose ~20%/yr for five years, so **every PKR figure in this corpus carries a review date**. BPA transfer to skin is ~10× higher onto wet or greasy fingers — which describes a kitchen line, not the cashier the EU limit was written for — and **no Pakistani regulator enforces it**, so BPA-free is a *purchasing requirement with supplier documentation* (14) or it is nothing.

**Queue navigation and aging (conflicts C1/C2/C6 closed, July 2026)**

- 03-F46 **The queue pages; it never scrolls — and the oldest ticket is always on page 1.** `27-F2` bans scrolling to reach a primary action (nearly half of field subjects did not know content existed below the fold); `03-N4` budgeted for a 40-order scroll. Both are satisfied by paging **within** the strictly-chronological order of `03-F13`: page 1 always holds the oldest tickets, bumping one pulls the next up, so **the work is always on the first page and reaching it is never a navigation act.** Later pages exist for situational awareness only — how much is queued — never to reach work. Paging inside one flat list is **lateral, not depth**, and does not spend `27-F1`'s depth budget (same rule as persistent tabs).
- 03-F47 **Aging colour is driven by fixed configured minutes, not by expected-prep.** `21 §5` said *"amber at expected-prep"*; `03-F14` says *"amber at X min"*. **`03-F14` wins and `21 §5` is amended.** Expected-prep is an ETA-derived per-item quantity that exists only once `03-F27/F28`'s confidence gate passes — and `03 §3` forbids the kitchen from displaying ETAs at all. A colour driven by a model that may never become confident is a colour that lies about how late the food is. Thresholds stay org-configurable per order type (defaults: dine-in 10/20, delivery 15/25).

- 03-F48 **The kitchen can reprint and reroute its own ticket where it has a screen; where it does not, that is a stated tier consequence (gap G5).** `03-F7` logs KOT reprints and `03-F6` puts one-tap reroute on the *alert* — which lives on the host device (`03-F5`), i.e. the counter. So a soaked, torn or mis-cut ticket currently costs a walk mid-rush. **Where a pass or station screen exists it carries reprint and reroute for its own lines.** Where the kitchen is printer-only (`27-F11e`, most deployments), the counter is the only path — and the counter's order list therefore carries a **one-tap reprint of the last KOT per station**, so the walk is short and does not require finding the order. This is a tier consequence, not a defect, but it must be stated: an unstated one becomes a workaround (`03-F44` — the paper is not the record, so reprinting is always safe).

**Fulfilment routing (bring-your-own-hardware — `DEC-HW-001`, August 2026)**

- 03-F51 **A station's fulfilment route is CONFIGURED, and the absence of a printer is a configuration rather than a fault (amends `03-F22`).** `03-F22` made KDS-vs-printer *"a layer-2 choice"* and nothing in the product ever read it, so **every station was assumed to have paper**. The consequence was measured, not predicted: with no printer attached every transmit reports no answer, `03-F4`'s budget exhausts, `03-F5` raises a permanent band with repeating sound, a **permanent** `kot.print_failed` is appended per exhausted job under `01-F1`, `05-F3` alarms the manager and `15-F14` pages vendor support on *"`kot.print_failed` rates"*. So a restaurant that owns no thermal printer generated unbounded real vendor-support load for ever and irreversibly polluted its own append-only ledger — and the corpus gave it no way to say it had no printer. Under `DEC-HW-001`'s bring-your-own-hardware ruling that is the product asserting a purchase, which is exactly what it must not do.
  - **The route, per station: `paper` · `screen` · `both`** — `03-F22`'s three cases, named. `03-F50` resolves *which* station cooks a line; this decides what happens to it there. A station routed `screen` **enqueues no print job at all**: no bytes, no attempt, no retry budget, no band, no `kot.print_failed`. That is not a suppressed failure — there is no job to fail. A station routed `both` is unchanged from today, and is what a kitchen running screen *and* paper (`03-F22`'s first case) configures.
  - **`03-F5` IS NOT WEAKENED, and the two are kept apart BY WHEN THEY ARE DECIDED.** Absence is decided **before a job exists**, from configuration. Failure is decided **after a job exists**, from a transport outcome. Wherever `paper` is the route, the whole of `03-F4` / `03-F5` / `03-F41` binds unchanged: a printer that is expected and does not answer is still loud, still appends `kot.print_failed`, still bands until acknowledged, and a stall is still not a failure. **No code path may turn a transport outcome into silence.** The only thing configuration may do is decline to create the job in the first place.
  - **A station with no route to the kitchen is refused at CONFIGURATION time, never per order** (`00 §5.7` — a surface reports what is true). A station routed `screen` at a branch with no pass/KDS device has nowhere for its lines to appear at all, and discovering that once per order at 20:40 on a Friday is the failure this clause exists to prevent. The refusal names the offending stations and the reason. Where the branch's device roster cannot be read — which is every device today, because `02-F31`'s registry reaches none (`01-F62`) — the check reports **unverifiable** rather than passing: an unknown is not a blessing, and reporting one as a pass is the same dishonesty `00 §5.7` forbids one surface over.
  - **A refused configuration never blocks a sale and never blocks the app** (`01-F17`, commandment 4). It is simply not applied: the branch keeps the shipped default route, `paper`, which is the pre-configuration behaviour, and the refusal is reported at length wherever the configuration was made. Falling back to paper is deliberate and is the conservative half of the trade — a route the product silently swallowed would lose tickets where paper it cannot print merely says so loudly (`03-F5`).
  - **Forward-looking only.** `01-F1` is append-only, so `kot.print_failed` events already written stay written and corrections are new linked events. Configuring a station to `screen` stops the next one; it removes nothing and cleans no ledger.
  - **Layer 2** (`§7` below, `00 §7`), because `03-F50` already lists *"KDS-vs-printer per station"* there: it is org policy, it is not per-item, and it is not per-device. It introduces **no new event type** — the whole change is which jobs are created (commandment 2).
  - **The CASH DRAWER (`03-F9`) is the same shape and is NOT a fault today — measured August 2026, and recorded here so the next session does not build one.** `cash.drawer_opened` is emitted, authorized and folded, and **nothing executes a kick**: `packages/escpos` ships no drawer-pulse encoder and no code path attempts one, so there is no transmit, no retry budget, no band and no event to model. Absence of a drawer therefore costs nothing at all. **The hazard is forward-looking and specific:** `03-F9` puts the kick on the *receipt printer's* RJ11, so whoever builds it inherits that printer's transport and, with it, the exact defect this FR removes — a branch with no printer, or a printer with no drawer attached, attempting a pulse that never answers. When the kick is built, the drawer must be a **declared capability** on the same footing as a station's route, refused at configuration time, and never a fault discovered per no-sale. `15-F10`'s checklist already stops requiring the drawer row of a branch that has no drawer.
  - **What a screen-only station needs from the rest of the product, stated so it is not mistaken for built:** nothing new. The lines are already in the ledger (`order.confirmed` → `order.line_state_changed`) and already in the branch queue projection the pass/KDS surface reads (`03-F13`/`03-F20`), filtered by the catalog's `station` (`03-F50`). What is owed is the SURFACE (`apps/pass-kds`) and, with it, `03-F24`'s ready-signal ownership — a screen-only station's lines advance when the screen bumps them (`03-F19`), which is the right owner and is why `02-F31`'s auto-advance must never be widened to cover them.

**Serve-signal ownership (August 2026)**

- 03-F52 **Who marks `served` is a role assignment at layer 2 — the second row of the key `00 §7` already declares — and the act is an explicit HANDOVER, never a widening of the ready-mark and never a widening of settlement.** `03-F24` filled in the `ready` row and the `served` row was left blank, so a branch with a pass screen had no producer for `served` at all: `02-F31`'s settlement half is tier-gated to T1, `04-F14`'s waiter is a T3 device, and a fully-bumped ticket therefore never satisfied `03-F17`'s exit condition. A pass screen that accumulates for ever after one service is not a degraded surface, it is an unusable one. `00 §7` layer 2 already reads *"signal ownership (which role advances which order state — e.g. who marks 'ready')"*, so this FR instantiates a declared key rather than introducing a lever.
  - **The owners:** `settlement` (no device signals handover — `02-F31`'s auto-advance, unchanged in behaviour), `pass` (the pass screen's handover control), `counter` (the POS queue panel, `02-F33`'s surface), `waiter` (the handheld, `04-F14`). **`kds` is deliberately NOT an owner**: a station cook hands food to a pass, never to a customer, and a bump is `03-F19`'s act. As in `03-F24` the emitted event is identical regardless of owner — `order.line_state_changed` to `served`, no owner field, no `source:` tag, no second event type — because an owner-tagged variant would make a projected value depend on which surface pressed the button (`01-F34`). Surfaces without the assignment are read-only for `served`, exactly as `02-F33` already says from the counter's side.
  - **The act is separate from the ready-mark, and that separation is the FR.** One press of DONE emits `ready` and only `ready` (`03-F16`/`03-F19`), because the moment the food reaches the pass is the moment the pass person's remaining job BEGINS — assemble (`03-F15`'s *"2 of 3 ready, waiting on naan"*), call, hand over — and `03-F17` clears the ticket off the screen on `served`. A DONE that emitted both would blank the screen at the start of the work the screen exists for, and would make `03-F19`'s two-minute undo window meaningless against a terminal state.
  - **It marks only lines already `ready`, and walks only `ready → served`.** The `in_prep → served` edge exists for `DEC-HW-002`'s no-pass case alone; reaching `served` from the pass by any route that skips `ready` destroys `03-F26`'s prep-time sample and `03-F15`'s assembly count, and a shortest-path walk over `LEGAL_NEXT` finds exactly that route. Lines still `in_prep` are untouched and the ticket stays on the pass — a partially-ready order has not been handed over. One press marks every remaining `ready` line at once, which is `03-F24`'s canonical rule for an owner's order-level mark.
  - **Dine-in, takeaway and pickup only.** Delivery is `picked_up → delivered`, rider-driven (`01 §4`), or the counter's on-behalf entry (`09-F8`); a delivery ticket bumped DONE gets its ready-marks and stays on the pass. The filter is an **allowlist** on the order's own `order_type`, matching `02-F31`'s: `order_type` is an open string, so a denylist marks an unrecognised value `served`, terminally and permanently under `01-F35`/`01-F1`.
  - **The handover press carries a confirm naming the order reference; the ready-mark does not.** This is the one control in the kitchen whose mis-tap cannot be taken back — `served` is terminal, and `03-F17`'s recall strip restores VISIBILITY, never STATE. The confirm is not friction bolted on for safety: at counter service the pass person calls the number aloud, so reading the reference off the confirm IS the call. **Naming the reference is required** — a bare *"Are you sure?"* is the tap people learn to dismiss.
  - **The tier stops being an input.** `02-F31`'s auto-advance ships unchanged in behaviour and changes its trigger: the till emits on settlement because the branch's serve-signal owner is `settlement`, not because a label reads `T1`. That is `DEC-HW-003`'s checkable test — *"no code may branch on the tier to decide whether a piece of hardware EXISTS"* — applied to the one producer that still failed it. Where the key is unset the default is derived from `02-F31`'s own detection rule over the branch device roster (the capability set, `DEC-HW-003` (b)), and where the roster cannot be read the surface REPORTS the assumption on its boot line rather than presenting it as configured (`00 §5.7`).
  - **One declaration, no per-app fallback.** The assignment is a single org value read by every surface. Two surfaces each carrying their own default is how a pass screen and a till come to disagree about who owns handover with every gate green — the failure `01-F60`'s enabled-set drift already cost this product once. An assignment naming a surface this product does not ship is refused **when it is configured**, with the offending value named, never discovered once per order at 20:40 on a Friday (`03-F51`'s precedent).
  - **REJECTED, with the reason recorded so it is not re-proposed:** (a) *widening `02-F31`'s auto-advance to T2* — `03-F51` forbids it in terms, and at counter service the customer pays before the food is cooked, so lines rest at `confirmed`, `confirmed → served` is illegal, and the trigger emits NOTHING while looking finished, which `DEC-HW-002` names as the worst available outcome; (b) *DONE emitting both* — see above; (c) *a new event type* — `order.line_state_changed` already carries this, and `handover.recorded` is `11-F17`'s shift note, an unrelated fact wearing the same English word; (d) *a new `PermissionAction`* — the owning key is a layer-2 role assignment enforced in main with the renderer's claim never trusted, which is commandment 8's property discharged through the control `00 §7` names.
  - **OWED, and named rather than left to look intentional:** (1) ~~`01-F26`'s PIN session does not run on the pass, so `actor_user_id` is `null` on every edge it writes~~ **— RULED by `03-F53` (August 2026), which decides both halves this FR left open: the session runs, and the gate sits on the ACT rather than on the queue.** (2) `02-F33`'s counter surface must exist before `counter` may be assigned. (3) `04-F14` is unchanged: the waiter surface SHIPPED in August 2026 (`04-F21`, a terminal of the till) and still cannot own a serve signal, because `04-F23` closes its event set at four order events and `order.line_state_changed` is not among them. (4) **`03-F19`'s un-bump is unbuildable against the shipped transition table** — `LEGAL_NEXT.ready` has no edge back to `in_prep`, so the FR's two-minute undo has never existed in code. That is a `packages/domain` question and a `DECISIONS.md` row, not this FR's to fix; it is recorded here because this FR makes the press that follows a bump irreversible, and a reader is entitled to know the press before it already is.

**Identity at the pass (August 2026 — closes `03-F52`'s OWED (1))**

- 03-F53 **The pass runs `01-F26`'s PIN session, and the gate is on the ACT, never on the QUEUE.** `03-F16` already says a ready-mark carries *"with actor"*, `02-F41` rules that attribution is whoever's PIN is in with no *"acting for"* concept, and `01-F27` puts user identity in the PIN session and **never** in the device token. `03-F52` then made the handover TERMINAL, so what `apps/pass-kds` emits today is an unattributable permanent claim that food reached a customer. What no FR answers is what a KITCHEN screen does with all of that, and that is what this one decides.
  - **Every state edge the pass writes carries the signed-in user, and with no session there is no edge.** A press with nobody signed in raises `01-F61`'s two steps — the fixed grid of staff tiles, then the PIN — and nothing is appended until one succeeds. **The refusal belongs to the emitter that builds the edge, not to the host that wires it**, so ONE read of the session decides both whether the act happens and whose name is on it: a gate in the host is a gate no test can drive (`03-F51`'s recorded lesson), and two reads of one fact is `02-F45`'s defect with a credential in it.
  - **The queue itself is never gated, and this is where the pass parts company with the till.** `02-F18`'s *"no anonymous mode exists; a locked device shows only the unlock screen"* is doc 02's rule for the device that holds the drawer. This surface shows no money at all (`03-F32`), no ETA (`03 §3`), and can make no claim but a state one — and its whole purpose is to be READ: `03-F13` is a queue a cook glances at, `03-F15`'s *"2 of 3 ready, waiting on naan"* is read with both hands full, and the screen map records that *"a cook glancing for one second cannot navigate"*. Gating that behind a PIN turns a roster this device has not yet synced into a kitchen that cannot see its own tickets — `01-F17`'s stopped till, on the surface where commandment 4 binds hardest. Identification is charged on the act and never on the look.
  - **There is no unattributable edge and no urgent bypass — and nothing about that stops the food.** A cook who cannot sign in still puts the plate on the counter; this product has never been able to prevent that and must not pretend to. What waits is the RECORD. An escape hatch would be exercised precisely in the chaos where attribution matters most, and `01-F1` makes whatever it writes permanent — `03-F52` refused a `source:` tag on this same event for a smaller reason. The harm `01-F17` really names here is the ticket that never leaves (`03-F17`), and it is bounded by two properties this FR **relies on rather than invents**: the queue stays visible whatever happens, and `01-F61`'s counter is per **(device, user)** and ends on a **time cooldown** — so one cook's five typos never lock the screen, never lock a colleague, and end by themselves. A device-wide counter here would be `01-F61`'s bricked till one surface along, and is refused for the same reason it was there.
  - **A refusal says WHICH refusal.** Being locked out is distinguishable on the glass from a PIN that was simply wrong, and a device whose registry is empty says so rather than drawing an empty grid (`00 §5.7`). A cook who cannot tell those apart re-keys instead of fetching a colleague, and that is the one behaviour that turns a five-minute cooldown into a stopped pass.
  - **Signing in at the pass grants no authority; it supplies attribution.** `03-F24` and `03-F52` put the authorization on the layer-2 signal assignment, and `03-F52` (d) refused a new `PermissionAction` outright; `packages/domain`'s action set carries no line-state member, so routing a bump through `can()` would deny every bump at every branch. Any member of the branch roster may therefore identify at the pass, and the session is read for `actor_user_id` and for nothing else.
  - **ONE session — and the counter's SECOND one is not a pattern to copy here.** `apps/pos-electron` builds a second `createPinSession` because `unlock()` MOVES the session and `02-F20` needs an actor and an approver in the same instant; approving through the cashier's own would sign her out and attribute her next twenty orders to whoever authorised one paid-out. The pass has one act-class and no approval inside it, so a second session here would be manufacturing the *"acting for"* concept `02-F41` ruled out. A change of cook is a lock and an unlock.
  - **Acting is activity; looking is not.** `01-F26`'s idle auto-lock is a device-layer setting (`00 §7` layer 3) and it applies here; this FR fixes no timeout VALUE. What it fixes is what feeds the timer — every edge the pass writes, and no read. A cook bumping every two minutes must not be signed out mid-service, and a screen nobody has touched must not hold a session open behind whoever walked away, because `02-F41` would go on naming them.
  - **OWED, named rather than left to look intentional:** (1) an explicit end-of-session control — a pass is passed between people far more often than a till is, and idle expiry is the only exit this FR requires; (2) `01-F61`'s `grid_ordinal` is unimplemented, so the grid is ordered by `user_id` and an ordinary Tuesday hire still shifts every tile after it (recorded in `packages/sync-client/src/staff.ts`, unchanged here); (3) nothing populates the staff registry on any device — `01-F47` admits *devices, not people* — so the pass runs on the same marked DEV SEED the counter does, and it is **one declaration read by both apps** (`DEC-ARCH-001`), never a second copy.

- 03-F54 **`printer.status_changed`'s payload, and the ONE distinction that decides it: a stalled printer is not an offline printer (August 2026; instantiates `03-F11`).** `03-F11` declared this extension in July and `01 §4` absorbed it, and no payload schema was ever written — so `01-F4` made the event an `UnknownEventTypeError` at emit, nothing produced it, and `05-F3`'s *second* alarm trigger has never existed. The measurement that makes this worth an FR rather than a schema commit: a symbol-precise `grep -a` across `apps/`, `services/` and `packages/` in August 2026 found the type in comments and specs only, with **no producer and no consumer anywhere** — one FR's worth of manager alarm silently absent for a month, in the same shape `05-F30` and `05-F28` each record one door over.
  - **Payload:** `printer_name` (non-empty) and `status`, a **closed two-member set — `online` | `offline`**. `03-F11`'s own words are *"emitted on online/offline transitions per registered printer"*, so the enumeration is transcription rather than design; it is CLOSED because an open string admits a typo permanently under `01-F1`, and `05-F3` matches on the literal `offline`.
  - **`printer_name`, not a printer id**, and the same field name `kot.print_failed` already carries — `05-F3` raises both onto one alarm list, so a second vocabulary for the same noun would force the console to join two spellings of one printer. `03-F5`'s alert copy already reads that field.
  - **A `stalled` job is NOT an offline transition, and this is the FR's substance.** `03-F41` separates `stalled` from `failed` precisely because a printer holding a job for a missing roll is *reachable* — it answered the `DLE EOT 4` query, which is how the stall was diagnosed at all. Emitting `offline` for paper-out would send a manager to check a cable, and would fire on the most ordinary event in a kitchen. Paper-out has its own machinery (`03-F41`'s hold, `03-F5`'s band) and does not borrow this one.
  - **The prior state is assumed `online`, so a first failure IS a transition and a first success is NOT.** `03-F11` says *"on transitions"*, and a device that boots with no prior state has two honest options: assume online (a dead printer announces itself on the first attempt; a healthy one stays silent) or treat the first observation as a transition (which emits `online` on the first successful print after every launch — an event that reports no change to anyone). The first is chosen because the alarm this feeds is about the printer being **down**, and because the second manufactures a per-launch event in an append-only ledger.
  - **Emitted by the device that owns the transport** — the same host that already appends `kot.print_failed` through the same seam, so this adds a producer and not a path. Consumed by `05-F3`'s alarm list and doc 15's fleet health.

**The addition after the confirm (August 2026)**

- 03-F55 **A line added to an order after its ticket has gone to the kitchen reaches the kitchen on its own chit, and the lines already on paper are never cooked twice.** `03-F2` fans **one** `order.confirmed` out to N station tickets and stops there; `02-F8` puts the boundary at the confirm and leaves post-confirm ADDITION legal (only *removal* is escalated to `void.recorded`), and `10-F3` counts *"`order.confirmed`, **or `line_added` after confirm**"* as one deduction case — so the corpus has always contemplated the addition and has never said what prints. **Measured on the shipped counter, August 2026, not predicted:** a KOT job's identifier is `<order_id>::<station>` and the printer skips a station whose job already exists, so a cashier who rings *one more naan* onto a confirmed order and presses **Send to kitchen** again gets **no ticket, no band, no event and no change on the glass** — the control still reads *Send to kitchen*. The naan is on the bill (`01-F53` captured its price at the line) and nobody in the kitchen has been told. Nothing in this product reveals that until the customer asks for food that was never started.
  - **The existing skip is CORRECT and is not deleted.** `03-F7`/`03-F37` make a reprint a deliberate, logged, REPRINT-banded act, and `03-F41` spends a paragraph on why a duplicate KOT is a real kitchen error rather than a cosmetic one. What the skip cannot do is tell *"same order, same lines, second press"* — which must stay silent — from *"same order, NEW lines"* — which must reach the kitchen. This FR separates those two cases. Deleting the guard instead trades a silent loss for a silent double-cook, which is worse.
  - **What prints, per station: exactly the lines routed there that this device has not yet committed to paper for this order.** The first such chit at a station is an ordinary KOT; every later one is an **addendum**, numbered from 1 in the order this device made them. A station reached for the FIRST time by an addition gets an ordinary KOT and **not** an addendum — `03-F2`'s fan-out is per station, and a tandoor that has never seen this ticket is not being handed an addition to anything.
  - **Where a station has nothing uncommitted, NOTHING is created** — no bytes, no spooled job, no attempt, no retry budget, no band, no `kot.print_failed`. A second press with nothing new is **silent**, exactly as it is today, and that silence is the correct answer rather than a degraded one. It is `03-F51`'s distinction one door along: absence is decided **before a job exists**, from what the paper already carries; failure is decided **after a job exists**, from a transport outcome.
  - **"Committed to paper" means A JOB EXISTS, not that the job PRINTED.** A ticket whose retries exhausted (`03-F5`) has been committed: its lines belong to that chit and to `03-F6`'s reroute and `03-F48`'s reprint, and repeating them on the next addendum would put one dish on two chits in a kitchen that has been told to expect exactly one. A document `03-F34` **refused** is the opposite — no job was created, nothing was committed, its lines are still owed to the kitchen, and the next press renders them again and is refused again (the band is already one per job).
  - **The device's record of what it has committed is the durable spool (`03-F4`), and it must survive the power cut that FR is written about.** A record held only in the process is defeated by a relaunch, and the relaunch is exactly when both failures appear: a spool re-read without it either reprints a chit already on the spike in the kitchen, or loses an addition that never got there. `03-F4` already carries the bytes across a restart — *"never drops it"* — and the coverage belongs to the same row. **The identifier of the FIRST chit at a station is unchanged (`<order_id>::<station>`)**, so a spool written before this FR is honoured rather than reprinted on the upgrade.
  - **The chit says WHICH addendum it is, in a locked region, and no profile can switch it off** — `03-F37`'s shape and `03-F32`'s reason: a marker an owner can suppress is not a mandatory marker, and the enforcement is structural (no slot addresses it) rather than a runtime check on a value the owner supplied. **It takes the document's ONE banner (`27-F56`), and where the same chit is also a reprint the two share that band rather than taking two** — *"a ticket that uses inversion twice has used it zero times"*, and a reprinted addendum is precisely the case where a naive implementation ships two banners and therefore none. The band carries the word and the ordinal in Western numerals (`00 §5.6`): **`ADDED 2`**, and **`REPRINT ADDED 2`** when it is both. `KotData` gains **exactly one field** for this — the ordinal, `0` on the chit that opened the station and `n ≥ 1` on the nth addition — and no other; there is still no money token and no field one could hide in (`03-F32`). **The wording is subject to `27-F35`'s ≥85% comprehension gate on real staff and this FR does not discharge it** — no cook has read this band, and the same is true of `REPRINT` today.
  - **The ordinal is on the paper because the content is not enough to tell two additions apart.** A family that asks for one naan and then, five minutes later, one more naan produces two chits with identical bodies. Without the ordinal a cook holding both cannot know he is not looking at a duplicate, and `03-F41`'s whole argument is that the cook who bins one is the failure this system exists to prevent.
  - **The stamp is the order's confirm anchor, unchanged.** `03-F14` makes the timer basis `order.confirmed` and the fold's anchor is the *earliest* confirm, so every chit of one ticket carries one time and the pass's aging (`03-F13`) is not forked by a late addition. The named alternative — stamping the addition's own append — is refused because the read path carries no per-line stamp, so the only value available at print time is the printing device's own clock, which `27-F62` and `01-F34` both forbid.
  - **No new event type, no new state, no new document type** (commandment 2). `order.line_added` is already consumed by this doc (§2); `kot.printed` is emitted per chit and a second one for the same order is TRUE rather than duplicated (`02-F31`'s auto-advance moves only lines a legal edge reaches, so lines already cooking are untouched); and an addendum differs from a KOT in none of the ways `03-F31` makes a TYPE — same columns, same station filtering, same reading order, same absence of price. It is a marker, exactly as `REPRINT` is.
  - **`03-F5`, `03-F34`, `03-F41` and `03-F51` bind unchanged**, and no code path may turn a transport outcome into silence. After it exists, an addendum is an ordinary print job in every respect.
  - **OWED, named rather than left to look intentional:** (1) **the counter cannot see that it owes the kitchen anything.** `02-F8`'s control reads *Send to kitchen* whether or not there is something to send, so a cashier who never presses it a second time still loses the naan and this FR does not stop her. The surface question — a state on that control which distinguishes *nothing to send* from *lines the kitchen has not been told about* — is doc 02's and doc 27's, and it is where the remaining half of this defect lives. (2) `03-F48`'s *"one-tap reprint of the last KOT per station"* must come to mean the last **chit**, not the whole order, or a reprint re-cooks everything the addendum was invented to avoid re-cooking. (3) A line VOIDED after its chit printed is untouched here — it stays committed and no chit says it was cancelled — because `void.recorded` has no producer in this product and `27-F56`'s `CANCEL`/`VOID` banners have never been rendered. (4) **K-8: no printer has ever printed any of this.**
- 03-F56 **The item note reaches the chit, and `03-F3`'s *"visually emphasized"* is spent in POSITION, not in ink (August 2026; instantiates `03-F3` against the `27-F56` budget).** `03-F3` has required *"item notes visually emphasized"* since Wave 0 and `03 §1` lists `order.note_added` among this module's consumed events — but `03-F31`'s data contract (`KotData` / `KotLine`) declares no note field at all, so `02-F6`'s *"printed prominently on the KOT"* was **unrenderable rather than merely unrendered**. This FR gives it the field and settles the two layout questions the corpus leaves colliding.
  - **`KotLine` carries `note`, and it is `user_text` — never `text`.** `document.ts`'s owner-note precedent decides this and the reason is not cosmetic: it chooses which refusal `03-F5`'s band shows. As `text` a non-Latin note refuses `non_ascii_system_text`, claiming the platform's own English is broken and making the field unprintable **for ever**, because `00 §5.6` makes English-only permanent for *interface* text. As `user_text` it refuses `raster_font_unavailable`, which names the actual state of the world (`03-F8`: the raster path is unwalked until a font and a shaping engine are chosen). A note an operator picked or typed is DATA. Byte output for a Latin note is identical either way.
  - **A note is NOT a modifier and must not be routed through `KotLine.modifiers`.** They arrive from different events (`order.note_added` vs `02-F3`'s line composition), and `27-F59` gives a *removal* modifier the item block's one inverted marker. Feeding a note in as a modifier would either steal that marker from a removal — the allergen case `27-F59` exists for — or make a note's emphasis depend on whether the dish happens to have a removal, which is the same fact printing two different ways on two tickets.
  - **The reading order applies WITHIN the item block: item → modifiers → note.** `27-F58` fixes it as *identifier → timing → items → modifiers → notes*, and that sentence is already read block-wise for its fourth term — `27-F59` puts modifiers *"indented under their item"* rather than in a document-terminal modifiers section. Reading the fifth term document-wide would separate a note from the dish it qualifies, which is exactly the mapping failure `27-F57` measures (readers who *decode* a line at ~71% *execute* it correctly at ~35%). So the note is the last row of its own item block, indented like a modifier.
  - **NO third inversion, and no 2×2.** `27-F56` allocates the ladder platform-wide and its July 2026 ruling closes both scopes: the banner is at most one per document, and the item scope is `27-F59`'s removal marker. 2×2 is allocated to the quantity and the order/table identifier. So a note has no ink level available, and taking one would be the third claimant on a budget whose own rule is that *"a ticket that uses inversion twice has used it zero times"*. `27-F58`'s answer is the one the document actually has: **vertical position encodes urgency; whitespace encodes grouping.** The note sits where nothing else sits — last in its block, below the modifiers — and that placement is the emphasis. Conditional inversion (*"invert the note when the block has no removal"*) is refused by name: it makes one fact print two ways depending on an unrelated property of the same dish.
  - **`27-F55`'s cheap-glass twin binds too.** `packages/ui`'s `QuantityItemLine` serves glass and paper from one arrangement, and it already renders a note by weight and position for this FR's reason. The two must not diverge — `03-F40`'s two sensor bit layouts is this corpus's own worked example of what one fact with two readings costs.
  - **What this does NOT decide.** No length cap and no truncation. `27-F59` bans wrapping because it destroys the vertical alignment `27-F57` depends on, and a long note therefore has a real layout cost — but `03-F49` states minimum COLUMNS and no FR states a maximum note, so capping here would be inventing one (commandment 2). The kernel must not truncate either: a ledger and a projection that disagree about the same event is `catalog-fetch.ts`'s dropped-field defect moved into `packages/domain`. `02-F50` keeps the input a bounded pick list in Wave 1, which is what makes the unbounded case rare rather than solved.

**The failed ticket after the power cut (August 2026)**

- 03-F57 **A kitchen ticket that failed and was never re-sent is still re-sendable after the device restarts, and the eligibility test is the DURABLE job — never a control that exists only in this process.** `03-F4` persists every job *"before the first transmit attempt"* and says a crash *"resumes or reprints the job on restart — **never drops it**"*; `03-F6` puts the recovery on the failure alert and `03-F48` puts a one-tap reprint on the counter's order list for the printer-only kitchen that is *"most deployments"*. **Measured on the shipped counter, August 2026, not predicted:** `main/printing.ts` holds the raised bands in a process-lifetime `Map` and refuses any resend whose `alarm_id` is not in it, so the durable spool still carried the `failed` job with its bytes and its `03-F55` coverage while the only control that could reach it had evaporated. A power cut — the event `03-F4`'s storage clause and `ops/startup/*.bat`'s `:loop` both exist to survive — therefore **permanently stranded a billed order the kitchen had never heard about**, with no ticket, no second chance and nothing on the glass admitting it. This is `03-F55`'s own sentence one door along — *"a record held only in the process is defeated by a relaunch, and the relaunch is exactly when both failures appear"* — applied to the RECOVERY rather than to the coverage.
  - **The property is a claim about the spool, and it is the only thing this FR fixes.** A job whose state is `failed`, whose bytes are on `03-F4`'s durable row, and which no later attempt has driven to `printed`, is re-sendable. `03-F44` is why this is safe rather than generous — *"the paper is not the record, so reprinting is always safe"* — and `03-F41`'s ban on double-printing is untouched, because it is decided from the same durable state this reads: `printed` is refused, `stalled` is refused, and a band `03-F34` raised with no job behind it has nothing to send. **What may not decide it is whether this process happens to be holding a band.**
  - **It is not a re-transmit and it is not an extension of `03-F4`'s budget.** Nothing here makes a terminal job resume by itself, and a restart must send no bytes on its own account: `03-F4` grants three attempts, `03-F5` hands what is left to a human, and a relaunch that quietly spent a fourth attempt would double-print the instant a printer that was merely unplugged came back. The act is a person's, and the resent chit carries `03-F37`'s `REPRINT` band exactly as it does today.
  - **A restart appends no second `kot.print_failed` for a failure that already has one.** `01-F1` makes every row permanent and `15-F14` pages vendor support on *"`kot.print_failed` rates"*, so a per-launch re-emission would forge a rising failure rate out of one dead printer and one `:loop`. The event belongs to the transition, and a restored row transitioned nowhere.
  - **What this does NOT decide, stated so silence is not read as blessing.** (a) **Whether the BAND comes back at launch is still open and is deliberately left open.** `03-F5`'s alert *"repeats until acknowledged"* and no acknowledgement is persisted anywhere — `01-F5`'s closed `audit.*` subtypes have no member for it, which `kot-printing.test.ts`'s DEFERRED block already records — so re-raising every restored `failed` job produces a band staff cannot clear and arrive to every morning, which is how `03-F5`'s loudest signal is taught to be ignored, while re-raising none loses the notice. Both directions need the same missing FR and this one does not supply it. (b) **Which restored jobs are still LIVE.** No FR gives a spooled job a lifetime, `03-F4` has no compaction clause and `22`/`25`'s retention question is unanswered, so *"yesterday's failed chit"* has no defined status here. (c) **How the operator REACHES the control after a restart.** `03-F48`'s one-tap reprint on the counter's order list is the corpus's own answer for a printer-only kitchen and it is **OWED**; until it ships, a resend that is eligible may still have no button, and a fix that closes only the eligibility half must say so rather than claim the defect closed.

**The band after the power cut (August 2026)**

- 03-F59 **`03-F5`'s band comes back at launch for a kitchen ticket that is still genuinely unrecovered, it comes back for the CURRENT business day only, and it appends nothing.** This supplies the missing FR `03-F57` (a) names — *"both directions need the same missing FR and this one does not supply it"* — and closes `03-F57` (c)'s reach half for today's tickets. `03-F57` made the durable spool the eligibility test, and its own closing clause records what that left: *"a resend that is eligible may still have no button"*. **Measured on the shipped counter, August 2026, not predicted:** after a relaunch the spool still held the `failed` chit, its bytes and its `03-F55` coverage, `resend(job_id)` would have transmitted it — and `main/printing.ts`'s `reconcile(before)` takes its baseline from `spooler.jobs()` at the top of every `pump()`, so a restored terminal row differed from nothing, transitioned nowhere and raised nothing. **No surface on the glass could call the control that was working.** A power cut therefore still stranded a billed order the kitchen had never heard about, which is the harm `03-F57` was written to remove.
  - **The band, and not a new surface, because the corpus already put the recovery there.** `03-F6` places the resend *"from the failure alert"* and `03-F5` places that alert on the host device — the counter. `03-F48`'s one-tap reprint on the order list is a SECOND route and it does not exist; inventing a third here would be a surface no FR names (commandment 2). Restoring the band restores `03-F6`'s own control on `03-F5`'s own surface, with `03-F57`'s refusal wording included, so a refused resend in a fresh process stops being silent as well.
  - **"Genuinely unrecovered" is a claim about LINES REACHING PAPER, not about a row's state alone.** A ticket qualifies when its job is `failed`, when `03-F55` recorded on its durable row which lines it carried, and when none of those lines has since been carried to paper by a later chit at the same station. `printed` and `stalled` are refused exactly as `03-F57` refuses them (`03-F41` — a duplicate KOT is a real kitchen error), a job still inside `03-F4`'s budget is not a failure yet, and a row written before `03-F55` kept no coverage and is skipped: `03-F57`'s resend refuses it by name, and a band offering a control that has already said no is `27-F5`'s failure mode.
  - **The current business day only (`01-F46`, Asia/Karachi, 05:00 cutover), and this is the narrowing that makes re-raising defensible at all.** `03-F57` (a)'s objection is exact and stands: re-raising every restored failure produces a band staff arrive to every morning and cannot clear, which is how `03-F5`'s loudest signal is taught to be dismissed unread. The date is the ORDER's confirm anchor — `03-F14`'s timer basis, branch time stamped at APPEND (`01-F43`) — compared against the device's own business day; never the spool row, which carries no time at all, and never a raw device clock (`01-F45`). **This does not give a spooled job a lifetime and does not close `03-F57` (b).** Eligibility is untouched: Friday's chit is still re-sendable on Monday, it simply does not shout. What that leaves is stated in the last bullet.
  - **It appends NOTHING** — no `kot.print_failed`, no `audit.*`, no event of any kind. `03-F57`'s third clause governs unchanged: `01-F1` makes every row permanent and `15-F14` pages vendor support on *"`kot.print_failed` rates"*, so one dead printer under `ops/startup/*.bat`'s `:loop` would forge a rising failure rate out of a single failure. The event belongs to the transition, and a restored row transitioned nowhere. A restart still transmits nothing on its own account either (`03-F57`): the band is an offer, the act is a person's.
  - **DECLARED, because the corpus does not resolve it: a band the operator ALREADY dismissed comes back.** Acknowledgement is persisted nowhere — `01-F5`'s closed `audit.*` subtypes have no member for it — so no implementation can tell an unacknowledged band from a dismissed one across a relaunch, and this FR does not invent one. The business-day bound is what bounds the cost: within one day, at most once per relaunch, and only for a ticket whose food genuinely never reached the kitchen. **Persisting the acknowledgement is the correct closure and is OWED**, and it is the same missing fact `main/printing.ts`'s cash-printer note already records.
  - **Cash slips and receipts are deliberately NOT restored.** Neither band carries a control: `01 §4` has no reprint act for either, `02-F16` makes a second receipt a named fraud vector, and a slip's reprint is owed with the surface that offers it. A restored band there would be a full-width red interrupt whose only control is `I SAW THIS`, with nothing to recover — the cost this FR's day bound exists to avoid, paid for nothing.
  - **What this does NOT decide, stated so silence is not read as blessing.** (a) **A chit that failed on an EARLIER business day has no button**, exactly as `03-F57` (c) says: it is eligible and unreachable, and `03-F48`'s one-tap reprint on the counter's order list is the corpus's answer and is still **OWED**. That is the residual harm of the day bound and it is chosen deliberately over the alternative harm. (b) **Acknowledgement persistence**, above. (c) `03-F57` (b)'s retention question, untouched. (d) **K-8** — no printer has printed any of this, and no plug has been pulled: every restart behind this FR is a process kill and a second launch over the same spool.
  - **Measured on a launched till, 2026-08-15, because a green suite cannot see this FR at all.** Two orders on a real counter under Xvfb against a TCP listener on 9100: one confirmed with the listener UP (chit taken, job `printed`), one with it DOWN (three attempts, job `failed`, `03-F5` banded). The till was **SIGKILLed** and relaunched over the same durable spool and device store: **the band came back — `KOT 01a002c6 did not print — TH230 / printing failed after 3 attempts / SEND AGAIN` — and the printed chit's did not.** The listener was restored, `SEND AGAIN` pressed, and the resent document arrived carrying `REPRINT`; the durable row kept its `job_id` and its `covers` and went `failed → printed`. **The ledger across the whole relaunch and resend: `order.confirmed` 2 → 2 and `kot.print_failed` 1 → 1.** The day bound was measured over that same durable state with `businessDay` as the only variable: its own day → one band; the next day → **zero**; the seam omitted → **zero**. **The rails saw none of it** — with the restore call removed all 1188 package tests stay green and `seams:check` stays exit 0, so a `03-F59` rung on the printer's `alarms()` after a second construction is OWED to a test session.

**The held ticket nobody was told about (August 2026)**

- 03-F58 **A printer holding a ticket for a missing roll is SAID on the counter, and it is said as a STATE that ends by itself — never as `03-F5`'s S1 band and never as a ledger event.** `03-F41` makes a stalled printer *"holding the job, not dropping it"* and holds it *"until the roll is replaced"* — an act by a human, which nothing in this product asks for. **Measured on the shipped counter, August 2026, against a TCP listener answering `DLE EOT 4` with `03-F40`'s bits 5,6 set:** the job reached `stalled` with `attempts: 0` (`03-F41` working exactly as written, never re-transmitted), and then **`alarms()` was `[]` for the whole run, the glass said nothing anywhere, and the ledger carried five rows and not one about the printer** — no `kot.print_failed`, no `printer.status_changed`. Worse than nothing: the counter's own `02-F55` control read **`the kitchen has this order`**, which is false — the PRINTER has it. So the one surface that could have said it said the opposite, and the ticket sits held until somebody walks into the kitchen and looks at the roll. Running out of paper is the most ordinary thing a thermal printer does.
  - **The corpus asserts that something says it and does not resolve what, which is why this is an FR and not a patch.** `03-F5`'s three consequences all hang off *"when retries exhaust"*, and `03-F41` takes a stall out of `03-F4`'s budget by design — so the budget never exhausts and `03-F5` never fires. The only sentence anywhere that points at a surface is `03-F54`'s *"Paper-out has its own machinery (`03-F41`'s hold, `03-F5`'s band)"*, and it points at a band whose trigger clause a stall cannot satisfy and whose two controls are `I SAW THIS` and `03-F6`'s resend, the second of which `03-F41` forbids by name.
  - **It is NOT `03-F5`'s S1 band, on three grounds that resolve rather than prefer.** (i) `27-F14` allocates red to a **closed** table of claimants and a printer holding a job for a roll is not on it. (ii) The band's controls are dismissal and `03-F6`'s resend — offering resend on a held chit is the duplicate KOT `03-F41` spends a paragraph on, and a band with the resend taken away is a full-width red interrupt whose only control is `I SAW THIS`. (iii) **A stall is a STATE, true until the roll is replaced; a band is for an EVENT that has already happened once.** An attributed acknowledgement would take a live condition off the honesty surface, which `00 §5.7` forbids — and this product has already made and shipped that exact argument one fact over: `01-F56`'s catalog refusal went to `CatalogHealth` on the strip and not to the band, for these three reasons in this order.
  - **Where it goes: the honesty strip beside `CatalogHealth` and `PanelHealth`** — amber (`27-F14`), **no control at all** (`27-F5`: a control that cannot act is not a control), **nothing whatever when healthy** (`27-F16`). It names `03-F5`'s two nouns, the printer and the ticket, and it names the act: replace the roll. The words are written in MAIN and never in the renderer (`AlarmSchema`'s precedent, `18 §9`).
  - **It appends NOTHING, and the gap that leaves is stated rather than filled.** `kot.print_failed` would be false — the job did not fail, it is held — and `05-F3` alarms on it while `15-F14` pages vendor support on its *rates*, so a nightly paper change would manufacture a support incident, permanently under `01-F1`. `printer.status_changed` is refused by `03-F54` **by name** (*"Emitting `offline` for paper-out would send a manager to check a cable"*). `01 §4` carries no third type, and commandment 2 stops here. **The consequence: doc 05's manager and doc 15's fleet health cannot see a held printer at all** — a branch manager learns about it by walking to the kitchen. Closing that needs an event type this corpus does not have, and inventing one here would be the thing this clause exists to refuse.
  - **It ends by itself, and that is what makes it safe to leave on the strip.** The spooler releases a held job on `!paper_out` and marks it `printed` (`03-F41`, `03-F40`'s query), so the notice is a READ of the spool and never a latch: when the roll returns the notice goes, with no acknowledgement and no act. A chip that could only be cleared by a human is the permanent chip staff stop reading, which is `03-F5`'s own failure mode arriving on a quieter surface.
  - **What this does NOT decide, stated so silence is not read as blessing.** (a) **A printer UNPLUGGED while a job is held.** It answers nothing, `main/printer-link.ts` reports its two-valued `PaperStatus` as `paper_out: true` because that is the only way the type can say *"I cannot tell you the roll came back"*, and the job stays held for ever with the notice up. That is correct and it is not comfortable: the till genuinely cannot tell *no roll* from *no printer* through a link that has gone silent, and `03-F41` forbids the retry that would find out. **How long a stall may last is an FR this corpus does not have**, `03-F4` gives a spooled job no lifetime, and this one does not supply either. (b) **`device://` and `windows://` are write-only.** `03-F40` is unreachable on them, a paper-out reads as a successful transmit, and **no notice can be raised for a state the link cannot observe** — the boot line already says so and `tcp://` is the answer wherever the printer has an Ethernet port. (c) `27-F35`'s ≥85% comprehension gate is untouched: no cashier has read these words. (d) **K-8. No printer has ever run out of paper here** — every measurement behind this FR is a TCP listener answering the bits this FR describes, and whether a real head holds, recovers on `DLE ENQ 2`, or wedges the way `03-F41`'s degenerate case does is a rig question.
  - **⚠ THE SURFACE IS OWED AND THE CHEAP VERSION IS FORBIDDEN, which is the load-bearing half of this FR.** `main/printing.ts` already sees the stall; the honesty strip is five files away — `shared/ipc.ts` (a `DeviceState` field), `main/gateway.ts` (the projection), `main/index.ts` (the seam), `packages/ui` (the chip, a `21 §2` closed-vocabulary addition) and `renderer/Counter.tsx`. Until that lands **a stall is still silent and this FR is not closed.** What must NOT be done in the meantime is the one-line version: `kot-printing.test.ts`'s *"a stalled printer raises NO band, emits NOTHING"* is **correct under this FR and must stay green**, because an implementation that reaches the glass by pushing the stall into `KotPrinter.alarms()` satisfies the letter of *"a human is told"* and breaks (i), (ii) and (iii) at once.


## 4. Key flows

**Addition after confirm (`03-F55`)**
1. Order confirmed → grill chit + tandoor chit print. The customer asks for one more naan → `order.line_added` → the cashier presses **Send to kitchen** again → the grill has nothing uncommitted and gets **nothing**; the tandoor gets a chit banded `ADDED 1` carrying the naan and no other line.
2. She presses it once more by mistake → both stations have nothing uncommitted → **no bytes, no band, no event**.

**Confirm → multi-printer KOT (happy + failure)**
1. `order.confirmed` arrives (LAN fast path) → routing resolves grill + Chinese printers → 2 jobs persisted → both print < 2 s from confirm.
2. Failure: grill printer offline → 3 retries fail → loud alert on the counter device + `kot.print_failed` → manager console alarm (doc 05) → operator reroutes the job to the Chinese printer (03-F6) or fixes the cable and reprints → every step logged.

**Cross-station assembly (T3)**
1. Order with grill + tandoor + fryer lines confirms → each station screen shows only its lines.
2. Grill bumps → pass shows "1 of 3 ready"; tandoor bumps → "2 of 3, waiting on fryer"; fryer bumps → card fully ready → the ready-signal owner marks the order ready → waiter notification fires (doc 04).

**Ready-mark trains the pipeline**
1. Pass marks a karahi line ready 14 min after confirm during rush → sample lands cloud-side on next sync → nightly job updates the `karahi × rush` cell → the cell crosses 30 samples with acceptable spread → `eta.estimates_published` → storefront (doc 06) begins quoting. Nothing changed for kitchen staff at any point.

**Plug-pull mid-print**
1. Power cut while a KOT is printing → on restart the spooler finds the job not in `printed` → reprints it marked "REPRINT" → no order lost (00 §4 durability protocol).

**Printer swap**
1. A printer dies permanently → replacement attached → layer-3 assignment updated on-device → test harness one-tap verification page → routing resumes; no cloud round-trip required.

## 5. Data

- **Owned (device-local):** `print_jobs` (durable spool: payload, printer, attempts, state), printer registry + live status, station map cache.
- **Owned (cloud read models):** `prep_time_samples`, `eta_estimates` (per item × segment × branch, versioned), printer compatibility list.
- **Emitted:**
  - `kot.printed / reprint_requested / print_failed`
  - `order.line_state_changed` (ready-marks, bumps, served)
  - `availability.changed` (pass toggle)
  - `cash.drawer_opened` (kick execution; POS-attributed)
  - `audit.*` (alert acknowledgments, reroutes, harness runs)
- **Extensions to 01 §4 introduced by this doc:** `printer.status_changed`, `eta.estimates_published`.
- **Consumed:** `order.created / confirmed / line_added / note_added / line_state_changed`, `availability.changed`, receipt job requests (doc 02).

## 6. Non-functional requirements (module-specific)

- 03-N1 Confirm → first byte at printer < 2 s (00 §5.3) with 3 routed printers in the fan-out.
- 03-N2 Spool durability: plug-pull mid-print never loses a job (00 §4 protocol); spool writes sit inside the same durability envelope as order events (01-F2).
- 03-N3 Print-failure alert raised ≤ 10 s after final retry exhaustion; alert audible at kitchen noise levels (device at max volume, tone chosen for cut-through).
- 03-N4 Pass/KDS render an incoming state change < 1 s over LAN (01-F15); a queue of 40 open orders **pages** without dropped input (`03-F46`; the earlier "scrolls" is SUPERSEDED, and the "2–3 GB reference tablet" is superseded by 27-F11f's 22-inch panel).
- 03-N5 Pipeline jobs never touch the serving path: estimation runs cloud-side only; a branch fully offline for a week keeps printing, marking ready, and buffering samples locally.

## 7. Customizability

- **Layer 2 (org):** category→printer routing, station map, KDS-vs-printer per station — the **station fulfilment route**, `paper | screen | both`, named and made refusable by `03-F51` — aging thresholds X/Y per order type, ready-signal ownership, **serve-signal ownership** (`03-F52`), rush threshold, course grouping on/off, retry count/window, assembly buffer minutes.
- **Layer 3 (branch/device):** printer assignments + **printer capability profile**, station identity, alert tone/volume, recall-strip length. **"Paper width" as a `58 | 80` enum is WITHDRAWN (July 2026) — it silently truncates the PRICE column.** The folklore that 80 mm means 42 or 48 columns is wrong twice over: 42-vs-48 is a *resolution* difference, not a font one, and there is a third value — a TH230 at 576 dots gives **44** Font-A columns while a TM-P80 at the same 576 dots gives 42 and a TM-T20II gives 48. Layout is therefore expressed in **columns**, derived as `print_dots ÷ font_cell_dots` (Font A = 12, Font B = 9) from a per-model capability record `{model_id, dots, dpi, cols_font_a, cols_font_b, has_native_qr, has_cutter, raster_ok}`, seeded from a shipped table, defaulting **conservatively to 32** for an unknown model, with a technician override in the 03-F10 test harness (which already maintains the compatibility list). Millimetres are used only where a regulator specifies mm — the fiscal QR.
- **Deliberately not configurable:** disabling print-failure alerts (never), any form of system-dictated cook order (never), sample capture (always on — it is a pure side-effect), the confidence-gate floor (platform layer only).

## 8. Tech notes

- `packages/escpos`: pure-TS encoder; transports — Electron main via node-usb/serial + net sockets; RN via a BT SPP/BLE module + TCP. Logos and QR codes rasterized at the target dot width and cached.
- The spooler is a plain SQLite table + state machine in the POS host process. In T2 default the counter prints for the branch; the service can run on any device with attached printers (layer 3) — the pass tablet never needs to own printers.
- Pass and KDS are one Expo app with a mode switch (pass vs station) — one codebase, one update channel (doc 15).
- Estimation job = SQL over the events read model + a small TS job; no ML dependency at this rung of the autonomy ladder (concept §4.6).
- The physical printer rig (00 §4) gates releases: a `packages/escpos` change that fails the rig's soak test does not ship.

## 9. Open questions

1. BLE printer reliability in the field vs SPP — harness data decides the default transport ranking per printer model.
2. Bump granularity: per-line vs whole-station-ticket un-bump semantics under heavy modifier use.
3. Should ETA cells segment by variant (or modifier count) where sample volume allows — decide from pilot sample density.
4. Whether the pass gets a dedicated expo-printer option (pass-side summary/sticker print) — pull from pilot demand.
5. Aging-timer basis for scheduled/pre-orders (doc 06 future) — confirm-time basis breaks for orders placed hours ahead; likely needs a `fire_at` concept before doc 06 ships pre-orders.
