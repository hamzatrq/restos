# 05 — Manager Console

**Module spec — Draft 1, July 2026** · Parent: `00-platform-overview.md` (conventions §5–§7 inherited), `01-kernel-sync.md`. Wave 1 core (alarms + ~~approval interrupts~~ **an approval QUEUE she reads and then acts on at the till — `05-F32`, R48**); Wave 4 full (floor state, channel pulse, day/cash flows, paid-out approvals).

**⚠ THE RUNTIME AND THE CORE SLICE BOTH MOVED IN AUGUST 2026 AND §3 BELOW IS WRITTEN AGAINST THE OLD SHAPE. READ `05-F31` AND `05-F32` FIRST.** `05-F31` (R22, R49): this is a **browser** surface and the Expo RN app is superseded, its scaffolding deleted. `05-F32` (R48): **approvals stay till-local and the console renders only.** Both are transcriptions of founder rulings that sat in `plans/saas-pivot/plan-of-record.md` and reached no spec — measured 2026-08-21, `grep -arno "R49" specs/` returned 0 — during which time this document went on ruling an RN app and an agent routed here would have built one. It is also one of R19/R45's **seven surfaces designed on the new language before any restaurant uses it**, and one of the two that are designed from nothing rather than restyled.

## 1. Purpose & scope

The manager's operational surface, running on the **manager's own phone** — ~~React Native, Android + iOS~~ **in a BROWSER (`05-F31`, founder rulings R22/R49: the Expo RN app is superseded and its scaffolding deleted)**. It replaces walk-to-counter interruptions with interrupts that come to the manager — ⚠ **but not with remote APPROVAL, which `05-F32` (R48) rules stays at the till; read both FRs before scoping anything below, because §3's Wave-1 core is written against the superseded shape**:

- **Wave 1 core:** late-order alarms, print-failure alarms, and ~~approval interrupts — void/comp/discount approvals arrive as one-tap PIN-authorized actions on the manager device, fully logged~~ **an approval QUEUE: void/comp/discount requests arrive as a render with enough context to decide (`05-F5`), and the decision itself is a one-tap PIN act at the TILL (`02-F20`, `05-F32`), fully logged. ⚠ This is less than `00 §1`'s "1 core" row claims and `05-F32` states the cost rather than hiding it.**
- **Wave 4:** floor state (table map), channel pulse (per-channel load; pause/throttle a channel when the kitchen is drowning), day open/close and cash-count flows, paid-out approvals.

~~Works in-branch over LAN and remotely over cloud;~~ **⚠ CLOUD ONLY (`05-F31`): a browser is not a LAN mesh peer and holds no branch slice, so every view on this surface is a remote view and** remote views always carry sync-honesty labels (00 §5.7). All tiers and profiles get the console; floor state requires T3 tables (doc 04). This is the *operational* manager surface — the owner's analytical surface is doc 12; back-office configuration is doc 14.

**⚠ Build status, August 2026 — this module's Wave 1 core is UNBUILT and nothing in `plans/` schedules it.** ~~`apps/manager` is a two-line scaffold stub, so~~ **⚠ STALE IN BOTH DIRECTIONS, re-measured 2026-08-21 (`05-F31`): `apps/manager` is not a stub — 6 source files, 7 acceptance suites, 3,718 lines, a real branch slice — and it is also not the surface any more, because R49 deletes it. What survives the deletion is the pure part (`alarms.ts`, `home.ts`); what does not is the Expo host.** As to coverage: 05-F1..05-F4 (alarms) and the remote half of 05-F5..05-F7/05-F9 (approvals) have no surface at all; 02-F20's *local* PIN path did ship on the counter (see 05-F8), and `05-F32` now rules that local path is **the** approval path rather than the fallback. Recorded here because it is a **scope** fact a reader of §3 cannot otherwise see: `restaurant-os.md` §8 names "manager alarms+approvals" in Wave 1 and `00 §1` rows this module **1 core / 4 full**, while `plans/wave-1/` covers six areas and none is doc 05. **Not a licence to build it** — sequence is a founder call, and this note cannot distinguish deliberate deferral from oversight. Reconciliation, with the same delta for docs 08/12/13: `plans/wave-1/wave-1-scope-reconciliation.md`. **⚠ Read `05-F28`, then `05-F31` and `05-F32`, before scoping any of it** — `05-F28`'s measurement stands (a cloud-plane surface cannot record a decision at all) and is no longer a *blocker*, because R48/R49 answer it by shipping less: a browser console that **renders only**, with the decision left at the till.

## 2. Position in platform

- **Depends on:** kernel (doc 01) — ~~full branch slice on the manager device, PIN auth, fast-path LAN~~ **`01-F7` read models over tRPC (`05-F31`, `18 §6`); no branch slice, no PIN session on this surface and no LAN leg. ⚠ It therefore depends on the TILL for every act (`05-F32`), which is a dependency this doc never had and is the reason `05-F30`'s ack has no emitter**; aging thresholds and print-failure events (doc 03).
- **References:** doc 02 (escalation flows; the day/cash ownership boundary in §3 below), doc 03 (aging config, `kot.print_failed`, `printer.status_changed`), doc 04 (`table.state_changed` for floor state), doc 06 (storefront pause behavior), doc 08 (foodpanda pause/availability push), doc 12 (owner app — a distinct surface), doc 13 (throttle suggestions via the autonomy ladder, future).
- **Events emitted** — ⚠ **read "OWNED AND DEFINED BY", not "emitted by this surface", which is nothing (`05-F31`, `05-F32`, `01-F62`; §5 has the per-type detail):** `approval.requested / granted / denied` (extension, defined here — **emitted by the till**), `channel.paused / resumed / throttled` (extension), `day.opened / closed`, `cash.deposit_recorded`, `availability.changed` (**Wave 4; emitting device unassigned, §9.7**), `audit.*` (alarm acknowledgments — **no emitter, §9.6** — and PIN authorizations, which happen at the till).
- **Events consumed:** `order.*` (aging, queue), `kot.print_failed`, `printer.status_changed`, `table.state_changed`, `shift.opened / closed`, `cash.*`, `void/comp/discount.recorded`, `eta.estimates_published` (context in channel pulse).

## 3. Functional requirements

**Wave 1 core — alarms**
- 05-F1 Late-order alarm: when an order (or line) crosses the red aging threshold (03-F14), the console raises an alarm naming order, channel, table, and age. Delivery:
  - in-branch: LAN fast path, < 2 s;
  - remote: high-priority push (FCM/APNs) whenever the branch has WAN.
- 05-F2 Alarms persist in an active list until the order goes ready/served or the manager acknowledges; acknowledgment is logged (`audit.*`, hash-chained per 01-F5). Alarms are never auto-dismissed silently.
- 05-F3 Print-failure alarms: `kot.print_failed` and `printer.status_changed(offline)` raise on the console with the same persistence rules — "the kitchen can't print" must reach the manager even off the floor (03-F5 companion).
- 05-F4 Alarm volume discipline: one alarm per order per threshold crossing; repeated crossings collapse into the existing alarm with an updated age. The console must stay useful during a bad rush, not become a siren wall.

**Wave 1 core — approval interrupts**
- 05-F5 POS escalations (02-F20) arrive as interrupts: void-after-KOT, comp, discount above threshold, price override. The interrupt card shows:
  - requester (name, role) and requesting device;
  - order/line refs, item names, amounts;
  - stated reason;
  - context: order total, channel, table — ~~enough to decide without walking over~~ **enough to decide BEFORE walking over (`05-F32`, R48: she walks). The card's content requirement is unchanged and if anything sharper — she arrives at the till having already decided, which is what keeps `05-F27`'s ≤3 taps meaningful — but this clause's stated purpose is inverted and is marked rather than reworded, because it is the sentence that names what the ruling costs.**
- 05-F6 One-tap approve/deny, authorized by the manager's PIN ~~**on the manager device**~~ **⚠ NOT IN FORCE — `05-F32` (R48): the PIN is the manager's, entered on the TILL (`02-F20`), and there is no remote decision surface. Everything after this clause is unchanged and shipped.** → `approval.granted` / `approval.denied` referencing the request; the grant propagates to the requesting POS over LAN < 1 s and unblocks it. The resulting `void/comp/discount.recorded` carries actor + approver; every decision is fully logged.
- 05-F7 Event extension defined by this doc: `approval.requested / granted / denied`. Request payload:
  - `approval_type`: `void | comp | discount | price_override | paid_out`;
  - `refs[]` (order/line/paid-out ids), `amounts`;
  - `requester_id`, requesting `device_id`;
  - `context` (order total, channel, photo ref for paid-outs).
  Grants reference the request id, are idempotent, and the first response wins (02-F20).
- 05-F8 Fallback and timeout: if no manager device responds within N s (default 30, layer 2) or none is reachable, the POS local manager-PIN path (02-F20) remains fully available. Remote approval augments service; it never gates it. **⚠ HALF TRUE AS BUILT (August 2026) — the fallback exists; the thing it is a fallback *for* does not.** ~~NEITHER of `02-F20`'s two paths exists … Owed: the `02-F20` local-PIN surface, before this sentence is true.~~ **That note, added earlier in August 2026, was false when written and is superseded here.** What is true: `02-F20`'s **local manager-PIN path is BUILT and reachable** on the Electron counter — the counter asks the matrix whether a manager credential would close a refused write, raises a PIN pad when it says `escalate`, and appends with actor + approver on `01-F61`'s durable per-(device, user) Argon2id lockout, so an above-threshold paid-out (05-F19) can now be performed. `02-F20`'s **remote path is genuinely absent** — the console is unbuilt, so *every* approval today is a walk to the counter, and the N s timeout has nothing to time out. **The consequence for this FR is inverted, and that is the point of recording it:** the risk is no longer an operator stranded with no path, it is a reviewer reading "remote approval augments service" as *shipped* and costing the paid-out flow its only authorization surface. ~~**Owed: the remote path (doc 05), and the timeout with it.**~~ **⚠ NOT OWED — WITHDRAWN by `05-F32` (R48): the remote path is not being built, so the local path is not a *fallback*, it is THE path, and the N s timeout has nothing to time out. That inverts this FR a second time: the first correction warned a reviewer against reading "remote approval augments service" as shipped; the ruling now says it will not be. `05-F27`'s tap budget is what governs, and layer-2's *approval timeout N* (§7) is inert until this reopens.** *Why the correction is kept rather than overwritten:* the sentence was copied from a package guide that had already gone stale, into `specs/`, where the 00 authority order makes it outrank every `CLAUDE.md` that said otherwise — a stale gap-claim is not a harmless lag, it re-opens closed work.
- 05-F9 ~~Approvals work remotely over cloud when WAN is up;~~ **⚠ FIRST CLAUSE NOT IN FORCE — `05-F32` (R48). The rest BINDS and is if anything more load-bearing than when written:** the remote approval card — now a **render**, not a decision surface — shows data age (00 §5.7) before the manager acts on it, ~~and the decision still lands as the same events~~ **and the decision she then takes at the till lands as the same events, from the till (`01-F62`). A stale queue is the only thing she can act on, so the age label is the whole honesty story here.**

**Wave 4 — floor state**
- 05-F10 Live table map folded from `table.state_changed` (doc 04) + open-order state: per table — state, age in state, order total, waiter. T3 only.
- 05-F11 The floor map is read-only except needs-bill acknowledgment; changing an order or table happens on the owning surface (docs 02/04). Same fold as POS and waiter map — one fold, three renderers (`packages/domain`).

**Wave 4 — channel pulse**
- 05-F12 Per-channel load view — the "is the kitchen drowning" glance. Per channel (dine-in, phone, storefront, WhatsApp, foodpanda):
  - open orders now;
  - confirms per 15 min (against the branch's usual rush curve);
  - aging distribution (count in amber, count in red);
  - print-failure count for the day.
- 05-F13 Pause a channel: `channel.paused` (extension; payload `{channel, reason, auto_resume_at?}`). Pausing the storefront makes it show an honest temporarily-closed state (doc 06); pausing foodpanda triggers the aggregator availability/closing push (doc 08). Resume is `channel.resumed`, manual or by the `auto_resume_at` timer.
- 05-F14 Throttle a channel: `channel.throttled` (payload `{channel, added_eta_minutes?, max_orders_per_15min?}`) — quoted ETAs stretch and/or intake caps, applied by the consuming channel doc (06/08).
- 05-F15 Every pause/throttle/resume is attributed and logged; the owner app surfaces the episode (doc 12), and doc 13 reads them as training signal for future suggestions.
- 05-F16 Staff-operated channels (dine-in, phone entry at POS) cannot be paused from the console — drowning there is a people decision, not a switch. Deliberate exclusion.

**Wave 4 — day, cash, paid-outs (ownership boundary with doc 02)**
- 05-F17 Boundary rule: **cashier-attributed drawer flows live in doc 02; manager-attributed flows live here, with a full POS fallback.** Counting always happens where the cash is; both surfaces emit the identical kernel events; no flow exists twice with different semantics.
  - Doc 02 owns: shift open/close per cashier, cashier drawer counts, float entry at the drawer, paid-out capture (reason + photo).
  - Doc 05 owns: day open/close initiation, the manager's day-close count entry, `cash.deposit_recorded`, paid-out approval.
- 05-F18 Day open/close from the console: the manager opens the day (float confirmed at the drawer via POS, or entered directly when the manager stands at the drawer with their phone) and closes it with the manager count + deposit record → `day.opened / closed`, `cash.deposit_recorded`.
- 05-F19 Paid-out approvals: `cash.paid_out` above the org threshold requires approval → arrives as an interrupt with the receipt photo inline → approve/deny per 05-F6/F7 (`approval_type: paid_out`).
- 05-F20 Shift-close visibility: cashier over/short results (02-F23) appear on the console as they land; variance beyond threshold is highlighted. The cashier-sees-own-reconciliation framing (02-F23) is unchanged — the console adds the manager's cross-cashier view, nothing about it replaces the cashier's own screen.

**Support inbox (Wave 2, with doc 07)**
- 05-F24 The console is the default target for WhatsApp support routing (07-F9): inbound customer support messages render as a threaded inbox (customer, order context if linkable, message history); replies send through doc 07 as attributed session messages. POS (doc 02) may be the org's configured alternative/additional target — same thread state, synced.
- 05-F25 Unanswered-support alerting: a thread with no staff reply past the org threshold (07 layer-2 setting) raises a console alarm — a support message silently ignored is a lost customer.

**Console home**
- 05-F21 The home screen is a glance, not a dashboard: active alarms, pending approvals, open-order count with aging summary, shift/day status, channel-pulse tile (Wave 4), support-inbox badge (Wave 2). Deep analytics stay in doc 12 — the console exists for acting in the next sixty seconds.

**Remote mode**
- 05-F22 The full console works over cloud from anywhere. Every screen shows last-synced age; when the branch is unreachable, the console says so plainly ("branch offline — last seen 12 min ago") and never renders stale state as live (00 §5.7).
- 05-F23 Remote alarm push continues via FCM/APNs whenever the branch has WAN; while the branch is offline, the console shows the alarm gap honestly instead of implying calm.

- 05-F26 **Pausing a channel offline takes effect LOCALLY and shows the aggregator leg as unsent (gap G15, founder ruling July 2026).** `05-F13`'s pause needs an aggregator API push that a WAN-down branch cannot make, and nothing said what the manager sees. **The branch has genuinely decided to stop accepting, so record that immediately** (`01-F17` spirit — never block the local decision on a remote system), and render the aggregator leg as a **separate, visibly unsent fact** per `00 §5.7`'s honesty rule, retried by the doc-08 outbox. The manager is told exactly which half took effect: one number of channels paused here, one count of pushes still owed. Orders that arrive from the still-live aggregator in the meantime are **not** auto-rejected — auto-rejection carries commercial consequences with the aggregator (`08`) and is not a side effect to take on a WAN blip.

- 05-F27 **The walk-over PIN approval budgets the TAPS, not the walk (gap G11, founder ruling July 2026).** `02-F20`/`05-F8` make an in-person manager PIN the fallback whenever the phone is unreachable, and `21 §4` budgets only the machine path (`05-N1`). The human path is now budgeted where software can actually move it: **the pending request is visible on the terminal without searching for it, and approval is ≤3 taps from that state.** The walk itself is deliberately unbudgeted — it is floor layout, not software, and a metric nobody can act on is worse than none. **⚠ PROMOTED, not amended (`05-F32`, R48): this FR was written to budget a *fallback* and it is now the budget for the ONLY approval path, so it carries weight `21 §4` never sized it for. Nothing in its text changes; what changes is that `05-N1` has no path to measure and this is the only number left. It is also the FR a pilot will complain against first — R48's reopen trigger is a manager saying the walk is the problem, and ≤3 taps from an unsearched-for state is the software half of what makes that complaint unlikely.**

- 05-F28 **THE CONSOLE IS A DEVICE, AND THAT IS A CONSTRAINT ON THE PLATFORM RATHER THAN A PREFERENCE — a cloud-plane surface cannot legally emit any event this module owns (measured August 2026).** §8 says the console runs `packages/sync-client` and "holds a normal full branch slice", and `18 §6` lists **"manager-remote"** under the *cloud* plane. Those read as a free choice between an RN device app and a responsive web console, and they are not: **the web option cannot deliver `05-F6` at all.**
  - **The measurement.** `01-F62` requires `branch_id`, `branch_created_at` and `time_basis` on every branch-scoped envelope, "stamped at append by an originating **device**", and it fixes the org-scoped set at exactly five types (`catalog.changed`, `device.registered / revoked`, `user.changed`, `config.changed`) with the test *"an event type is org-scoped when its only legitimate emitter is the cloud plane"*. Every event this doc emits — `approval.granted / denied`, `channel.paused / resumed / throttled`, `day.opened / closed`, `cash.deposit_recorded`, `availability.changed`, and `audit.*` for `05-F2`'s alarm acknowledgment — is **branch-scoped**, and `01-F62` names `audit.*` as its own worked example of a type that stays branch-scoped *because the emitter is a device*. `services/sync-gateway`'s `appendOrgEvent` refuses all of them at the writer, and the cloud's branch-event table is written only by the merge gateway from a device `push`. **There is therefore no sanctioned path by which a browser records a manager's decision**, and inventing one would be either a sixth org-scoped type (a `01-F62` amendment) or a service minting an envelope on a device's behalf (a `01-F43`/`02-F41` attribution hole).
  - **What this forbids, because it is the obvious next move.** A web console that renders alarms and an approval queue is buildable today and **must not be built as the module's Wave-1 core**: it would be a correct subsystem whose only write is impossible, i.e. AGENTS.md's recurring defect shipped deliberately. A read-only remote *view* is a legitimate smaller thing and is not what `00 §1` rows "1 core" against — `05-F5`/`05-F6`'s approval interrupt is the core.
  - **⚠ ANSWERED, and by NONE of the three — `05-F32` (R48): approvals stay till-local and the console renders only, i.e. this FR's own *"legitimate smaller thing"* below is the ruling. `05-F31` (R22/R49) separately makes the surface a browser. The three-way choice is kept below because reopening it is a live possibility with a named trigger (`05-F32`), and (b)'s rejection in `05-F29` is the reasoning that survives.** The choice as posed: (a) build the Expo device app §8 already specifies, which needs a `sync-client` storage adapter for RN plus an `01-F25` pairing path for a phone; (b) amend `01-F62` so a decision recorded by an authenticated cloud user has a legal envelope, which is a kernel change and reopens `02-F41`'s attribution question on a plane with no PIN session; or (c) rule that the console **decides** and the requesting POS **records**, i.e. the grant travels back as a wire message and the till appends `approval.granted` plus `05-F6`'s resulting `void/comp/discount.recorded` — which needs a new `packages/sync-protocol` kind (a closed 14-member vocabulary today) and is the reading `05-F6` itself is closest to, since it already says the grant "propagates to the requesting POS over LAN < 1 s and unblocks it".
  - **What did NOT block, and is closed rather than owed:** `05-F7`'s three payload schemas, which had no entry in `packages/domain` at all — so until August 2026 `01-F4` made every `approval.*` emit an `UnknownEventTypeError` and this module's remote path was *unbuildable* rather than merely unbuilt. They now exist, and `approval.granted` carries `approver_user_id` **and** `requester_user_id` as two separately-required fields so that `02-F41`'s two-identity property (actor unchanged, approver recorded) cannot be lost by a remote implementation the way `unlock()` moving a session would lose it locally.
  - ~~**Still absent, and it is the input any of (a)/(b)/(c) needs first:** nothing anywhere **emits** `approval.requested`. `apps/pos-electron` resolves an `02-F20` escalation entirely in-process and never announces it, so the queue this module renders has no producer regardless of which resolution is chosen; and the four escalatable WRITES (`void.recorded`, `comp.recorded`, `discount.recorded`, `order.line_price_overridden`) still have no payload schema, leaving `05-F19`'s paid-out the only act an approval can currently complete.~~ **⚠ CLOSED, and it was closed by `05-F29`'s own landing before this bullet was ever read as current — re-measured 2026-08-21: `apps/pos-electron/src/main/approval-record.ts:170` emits `approval.requested`, wired at `main/index.ts:1240`, and all four escalatable writes carry payload schemas in `packages/domain/src/registry.ts`. `05-F32` records that the same stale claim then propagated into R22's owed-list in `plans/saas-pivot/plan-of-record.md`, which is the cost of leaving a superseded present-tense sentence in place: it gets copied forward as a premise.**

- 05-F29 ~~**RULED (founder, August 2026): resolution (a) — the manager surface is the Expo RN DEVICE app `§8` already specifies. Answers `05-F28`'s three-way choice.**~~ **⚠ SUPERSEDED by `05-F31` (R22, R49): the manager surface is a BROWSER and the RN scaffolding is deleted; and by `05-F32` (R48), which answers `05-F28` with a fourth option — approvals stay till-local and the console renders only. The text below is KEPT UNSTRUCK BELOW THIS LINE because its argument is what `05-F32` reuses: it is the measurement that a verified credential and the ledger write must sit in one process, and that is precisely why a browser may not decide.** The superseded ruling read: the console authorizes on a device holding a `01-F26` PIN session, and that device appends `approval.granted`/`denied` itself; the resulting `void/comp/discount.recorded` stays on the **requesting POS** (`05-F6`, `05 §4`), which is where it already is and must not migrate to the manager device.
  - **The decisive reason is `02-F41`, and it is about which process holds the credential.** *"Attribution is whoever's PIN is in, with no 'acting for' concept"*, and `01-F27` puts user identity in the PIN session. `05-F7`'s `approval.granted` must name the **approver** on the envelope. Only (a) puts the verified credential and the ledger write in the same process. **Measured, not reasoned:** `apps/pos-electron/src/main/gateway.ts:512`, `:570` and `:618` stamp `actor_user_id: deps.session()?.user_id ?? null` — **unconditionally from the live session** — so under (c) a till appending a grant would name the **cashier**. That is the exact envelope `registry.ts:419` forbids and the shape `02-F38` exists to refuse; it is a property of the shipped append path, not a matter of taste.
  - **`05-F28`'s own parenthetical is a MISREAD of `05-F6` and is corrected here, because it is the sentence most likely to decide this question by default.** It says (c) *"is the reading `05-F6` itself is closest to"*, citing *"the grant propagates to the requesting POS"*. Read whole, `05-F6` is: *"authorized by the manager's PIN **on the manager device** → `approval.granted` / `approval.denied` … **the grant propagates** to the requesting POS."* The grant is **authored on the manager device** and then propagates. `05 §4`'s void flow and `packages/sync-client/src/folds/merge.ts:752` both put only the **resulting write** on the POS, never the grant. A partial quotation of an FR read through a summary of it is how this corpus keeps making the same mistake; the FR text governs.
  - **(b) is rejected on the kernel, not on cost.** Amending `01-F62` so a cloud user has a legal envelope does more than add a case: that FR's discriminant **is** *"scope follows emitter"*. Let the cloud emit branch-scoped events and the test has no content left, so every later *"can the server just write it?"* (doc 08 ingest, doc 13 `action.executed`, doc 09, doc 16) becomes a judgement call instead of a decidable question. It also gives the cloud a branch **clock** it cannot have: `01-F43` requires branch time to be *consistent, not correct*, so a cloud origin either stamps its own accurate clock (the wrong property) or estimates the branch offset — a second, unelected authority for a fact that has an election (`01-F13`), permanent under `01-F1`.
  - ~~**(a) needs NO amendment to `01-F62`, NO amendment to `02-F41`, and NO fifteenth `sync-protocol` kind.** It is executing a decision the corpus already made: `01-F39` declares the `manager` device class (shipped, `packages/domain/src/device-classes.ts`, correctly not hub-eligible), `18 §14` already allowlists the RN stack, and `18 §8` already rules manager *"pure-JS installable"* — no custom native module and no dev client.~~ **⚠ SUPERSEDED with the ruling (`05-F31`). Every clause in it was TRUE and it still lost, which is the instructive part: R49 overrules on a cost this bullet does not price — the toolchain tax of keeping one surface on RN, and `packages/ui` having zero RN components, so the surface that "needs no amendment" breaches commandment 6 instead. `01-F39`'s `manager` device class is not withdrawn by this and stays shipped; what no longer follows is that anything CLAIMS it.**
  - **Grafted from (c), and written down so a later session does not "improve" it:** the resulting write stays on the POS. Grafted from `05-F28` itself: a cloud-plane console may **render** `05-F1`..`05-F4` alarms from a `01-F7` read model, and may **not** acknowledge them — `05-F2`'s acknowledgment is `audit.*`, which `01-F62` names as its own worked example of a type that stays branch-scoped.
  - **The grant travels over the CLOUD path, not the LAN mesh.** `05-F6`'s *"over LAN < 1 s"* describes the T3 case; `packages/sync-client/src/mesh-session.ts` is hosted by nothing, so a manager device that required the mesh would ship unreachable. `05-F9`'s honesty rules already cover the remote case.
  - **⚠ THE PREREQUISITE WAS INDEPENDENT OF THIS RULING AND HAS LANDED — CLOSED the same day this FR was written, and the paragraph below is kept with its error marked because the error is the instructive part.** ~~Nothing anywhere emits `approval.requested` (`05-F28`), so the queue has no producer under any resolution — and **the local path emits no approval record at all**: `apps/pos-electron/src/main/authorize.ts` appends only the escalated write with `approver_user_id` in its payload, so `05-F6`'s *"every decision is fully logged"* is **unmet today on the one path that exists**. Three of the four escalatable writes (`void.recorded`, `comp.recorded`, `discount.recorded`, `order.line_price_overridden`) still have no payload schema, so `01-F4` makes them unemittable.~~ Closing that on the till also settles the seam (a) will reuse — an append accepting an **explicitly verified** actor rather than the session's — and `verifyApprover` deliberately does not move the session, so the till could not author a correctly-attributed grant even locally until it existed.
    - **What is now true:** all **four** escalatable writes carry payload schemas in `packages/domain/src/registry.ts`, and `apps/pos-electron/src/main/approval-record.ts` emits `approval.requested` / `granted` / `denied` through `createVerifiedAppend`, wired in `main/index.ts`. `05-F6`'s *"every decision is fully logged"* is met on the local path.
    - **TWO ERRORS IN THE STRUCK TEXT, and both are this corpus's own recurring shape.** (i) It says *"Three of the four"* and then lists **four** — a count contradicted by its own enumeration, one clause later. (ii) It was falsified **by the same commit that wrote it**: the ruling and its prerequisite landed together, so the FR described a state that had already stopped being true when it was committed. Nothing external went stale; the document was internally inconsistent at birth. **The lesson is narrower than "docs rot" and worth stating: a prerequisite written in the present tense inside a ruling is a measurement, and a measurement made before the work and committed after it is wrong at the moment of writing.** State prerequisites as *conditions to satisfy*, not as *facts about the tree*, or re-measure them immediately before committing. Found by an adversarial review of the protected-path diffs, not by reading.

- 05-F30 **`05-F2`'s acknowledgment is `audit.alarm_acknowledged`, `01-F5`'s SEVENTH subtype (August 2026). Until it existed, `01-F4` refused the emit and EVERY alarm this console raises was permanent.** The gap was not that the ack was unbuilt; it was that the act was **unexpressible**, so no implementation could have been correct. `apps/manager/src/alarms.ts` had already refused to model an exit it could not produce — the right call, and the reason this FR is written rather than the code.
  - **Why not the sixth subtype, stated because it is the obvious reach.** `audit.print_acknowledged` is `03-F5`'s **till band**: a cashier at the counter dismissing an S1 about a print job the spooler minted an id for. `05-F2`'s ack is a **manager**, on the manager device, clearing a row of `05 §5`'s active alarm list — a list whose commonest member (`05-F1`) involves no printer at all. Recording a late-order ack as `print_acknowledged` would state permanently (`01-F1`) that a print was acknowledged when nothing printed, and would put two incompatible id spaces in one field name (`03-F40`'s named defect). Two acts, two surfaces, two people, two types. **Neither ack clears the other's alert, and this FR deliberately does not join them** — a cashier dismissing the counter band is not the manager seeing the alarm, which is `05-F3`'s entire reason for existing ("must reach the manager **even off the floor**"). Joining them is a later ruling with a real argument on both sides; it is not a default.
  - **Payload — and these fields are REQUIRED rather than `01-F5`'s additive extras, which is forced by `05 §5` rather than chosen.** `05 §5` materializes the alarm list on the device with *"no console-only source-of-truth entities — everything folds from the ledger, so a reinstalled phone reconstructs its state completely (`01-F6`)"*. An ack that cannot say **which** alarm it cleared re-derives to nothing, so every acknowledged alarm returns on reinstall and `05-F2`'s second exit silently stops existing. `01-F5` already anticipates this: its v1 payload contract is `prev_audit_hash` alone and *"the business fields (who/what) land additively with the emitting modules (docs 05/14/15)"*. This is doc 05 landing them.
    - `alarm_kind`: **`late_order` | `print_failed`** — `05-F1`'s and `05-F3`'s two categories, closed. Required because the two carry different remedies: acknowledging "this order is late" must not silently clear "the kitchen never got the ticket" on the same order.
    - `order_id`: the order the alarm is about. Required on both current kinds.
    - `printer_name`: `03-F5`'s printer on a `print_failed` ack, and **`null` on a `late_order` ack**. It is **non-null and non-empty whenever `alarm_kind` is `print_failed`** — `05-F4`'s volume discipline puts two failed printers on one order in two alarms precisely because `03-F5` says the manager has to know which one to walk to, so an ack naming neither would clear both or neither.
  - **The FACTS, not a composed alarm id — a choice, with the simpler alternative named.** The alternative is to write the derived view's own alarm id (`kind:order:printer`) as one string, which is what the sixth subtype does with the spooler's job id. It is refused because that id is a **format**, not a fact: change the format and every historical ack silently stops matching, and every acknowledged alarm comes back — an invisible failure in an append-only ledger. The fields above are already the ledger's own vocabulary (`kot.print_failed` carries `order_id` and `printer_name` under those exact names), so a view may recompose its ids freely without invalidating history.
  - **Who** is the envelope's `actor_user_id`, read at append (`02-F41`), never a payload copy. **`prev_audit_hash` is store-owned** (`01-F5`): the device stamps it inside the append transaction and a caller-supplied value is rejected.
  - ~~**Emitted on the MANAGER DEVICE** (`05-F29`, `01-F62`).~~ **⚠ THE EMITTER IS GONE — `05-F31` (R49) deletes the manager device, and the clause below still forbids the browser. Both halves hold and NOTHING is left that can ack, so this payload is specified and unproducible.** A cloud-plane console may render `05-F1`..`05-F4` and may **not** acknowledge — that graft is `05-F29`'s and this FR does not reopen it. **Where the ack goes instead is a founder call with three named shapes and it is NOT decided here: see `05-F32`'s closing bullet. This FR is unaffected in every other respect — the subtype, the three required facts and the reasoning for them are all about WHAT is recorded, not by whom.**
  - **⚠ TWO THINGS THIS FR DOES NOT DECIDE, named so that a session does not fill them in by accident.** (i) **`05-F3`'s `printer.status_changed(offline)` alarm has no order**, and `05-F2`'s persistence rule enumerates exits — ready/served, or acknowledged — of which the first cannot apply to a printer. So a printer-offline alarm would have exactly one exit (the ack) and no order to name, which the alarm shape as built (`05-F1`'s "order, channel, table, and age") cannot express. Whether that alarm is a third `alarm_kind`, and whether `printer.status_changed(online)` may clear it against *"never auto-dismissed silently"*, is a **founder call**; `alarm_kind` is closed at two so that ruling has to widen it visibly rather than arrive as a string nobody enumerated. (ii) Whether an ack on one surface clears the other's alert — see above.

- 05-F31 **RULED (founder, August 2026, R22 + R49): THE MANAGER CONSOLE IS A BROWSER SURFACE. THE EXPO RN APP IS SUPERSEDED AND ITS SCAFFOLDING IS DELETED. Supersedes `05-F29`.** R22 chose a browser console with push over `05-F29`'s Expo device app; R49 confirms and extends it — browser only, the RN scaffolding goes.
  - **⚠ THIS AMENDMENT WAS OWED FOR THE WHOLE INTERVAL BETWEEN R22 AND THIS FR, AND THE HARM IS THE REASON IT IS WRITTEN FIRST.** R22 names *"the `05-F29` amendment"* in its own owed-list and R49 names `specs/05` outright as *"still ruling the Expo app R22 superseded"*. Until this line existed, `05-F29` read as the standing ruling, §1 and §8 and `05-N3` all described an Expo app, and `apps/manager/CLAUDE.md`'s second bullet said *"the platform question is CLOSED … `05-F29` ruled resolution (a)"* — so **a session routed here by AGENTS.md built an RN app**, correctly, from the corpus. Measured 2026-08-21: `grep -arno "R49" specs/` returned **0** before this edit. *A ruling the corpus cannot cite is a ruling that cannot be checked*, and this is the third time in one session that the same failure produced work against a superseded position.
  - **R49's reason is the toolchain tax, and it is a cost of keeping ONE surface on RN rather than a cost of RN.** Two TypeScript versions; `apps/manager` outside the root tsconfig program; **zero `packages/ui` components exist for RN**, so an RN surface breaches commandment 6 in a file whose own header admits it; and EAS builds. **What it costs, stated:** no native push and no app-store presence — a PWA covers most of it, and `05-N2`'s push clause is re-read below.
  - **What "the scaffolding is deleted" actually measures, because `apps/manager` is NOT the two-line stub §1's build-status note still called it (measured 2026-08-21):** 6 source files and 7 acceptance suites, **3,718 lines**, a real `packages/sync-client` branch slice on `op-sqlite`, and a bundle its own guide records at 699 modules / 2.6 MB of Hermes bytecode. **The deletion is far cheaper than that number, and the reason is R20:** the screens are built runtime-agnostically, and `src/alarms.ts` (422 lines — `05-F1`..`05-F4`'s derivation) and `src/home.ts` (239 — `05-F22`/`05-F23`'s model) are **import-pure and testable under Node by their own construction**, so the derivations survive the host change and only the host does not: `App.tsx`, `branch.ts`'s composition root, the Expo config, and the RN storage adapter's only consumer.
  - **⚠ A RAIL WILL REPORT SOMETHING NEW WHEN THIS LANDS, AND IT IS EXPECTED RATHER THAN A REGRESSION.** `packages/sync-client/src/storage-op-sqlite.ts` and `src/rn.ts` landed **for** `05-F29`, and `apps/manager` is their only app consumer (comment-blind measurement, 2026-08-21). Deleting the surface makes them unreached value exports — Rule A on `pnpm seams:check` — i.e. AGENTS.md's recurring defect arriving **by subtraction** rather than by construction. Whether they are deleted with the surface or carry an `@unreached-owed` marker against `apps/pos-rn`'s wave is `18`'s call and not this doc's; what this FR forbids is letting them sit unmarked, because an unmarked unreached export is how the register rots.
  - **`05-F28`'s measurement STANDS and is not weakened by this ruling — what changes is that the corpus stops trying to beat it.** A cloud-plane surface still cannot legally emit any event this module owns; `05-F32` is how that constraint is honoured rather than amended. **And the pair `05-F28` said *"read as a free choice … and they are not"* resolves the other way round from `05-F29`:** `18 §6` already lists **manager-remote** under the *cloud* plane and was right; `05 §8`'s *"same `packages/sync-client`; the manager device holds a normal full branch slice"* is the half that gives way.
  - **Push is WEB push now, and that is a different capability with a named cost.** R22 says *"with push notifications"*; a browser gets the Push API, not FCM/APNs directly, and **on iOS that requires the site be added to the Home Screen first** — a manager who never does it gets no buzz during rush, which is `05-N2`'s whole point. `05-F23`'s honest-gap rule is unaffected. **Owed to doc 18 rather than decided here:** the Push API and any web-push server library are not on `18 §14`'s registry, so adding them is a `18 §15` governance event.
  - **Spec PRs owed elsewhere by name (R49), none of them this doc's to make:** `18 §2`'s tree (`manager/ # Manager console (Expo, Android + iOS)`), `18 §8`'s *"waiter/rider/owner/manager stay pure-JS installable"*, and — ruled in the same breath and listed so the pair is not split — `12 §8` and `14-F31`, which reserve `apps/owner` for Expo and are now wrong for the same reason.

- 05-F32 **RULED (founder, August 2026, R48): APPROVALS STAY TILL-LOCAL; THE BROWSER CONSOLE RENDERS ONLY. Closes `05-F28`'s three-way choice with a fourth answer — none of the three.** `05-F28` already named this as *"a legitimate smaller thing"*; this makes it the ruling rather than the consolation.
  - **THE MEASUREMENT THAT DECIDES IT, AND R22 DID NOT HAVE IT.** The shipped till approval is **PIN-gated**: `apps/pos-electron/src/main/authorize.ts:568` refuses unless `deps.verifyApprover(approver_user_id, pin)` passes, and `main/index.ts:1892`'s `CHANNELS.escalate` handler is what passes the PIN. So a browser decision arriving with **no PIN** forces one of two costs R22 never named — (i) the till appends `approval.granted` naming an approver it **never verified**, which is commandment 8 (server-side authorization; a client's claim is never trusted) and an `02-F41` hole on a money path (`05-F19`'s paid-out, `01-F29`'s manager-approved refund); or (ii) the cloud becomes a PIN **verifier**. **(i) is the same defect `05-F29` measured for resolution (c), one indirection further in:** (c) got the *actor* wrong by reading the live session, this gets the *approver* unverified. R22 said (c) needs *"no amendment to `01-F62` or `02-F41`"* — **true of the ENVELOPE and silent about the CREDENTIAL**, and the credential is the half `05-F29` proved was load-bearing.
  - **⚠ THE SECOND COST IS REAL AND THE PLAN'S CITATION FOR IT IS WRONG — correcting it here is the entire point of moving a ruling into the corpus.** `plans/saas-pivot/plan-of-record.md` R48 attributes to `28-F7` the words *"no cloud surface holds device credential material"*. **`28-F7` does not say that**: it says entitlement is a cloud-plane gate that no device asks, which is the opposite direction of travel, and a search of `specs/` for that phrase returns nothing (measured 2026-08-21). Worse for the argument as the plan states it, `11-F21` (R23) rules that **the cloud already holds the Argon2id PIN hash and is its source**, so *"the cloud would hold credential material"* is not a new cost at all. **The outcome is unchanged — horn (i) alone decides this ruling** — and what changes is that a reviewer can now dispute the reasoning by FR id instead of by a quotation that resolves to nothing.
  - **Stated so it is disputable: the second horn's real costs are three, and each is a READING of an existing FR rather than a transcription of the ruling.** (1) **`01-F28` puts PIN verification on-device** *"against synced credential hashes"* and `11-F21` adds that the hash *"never leaves the process that verifies it"*; a cloud verifier is a **second** verifier of one credential, which neither FR forbids in those words — hence *reading*, not transcription. (2) **It is a second LOCKOUT the first cannot see, and this is the concrete harm, larger than the one the plan named.** `01-F61`'s lockout is durable and per-(device, user) **on the device**; a cloud verifier holds no share of that counter, so a 4-digit PIN — which `01-F61` itself calls *"a convenience credential, not a secret"*, safe **only** because it is paired with a registered device (`01-F25`/`01-F47`) — becomes guessable from anywhere, with no pairing and with nothing charging the attempt to the pad's counter. (3) **It puts the WAN on the approval path.** `05-N4` requires console absence to cost the branch nothing and `05-F8` says remote approval *"augments service; it never gates it"*; a cloud-verified approval is one a WAN-down branch cannot perform — commandment 4 and `00 §5.1`, and the case `01-F28` exists to prevent for this exact credential.
  - **⚠ WHAT THIS COSTS, STATED RATHER THAN DISCOVERED. `00 §1` rows this module at *"1 core"* and this ships LESS than that row claims.** `05-F28` says in terms that *"`05-F5`/`05-F6`'s approval interrupt is the core"*, and it is not being built. What the manager gets is alarms and a pending-approval queue on her phone, and **a walk to the till to approve**. `05-F27` is therefore promoted from a *fallback* budget to **the** approval budget — the pending request visible on the terminal without searching for it, approval ≤3 taps from that state — and `05-N1`'s machine round trip has no path to measure until this reopens.
  - **The signal that should reopen it, named so it is a trigger and not a mood: a pilot manager saying the walk is the problem.** Not a design review and not a reviewer's discomfort — an operator report from a dev-pilot restaurant. Reopening means picking (b) or (c) from `05-F28` **with the credential cost paid explicitly**, and `05-F29`'s argument against (b) is untouched by R22/R49: it still stands on the kernel, because letting the cloud emit branch-scoped events empties `01-F62`'s discriminant and hands the cloud a branch clock `01-F43` refuses it.
  - **What each affected FR now means, enumerated so nobody has to infer it.** `05-F5`'s interrupt card is a **render**; the decision surface is the till. `05-F6`'s *"authorized by the manager's PIN **on the manager device**"* is **NOT in force** — it is the manager's PIN, on the **till**, per `02-F20`, and the rest of `05-F6` (grant references the request, first response wins, actor + approver, fully logged) is unchanged and shipped. `05-F7`'s three payload schemas are unchanged and shipped: they are the till's payloads. `05-F8`'s fallback becomes **the only path**, so its N s timeout has nothing to time out. `05-F9`'s remote-approval clause is not in force; its **honesty** clause — data age shown before the manager commits (`00 §5.7`) — binds the console's render and is if anything more load-bearing, because a stale queue is now the only thing she can act on.
  - **⚠ AND WHAT IS NOW TRUE THAT R22's OWED-LIST STILL CALLS MISSING.** R22 closes with *"nothing yet emits `approval.requested`"* and `05-F28`'s last bullet says the same. **Both are stale, measured 2026-08-21:** `apps/pos-electron/src/main/approval-record.ts:170` (`raiseFor`) appends `approval.requested` through `createVerifiedAppend`, wired at `main/index.ts:1240`, with `requester_id` from the live session and `requesting_device_id` from the store's own identity — both read on the trusted side. So **the queue this console renders has a producer, and it is the till**, which is the shape R48 rules anyway. *This is the third doc-05 clause to claim a gap that had already closed* — `05-F8`'s struck note, `05-F29`'s struck prerequisite, now R22's owed-list — and all three were **prose describing the tree** rather than a condition to satisfy, which is `05-F29`'s own recorded lesson arriving a third time.
  - **⚠ WHAT THIS DOES NOT DECIDE — AND IT IS A HOLE R49 OPENED, NOT ONE THIS FR LEFT: `05-F2`'s ACKNOWLEDGMENT NOW HAS NO EMITTER.** `05-F30` puts `audit.alarm_acknowledged` on the **manager device** (`01-F62`: branch-scoped, stamped at append by an originating device), and grafts from `05-F29` that a cloud-plane console **may render `05-F1`..`05-F4` and may not acknowledge**. Both halves still hold; with no manager device, **nothing is left that can ack.** So `05-F2`'s second exit (*"or the manager acknowledges"*) has no producer, and `05-F3`'s `printer.status_changed(offline)` alarm — which has no order to go ready — is left with **no exit at all**, which `05-F30` (i) had already flagged as a founder call for a different reason. Three shapes exist and **none is ruled here**: (i) the manager acks at the till under her own PIN, which is this ruling's own shape applied to the second act; (ii) the console renders without an ack and alarms exit only by resolution — `05-F2`'s *"never auto-dismissed silently"* survives, `05-F3` does not; (iii) a till-side ack under the **cashier's** session, legal on `01-F62` and a defeat of `05-F3`'s *"must reach the manager even off the floor"* as an exit. Named with all three so the question cannot be answered by whoever writes the first ack handler.

## 4. Key flows

**⚠ THE APPROVAL FLOWS BELOW DESCRIBE THE SUPERSEDED SHAPE (`05-F32`, R48).** They are kept unedited except where marked, because they are what a reopened remote path would be built to — but under the ruling every "approve + PIN on the phone" step is *"see it on the phone, walk to the till, approve there"*, and step 2's fallback is step 1. The alarm and channel flows are unaffected except for the acknowledgment, which has no emitter (`05-F30`, `05-F32`).

**Void approval (happy + fallback)** — **⚠ steps 1 and 2 collapse into one under `05-F32`**
1. Cashier requests void-after-KOT → `approval.requested` **is appended by the till** (`main/approval-record.ts`, shipped) and reaches the manager's phone as a **render** < 2 s → the card shows item, amount, reason → ~~manager taps approve + PIN~~ **manager walks to the till and approves there with her PIN (`02-F20`)** → `approval.granted` → POS records the void with approver. ~~Seconds elapsed; nobody walked anywhere.~~ **Somebody walked; that is the cost R48 accepts by name.**
2. ~~Fallback: manager's phone is dead → 30 s timeout → POS offers local manager-PIN → same recorded outcome, different authorization surface.~~ **This IS step 1's authorization surface now, not a fallback to it, and there is no timeout — `05-F8`.** A dead phone costs only the render: the request is still raised, and the escalation is still offered at the till by the matrix (`02-F20`), which is why `05-N4` is satisfied trivially rather than by design.

**Late-order alarm**
1. Order crosses the red threshold (03-F14) → alarm with order/channel/age → ~~manager acknowledges (logged)~~ **⚠ no surface can acknowledge under `05-F31`/`05-F32` — three shapes named, none ruled** → checks the pass screen or floor map → alarm clears itself when the order goes ready. **That last exit is the only one that currently works, which is exactly why `05-F3`'s printer-offline alarm — no order, so no ready — is the case that breaks.**

**Kitchen drowning → pause foodpanda**
1. Channel pulse shows foodpanda confirms spiking and reds accumulating.
2. Manager taps pause foodpanda, reason "kitchen overloaded", auto-resume 30 min → `channel.paused`.
3. Doc 08 pushes closed/unavailable to the aggregator; dine-in service continues undisturbed.
4. The timer fires `channel.resumed`. The whole episode sits in the ledger for docs 12/13.

**Paid-out approval**
1. Storekeeper records a PKR 4,000 paid-out with receipt photo at the POS (02-F26) → above threshold → `approval.requested` (type `paid_out`).
2. Interrupt with the photo inline on the manager's phone → ~~approve + PIN~~ **render only; approve + PIN at the till (`05-F32`)** → `cash.paid_out` completes with approver attached.
3. Denial: the paid-out stays pending at the POS with the denial reason; cash does not leave the drawer against the ledger.

**Day close**
1. Console shows all shifts closed + variances → manager counts the drawer at the counter, enters the count on their phone → deposit recorded → `day.closed` → owner nightly summary triggers (doc 12).

**Remote evening check (honesty path)**
1. Manager at home opens the console → screens show "synced 40 s ago" → an approval request arrives via push → the card shows data age before ~~the PIN step → approve → events land at the branch over cloud~~ **any action — and under `05-F32` there is no action available from home at all. This is the flow the ruling costs most: a manager at home can SEE that a void is waiting and can do nothing about it until somebody is at the till. Recorded plainly because it is the scenario a pilot complaint will describe, and R48's reopen trigger is exactly that complaint.**
2. Branch WAN drops → the console banner flips to "branch offline — last seen 3 min ago"; alarm silence is labeled as unknown, not calm (05-F23).

## 5. Data

- **Materialized (~~device~~ CLOUD READ MODEL):** active alarm list, pending approval queue, channel stats (15-min windows), floor map, day/shift status. ~~No console-only source-of-truth entities — everything folds from the ledger, so a reinstalled phone reconstructs its state completely (01-F6).~~ **⚠ THE MECHANISM CHANGED AND THE PROPERTY DID NOT (`05-F31`, `18 §6`): a browser console is a cloud-plane surface, so these are `01-F7` read models read through TanStack Query + tRPC (commandment 5), not device folds over a branch slice. Still no console-only source-of-truth entities, still nothing derived from anywhere but the ledger — and a reopened browser reconstructs completely because it holds nothing, which is `01-F6`'s guarantee reached by a different route. ⚠ It is a DIFFERENT route and not a free one: a device fold works with the WAN down and a cloud read model does not, so `05-F22`/`05-F23`'s honesty labels carry the whole burden that offline-first used to carry here.**
- **Emitted — ⚠ BY THIS SURFACE, NOTHING (`05-F31`, `05-F32`, `01-F62`).** Doc 05 still **owns and defines** every type below, and they remain this doc's `01 §4` extensions; what changed is that the emitter is always a branch **device**. `approval.*` is the till (`05-F32`, shipped). `audit.*`'s alarm ack has **no emitter at all** — `05-F30`, `05-F32`'s closing bullet. And every Wave-4 row here (`channel.*`, `day.*`, `cash.deposit_recorded`, `availability.changed`) is branch-scoped with no device that claims it, so **the emitting surface is an open question for each**, to be answered when Wave 4 is scoped and not by whoever writes the first handler:
  - `approval.requested / granted / denied`
  - `channel.paused / resumed / throttled`
  - `day.opened / closed` · `cash.deposit_recorded`
  - `availability.changed`
  - `audit.*` (alarm acks, PIN authorizations)
- **Extensions to 01 §4 introduced by this doc:** `approval.requested / granted / denied`, `channel.paused / resumed / throttled`.
- **Consumed:** `order.*`, `kot.print_failed`, `printer.status_changed`, `table.state_changed`, `shift.*`, `cash.*`, `void/comp/discount.recorded`, `eta.estimates_published`.

## 6. Non-functional requirements (module-specific)

- 05-N1 Approval round trip, machine portion (request emitted → POS unblocked after the manager's tap), ≤ 2 s p95 on LAN. **⚠ NOT MEASURABLE and not withdrawn (`05-F32`): there is no manager tap to end the interval, so this NFR has no path until the remote decision is reopened. It is kept rather than struck because it is the number a reopened path must hit — and `05-F27`'s ≤3 taps is the budget that binds in the meantime.**
- 05-N2 Alarm delivery: LAN ≤ 2 s; remote push best-effort with a 60 s in-app poll fallback while the app is foregrounded. **⚠ THE PUSH IS WEB PUSH NOW (`05-F31`), which is a different capability with a different failure mode: on iOS the Push API requires the site be installed to the Home Screen first, so an uninstalled console is silently a poll-only surface. Neither bound changes; what changes is that "remote push best-effort" now has a named reason to be absent, and it must be visible to the manager rather than assumed (`00 §5.7`, `05-F23`).**
- 05-N3 ~~Runs on the manager's personal phone:~~
  - ~~Android 10+ and iOS (current − 2);~~
  - ~~steady-state RAM ≤ 300 MB;~~
  - ~~off-LAN the app is push-driven, not socket-polling — no battery-drain reputation.~~
  - **⚠ SUPERSEDED by `05-N6` (`05-F31`, R49). It sized an installed RN app; there is no installed app.**
- 05-N4 Console offline (phone dead, manager absent) must cost the branch nothing: every console flow has its POS fallback (05-F8, 05-F17). **⚠ Under `05-F32` this is satisfied TRIVIALLY rather than by design — for approvals the POS is not the fallback, it is the only surface — and that is worth stating because a trivially-satisfied NFR is one nobody tests. The Wave-4 flows `05-F17` names are the half where it still has content.**
- 05-N5 The approval queue and alarm list survive app kill/restart without loss — ~~they are folds over the branch stream, re-derived on start (01-F6)~~ **⚠ the REQUIREMENT stands and its mechanism is superseded (§5): a browser console re-reads a `01-F7` read model, so a closed tab loses nothing because it held nothing. What this no longer buys is survival of a WAN outage, which the fold did buy — see §5 and `05-F22`.**
- 05-N6 **Runs in a mobile BROWSER on the manager's own phone (`05-F31`, R49) — replaces `05-N3`.** Current-major Chrome on Android and Safari on iOS; no install step, no app-store presence, no EAS build. **No steady-state RAM budget of `05-N3`'s kind is set, and the absence is deliberate rather than an omission:** that number sized a process holding a full branch slice, and this surface holds a cloud read model (§5), so a budget carried over would be a number measuring nothing. **What IS owed a number and does not have one: the render's own latency and payload against `05-N2`'s 2 s alarm bound over a phone's mobile data** — sized when the surface is designed (R19's seven surfaces), not invented here.

## 7. Customizability

- **Layer 2 (org):** — **⚠ the first two settings are INERT under `05-F32`: every approval type requires an at-counter PIN and there is no timeout to configure. They are kept because reopening the remote path (R48's trigger) restores both, and a setting deleted is a setting re-invented differently.**
  - which approval types allow remote approval vs require at-counter PIN;
  - approval timeout N (05-F8);
  - paid-out approval threshold and photo requirement;
  - alarm thresholds (shared with doc 03 aging config);
  - variance highlight threshold;
  - which roles may pause/throttle channels;
  - auto-resume defaults.
- **Layer 3 (device):** alarm tone, on-duty routing (approvals routed to a designated on-duty manager rather than broadcast). **⚠ "Device" layer on a browser needs a home: `00 §7`'s layer 3 is keyed to a registered device (`01-F25`) and a browser is not one, so under `05-F31` these two settings have no key to hang on. Named, not answered — it is the same question `05-F32` leaves open for the ack, one layer down.**
- **Deliberately not configurable:** disabling late-order or print-failure alarms while a business day is open; unlogged or PIN-less approvals; pausing staff-operated channels (05-F16); editing any recorded decision (append-only, 00 §5.5).

## 8. Tech notes

- ~~Expo RN, Android + iOS; APNs matters — many owners/managers carry iPhones. Same `packages/sync-client`; the manager device holds a normal full branch slice (trusted role — doc 04's scoped-slice mechanism is not used here).~~ **⚠ SUPERSEDED by `05-F31` (R22, R49) — and this sentence is the one `05-F28` was arguing against when it said `05 §8` and `18 §6` "read as a free choice … and they are not". `18 §6` was the correct half. There is no manager device, no `packages/sync-client` here and no branch slice: a BROWSER on the cloud plane, tRPC + TanStack Query only (commandment 5), built on the back-office template R49 measured as already the target stack.**
- ~~iOS LAN participation needs the local-network permission prompt and falls back to cloud relay when the OS parks the socket; approvals must be correct, not LAN-dependent (05-F8 covers the gap).~~ **⚠ MOOT — a browser has no LAN participation at all, so the gap this bullet manages cannot arise. Its conclusion outlives it and is now unconditional: approvals are correct because they never leave the till (`05-F32`).**
- Channel-pulse windows are computed ~~on-device from the branch stream — no cloud dependency in-branch~~ **⚠ NOT on this surface any more (`05-F31`, §5): a browser console has no branch stream, so the windows are a cloud read model and the "no cloud dependency" property is lost here. Wave 4, and it is a real consequence to price then rather than a wording change: `05-F12`'s glance is the one screen whose whole value is being right during a rush, which is when the WAN is least trustworthy.**
- ~~Approval interrupts use high-priority push categories (FCM high priority / APNs time-sensitive) so a pocketed phone still buzzes during rush.~~ **⚠ SUPERSEDED — `05-N2` and `05-F31`: Web Push, with no APNs category to choose and an iOS install-to-Home-Screen precondition. And there are no approval INTERRUPTS to push under `05-F32`; what is pushed is an alarm and a queue notification she then walks to act on.**
- The console ships in the same staged-rollout discipline as the fleet (doc 15); a broken console build must never take the approval path down — hence 05-N4.

## 9. Open questions

1. Multi-manager concurrency: several managers on duty — broadcast approvals to all vs route to a designated on-duty manager (default: broadcast, first grant wins, idempotent; revisit at pilots).
2. Approval timeout default (30 s assumed) — tune against real kitchen rhythm at Wave 1 pilots.
3. Whether doc 13 may *suggest* a channel pause (autonomy ladder "act with approval" rung) as a pre-filled interrupt on this surface — gated on doc 13 maturity rules per restaurant.
4. ~~iOS background socket limits vs alarm latency guarantees off-LAN~~ **RESHAPED, not closed (`05-F31`): there is no background socket on a browser, so the question becomes whether Web Push on iOS Safari — which needs the console installed to the Home Screen — can meet `05-N2` at all, and what the console shows when it cannot.** Measure; may need delivery-latency telemetry (doc 15).
5. Whether day-open float entry directly on the console (manager at the drawer with their phone) needs a second-person confirmation on the POS for cash-control hygiene — decide with pilot accountants. **⚠ `05-F31`/`01-F62` change the premise: a browser cannot emit `day.opened` at all, so Wave 4 must first say which device does — see §5.**
6. **⚠ BLOCKING for `05-F2`, opened by R49 and named in `05-F32`'s closing bullet: WHO ACKNOWLEDGES AN ALARM?** `05-F30`'s `audit.alarm_acknowledged` is branch-scoped and must be stamped by a device (`01-F62`); the manager device is deleted and the browser is forbidden from acking, so the type is specified and unproducible and `05-F3`'s printer-offline alarm has no exit at all. Three shapes are named there and **none is ruled**; this is a founder call, and until it is taken no implementation of `05-F2`'s second exit can be correct.
7. **Which device emits each Wave-4 event this module owns** — `channel.paused / resumed / throttled`, `day.opened / closed`, `cash.deposit_recorded`, `availability.changed`. All branch-scoped (`01-F62`), none claimed by a surface that exists, and §5 now says so. Not urgent (Wave 4) and recorded now because the answer was implicitly "the manager device" for the whole life of this doc and that answer is gone.
