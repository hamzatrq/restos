# Decision Register

**Purpose:** cross-cutting decisions that span module boundaries live here until promoted into their owning document. A leaf module must not implement against a `proposed`/`blocked` decision. Format per row: stable ID · status (`proposed | accepted | blocked | superseded`) · authority (owning doc once promoted) · one-line decision · open dependency · target wave.

| ID | Topic | Status | Authority | Decision / current state | Open dependency | Target |
|---|---|---|---|---|---|---|
| DEC-MONEY-001 | Refunds & post-settlement corrections | **accepted** | 01-F29..F33, 02-F36 | Linked event pairs, manager-approved, conservation-checked | — | W1 |
| DEC-MONEY-005 | Money arithmetic safety in a float language | proposed | 00 §6 / 18 §4 | JS has no integer type — "integer paisas" are integers *stored in a double*. Today `paisa()` + Zod `int()` guard the boundaries and `sumPaisa` accumulates in BigInt, but (a) the fold engine deliberately uses PLAIN numbers, not branded types, and (b) no divide/multiply/rate helper exists anywhere — so the first tax, percentage-discount or split-bill code is where floats enter. Proposal: rates as integer **basis points** (1700 = 17%) so no float literal ever appears; mandatory `splitPaisa(total, n)` / `applyRateBps(amount, bps)` in `domain` with an explicit stated rounding policy and a property-proven parts-sum-to-total; a **lint rule** banning raw arithmetic on money (18 §4 says "banned by convention + review" — convention is not enforcement); folds switched to branded types. MUST land before doc 16 (tax), 17 (discounts) and 02 split-bill code. **T-01-13 LANDED (helpers + lint rule, review-clean): `splitPaisa`/`applyRateBps` in `domain`, GritQL ban on bare-identifier raw money arithmetic. The fold-brand clause is DEFERRED (T-01-15 rewrote the fold plane; re-state as a follow-up). Gate carried: the lint rule's compound-assignment arms (`+=`/`++`) land with T-01-13's fix round — accumulation is the idiom the ban exists for** | fold-brand follow-up | before tax/discount/split code |
| DEC-MONEY-006 | What a suppressed duplicate settlement leaves behind | **accepted** | 01-F31 / 01-F35 / 01-F29 | `01-F31` is unimplemented: `settlement_attempt_id` is declared on `payment.recorded` (`registry.ts:51`) and read by **no fold and no dedupe path**, so a double-tap or crash-retry bearing a NEW event id double-records. Closing it raises two questions the FR does not answer. **(a) Parent resolution.** `replay.ts:318` keys `appliedPayments` by *event* id for refund resolution. If the fold collapses two events into one attempt, must the SUPPRESSED event's id still resolve? If not, a refund whose UI captured the losing id parks forever and the money never lands — severe, but a *derived* requirement, not spec text. **(b) Anomaly flagging.** `01-F35` retains-and-flags ignored events for terminal-state monotonicity; `01-F31` is silent on whether a suppressed duplicate is an anomaly or routine. Also note `payment.refunded` carries **no** `settlement_attempt_id` at all, though 01-F31 says *every* emission does — that half is code-violates-spec and needs no decision. **RESOLVED BY DESIGN (ratified with DEC-PERF-001):** under the 26 model nothing is "suppressed" — all members of an attempt key are retained in the cell (`event_ids`), and (a) dissolves because refunds resolve parents by `settlement_attempt_id`, not envelope id (promoted into 01-F29); a refund captured against either envelope alias resolves identically. (b): divergent members raise the 01-F31 anomaly. The `fix/01-f31-settlement-idempotency` branch is **superseded by T-01-15**; its seam/retention tests carry forward where compatible | — | W0 |
| DEC-MONEY-007 | Khata settlement vs repayment — a live double-count | **accepted** | 01-F30 / 01-F32 / 02-F12 / 02-F14 | **Not an ordering defect; a live money defect.** `02-F12` makes khata credit a payment *method*, and `02-F14` + `01-F32` make the later repayment **another `payment.recorded` on the same order**. Two genuine acts carry two `settlement_attempt_id`s, so any correct idempotency algebra rightly refuses to dedupe them — **the money doubles on the happy path, under full observation, converged identically on every device**. A khata regular paying their ₨1,850 tab makes every screen read "OVERPAID ₨1,850 — refund to close". `01-F30`'s receivable is meanwhile structurally always 0. The fold cannot discriminate: `payment.recorded.method` is `z.string()` (`registry.ts:50`) while `payment.refunded.method` **is** a closed enum (`:56`). Fix is a payload discriminator — `purpose: 'settles_order' \| 'repays_receivable'`, or a closed `method` enum with a declared non-tendering subset — plus restating `01-F30` over tendering payments only. **RATIFIED: the `purpose: 'settles_order' \| 'repays_receivable'` discriminator lands; 01-F30/F32 restated over tendering payments (promoted).** Khata settlement stays a `payment.recorded` | T-01-15 (schema + fold) | W0 |
| DEC-MONEY-008 | `settlement_attempt_id` uniqueness scope (unstated platform law) | **accepted** | 01-F31 | `01-F31` says the token is "UI-layer" and **never states its uniqueness scope**. The entire unique-keyed-sum money algebra (`26 §7`) rests on it. If any device mints a per-device counter rather than a UUID, two genuinely distinct payments collapse into one key and **cash vanishes silently, converged identically everywhere**. **RATIFIED and promoted into 01-F31**: org-globally-unique, UI-minted, UUID-class. **Enforcement posture ratified July 2026 (audit-1 #11 — it was trusted-not-enforced): DETECT AND ALARM, NEVER REJECT.** The gateway checks org-global uniqueness and raises a loud fleet-health + Auditor anomaly on collision, but still merges. Rejecting is the stronger algebraic guarantee and is refused deliberately — a device on a bad build would have real rung-up sales quarantined, breaking 01-F17. The failure being closed is that a colliding key makes cash vanish *silently and identically everywhere*; converting silence into an alarm is the entire win, since a loud wrong number gets fixed and a silent one does not | — | W0 |
| DEC-MONEY-002 | Split-payment tax apportionment | proposed | 16 §9.1 | Apportion by payment share per method (provisional) | Authority guidance per adapter | Tax enablement |
| DEC-MONEY-003 | Khata fiscalization timing | proposed | 16-F16 / §9.6 | Fiscalize at receipt issuance, rate per rule-pack credit mapping | Legal verify per adapter | Tax enablement |
| DEC-MONEY-004 | Tips (capture, pooling, payout) | **accepted** | 01-F30/01-F31 / 02 / 11 | **RATIFIED (founder, July 2026): FULL tips — capture, pooling AND payout.** Previously unmodelled, so cash tips were invisible to the system and surfaced as unexplained till variance at shift close. **The modelling collision, and the shape that resolves it.** A tip is money TENDERED that is not part of `billed_total`, so recording it as an ordinary payment breaks `01-F30`'s conservation equation on the happy path — Rs 1,100 taken against a Rs 1,000 bill would read as a Rs 100 overpayment demanding a refund to close. This is exactly the failure DEC-MONEY-007 found for khata repayments, and it takes the same fix: **extend `01-F31`'s closed `purpose` discriminator with `tip`**, excluded from the `settles_order` sum, so conservation holds untouched and the till reconciles against cash actually in the drawer. **Pooling and payout need NEW EVENTS that do not exist in the `01 §4` catalog** — a captured tip is a LIABILITY to staff until paid, so it must be trackable from capture through pooling to payout, and `staff.*` today has only `clocked_in/clocked_out/advance_recorded/advance_repaid`. Candidate additions (`tip.pooled`, `tip.paid_out`) are NAMED HERE ONLY; per commandment 2 they enter the catalog by spec PR to `01 §4` + doc 11 before any code, not by implementation. **Scope note (founder-visible):** this is the largest money-model addition since the kernel and it reaches payroll (doc 11), so it is deliberately NOT a Wave-1 POS-only change. `DEC-MONEY-005`'s helpers must be in place first (they are). Blocks nothing today; the POS payment screen must not ship a tip field until the events exist | spec PR to 01 §4 + 11 | W1/W2 |
| DEC-SYNC-001 | Convergence contract | **accepted** | 01-F34..F38 | Commutative folds + global-seq tiebreak; quarantine model | — | W0 |
| DEC-SYNC-002 | Device classes & slices | **accepted** | 01-F39..F42 | Six classes (counter split by host runtime: electron/rn); server/hub-enforced predicates | — | W0 |
| DEC-PERF-001 | Ordering authority: cloud-assigned `global_seq` vs stable append-time order | **accepted** | 25 (full analysis) | Adopting a cloud-assigned sequence retroactively reorders events, which forces a full re-fold — **measured O(N²)** on the live sync path (~6.6 ms/event at N=1600 on a laptop; extrapolates to minutes of CPU on a 2 GB tablet at a realistic rolling window). External research verdict: general IVM engines (DBSP/Materialize) do **not** solve this — they are order-agnostic, and DBSP's own degenerate case is exactly ours. Options in `25 §7`: cheap wins (skip no-op adoptions, entity-scoped recompute, batch per page) are decision-free. **Structural recommendation now settled in `25 §11–§16`: a clock-free causal order** — key `(causal_seq, device_id)` where `causal_seq` is a true Lamport clock bumped on append AND receive, assigned once and never revised; `global_seq` demoted to a delivery/catch-up cursor. Chosen over HLC because the founder's clock threat model allows EVERY device to be arbitrarily wrong in either direction, which removes HLC's premise (it needs some roughly-right clock) while keeping its hazard (a fast clock poisons the org's monotone max irrecoverably). A key with no physical-time term is immune by construction. `causal_seq` must be a NEW field — `lamport_seq` must stay gap-free for 01-F8 watermarks and the Auditor gap check, and a causal clock is inherently gappy. Rewrites 01-F34's tiebreak; **01-F18 unaffected** (cloud-plane back-office edits, server time trustworthy). Near-free to land in W0 (no production data, no migration). **⚠️ NOT SETTLED — a 74-scenario adversarial review (`plans/wave-0/fold-scenario-catalog.md`) returned P0 refutations:** causality is false under partial observation (a sliced waiter's clock is permanently deflated, F60); `causal_seq` is forgeable and a `MAX_SAFE_INTEGER` peer event bricks `append` branch-wide — the HLC hazard inherited in harsher form (F62); no sound backfill exists (rank-in-set is not subset-independent, F103); the O(k) budget assumed for option B is really O(N) as no index exists and rush events are branch-global (F65); residency gets worse since `rebuild()` is the only thing resetting the accumulators (F07). **F69 ablation now RUN (`25 §17`)**: A+B+C solves the offline-skew profile (O(1)/append, 123.6 s → 1.35 s at N=10,000) but leaves the cloud-reordering profile **quadratic** (25.8 s on a laptop = 129–258 s on target hardware, a 2–4× breach); option A alone is worthless once cloud order genuinely differs; option B's entity index is **itself order-dependent** and was measurably *incorrect* (30–50 % divergence) before fixes. **Neither path is clean** — the cheap fixes do not close it and §13 carries P0 refutations. Blocking gap: the branch-global event types that decide the residual have **no payload schema in `domain`** and cannot be measured at all **REFRAMED (`26`, external review): the question itself was wrong.** Deterministic folds need deterministic *merge semantics* — commutative ops, monotonic state, explicit concurrency — **not a universal total order**; `01-F34` already says so while the comparator does the opposite. So "cloud authority vs stable append-time order" is a choice between two ways of arbitrating a global order that most projections should not need. Today the comparator lets **sync metadata decide business outcomes** (cloud arrival order picks the winning table assignment and the anchoring timestamp) — a delivery cursor standing in for staff intent. Also corrected: the "branch-global events are entity-less" finding that drove the negative verdict is **false** — `FOLDS.md` keys availability by `item_id`, tables by `table_id`, shifts by `shift_id`; sync scope and fold scope were conflated. Direction: `global_seq` = delivery cursor with **zero fold work**, a **projection-key** sidecar, and per-fold merge algebra. **T-01-14 paused; `causal_seq` not to be implemented** **MATRIX DELIVERED (`plans/wave-0/merge-semantics-matrix.md`, summary `26 §7`): the design works.** The "still needs an ordering mechanism" list is **four entries, only one of which touches a device fold** (compaction/prune watermark); one more is deletable by making `line_context` required. Everything else across all six folds converges with **zero ordering metadata**. **All three hard-case prototypes GREEN (`26 §8`)** — money 13/13, tables 6/6, lines 13/13, ~470 lines of algebra total, zero ordering-metadata reads enforced dynamically; every critic counterexample survived, incl. the ack-boundary flip and the khata case. The `04-F17` privacy exposure is now **measured** (one business day of order/line-plane visibility, Rs-hundreds scale, zero money-plane leakage; trim demonstrably re-creates the slice-blind defect). **RATIFIED by the founder (July 2026): merge semantics is the selected design.** Promoted into `01-F34` (rewritten), `01-F29/F30/F31/F32/F33`, and the `01 §4` catalog (`order.settlement_closed`). The `26 §7` schema additions, the measured `04-F17` privacy trade (union routing, business-day-bounded), and hub-as-business-emitter are ratified with it. Implementing task: **T-01-15** | — | W0 |
| DEC-TIME-001 | Device time: what is trustworthy, and what depends on it | **accepted** | 01-F43..F46 / 01-N2 / 25 §14 / 03 | Ordering must not depend on clocks (DEC-PERF-001) — but *time itself* still must be answered, and today it is not. Live defect: `folds/merge.ts:428/733/751` stamp `confirmed_at`/`kot_at`/`age_basis` from raw `device_created_at` (was `folds/replay.ts` pre-T-01-15), so a wrong device clock writes wrong values straight into the timing read models doc 03 consumes; and `01-N2`'s skew health flag is specced but **has no implementation** and is deferred out of Wave 0 (`conformance/wave-0-scope.yml:75`). **RATIFIED by the founder (July 2026) as proposed, promoted into `01-F43..F46` and `01-N2`:** (a) **durations need a consistent clock, not a correct one** — a uniform offset cancels in a difference, so kitchen age/ETA are safe on branch-consensus time (01-F43); (b) the elected hub (01-F13) is the branch time authority, and since DEC-SYNC-009 makes the hub the WAN uplink it is normally the device that HAS internet, so branch time is genuinely correct in the common case and merely self-consistent when fully offline (01-F43); (c) absolute business stamps (tax 16, audit 01-F5, day boundary) use `server_received_at` when available, else branch time **marked provisional** via an explicit `time_basis` marker and reconciled at contact — 16-N3 already assumes this (01-F44); (d) `device_created_at` demoted to untrusted display/forensic hint, banned from folds and read models alongside the 01-F34 ordering-metadata ban (01-F45); (e) 01-N2 detection un-deferred into Wave 0 (`conformance/wave-0-scope.yml`); (f) business-day boundary anchored to Asia/Karachi regardless of cloud region (01-F46). Needed under ANY ordering choice. **Two amendments from the T-01-17 oracle pass (July 2026), both corrections to the first draft's FR text rather than to the decision:** (i) the envelope `time_basis` marker is `'branch' \| 'branch_provisional'` — the original `'server' \| 'branch_provisional'` was incoherent because a device cannot know server time at append; `'server'` belongs to the DERIVED business stamp, and 01-F44 now separates the two explicitly; (ii) 01-F46 never stated when a day *starts*, which silently meant midnight — **ratified: a configurable cutover hour, default 05:00** (layer-2 org setting), so a 01:30 sale banks to the night it was served rather than splitting a late-closing restaurant's takings across two days' reports. Also scoped: 01-N2's direct device↔cloud skew leg is **explicitly deferred** (no protocol field carries a server clock at hello time); the hub leg ships. Implementing task: **T-01-17** | — | W0 |
| DEC-AUTH-001 | Device token lifetime and binding | **accepted** | 01-F47 | Shipped tokens carry **no `exp` and no `iss`/`aud` binding** (senior review `audit-1.md` #9), so a token survives forever until someone explicitly revokes it and a token minted for one deployment validates against another. Short TTLs are the reflexive answer and are **wrong here**: a device offline for a day would stop working, violating 01-F17 / 00 §5.1. **RATIFIED (founder, July 2026):** mandatory `exp` with a 90-day default; `iss`/`aud` binding; silent renewal on any authenticated cloud contact **including hub-relayed** (01-F13/DEC-SYNC-009) so a LAN-only device renews without ever holding WAN; host warns below 25% remaining life; expiry withdraws cloud/LAN **admission only** and never blocks a local sale; an expired-but-unrevoked device is admitted solely to drain its backlog and take a renewal; expired **and** revoked is purged (01-F42). Revocation remains the operative kill switch — expiry is the backstop against indefinitely-valid credentials. Implementing task: **T-01-18** | — | W0 |
| DEC-AUTH-002 | Revocation eviction latency | **accepted** | 01-F48 | Revocation previously took effect only at a device's next *voluntary* contact — a revoked tablet holding a live session kept participating indefinitely (senior review `audit-1.md` #1 residual / F3-eager). The read half is already fixed (`9a0c1ff`: revoked peers culled from fan-out, fail-closed) but no latency bound was ever stated. **RATIFIED:** eviction within 30 s wherever any path reaches the device — the cloud drops live sessions and culls fan-out on the revoking transaction, the hub does the same on LAN; fail-closed (unreadable revocation state refuses participation rather than granting it); revocation blocks **reads as well as writes**. Implementing task: **T-01-18** | — | W0 |
| DEC-SYNC-010 | Compressed wire framing | **accepted** | PROTOCOL.md / 01 §8 / 26 §6.4 | `01 §8` always said "JSON + zstd batch compression"; T-01-16 landed the codec (`node:zlib` zstd, no new dependency per 18 §15 rule 1) and proved it at codec level — `zstd(utf8(JSON))`, transparent and deep-equal to the plain codec for every message kind, carrying a `ZSTD_c_checksumFlag` content checksum so a corrupted frame fails loudly instead of decompressing into a schema-valid but *wrong* message. What was never decided or wired is **which connections use it**. **RATIFIED:** compression is negotiated per connection at `hello`/`hello_ack`, opt-in from both ends, and applies to the catch-up path where the 26 §6.4 transfer budget lives; a peer that does not advertise it receives plain JSON forever, so the negotiation can never strand an older device. **Scope amended July 2026 (T-01-19 oracle round): the CLOUD connection only — the LAN mesh never advertises.** The budget this exists for is the cloud catch-up transfer (`26 §6.4`, <60 s on 4G); LAN propagation is shop-grade Wi-Fi at <1 s p95 (`01-F15`), so compressing it buys nothing worth a wire change. And structurally the LAN transport wraps every message in a JSON envelope whose discriminant lives *inside* the JSON, so a compressed LAN payload would need either a spec-reviewed binary frame variant or base64 inside the wrapper (which throws away most of the gain) — speculative work against a budget under no pressure (`24 §3b`). If a LAN transfer budget ever binds, that is its own spec change. Implementing task: **T-01-19** | — | W0 |
| DEC-SYNC-011 | Catch-up blocked on a permanently-rejected event (version skew) | **accepted** | 01-F9 / 01-F11 / 01-F37 | Catch-up applies a **contiguous prefix**; an event the device can never accept — unknown event type, payload from a newer schema version — is not divergence and not quarantinable at the device, so the cursor simply stops. Today it wedges **silently**: `status()` reports no reason and the honesty UI (00 §5.7, 01-F11) shows a device that looks merely idle while it is permanently stuck (senior review `audit-1.md` #4). **RATIFIED:** (a) a blocked cursor is **observable** — **`CloudSessionStatus.blocked`** carries the blocking `global_seq`, the rejected event type, and a machine-readable reason (**wording corrected July 2026: the original said `status()`, which sent implementers to the device store's `SyncStatus`, where it is not and should not be — the cursor belongs to the cloud session, and a LAN-only device under DEC-SYNC-009 has none at all**), surfaced to fleet health (doc 15) and the honesty UI; (b) forward-skew policy is **stop-and-report, never skip** — silently skipping an unparseable event would fabricate a gap in a log whose whole value is completeness, and skipping is unrecoverable while stopping is not; (c) the device keeps operating locally on what it already holds (01-F17). Implementing task: **T-01-20** | — | W0 |
| DEC-SYNC-003 | Event archive tier at scale | proposed | 01 §9.4, 22 | S3 parquet archive vs keep-hot | Year-2 volume data | Post-launch |
| DEC-SYNC-004 | Cloud uplink topology (hub-proxied vs per-device) | **superseded** → DEC-SYNC-009 | 01-F13 / HUB-ELECTION.md | Was: per-device cloud sessions only; hub-proxied uplink deferred "until a constrained-WAN branch needs it". Superseded because that trigger is the NORM in the target market, not an edge case — see DEC-SYNC-009. Code and tests citing this row implement the superseded rule until the DEC-SYNC-009 task lands | — | W0 |
| DEC-SYNC-009 | Hub-proxied cloud uplink for WAN-less devices | **accepted** | 01-F13 / PROTOCOL.md | Founder call (from the kernel review): hub-proxied uplink is REQUIRED, not deferred. Per-device cloud sessions remain the default wherever a device has WAN; **additionally** the branch hub relays WAN-less devices' events upward on their behalf. Trigger: the common deployment gives internet only to the counter terminal (cost + keeping waiters off the internet), so waiter/kitchen devices are LAN-only *by design* — under the superseded rule their events reached the counter over LAN but NEVER reached the cloud, silently truncating the cloud log (reporting, analytics, multi-branch roll-up, backup/DR all quietly wrong). PROTOCOL.md already assumed this ("hub relays `push` upward"); the contradiction was with the `push` row's "own events only". Mechanism (additive under `v:1`): a hub session may push events whose `device_id` differs from the session's, authenticated as branch hub; the gateway tracks lamport contiguity per ORIGIN device, not per session; relayed events are attested by the hub, never re-authored (01-F1) | — (T-01-09 landed both: relay = token `hub_relay` claim ∧ unrevoked hub-eligible registry row, and the relayed-ORIGIN registry check at the merge boundary — `origin_unregistered`/`origin_revoked` quarantine classes; the fix-round-F6 hole is closed) | W0/W1 |
| DEC-SYNC-005 | Cloud ack watermark advances over quarantined events | **accepted** | 01-F37 | Ratified: a durably-stored quarantine row fills its lamport slot, so the push ack advances past a poison event and the outbox never wedges (01-F17 spirit). 01-F37 carries a clarifying sentence; the Auditor counts quarantine rows as slot-filling (T-01-11) | — | W0 |
| DEC-SYNC-006 | LAN heal set-union when the origin departed | **accepted** | 01-F38 / HUB-ELECTION.md | Founder call: accept the cloud-catchup dependency for the narrow edge (origin departs mid-partition, WAN down) — documented in HUB-ELECTION.md; follower third-party relay deferred. T-01-06 X7 demonstrates the cloud path closes it | — | W0 |
| DEC-SYNC-007 | Invariant-validation split (gateway vs Auditor) | **accepted** | 01-F29/F30/F37 / T-01-08 | Gateway enforces only fold-FREE provable invariants inline at merge (refund ≤ un-refunded remainder, 01-F29, via targeted event reads) and quarantines violators (new `invariant_violation` reason); fold-requiring settle-time conservation (01-F30 full equation) is the Auditor's refold job. **Divergent-parent tolerance ratified July 2026 (audit-1 #12):** the two planes had two different tie policies — the gateway resolved a divergent parent attempt key by `order by global_seq asc limit 1` (first-merged wins) while the engine marks a divergent key DISPUTED and contributes zero (01-F31, "a fold never picks a winner"). **The gateway stops picking:** a disputed parent makes the cap UNPROVABLE, so the refund passes through with the anomaly flagged, which is what this row's own "fold-FREE provable invariants" scope already implied. The first-merged read let *delivery order decide a money outcome* — the exact failure `26 §2` exists to remove — and two clouds replaying the same events in different orders could reach different verdicts on one refund (T-01-11), not the merge path. A refund whose parent isn't merged yet passes through (01-F17) | Founder ratify | W0 |
| DEC-SYNC-008 | Quarantine-notice delivery guarantee | **accepted** | 01-F37 / PROTOCOL.md | At-least-once now: a durable `kernel.quarantine_notices` outbox keyed by origin device, live-sent + redelivered on next `hello` (mark-on-send). Exactly-once (a `quarantine_notice_ack` wire kind — a PROTOCOL.md addition) deferred | Founder ratify | W0 |
| DEC-AUDIT-001 | Audit hash-chain rule (01-F5) | **accepted** | 01-F5 / 18 §14 | `prev_audit_hash` in the event PAYLOAD (the envelope `z.object` strips unknown keys, so an envelope field would be silently dropped); SHA-256-hex over canonical-JSON of the envelope MINUS `server_received_at` (device-`null` and cloud-stamped must hash identically); `@noble/hashes` (pure-JS, sync, RN-portable — `node:crypto` unusable in RN, Web Crypto is async so unusable in the sync append path) — a NEW 18 §14 allowlist dependency | Founder ratify (dependency + scheme) | W0 |
| DEC-TRAIN-001 | Training mode: where rehearsal events live | **accepted** | 01-F49..F51 / 27-F52 / 27-F63 / 02 / 22 | `27-F52` made a training mode a product requirement and said it "reaches the kernel… needs an architectural answer, not a UI toggle", and nothing in doc 01 or 02 answered it (inventory gap **G1**, which blocks every role plus `21-F11` RITE rounds and `27-F53`'s speed test, since neither has anywhere to run). The research finding behind it: staff either train on live tickets — polluting an append-only ledger and every report built on it — or they do not train at all. **PROPOSED: a training session is an ordinary session against a training BRANCH; the kernel gains nothing.** A branch carries `class: production \| training`; branch-scoped credentials (01-F9), fan-out (01-F13) and reporting already isolate it, so the boundary is enforced once at admission rather than in every query. **Rejected: an `is_training` flag on the envelope** — it makes correctness depend on every fold, report, export and analytics query remembering to filter, one forgotten filter is invisible in review, and `01-F1` then makes the pollution permanent because it cannot be deleted. **Rejected: an ephemeral scratch store** — the things most needing rehearsal (shift close, cash count, day close) span a shift, so training that evaporates cannot teach them. Entering/leaving is an attributed credential swap with the production session draining first (01-F50), never a toggle, so a trainee cannot reach production data mid-flow. A training branch is **droppable wholesale** (01-F51) because it is by construction not history — which is both the retention answer (22) and what makes repeated RITE rounds affordable — while deletion of individual events *inside* it stays forbidden, or the rehearsal teaches the wrong habit. Catalog mirrors read-only from production so staff train on the real menu. UI obligation is 27-F63: a persistent band and surface tint, achromatic (the 27-F14 colour budget has no training slot). **RATIFIED as filed (founder, July 2026), with governance settled: the layer-2 BACK OFFICE creates a training branch, and it is NOT billable** — charging for training suppresses training, and untrained staff on live tickets is the exact failure the feature exists to prevent. | — | W1 |
| DEC-AUDIT-002 | `audit.*` channel modeling | proposed | — (01/02 candidates) | Dedicated tamper-evident `audit.*` events vs pairing with operational events (does `cash.drawer_opened` also emit `audit.drawer_opened`?). Recommend DEFER to the POS wave to avoid double-modeling drift; T-01-10 ships only the `audit.*` emitter mechanism | Founder product call | W2 |
| DEC-TEST-001 | Wave-0 conformance gate scope (24-F5 vs multi-wave kernel) | **accepted** | 24-F5 / conformance/wave-0-scope.yml | Founder call: a declared Wave-0 FR subset (`conformance/wave-0-scope.yml`) — the gate enforces every in-scope FR mapped, out-of-scope FRs carry an explicit deferral reason. `verify:01 --gate` wiring to read the subset is a scheduled harness follow-up | Gate-wiring | W0 |
| DEC-TEST-002 | Where 01 §8 exit criteria "run" (sim vs containerized vs rig) | **accepted** | 01 §8 / T-01-06 | Founder call: the deterministic sim leg AND the real-process smoke leg (real WebSocket + real SIGKILL-mid-print, in-process Testcontainers) are BOTH binding pass/fail gates; only physical-rig wall-clock p95 stays D3 (post-spike). The 19 §6 Ditto ramp reads pass/fail off the combined sim+smoke gate | — | W0 |
| DEC-TEST-003 | Auditor v1 scope ("ships in Wave 0" vs missing prerequisites) | **accepted** | 20 §4.2 | Ratified: Auditor v1 = refold-vs-readmodel diff + lamport gap-free (counting quarantine slots per DEC-SYNC-005) + money conservation (01-F30) + state-machine legality. Device-state diff and audit-chain verification follow their producers; the 01-F5 hash-chain emitter is scheduled (T-01-10) since the chain check depends on it | 01-F5 emitter (T-01-10) | W0 |
| DEC-ORDER-001 | Pre-orders / scheduled orders (`fire_at`) | proposed | — (01/03/06 candidates) | Unmodeled; storefront "order for 8pm" not supported | Founder product call | W2 |
| DEC-SUPPLY-001 | Purchase-order events (auto-PO from suggestions) | proposed | — (10/13 R3) | `action.proposed` draft-PO exists in ladder; PO entity undefined | Design with 10 forecasting maturity | W4 |
| DEC-PEOPLE-001 | Shared rider phones (one device, several riders) | proposed | 09 §9 | PIN-switch on one device vs disallow | Field reality at pilots | W2 |
| DEC-CUST-001 | Customer merge conflict UX (multi-branch same-phone) | proposed | 01 §9.3 | Kernel merges; who resolves name/address conflicts, where | UX design | W2 |
| DEC-DATA-001 | PII erasure mechanism (final) | proposed | 22 (crypto-shredding default) | Envelope-encrypted PII fields, key destruction on erasure | Legal verify + build spike | Pre-pilot |
| DEC-TAX-001 | Is a pre-payment BILL legal under Sindh integration? | **closed — founder risk acceptance** | 16 / 02 / 03 | **Sindh rule 6(3): an integrated person "shall issue only the invoice specified in sub-rule (1)… and shall not issue any *katcha* or provisional invoice, by whatever name called, to the customer."** Handing the customer a bill before payment is the NORMAL Pakistani dine-in flow, and the plain reading of 6(3) prohibits it for an SRB-integrated restaurant. The corpus does not model the document at all — `grep -rn "proforma\|pre-check\|bill_requested" specs/` returns one hit, a storefront status label — so this is simultaneously a missing document type and a possible legal bar on the flow that would use it. If the reading is right, the standard dine-in sequence must change under Sindh integration (bill becomes a screen-shown total, or the fiscal invoice is issued before payment). **Not an engineering call: needs a Pakistani tax lawyer.** Blocks the `bill` document type in doc 03 and the doc-02 dine-in flow for Sindh only **FOUNDER RULING (July 2026): proceed as designed — the pre-payment bill ships, with NO Sindh special-casing.** Recorded as an explicit risk acceptance rather than an unexamined default: the concern above was raised, considered and declined by the founder, who owns the commercial and legal exposure. The bill is a first-class document type in `03-F31` and is no longer marked blocked. **Revisit trigger:** any SRB enforcement notice, or advice from counsel if engaged later. | legal verification | before any Sindh customer |
| DEC-TAX-002 | Vendor liability for invoicing software in Sindh | **accepted** | 16 / commercial | **Sindh Finance Act 2026 appears to insert s.43 entry 2AA:** a penalty up to **Rs 1,000,000 (minimum Rs 100,000)** on *"any person who designs, develops, customizes or supplies invoicing software"* enabling non-conforming invoices — i.e. **on US, not the restaurant** — and rule 8 requires a PoS vendor to have its place of business in Sindh. **Not indemnifiable by a contract term with the customer.** **UNVERIFIED:** SRB publishes the Act as an image scan with no text layer; read by OCR, single-source. Compounding context from the same research: Pakistan provides no certified rendering device (rule 150ZEB — the SDC may be *software attached to the POS*), so RestOS **is** the trusted renderer, and rule 150ZEO(4) seals premises after three unverified invoices in a day. So a rendering defect is legally indistinguishable from evasion, and the liability may land on the vendor. This is the evidential basis for the slot-not-template customisation model. **Founder + lawyer, before any Sindh customer is signed** **RATIFIED (founder, July 2026): a contractual liability cap PLUS a per-region fiscal conformance suite that must pass before any region goes live.** The suite is the substantive half — it converts an open legal exposure into a testable gate, and `03-F34` already builds most of it (mandatory adapter blocks present, QR physical size meets the adapter minimum at the target dpi, no owner slot rendered inside a locked region, hard print refusal plus an S1 band on failure). `03-F35` (always rasterise the fiscal QR, never the native ESC/POS command, because cheap printers fail silently) and `03-F36` (every spec renders at 32 columns) are conformance rows too. A cap alone would not be defensible when we are the trusted renderer and the merchant's exposure is *premises sealed* rather than a refund. | legal verification | before any Sindh customer |
| DEC-DATA-002 | Pakistan data-residency posture | proposed | 22 | Region near PK; residency law unverified | Legal verify | Pre-pilot |
| DEC-CHAN-001 | Customer-chat bilingual (roman-Urdu) output | **blocked** (eval gate) | 07-F23 | English replies until native-speaker eval bar passes | Eval corpus + model quality | Post-launch |
| DEC-CHAN-002 | Instagram DM automation | proposed | 07 / concept §4.2 | Link-in-bio only; DM bot when API allows | Meta API capability | W4+ |
| DEC-UI-001 | Is a `dp` a physical size or a CSS pixel? | **accepted — RATIFIED** | 27-F68 / 27-F11c / 27-F8 / 00 §7 | **The question.** `packages/ui`'s `targetFor("keypad")` returns `126` and every app spends it as **126 CSS pixels**. That identity — dp ≡ css px — is true only at 160 PPI, and it is stated **nowhere** in doc 21 or doc 27. `27 §1a`'s own hardware table contradicts it: the counter POS is 15.6″ at **100–141 PPI**, where a 76 dp tile is **47–67 px** and a 126 dp keypad key is **79–111 px**. That table *is* the millimetre conversion, and `27-F11c` already states the rule in words — *"design in millimetres, render in pixels."* So this was an implementation that never read its own spec, not a gap in the spec. **Measured (August 2026, real `BrowserWindow`, 1366×768 at 100.5 PPI; `apps/pos-electron/CLAUDE.md`).** The keypad is 4 × 126 + 3 gaps = **528 px**, and under `03-F5`'s alarm band `main`'s content box is **498 px** — *the pad alone does not fit*, before any label, DUE figure or TAKE CASH button. The whole budget in one line: **51 + 85 + 102 + 528 = 766 in a 768 px panel** (status strip, tab rail, band, pad), leaving 2 px for all padding everywhere. Separately, `ManagerApproval` lays out **1162 px in a 632 px box in BOTH device states**, so `Approve`, `Not them?` and `Cancel` sit entirely below the viewport (top edges y=852, 1002, 1152) — `02-F20`'s local manager-PIN escalation is the only one built, doc 05's remote path is unbuilt, and `05-F19`'s over-threshold paid-out is the live case that reaches it, so that route is dead on arrival. At `27 §1a`'s own 79 px the pad is **340 px** and the surface fits with room. **RULED (founder, August 2026): option A — implement the physical conversion.** A dp is a **physical size**: 1 dp = 1/160 inch = 0.15875 mm, so 126 dp is 20 mm and **20 mm is what must reach the glass**, rendered through the panel's actual pixel density per `27-F11c`. The renderer therefore takes the panel's density as an input — `00 §7` layer 3, `panel_ppi`, **measured from the display by default** and configured only to correct a display that reports nothing or reports wrong. Promoted into **`27-F68`**; `27-F11c` needed no amendment because it already said this. **Two traps the FR text forbids explicitly, because each is the obvious next error.** (1) **A hardcoded 79 px is WRONG.** 79 px is the answer for *one* panel: on the 141-PPI 1920×1080 panel `27 §1a` also lists it is **14.2 mm**, 29% below `27-F8`'s floor, on the highest-consequence entry surface in the product. The conversion is computed from the panel's real density, never pinned to one panel's answer. (2) **The floor is in millimetres, not pixels.** `27-F8`'s 126 dp is a measured ergonomic minimum; this ruling changes how it is *rendered* and never what it *is*. Trimming the millimetres to make a layout fit is the `01-F61` cost-floor move one domain over. **Rejected, each on a resolving FR rather than on preference.** *Option B, "the counter ships 1920×1080 only"* — 1080 px of panel fits everything at dp ≡ css px, and it is refused as **exactly the category error `27-F11c` names**: it answers a physical-ergonomics question by constraining a pixel count, when `27-F11c` says both resolutions are the same physical surface holding the same tiles. It also re-breaks silently on the next panel, and it converts a design law into a hardware purchasing constraint nobody enforces at a counter. *Overlay the band instead of displacing the work area* — the band would cover the top 102 px, which on Pay is DUE, the `02-F12` method row and the top of the pad: you lose `1` `2` `3` and the method selector instead of the bottom of `C` `0` `⌫`, `elementFromPoint` then reports COVERED, and `27-F11d`'s *"the work underneath stays visible and usable"* is broken either way. It relabels the loss. *Scroll or page the work area* — `27-F2`, and the keys are not a list. *Hide the pad behind a SETTLE button* — `27-F5`. *Shrink the keys* — `27-F8`, see trap 2. **Consequences.** (a) **Every touch target in the product changes** — shrinking on the 1366×768 counter, and *growing* on anything above 160 PPI: `27 §1a`'s ~224-PPI waiter tablet and ~405-PPI phone rows are 1.4× and 2.5× today's rendered size. This is not a POS-only edit. (b) **Apply the conversion ONCE at the token boundary, to every dp in the layout** — a session that converts `targetFor()` and leaves the status strip, tab rail and band in raw CSS px ships a layout whose proportions are wrong on every panel, which is the same defect in a new place. (c) `27-F2` / `27-F11a` page capacity is **unaffected** — it already computes in millimetres, which is the half of `27-F11c` that was implemented. (d) **It closes defect 4 and is necessary-but-not-sufficient for defect 5:** at 100.5 PPI the conversion scales `ManagerApproval`'s 1162 px to ≈**730 px** against a content box of at most ≈662 px quiet, so that surface still overruns and additionally needs the `27-F4` positional change its own register entry names — three trailing full-size buttons cannot all be keypad-posture under a keypad in a 768 px panel. (e) The layout gate measures **one** panel at devicePixelRatio 1; `27 §1a`'s second counter panel must enter its fixture with this work, or the ruling ships untested on precisely the case that produced trap 1 | implementing session (renderer density input, second panel in the layout-gate fixture) | W1 |
| DEC-HW-001 | Bring-your-own-hardware: the product adapts to the device, the device is not specified to the customer | **accepted — RATIFIED (premise)**, two sub-questions OPEN | restaurant-os.md:47 / 00 §7 / 02-F31 / 02-F40 / 03-F22 / 03-F24 / 27-F11c / 27-F11e / 27-F68 | **RULED (founder, August 2026):** *"the system should adapt to the device it runs on and not assume that everyone will have a proper screen and KDS and all. It would be foolish, we have a bring-your-own-hardware software, not a we-will-give-you-hardware."* A Pakistani restaurant runs RestOS on what it already owns. **The surprising finding is that this ruling was already the corpus's position and only the CODE dissented.** `restaurant-os.md:47` — top of the authority order — reads *"Hardware tiers: **T1 Counter** (terminal + printers) · **T2 Counter+Pass** · **T3 Full mesh**. Tiers upgradeable; lower tiers degrade gracefully, never feel crippled."* `02-F31` specifies T1 detection and its auto-advance rules; `03-F22` makes KDS-vs-printer a per-station layer-2 choice; `03-F24` makes ready-signal ownership a layer-2 role assignment; `27-F11e` says the pass screen is optional; `02-F40` reasons explicitly about *"a T1/T2 kitchen (27-F11e — most deployments)"*. **Measured August 2026: `grep -arn "hardware_tier\|hardwareTier" apps packages services` returns ZERO hits.** The implementation collapsed three specified tiers into the richest one and then hardcoded that tier's hardware. So this is an implementation that never read its own spec — the same shape as `DEC-UI-001`, one layer up, and the second time that sentence has had to be written. **Register (August 2026): 8 hard refusals, 6 absence-modelled-as-fault, 11 capacity claims pinned to a panel, 15 genuine physical laws that must NOT be adapted away.** The severe ones, each verified: (a) **`order.line_state_changed` has a Zod schema (`domain/registry.ts:213`) and a fold consumer (`sync-client/folds/merge.ts:780`) and no production emitter anywhere** — so on a T1 branch, the configuration the corpus calls most deployments, **no order line ever advances past `placed`**. A missing PRODUCER for an event type, which `seams:check` cannot express because a key in an object literal is not an export; the prior instance was `audit.print_acknowledged`. (b) **The size floor is on the wrong axis.** `window-options.ts:42` declares `minWidth: 1366, minHeight: 768` and Electron *prevents* the resize — measured, 1280×800 on 13.3″ glass lays out with **zero violations and is refused**, while 1366×768 on 10.1″ glass **satisfies the pixel floor and clips two surfaces**. The requirement is physical: chrome is 37.4 mm with the alarm band up and the Cash tab needs 94–96 mm of work area, so **≈131–134 mm of glass height** is the real floor (10.1″ panels have 126–130 and fall 2–6 mm short; 13.3″/14″ have 174–179 and clear it). **130–174 mm is unmeasured, not known-good.** (c) **Absence of a printer is modelled as failure, not configuration.** `unattachedPrinter` manufactures a failed transmit per job, retries exhaust, `03-F5` raises a permanent band, `printing.ts:403` appends a **permanent** `kot.print_failed` to an append-only ledger under `01-F1`, `05-F3` alarms the manager and `15-F14` pages vendor support on `kot.print_failed` rates — so a printerless branch generates real support load forever. There is **no `printer: none` mode** in specs or code. (d) **The density fallback assumes a 15.6″ PANEL, not merely a density**: on a 10.1″ tablet it yields ~100 PPI against ~224 PPI of real glass, so every `27-F8` target renders at **~45% of its ergonomic size, silently**. (e) `surface-mode.tsx` has two consumers repo-wide and changes three numbers; a 6.5″ phone and a 13.3″ laptop resolve to the **same mode** at 151 violations and zero respectively — **the product has dispatch, not a degraded mode.** **RATIFIED, and implementable without further ruling:** tier becomes a real read value **derived** from the device registry per `02-F31` (following `panel_ppi`'s precedent in `00 §7` — measure first, configure only to correct, because *"a number a technician types is a number a technician mistypes, and the failure is silent"*); `02-F31`'s auto-advance ships exactly as written, **including its prohibition — no `ready` state is ever fabricated**, since `03-F26` makes T1's absence of ready samples the honest reason T1 restaurants get aging timers and never learned ETAs; the floor is restated in millimetres through the measured density. **TWO SUB-QUESTIONS ARE OPEN AND ARE THE FOUNDER'S:** (1) **`03-F49` on legibility grounds.** It rules a 58 mm printer cannot print a KOT, and `min-columns.ts` gates on **Font A** *specifically to keep that FR true* — its own comment concedes 58 mm yields `floor(384÷9) = 42` Font-B columns, **exactly `MIN_COLUMNS.kot`**, so *"a Font-B gate would admit the printer the FR says is excluded."* 58 mm is the named Pakistani installed base and `27-F11e` makes the KOT the primary kitchen interface, so under BYO the question is no longer arithmetic but **whether Font B is legible on a kitchen ticket in steam and grease at arm's length** — a `27-F27` angular question that may well resolve the same way, but must be decided on legibility rather than inherited from a gate chosen to protect a sentence. Live symptom of the avoidance: `main/index.ts:194` defaults to `printerCapability("TH230")`, a model the till has never seen, and the comment concedes the pin exists because the honest default would refuse every KOT. (2) **Is there a tier BELOW T1?** T1 is defined as *"terminal + printers"*, so a printerless restaurant is outside the corpus entirely, and `27-F11e` makes the pass screen optional — exactly backwards for it. If a screen-only T0 is accepted, `apps/pass-kds` moves from deferred to load-bearing, because paper and glass are two routes to one job and **neither is currently built as the optional one**. **✅ SUB-QUESTION (1) IS ANSWERED — NO. FONT B DOES NOT CLEAR, AND `03-F49` STANDS AS WRITTEN (August 2026), decided on legibility as the re-opening required rather than inherited from the arithmetic gate.** **The geometry, at 203 dpi (0.1251 mm/dot).** The corpus states font cell **widths only** (`03 §7`: *"Font A = 12, Font B = 9"*) and states **no glyph height and no cap height for either font anywhere** — a corpus-wide sweep for `12×24` / `9×17` / cap height returns **zero hits** — so every height below is an **IMPORT** from published ESC/POS geometry, marked as such rather than cited. Font A's cap **measures 14 dots = 1.75 mm** in the only renderer that exists (`simulate.ts` draws a 5×7 face at 2× inside a 12×24 cell it declares as an interpretation and explicitly disclaims as a legibility claim); the conventional published figure is 17 dots = **2.13 mm**. Font B's conventional 9×17 cell puts its cap at **9–12 dots = 1.13–1.50 mm**. **The angular answer.** The corpus states **no viewing distance for paper**: `27-F27` is scoped to glass by its own words (*"KDS type"*, *"KDS primaries"*) and by `27-F11e` assigning §3–§5 to glass, `27-F11h` says outright *"this document has no design language for thermal paper"*, and the only paper-side phrase in the corpus is `27-F55`'s unquantified *"at arm's length"*. **0.45 m is therefore a STATED ASSUMPTION, not a citation** — a chit clipped at a station or laid on a pass while both hands work. At 0.45 m Font A subtends **13.4–16.2 arcmin** and Font B **8.6–11.5**, against ISO 9241-303's **16 minimum** and 20–22 recommended. Font B reaches the bare minimum only within **242–323 mm**, the recommended band only within **194–258 mm**, and `27-F27`'s **30 arcmin** — the factor it prescribes for *steam, grease and a moving reader*, which are the kitchen's conditions and not glass's properties — only within **129–172 mm**, which is not a reading distance. **The decisive framing is that the budget has no slack to spend: the KOT is ALREADY at ISO's floor in Font A** (Font A clears 16 arcmin only out to 376–457 mm), and the item **NAME** — the thing the cook must actually read — renders at `normal`, because `27-F56` allocates the 2× rung to the quantity and the identifier. Font B removes ~30% of a cap height that is already at the minimum. **The NO is robust to the imported heights**: even the most generous Font B reading (12 dots, 1.50 mm) misses the recommended band beyond 26 cm. **AND IT IS NOT A ONE-LINE GATE FLIP — THE ENCODER CANNOT EMIT FONT B AT ALL.** `packages/escpos/src/encoder.ts` emits exactly `ESC @`, `GS ! n`, `GS B n`, `ESC d n`, `GS V m` and `GS v 0`; **there is no `ESC M`**, `simulate.ts` carries one 5×7 face and no font state, and `ESC M` is **not in K-2's oracle allowlist**, whose walker fails any un-admitted `ESC` command as an unaccounted byte — so admitting Font B means an encoder change, a simulator font model, a Font B cell height the corpus does not state, and a finding for the test-owning session, all resting on the legibility claim this row refuses. **`cols_font_b` has ZERO production readers** (measured, comment-blind): a capability field the product declares and never uses, the wave's named defect in dormant form, and `packages/testing`'s own oracle says so — *"FONT B IS NEVER EXERCISED … no document below selects Font B (`ESC M 1`)"*. **MEASURED, because a flipped gate over a Font-A encoder is `03-F34`'s banned silent degradation rather than a hypothesis:** with the gate mutated to read `cols_font_b` (42) and the paper left at 384 dots, a realistic two-line ticket (longest name 35 chars, inside the budget `27-F57` sets) **discards 320 dots and overhangs by 95** — the word `Portion` is simply absent from the paper, with no signal to the cook that anything was cut. Under the shipped `example_data` the same mutant discards **zero**, which is exactly why no suite would catch it: `03-F36`'s build-time gate renders the KOT at **42 Font-A columns = 504 dots** and has never rendered it on a 384-dot page. **WHAT WOULD REOPEN IT IS A PHYSICAL TEST, AND THE CORPUS ALREADY SAYS SO.** `27-F61` is the precedent — Urdu on thermal paper is *"UNPROVEN and must be physically tested before it is designed around … Until that sheet exists, no FR may assume Urdu is usable on paper"* — and §2b subordinates every FR in it to `27-F35`'s ≥85% / ≤5% gate, stating *"where the pilot contradicts this section, this section loses"*. **Folded into K-8:** print the `kot` `example_data` and a 42-column worst case on the **48 GSM stock actually sold in Pakistan** (`03-F45`), Font A on 80 mm against Font B on a BC-58U, and run `27-F35`'s post-training retest with real kitchen staff **at the distance the ticket is actually read, measured rather than assumed** — that measurement is the missing corpus figure and is worth recording whatever the verdict. Until that sheet exists **no FR may assume Font B is legible on a KOT**. Two riders for the rig: the unresolved `size_2x2`-vs-line-advance overlap (`apps/pos-electron/CLAUDE.md`) is visible on every rendered KOT and only hardware separates its two readings; and 42 Font-B columns is **exactly** 42 with **zero slack**, so any future KOT content change breaks 58 mm silently while 80 mm keeps 14 columns of headroom — a standing argument against admitting it even if the type were legible. **THE LIVE SYMPTOM IS FIXED, AND IT WAS WORSE THAN A DISHONEST LABEL.** `main/index.ts` defaulted to `printerCapability("TH230")`, claiming 44 Font-A columns, a cutter and raster for a printer the till has never seen. That is not only `00 §5.7`-adjacent dishonesty: because `render()` lays out against the *claimed* record, attaching the corpus's own baseline **BC-58U** (`03-F10`) without setting `RESTOS_KOT_PRINTER` produces exactly the 320-discarded-dot silent truncation measured above — **the TH230 pin was itself a latent `03-F34` violation waiting on the named installed base.** The default is now `03 §7`'s own *"defaulting conservatively to 32 for an unknown model"*, under the model id `no printer configured`, so an unconfigured till takes `03-F49`'s refusal and `03-F34`'s S1 band naming both column counts — which is the correct signal and the one this row's original text predicted | (2) whether a screen-only tier below T1 exists | W1 |
| DEC-HW-002 | `02-F31`'s settlement half is unreachable: `served` has no legal predecessor at T1 | **accepted — RULED**, protected path, senior review owed | 02-F31 / 03-F26 / 01 §4 / 01-F1 / 01-F34 / 26 §7 | **A contradiction between an FR and the kernel state machine, found while building `02-F31`'s producer (August 2026).** `02-F31` requires *settlement → lines `served`* and in the very next clause forbids fabricating `ready`. `packages/domain/src/states.ts:31-32` reads `in_prep: ["ready", …EXITS]` and `ready: ["served", "picked_up", …EXITS]` — so **`served` is reachable only through `ready`**, and the two clauses cannot both hold. `03-F26` closes the escape route that the prohibition might have been about *samples* rather than *states*: it says outright *"T1 branches produce no ready-marks (02-F31), so they honestly produce no samples."* Two documents agree the state is never entered. **Every route out fails except one, and the failures are instructive.** ⚠ **This row first said an `in_prep → served` edge against the old matrix would be *"recorded `illegal_transition` permanently under `01-F1`"*, and that prediction is WRONG — corrected August 2026 from the implementing session's M4 mutant, which is the only thing that could have found it.** `advanceEdgesFor`'s pre-existing legality filter refuses the line *before* anything is emitted, so the real outcome is **wired and emitting nothing** — which is the option this very row calls **the worst**, because it looks finished with every gate green. It is milder than predicted in one way (no ledger is polluted, nothing is permanent) and worse in the way that matters (it is silent, where an `illegal_transition` flag would at least have been a record). **The general lesson is the one this corpus keeps paying for: a failure mode reasoned from the FRs was plausible, cited, and wrong, and only mutating the code told anyone.** A ruling may state the decision from the specs; it may not state the *runtime consequence* without running it. Declaring `from_states: ["ready"]` is a legal-looking lie that trips `inconsistent_predecessor`. Filtering by legality and emitting nothing is the **worst** option, because it looks finished with every gate green — this wave's named defect exactly. And **a tier-conditional legality is not merely inexpressible, it is a law-1 violation**: `26 §7` row 65 makes legality a pure function of one edge's payload, so gating it on the branch's tier would make a projected value depend on the reading device's configuration and convergence would depend on who is looking (`01-F34`). That is the decisive argument — it eliminates the option that would otherwise look safest. **RULED: `LEGAL_NEXT.in_prep` gains `served`.** Not as a concession to T1 but because it is the honest model — **in a restaurant with no pass, a line goes from being cooked to being handed over with no observed moment of readiness**, and the machine as written encodes *"a pass exists to observe readiness"* as universal law. That is the same T3-assumed-universal error `DEC-HW-001` names throughout, reaching the kernel. **Safe on all three standing laws:** `served` is terminal so nothing downstream can reorder (law 1); no clock is read (law 2); no money is touched (law 3). **And it loses no timing fidelity**, because `03-F26` keys sample capture on ready-marks, which T1 still does not produce. Permissive-legality is not mandate: a T2/T3 branch has a pass screen emitting `ready`, and only the T1 producer ever walks the new edge. **`packages/domain` is a protected path (commandment 10) — this ships with senior review, and the mutation requirement is that a control differing in exactly one branch attributes the kills** | senior review of the `states.ts` amendment | W1 |

| DEC-SYNC-012 | The envelope has no shape for a CLOUD-ORIGINATED, ORG-SCOPED event | **accepted** | 01 §4 / 01-F18 / 01-F43..F46 / 01-F9 / 14-F3 | `EventEnvelope` requires `branch_id`, `branch_created_at` and `time_basis` on every event, because `01-F43`..`01-F46` make time branch-consensus and stamped at append by an originating DEVICE. A back-office catalog edit (`14-F3`'s `catalog.changed`) has **neither a branch nor a hub** — it is org-scoped, authored on the cloud plane, and `01-F18` already rules its ordering by `server_received_at` rather than by any branch clock. So the ORDERING half is settled and the ENVELOPE half is not: there is no legal shape for the event `14-F3` requires. Found August 2026 by B-3/B-4, which built the publish path against a `LedgerAppender` PORT and declined to mint an envelope rather than guess (`01-F43` "a fold applying its own offset would break law 1 silently" is the hazard being respected). **Not blocking today** — the adapter to `sync-gateway`'s `publishCatalog` is unbuilt, so nothing appends yet. Candidate shapes, none chosen: (a) a `server` value on `time_basis` with the server's own stamp in `branch_created_at`, which keeps one envelope but makes a branch field carry a non-branch value; (b) an org-scoped envelope variant, which is a kernel-wide change touching every reader; (c) `catalog.changed` lands in an org-scoped audit store that is not the branch ledger at all — note `01-F9` already treats org-scope reference data as a SEPARATE path from the branch stream, and the catalog artifact itself travels that way (T-C2/T-C3), so (c) may be the shape the corpus already implies. **RULED (August 2026) and promoted into `01-F62`: shape (c).** The `01 §4` catalog holds TWO SCOPES, and the test is WHO MAY LEGITIMATELY EMIT — not which doc declares the type. Org-scoped = cloud plane is the only legitimate emitter: `catalog.changed`, `device.registered/revoked`, `user.changed`, `config.changed`. Everything else stays branch-scoped INCLUDING `audit.*`, because `audit.login` is emitted by a device at a PIN unlock and `01-F5`'s chain is per-device — so the admin *family* does not split cleanly and the emitter does. An org-scoped event carries `org_id`, no `branch_id`, no branch stamp; its ordering authority is `server_received_at`, which `01-F18` already rules for catalog edits and which is trustworthy precisely because the cloud plane is the one place a clock is not a threat — the inverse of the device-clock threat model `01-F43` exists for. It never enters a branch stream and no device folds it. (a) was rejected because a `server` `time_basis` puts a non-branch value in a branch field; (b) because an org-scoped envelope variant touches every fold and reader for events no fold reads | promoted into `01-F62` | W1 |

**Rules:** promoting a decision = writing it into the owning doc and flipping status to accepted with the FR reference; a superseded row keeps its ID and points at its successor. CI (20 §2.1 static checks) will eventually lint FR references in this table against the specs.
