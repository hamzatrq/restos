# Decision Register

**Purpose:** cross-cutting decisions that span module boundaries live here until promoted into their owning document. A leaf module must not implement against a `proposed`/`blocked` decision. Format per row: stable ID · status (`proposed | accepted | blocked | superseded`) · authority (owning doc once promoted) · one-line decision · open dependency · target wave.

| ID | Topic | Status | Authority | Decision / current state | Open dependency | Target |
|---|---|---|---|---|---|---|
| DEC-MONEY-001 | Refunds & post-settlement corrections | **accepted** | 01-F29..F33, 02-F36 | Linked event pairs, manager-approved, conservation-checked | — | W1 |
| DEC-MONEY-005 | Money arithmetic safety in a float language | proposed | 00 §6 / 18 §4 | JS has no integer type — "integer paisas" are integers *stored in a double*. Today `paisa()` + Zod `int()` guard the boundaries and `sumPaisa` accumulates in BigInt, but (a) the fold engine deliberately uses PLAIN numbers, not branded types, and (b) no divide/multiply/rate helper exists anywhere — so the first tax, percentage-discount or split-bill code is where floats enter. Proposal: rates as integer **basis points** (1700 = 17%) so no float literal ever appears; mandatory `splitPaisa(total, n)` / `applyRateBps(amount, bps)` in `domain` with an explicit stated rounding policy and a property-proven parts-sum-to-total; a **lint rule** banning raw arithmetic on money (18 §4 says "banned by convention + review" — convention is not enforcement); folds switched to branded types. MUST land before doc 16 (tax), 17 (discounts) and 02 split-bill code. **T-01-13 LANDED (helpers + lint rule, review-clean): `splitPaisa`/`applyRateBps` in `domain`, GritQL ban on bare-identifier raw money arithmetic. The fold-brand clause is DEFERRED (T-01-15 rewrote the fold plane; re-state as a follow-up). Gate carried: the lint rule's compound-assignment arms (`+=`/`++`) land with T-01-13's fix round — accumulation is the idiom the ban exists for** | fold-brand follow-up | before tax/discount/split code |
| DEC-MONEY-006 | What a suppressed duplicate settlement leaves behind | **accepted** | 01-F31 / 01-F35 / 01-F29 | **The question:** `01-F31`'s `settlement_attempt_id` was declared on `payment.recorded` (`registry.ts`) and read by **no fold and no dedupe path**, so a double-tap or crash-retry bearing a NEW event id double-recorded. Closing it raised two questions the FR does not answer — (a) whether a SUPPRESSED event's id must still resolve as a refund parent (if not, a refund whose UI captured the losing id parks forever and the money never lands), and (b) whether a suppressed duplicate is an anomaly or routine (`01-F35` retains-and-flags for terminal-state monotonicity; `01-F31` is silent). **RESOLVED BY DESIGN (ratified with `DEC-PERF-001`):** under the `26` model nothing is "suppressed" — all members of an attempt key are retained in the cell (`event_ids`). (a) dissolves: refunds resolve parents by `settlement_attempt_id`, not envelope id (promoted into `01-F29`), so a refund captured against either envelope alias resolves identically. (b): divergent members raise the `01-F31` anomaly. Separately, `payment.refunded` carries **no** `settlement_attempt_id` at all though `01-F31` says every emission does — code-violates-spec, no decision needed. The `fix/01-f31-settlement-idempotency` branch is **superseded by T-01-15**; its seam/retention tests carry forward where compatible | — | W0 |
| DEC-MONEY-007 | Khata settlement vs repayment — a live double-count | **accepted** | 01-F30 / 01-F32 / 02-F12 / 02-F14 | **Not an ordering defect; a live money defect.** `02-F12` makes khata credit a payment *method*, and `02-F14` + `01-F32` make the later repayment **another `payment.recorded` on the same order**. Two genuine acts carry two `settlement_attempt_id`s, so any correct idempotency algebra rightly refuses to dedupe them — **the money doubles on the happy path, under full observation, converged identically on every device**. A khata regular paying their ₨1,850 tab makes every screen read "OVERPAID ₨1,850 — refund to close". `01-F30`'s receivable is meanwhile structurally always 0. The fold cannot discriminate: `payment.recorded.method` is `z.string()` (`registry.ts:50`) while `payment.refunded.method` **is** a closed enum (`:56`). Fix is a payload discriminator — `purpose: 'settles_order' \| 'repays_receivable'`, or a closed `method` enum with a declared non-tendering subset — plus restating `01-F30` over tendering payments only. **RATIFIED: the `purpose: 'settles_order' \| 'repays_receivable'` discriminator lands; 01-F30/F32 restated over tendering payments (promoted).** Khata settlement stays a `payment.recorded` | T-01-15 (schema + fold) | W0 |
| DEC-MONEY-008 | `settlement_attempt_id` uniqueness scope (unstated platform law) | **accepted** | 01-F31 | `01-F31` says the token is "UI-layer" and **never states its uniqueness scope**. The entire unique-keyed-sum money algebra (`26 §7`) rests on it. If any device mints a per-device counter rather than a UUID, two genuinely distinct payments collapse into one key and **cash vanishes silently, converged identically everywhere**. **RATIFIED and promoted into 01-F31**: org-globally-unique, UI-minted, UUID-class. **Enforcement posture ratified July 2026 (audit-1 #11 — it was trusted-not-enforced): DETECT AND ALARM, NEVER REJECT.** The gateway checks org-global uniqueness and raises a loud fleet-health + Auditor anomaly on collision, but still merges. Rejecting is the stronger algebraic guarantee and is refused deliberately — a device on a bad build would have real rung-up sales quarantined, breaking 01-F17. The failure being closed is that a colliding key makes cash vanish *silently and identically everywhere*; converting silence into an alarm is the entire win, since a loud wrong number gets fixed and a silent one does not | — | W0 |
| DEC-MONEY-009 | Two cashiers settle one order and the cash DOUBLES, silently, on every screen | **RULED (founder, August 2026): REFUSE the second settlement at the till.** Observed on a running system, not derived | 01-F30 / 01-F31 / 01-F33 / 01-F17 / 01-F1 / 26 §7 / DEC-MONEY-008 | **Measured 2026-08-10 in the first two-till run this product has ever had** (two real Electron tills, one gateway, one Postgres). Both cashiers keyed **Rs 2,240** against a Rs 2,240 order and pressed `TAKE CASH` together. Result: **two `payment.recorded` events, 224000 paisa each, two different `settlement_attempt_id`s; both tills' `shift_cash` fold reads Cash Rs 4,480; both screens read `DUE Rs 0`; nothing anywhere flagged it.** The shift then closes **Rs 2,240 short against a correct drawer**, both devices agree so there is no discrepancy for a human to notice, and `01-F1` makes it permanent. **The idempotency algebra is not broken — it is answering a different question.** `01-F31`'s `settlement_attempt_id` protects **double-taps and crash-retries**; two cashiers are **two genuine attempts**, so the unique-keyed sum (`26 §7`) correctly adds them. `DEC-MONEY-008` guards the *opposite* failure — a colliding key making cash *vanish*. Neither was ever aimed at concurrent human settlement, and no suite could see it because the gateway proves fan-out with two **synthetic** sessions inside one process. **`01-F30`'s conservation equation is the specified detector and it is open in THREE independent places, each verified by grep 2026-08-10:** (a) the check is gated on `order.settled === 1`, and **`order.settlement_closed` has no production emitter anywhere** — `apps/pos-electron/src/main/printing.ts:850` says so in its own comment (*"nothing in this product emits"* it); (b) even if it ran, the residual here is `2240 − 4480 = −2240`, i.e. **excess tender**, and `packages/domain/src/product-constants.ts:49` ships `EXCESS_TENDER_IS_EXCEPTION = false` — deliberately not flagged at v1; (c) **nothing schedules the Auditor** — `runAuditor` carries `@unreached-owed` and is imported only by a purity-test entry. Run **by hand against the very database that held the doubled payment, it returned `ok: true`, 0 findings.** Three detectors, three independent reasons for silence. **A CONTRIBUTING DEFECT, separately serious:** `apps/pos-electron/src/renderer/Counter.tsx` binds `current = orders[0]`, so **there is no way to start a second order** — two cashiers serving two customers ring into one bill, which is what made the double settlement trivial to trigger rather than exotic. **Why no fix was attempted, and why this is a ruling and not a task:** every remedy contradicts a standing law or flips a deliberately-set constant. *Refusing the second settlement* breaks `01-F17`/commandment 4 — a sale is never blocked, and a till that refuses tender because another device is mid-settle is a till that stops working when the network hiccups. *Flagging excess tender* flips `EXCESS_TENDER_IS_EXCEPTION`, which is open on purpose (overpayment is ordinary at a counter — a customer hands Rs 500 for a Rs 460 bill). *Emitting `order.settlement_closed`* is the cleanest and touches `01-F33`'s closing act, which no session may define alone. **Detect-and-alarm is the shape `DEC-MONEY-008` already chose for the mirror-image failure** (*"a loud wrong number gets fixed and a silent one does not"*), which is the strongest precedent in the corpus for how this should resolve — but it is the founder's call, and the honest summary is that **RestOS today loses money quietly whenever two cashiers settle the same bill at once.** **RULED (founder, August 2026): refuse the second settlement at the till.** The `01-F17` objection was put to the founder in those terms and the ruling stands, so it is implemented — and the implementation that makes it coherent with commandment 4 is narrow and load-bearing: **the refusal is a LOCAL decision against the device's own converged fold, never a network round-trip.** A till that must ask the cloud whether an order is settled is a till that stops selling when the WAN drops, which is exactly the break `01-F17` forbids; a till that reads its own state and declines to settle an order it already knows is settled is refusing a **duplicate**, not blocking a sale. That distinction is the whole difference between an implementable ruling and a commandment-4 violation, and any implementation that reaches the network to answer it has got it wrong. **What refusal CANNOT close, stated because the ruling does not make it go away:** two tills partitioned from each other have not converged, so neither can know, and both will accept — the doubling survives in exactly the case where nothing local can see it. Refusal therefore closes the **common** case (both tills online, which is when two cashiers actually race) and leaves the partition case open by construction. **So the detection work is not superseded by this ruling and stays owed:** an emitter for `01-F33`'s closing act, a scheduled Auditor, and a decision on `EXCESS_TENDER_IS_EXCEPTION` — without them the partition residual is still silent, still permanent under `01-F1`, and still agreed-upon by every screen. The contributing defect (`Counter.tsx` binding `current = orders[0]`, so two cashiers serving two customers ring into one bill) is a plain bug and is fixed without a ruling | the partition residual: `01-F33` closing-act emitter + scheduled Auditor + `EXCESS_TENDER_IS_EXCEPTION` decision | W1 — blocks a dev-pilot with two tills |
| DEC-MONEY-010 | `01-F30`'s three missing conservation terms — `void_value`, `comp_value`, `discounts` | **RULED (founder, August 2026): NOT YET — and the ruling is a GATE, not a deferral.** The terms stay ABSENT; a rule for admitting them is stated so the next session is not starting from prose | 01-F30 / 01-F31 / 01-F33 / 26 §7 / DEC-MONEY-008 / DEC-MONEY-009 | **The question, restated so it can be argued with:** `01-F30` conserves `Σ tendering payments − Σ refunds = billed_total − void_value − comp_value − discounts` once settled, and its one executable form (`settledConservationResidualPaisa`, `packages/domain/src/invariants.ts`) has **no parameter for any of the three**. A legitimately comped order therefore reads as a conservation SHORTFALL. `plans/wave-1/f30-conservation-terms-options.md` is the worked options paper and its refusal is upheld. **The decisive measurement (2026-08-12): all four escalatable types have ZERO PRODUCTION EMITTERS.** `void.recorded`, `comp.recorded`, `discount.recorded` and `order.line_price_overridden` each carry a payload schema (`packages/domain/src/registry.ts`), an authorization row and a fold arm, and **nothing in `apps/` or `services/` constructs one** — `main/approval-record.ts` and `main/authorize.ts` name them only in lookup tables keyed by an incoming request, and `02-F20`'s void/comp/price-override surfaces do not exist. So a term added today would be computed from a set that is empty **for a second, independent reason**, and it would be this wave's named recurring defect committed on purpose — with `pnpm seams:check` structurally blind to it, since a missing PRODUCER for an event type is one of the three shapes that rail cannot see. **THE GATE (this is the ruling, and it is testable):** a term enters `01-F30`'s executable form when, and only when, its event type has **(i)** at least one production emitter, **(ii)** an `01-F31`-class idempotency key in its payload — org-globally unique, UI-minted, UUID-class per `DEC-MONEY-008` — and **(iii)** an oracle-pinned merge rule in `26 §7`. Until all three, the term is **ABSENT — never defaulted to zero behind an optional parameter**, because an optional zero term is a term with no producer wearing a signature that says it has one. **Why (ii) is not optional and is the reason this cannot be an implementer's call:** `01-F31`'s mechanism IS a key (*"folds dedupe by attempt key … a fold never picks a winner"*), and both Σ-over-members and Σ-over-event-ids make a double-tapped "void Rs 500" subtract Rs 1,000 — the failure `01-F31` exists to prevent, reintroduced one family over. `DEC-MONEY-008` had to ratify the *uniqueness scope* of the key that already exists, so a second key minted without that ruling is the same failure a second time. **RECOMMENDED SHAPE when the gate opens, recorded so the next session inherits an argument rather than a blank page — recommendation, NOT ratification:** (1) mint the key on all four types at once, under `01-F31`'s existing law and named for the act (`adjustment_attempt_id`), UI-minted at `02-F20`'s approval path — **since RULED and carried into `01-F83`** (founder ruling R56, August 2026), which corrects the four-member list by measurement; (2) resolve the double-count of §3 — a void expressed BOTH as line exits (already netted out of `billed_effective`) and as an order-keyed `void.recorded { amount_paisa }` subtracts the same money twice, permanently and converged — by making the two representations **disjoint at the emitter and checkable at the fold**, with the LINE authoritative wherever a line exists (`01 §4`'s `voided / cancelled` exit states are the only vocabulary any module may use, and `billedCellPaisa` already honours them), so `void.recorded` carries the approval facts and contributes value only for what no line exit removed; (3) keep the terms in the **residual and the Auditor**, not in a device fold — `26 §7`'s own *"looks like ordering, actually needs"* table maps `01-F30` to a **closure** mechanism and conservation is absent from its four-entry ordering list. **WHAT UNBLOCKS IT, and it is now one item rather than three:** `01-F63` (August 2026) gives `01-F33`'s closing act a production emitter, so `01-F30`'s equation — gated on `settled` — **runs on a real order for the first time**. That is the `O2` ordering the options paper recommended and `DEC-MONEY-009` implied: make the check execute, then add terms to a check whose output can be observed to change. Adding them first would have been unobservable in both directions | (i) an emitter for the four escalatable types (`02-F20`'s void/comp/price-override surfaces); (ii) **RULED — `01-F83`**; (iii) a `26 §7` merge rule. The `01-F30`-runs-at-all prerequisite is CLOSED by `01-F63` | W1/W2 |
| DEC-MONEY-002 | Split-payment tax apportionment | proposed | 16 §9.1 | Apportion by payment share per method (provisional) | Authority guidance per adapter | Tax enablement |
| DEC-MONEY-003 | Khata fiscalization timing | proposed | 16-F16 / §9.6 | Fiscalize at receipt issuance, rate per rule-pack credit mapping | Legal verify per adapter | Tax enablement |
| DEC-MONEY-004 | Tips (capture, pooling, payout) | **accepted** | 01-F30/01-F31 / 02 / 11 | **RATIFIED (founder, July 2026): FULL tips — capture, pooling AND payout.** Previously unmodelled, so cash tips were invisible to the system and surfaced as unexplained till variance at shift close. **The modelling collision, and the shape that resolves it.** A tip is money TENDERED that is not part of `billed_total`, so recording it as an ordinary payment breaks `01-F30`'s conservation equation on the happy path — Rs 1,100 taken against a Rs 1,000 bill would read as a Rs 100 overpayment demanding a refund to close. This is exactly the failure DEC-MONEY-007 found for khata repayments, and it takes the same fix: **extend `01-F31`'s closed `purpose` discriminator with `tip`**, excluded from the `settles_order` sum, so conservation holds untouched and the till reconciles against cash actually in the drawer. **Pooling and payout need NEW EVENTS that do not exist in the `01 §4` catalog** — a captured tip is a LIABILITY to staff until paid, so it must be trackable from capture through pooling to payout, and `staff.*` today has only `clocked_in/clocked_out/advance_recorded/advance_repaid`. Candidate additions (`tip.pooled`, `tip.paid_out`) are NAMED HERE ONLY; per commandment 2 they enter the catalog by spec PR to `01 §4` + doc 11 before any code, not by implementation. **Scope note (founder-visible):** this is the largest money-model addition since the kernel and it reaches payroll (doc 11), so it is deliberately NOT a Wave-1 POS-only change. `DEC-MONEY-005`'s helpers must be in place first (they are). Blocks nothing today; the POS payment screen must not ship a tip field until the events exist | spec PR to 01 §4 + 11 | W1/W2 |
| DEC-SYNC-001 | Convergence contract | **accepted** | 01-F34..F38 | Commutative folds + global-seq tiebreak; quarantine model | — | W0 |
| DEC-SYNC-002 | Device classes & slices | **accepted** | 01-F39..F42 | ~~Six~~ classes (counter split by host runtime: electron/rn); server/hub-enforced predicates. ⚠ **The COUNT is not stated here and this row said "Six" through the amendment that made it seven** — `01-F39` gained `storefront_cloud` in August 2026 for `06-F30` and this row did not move, while `packages/sync-protocol`'s oracle went on asserting *exactly six* and failed the correct implementation. **`01-F39` is the census; read it there.** The decision itself is unchanged: classes carry fixed slice + hub rules and the predicates are enforced server/hub-side, never client-declared (`01-F40`) | — | W0 |
| DEC-PERF-001 | Ordering authority: cloud-assigned `global_seq` vs stable append-time order | **accepted** | 25 (full analysis) / 26 (selected design) | **RATIFIED (founder, July 2026): per-fold MERGE SEMANTICS is the selected design — the question itself was wrong.** Deterministic folds need deterministic *merge* semantics — commutative ops, monotonic state, explicit concurrency — **not a universal total order**; `01-F34` already said so while the comparator did the opposite, letting sync metadata decide business outcomes (cloud arrival order picking the winning table assignment). Promoted into `01-F34` (rewritten), `01-F29/F30/F31/F32/F33` and the `01 §4` catalog (`order.settlement_closed`); the `26 §7` schema additions, the measured `04-F17` privacy trade (union routing, business-day-bounded) and hub-as-business-emitter are ratified with it. Design in `26`; matrix in `plans/wave-0/merge-semantics-matrix.md` (summary `26 §7` — four entries still need an ordering mechanism, only one of them in a device fold); prototypes green in `26 §8`. **WHAT BINDS CODE:** `global_seq` is a delivery/catch-up cursor and compaction watermark with **zero fold work** — never an ordering input; folds read no ordering metadata at all (`01-F34`); `lamport_seq` stays gap-free for `01-F8` watermarks and the Auditor gap check; **`01-F18` is unaffected** (cloud-plane back-office edits, server time trustworthy). **Why cloud-assigned ordering was refused:** adopting a cloud sequence retroactively reorders events and forces a full re-fold — **measured O(N²)** on the live sync path (~6.6 ms/event at N=1600 on a laptop; minutes of CPU on a 2 GB tablet at a realistic rolling window), and general IVM engines (DBSP/Materialize) do not solve it (they are order-agnostic; DBSP's degenerate case is ours). Analysis in `25 §7–§16`. **Why a clock-free causal order was ALSO refused — `causal_seq` is REFUTED, do not re-propose:** a 74-scenario adversarial review (`plans/wave-0/fold-scenario-catalog.md`, `25 §13`) returned P0s — causality is false under partial observation, `causal_seq` is forgeable and one `MAX_SAFE_INTEGER` peer event bricks `append` branch-wide, no sound backfill exists — and the `25 §17` ablation left the cloud-reordering profile quadratic. HLC was rejected before it for the reason everything here inherits: the founder's clock threat model allows **every device to be arbitrarily wrong in either direction**, which removes HLC's premise while keeping its hazard. **T-01-14 paused; `causal_seq` is not to be implemented.** Implementing task: **T-01-15** | — | W0 |
| DEC-TIME-001 | Device time: what is trustworthy, and what depends on it | **accepted** | 01-F43..F46 / 01-N2 / 25 §14 / 03 | Ordering must not depend on clocks (`DEC-PERF-001`) — but *time itself* still must be answered. The defect it exists to close: the fold plane (`sync-client/src/folds/merge.ts`) stamps `confirmed_at`/`kot_at`/`age_basis` from raw `device_created_at`, so a wrong device clock writes wrong values straight into the timing read models doc 03 consumes. **RATIFIED by the founder (July 2026) as proposed, promoted into `01-F43..F46` and `01-N2`:** (a) **durations need a consistent clock, not a correct one** — a uniform offset cancels in a difference, so kitchen age/ETA are safe on branch-consensus time (`01-F43`); (b) the elected hub (`01-F13`) is the branch time authority, and since `DEC-SYNC-009` makes the hub the WAN uplink it is normally the device that HAS internet, so branch time is genuinely correct in the common case and merely self-consistent when fully offline (`01-F43`); (c) absolute business stamps (tax 16, audit `01-F5`, day boundary) use `server_received_at` when available, else branch time **marked provisional** via an explicit `time_basis` marker and reconciled at contact — `16-N3` already assumes this (`01-F44`); (d) `device_created_at` demoted to untrusted display/forensic hint, banned from folds and read models alongside the `01-F34` ordering-metadata ban (`01-F45`); (e) `01-N2` detection un-deferred into Wave 0 (`conformance/wave-0-scope.yml`); (f) business-day boundary anchored to Asia/Karachi regardless of cloud region (`01-F46`). Needed under ANY ordering choice. **Two amendments from the T-01-17 oracle pass (July 2026), corrections to the first draft's FR text rather than to the decision:** (i) the envelope `time_basis` marker is `'branch' \| 'branch_provisional'` — the original `'server' \| 'branch_provisional'` was incoherent because a device cannot know server time at append; `'server'` belongs to the DERIVED business stamp, and `01-F44` now separates the two explicitly; (ii) `01-F46` never stated when a day *starts*, which silently meant midnight — **ratified: a configurable cutover hour, default 05:00** (layer-2 org setting), so a 01:30 sale banks to the night it was served rather than splitting a late-closing restaurant's takings across two days' reports. Also scoped: `01-N2`'s direct device↔cloud skew leg is **explicitly deferred** (no protocol field carries a server clock at hello time); the hub leg ships. Implementing task: **T-01-17** | — | W0 |
| DEC-AUTH-001 | Device token lifetime and binding | **accepted** | 01-F47 | Shipped tokens carry **no `exp` and no `iss`/`aud` binding** (senior review `audit-1.md` #9), so a token survives forever until someone explicitly revokes it and a token minted for one deployment validates against another. Short TTLs are the reflexive answer and are **wrong here**: a device offline for a day would stop working, violating 01-F17 / 00 §5.1. **RATIFIED (founder, July 2026):** mandatory `exp` with a 90-day default; `iss`/`aud` binding; silent renewal on any authenticated cloud contact **including hub-relayed** (01-F13/DEC-SYNC-009) so a LAN-only device renews without ever holding WAN; host warns below 25% remaining life; expiry withdraws cloud/LAN **admission only** and never blocks a local sale; an expired-but-unrevoked device is admitted solely to drain its backlog and take a renewal; expired **and** revoked is purged (01-F42). Revocation remains the operative kill switch — expiry is the backstop against indefinitely-valid credentials. Implementing task: **T-01-18** | — | W0 |
| DEC-AUTH-002 | Revocation eviction latency | **accepted** | 01-F48 | Revocation previously took effect only at a device's next *voluntary* contact — a revoked tablet holding a live session kept participating indefinitely (senior review `audit-1.md` #1 residual / F3-eager). The read half is already fixed (`9a0c1ff`: revoked peers culled from fan-out, fail-closed) but no latency bound was ever stated. **RATIFIED:** eviction within 30 s wherever any path reaches the device — the cloud drops live sessions and culls fan-out on the revoking transaction, the hub does the same on LAN; fail-closed (unreadable revocation state refuses participation rather than granting it); revocation blocks **reads as well as writes**. Implementing task: **T-01-18** | — | W0 |
| DEC-SYNC-010 | Compressed wire framing | **accepted** | PROTOCOL.md / 01 §8 / 26 §6.4 | `01 §8` always said "JSON + zstd batch compression"; T-01-16 landed the codec (`node:zlib` zstd, no new dependency per `18 §15` rule 1) — `zstd(utf8(JSON))`, transparent and deep-equal to the plain codec for every message kind, carrying a `ZSTD_c_checksumFlag` content checksum so a corrupted frame fails loudly instead of decompressing into a schema-valid but *wrong* message. What was never decided or wired is **which connections use it**. **RATIFIED:** compression is negotiated per connection at `hello`/`hello_ack`, opt-in from both ends, and applies to the catch-up path where the `26 §6.4` transfer budget lives; a peer that does not advertise it receives plain JSON forever, so the negotiation can never strand an older device. **Scope amended July 2026 (T-01-19 oracle round): the CLOUD connection only — the LAN mesh never advertises.** The budget this exists for is the cloud catch-up transfer (`26 §6.4`, <60 s on 4G); LAN propagation is shop-grade Wi-Fi at <1 s p95 (`01-F15`), so compressing it buys nothing worth a wire change — and structurally the LAN transport wraps every message in a JSON envelope whose discriminant lives *inside* the JSON, so a compressed LAN payload would need either a spec-reviewed binary frame variant or base64 inside the wrapper (which throws away most of the gain): speculative work against a budget under no pressure (`24 §3b`). If a LAN transfer budget ever binds, that is its own spec change. Implementing task: **T-01-19** | — | W0 |
| DEC-SYNC-011 | Catch-up blocked on a permanently-rejected event (version skew) | **accepted** | 01-F9 / 01-F11 / 01-F37 | Catch-up applies a **contiguous prefix**; an event the device can never accept — unknown event type, payload from a newer schema version — is not divergence and not quarantinable at the device, so the cursor simply stops. It used to wedge **silently**, with no reason reported and the honesty UI (`00 §5.7`, `01-F11`) showing a device that looks merely idle while it is permanently stuck (senior review `audit-1.md` #4). **RATIFIED:** (a) a blocked cursor is **observable** — **`CloudSessionStatus.blocked`** carries the blocking `global_seq`, the rejected event type, and a machine-readable reason (the cursor belongs to the **cloud session**, not the device store's `SyncStatus`; a LAN-only device under `DEC-SYNC-009` has none at all), surfaced to fleet health (doc 15) and the honesty UI; (b) forward-skew policy is **stop-and-report, never skip** — silently skipping an unparseable event would fabricate a gap in a log whose whole value is completeness, and skipping is unrecoverable while stopping is not; (c) the device keeps operating locally on what it already holds (`01-F17`). Implementing task: **T-01-20** | — | W0 |
| DEC-SYNC-003 | Event archive tier at scale | proposed | 01 §9.4, 22 | S3 parquet archive vs keep-hot | Year-2 volume data | Post-launch |
| DEC-SYNC-004 | Cloud uplink topology (hub-proxied vs per-device) | **superseded** → DEC-SYNC-009 | 01-F13 / HUB-ELECTION.md | Was: per-device cloud sessions only; hub-proxied uplink deferred "until a constrained-WAN branch needs it". Superseded because that trigger is the NORM in the target market, not an edge case — see DEC-SYNC-009. Code and tests citing this row implement the superseded rule until the DEC-SYNC-009 task lands | — | W0 |
| DEC-SYNC-009 | Hub-proxied cloud uplink for WAN-less devices | **accepted** | 01-F13 / PROTOCOL.md | Founder call (from the kernel review): hub-proxied uplink is REQUIRED, not deferred. Per-device cloud sessions remain the default wherever a device has WAN; **additionally** the branch hub relays WAN-less devices' events upward on their behalf. Trigger: the common deployment gives internet only to the counter terminal (cost + keeping waiters off the internet), so waiter/kitchen devices are LAN-only *by design* — under the superseded rule their events reached the counter over LAN but NEVER reached the cloud, silently truncating the cloud log (reporting, analytics, multi-branch roll-up, backup/DR all quietly wrong). PROTOCOL.md already assumed this ("hub relays `push` upward"); the contradiction was with the `push` row's "own events only". Mechanism (additive under `v:1`): a hub session may push events whose `device_id` differs from the session's, authenticated as branch hub; the gateway tracks lamport contiguity per ORIGIN device, not per session; relayed events are attested by the hub, never re-authored (01-F1) | — (T-01-09 landed both: relay = token `hub_relay` claim ∧ unrevoked hub-eligible registry row, and the relayed-ORIGIN registry check at the merge boundary — `origin_unregistered`/`origin_revoked` quarantine classes; the fix-round-F6 hole is closed) | W0/W1 |
| DEC-SYNC-005 | Cloud ack watermark advances over quarantined events | **accepted** | 01-F37 | Ratified: a durably-stored quarantine row fills its lamport slot, so the push ack advances past a poison event and the outbox never wedges (01-F17 spirit). 01-F37 carries a clarifying sentence; the Auditor counts quarantine rows as slot-filling (T-01-11) | — | W0 |
| DEC-SYNC-006 | LAN heal set-union when the origin departed | **accepted** | 01-F38 / HUB-ELECTION.md | Founder call: accept the cloud-catchup dependency for the narrow edge (origin departs mid-partition, WAN down) — documented in HUB-ELECTION.md; follower third-party relay deferred. T-01-06 X7 demonstrates the cloud path closes it | — | W0 |
| DEC-SYNC-007 | Invariant-validation split (gateway vs Auditor) | **accepted** | 01-F29/F30/F37 / T-01-08 | Gateway enforces only fold-FREE provable invariants inline at merge (refund ≤ un-refunded remainder, 01-F29, via targeted event reads) and quarantines violators (new `invariant_violation` reason); fold-requiring settle-time conservation (01-F30 full equation) is the Auditor's refold job. **Divergent-parent tolerance ratified July 2026 (audit-1 #12):** the two planes had two different tie policies — the gateway resolved a divergent parent attempt key by `order by global_seq asc limit 1` (first-merged wins) while the engine marks a divergent key DISPUTED and contributes zero (01-F31, "a fold never picks a winner"). **The gateway stops picking:** a disputed parent makes the cap UNPROVABLE, so the refund passes through with the anomaly flagged, which is what this row's own "fold-FREE provable invariants" scope already implied. The first-merged read let *delivery order decide a money outcome* — the exact failure `26 §2` exists to remove — and two clouds replaying the same events in different orders could reach different verdicts on one refund (T-01-11), not the merge path. A refund whose parent isn't merged yet passes through (01-F17) | Founder ratify | W0 |
| DEC-SYNC-008 | Quarantine-notice delivery guarantee | **accepted** | 01-F37 / PROTOCOL.md | At-least-once now: a durable `kernel.quarantine_notices` outbox keyed by origin device, live-sent + redelivered on next `hello` (mark-on-send). Exactly-once (a `quarantine_notice_ack` wire kind — a PROTOCOL.md addition) deferred | Founder ratify | W0 |
| DEC-AUDIT-001 | Audit hash-chain rule (01-F5) | **accepted** | 01-F5 / 18 §14 | `prev_audit_hash` in the event PAYLOAD (the envelope `z.object` strips unknown keys, so an envelope field would be silently dropped); SHA-256-hex over canonical-JSON of the envelope MINUS `server_received_at` (device-`null` and cloud-stamped must hash identically); `@noble/hashes` (pure-JS, sync, RN-portable — `node:crypto` unusable in RN, Web Crypto is async so unusable in the sync append path) — a NEW 18 §14 allowlist dependency | Founder ratify (dependency + scheme) | W0 |
| DEC-TRAIN-001 | Training mode: where rehearsal events live | **accepted** | 01-F49..F51 / 27-F52 / 27-F63 / 02 / 22 | `27-F52` makes a training mode a product requirement needing *"an architectural answer, not a UI toggle"*, and nothing in doc 01 or 02 answered it (inventory gap **G1**; `21-F11` RITE rounds and `27-F53`'s speed test have nowhere to run without it). Without one, staff either train on live tickets — polluting an append-only ledger and every report built on it — or they do not train at all. **RATIFIED as filed (founder, July 2026): a training session is an ordinary session against a training BRANCH; the kernel gains nothing.** A branch carries `class: production \| training`; branch-scoped credentials (`01-F9`), fan-out (`01-F13`) and reporting already isolate it, so the boundary is enforced once at admission rather than in every query. **Rejected: an `is_training` flag on the envelope** — it makes correctness depend on every fold, report, export and analytics query remembering to filter, one forgotten filter is invisible in review, and `01-F1` then makes the pollution permanent because it cannot be deleted. **Rejected: an ephemeral scratch store** — the things most needing rehearsal (shift close, cash count, day close) span a shift, so training that evaporates cannot teach them. Entering/leaving is an attributed credential swap with the production session draining first (`01-F50`), never a toggle, so a trainee cannot reach production data mid-flow. A training branch is **droppable wholesale** (`01-F51`) because it is by construction not history — which is both the retention answer (22) and what makes repeated RITE rounds affordable — while deletion of individual events *inside* it stays forbidden, or the rehearsal teaches the wrong habit. Catalog mirrors read-only from production so staff train on the real menu. UI obligation is `27-F63`: a persistent band and surface tint, achromatic (the `27-F14` colour budget has no training slot). **Governance settled: the layer-2 BACK OFFICE creates a training branch, and it is NOT billable** — charging for training suppresses training, and untrained staff on live tickets is the exact failure the feature exists to prevent | — | W1 |
| DEC-AUDIT-002 | `audit.*` channel modeling | proposed | — (01/02 candidates) | Dedicated tamper-evident `audit.*` events vs pairing with operational events (does `cash.drawer_opened` also emit `audit.drawer_opened`?). Recommend DEFER to the POS wave to avoid double-modeling drift; T-01-10 ships only the `audit.*` emitter mechanism | Founder product call | W2 |
| DEC-TEST-001 | Wave-0 conformance gate scope (24-F5 vs multi-wave kernel) | **accepted** | 24-F5 / conformance/wave-0-scope.yml | Founder call: a declared Wave-0 FR subset (`conformance/wave-0-scope.yml`) — the gate enforces every in-scope FR mapped, out-of-scope FRs carry an explicit deferral reason. `verify:01 --gate` wiring to read the subset is a scheduled harness follow-up | Gate-wiring | W0 |
| DEC-TEST-002 | Where 01 §8 exit criteria "run" (sim vs containerized vs rig) | **accepted** | 01 §8 / T-01-06 | Founder call: the deterministic sim leg AND the real-process smoke leg (real WebSocket + real SIGKILL-mid-print, in-process Testcontainers) are BOTH binding pass/fail gates; only physical-rig wall-clock p95 stays D3 (post-spike). The 19 §6 Ditto ramp reads pass/fail off the combined sim+smoke gate | — | W0 |
| DEC-TEST-003 | Auditor v1 scope ("ships in Wave 0" vs missing prerequisites) | **accepted** | 20 §4.2 | Ratified: Auditor v1 = refold-vs-readmodel diff + lamport gap-free (counting quarantine slots per DEC-SYNC-005) + money conservation (01-F30) + state-machine legality. Device-state diff and audit-chain verification follow their producers; the 01-F5 hash-chain emitter is scheduled (T-01-10) since the chain check depends on it | 01-F5 emitter (T-01-10) | W0 |
| DEC-ORDER-001 | Pre-orders / scheduled orders (`fire_at`) | proposed | — (01/03/06 candidates) | Unmodeled; storefront "order for 8pm" not supported | Founder product call | W2 |
| DEC-SUPPLY-001 | Purchase-order events (auto-PO from suggestions) | proposed | — (10/13 R3) | `action.proposed` draft-PO exists in ladder; PO entity undefined | Design with 10 forecasting maturity | W4 |
| DEC-PEOPLE-001 | Shared rider phones (one device, several riders) | proposed | 09 §9 | PIN-switch on one device vs disallow | Field reality at pilots | W2 |
| DEC-CUST-001 | Customer merge conflict UX (multi-branch same-phone) | proposed | 01 §9.3 | Kernel merges; who resolves name/address conflicts, where | UX design | W2 |
| DEC-DATA-001 | PII erasure mechanism (final) | proposed | 22 (crypto-shredding default) | Envelope-encrypted PII fields, key destruction on erasure | Legal verify + build spike | Pre-pilot |
| DEC-TAX-001 | Is a pre-payment BILL legal under Sindh integration? | **closed — founder risk acceptance** | 16 / 02 / 03 | **Sindh rule 6(3): an integrated person "shall issue only the invoice specified in sub-rule (1)… and shall not issue any *katcha* or provisional invoice, by whatever name called, to the customer."** Handing the customer a bill before payment is the NORMAL Pakistani dine-in flow, and the plain reading of 6(3) prohibits it for an SRB-integrated restaurant — so on that reading the standard dine-in sequence would have to change under Sindh integration (bill becomes a screen-shown total, or the fiscal invoice is issued before payment). **Not an engineering call: it needs a Pakistani tax lawyer. FOUNDER RULING (July 2026): proceed as designed — the pre-payment bill ships, with NO Sindh special-casing.** Recorded as an explicit risk acceptance rather than an unexamined default: the concern above was raised, considered and declined by the founder, who owns the commercial and legal exposure. The bill is a first-class document type in `03-F31` and is not blocked. **Revisit trigger:** any SRB enforcement notice, or advice from counsel if engaged later | legal verification | before any Sindh customer |
| DEC-TAX-002 | Vendor liability for invoicing software in Sindh | **accepted** | 16 / commercial | **Sindh Finance Act 2026 appears to insert s.43 entry 2AA:** a penalty up to **Rs 1,000,000 (minimum Rs 100,000)** on *"any person who designs, develops, customizes or supplies invoicing software"* enabling non-conforming invoices — i.e. **on US, not the restaurant** — and rule 8 requires a PoS vendor to have its place of business in Sindh. **Not indemnifiable by a contract term with the customer.** **UNVERIFIED:** SRB publishes the Act as an image scan with no text layer; read by OCR, single-source. Compounding context from the same research: Pakistan provides no certified rendering device (rule 150ZEB — the SDC may be *software attached to the POS*), so RestOS **is** the trusted renderer, and rule 150ZEO(4) seals premises after three unverified invoices in a day. So a rendering defect is legally indistinguishable from evasion, and the liability may land on the vendor. This is the evidential basis for the slot-not-template customisation model. **Founder + lawyer, before any Sindh customer is signed** **RATIFIED (founder, July 2026): a contractual liability cap PLUS a per-region fiscal conformance suite that must pass before any region goes live.** The suite is the substantive half — it converts an open legal exposure into a testable gate, and `03-F34` already builds most of it (mandatory adapter blocks present, QR physical size meets the adapter minimum at the target dpi, no owner slot rendered inside a locked region, hard print refusal plus an S1 band on failure). `03-F35` (always rasterise the fiscal QR, never the native ESC/POS command, because cheap printers fail silently) and `03-F36` (every spec renders at 32 columns) are conformance rows too. A cap alone would not be defensible when we are the trusted renderer and the merchant's exposure is *premises sealed* rather than a refund. | legal verification | before any Sindh customer |
| DEC-DATA-002 | Pakistan data-residency posture | proposed | 22 | Region near PK; residency law unverified | Legal verify | Pre-pilot |
| DEC-CHAN-001 | Customer-chat bilingual (roman-Urdu) output | **blocked** (eval gate) | 07-F23 | English replies until native-speaker eval bar passes | Eval corpus + model quality | Post-launch |
| DEC-CHAN-002 | Instagram DM automation | proposed | 07 / concept §4.2 | Link-in-bio only; DM bot when API allows | Meta API capability | W4+ |
| DEC-UI-001 | Is a `dp` a physical size or a CSS pixel? | **accepted — RATIFIED** | 27-F68 / 27-F11c / 27-F8 / 00 §7 | **The question.** `packages/ui`'s `targetFor("keypad")` returns `126` and every app spent it as **126 CSS pixels**. That identity — dp ≡ css px — is true only at 160 PPI and is stated **nowhere** in doc 21 or doc 27; `27 §1a`'s own hardware table contradicts it (the counter POS is 15.6″ at **100–141 PPI**, where a 76 dp tile is **47–67 px** and a 126 dp keypad key is **79–111 px**), and `27-F11c` already stated the rule in words — *"design in millimetres, render in pixels."* An implementation that never read its own spec, not a gap in the spec. **Measured (August 2026, real `BrowserWindow`, 1366×768 at 100.5 PPI; `apps/pos-electron/CLAUDE.md`).** The keypad is 4 × 126 + 3 gaps = **528 px** against a content box of **498 px** under `03-F5`'s alarm band — *the pad alone does not fit*, before any label, DUE figure or TAKE CASH button. The whole budget in one line: **51 + 85 + 102 + 528 = 766 in a 768 px panel** (status strip, tab rail, band, pad), leaving 2 px for all padding everywhere. At `27 §1a`'s own 79 px the pad is **340 px** and the surface fits with room. **RULED (founder, August 2026): option A — implement the physical conversion.** A dp is a **physical size**: 1 dp = 1/160 inch = 0.15875 mm, so 126 dp is 20 mm and **20 mm is what must reach the glass**, rendered through the panel's actual pixel density per `27-F11c`. The renderer therefore takes the panel's density as an input — `00 §7` layer 3, `panel_ppi`, **measured from the display by default** and configured only to correct a display that reports nothing or reports wrong. Promoted into **`27-F68`**; `27-F11c` needed no amendment because it already said this. **Two traps the FR text forbids explicitly, because each is the obvious next error.** (1) **A hardcoded 79 px is WRONG.** 79 px is the answer for *one* panel: on the 141-PPI 1920×1080 panel `27 §1a` also lists it is **14.2 mm**, 29% below `27-F8`'s floor, on the highest-consequence entry surface in the product. The conversion is computed from the panel's real density, never pinned to one panel's answer. (2) **The floor is in millimetres, not pixels.** `27-F8`'s 126 dp is a measured ergonomic minimum; this ruling changes how it is *rendered* and never what it *is*. Trimming the millimetres to make a layout fit is the `01-F61` cost-floor move one domain over. **Rejected, each on a resolving FR rather than on preference.** *"The counter ships 1920×1080 only"* — **exactly the category error `27-F11c` names**: it answers a physical-ergonomics question by constraining a pixel count, re-breaks silently on the next panel, and converts a design law into a hardware purchasing constraint nobody enforces at a counter. *Overlay the band instead of displacing the work area* — it relabels the loss: the band covers DUE, the `02-F12` method row and `1` `2` `3` instead of the bottom of `C` `0` `⌫`, `elementFromPoint` then reports COVERED, and `27-F11d`'s *"the work underneath stays visible and usable"* breaks either way. *Scroll or page the work area* — `27-F2`, and the keys are not a list. *Hide the pad behind a SETTLE button* — `27-F5`. *Shrink the keys* — `27-F8`, see trap 2. **Consequences.** (a) **Every touch target in the product changes** — shrinking on the 1366×768 counter, and *growing* on anything above 160 PPI: `27 §1a`'s ~224-PPI waiter tablet and ~405-PPI phone rows are 1.4× and 2.5× today's rendered size. This is not a POS-only edit. (b) **Apply the conversion ONCE at the token boundary, to every dp in the layout** — a session that converts `targetFor()` and leaves the status strip, tab rail and band in raw CSS px ships a layout whose proportions are wrong on every panel, which is the same defect in a new place. (c) `27-F2` / `27-F11a` page capacity is **unaffected** — it already computes in millimetres, which is the half of `27-F11c` that was implemented. (d) **It closes defect 4 and is necessary-but-not-sufficient for defect 5:** at 100.5 PPI the conversion scales `ManagerApproval`'s 1162 px to ≈**730 px** against a content box of at most ≈662 px quiet, so that surface still overruns and additionally needs the `27-F4` positional change its own register entry names — three trailing full-size buttons cannot all be keypad-posture under a keypad in a 768 px panel, and `05-F19`'s over-threshold paid-out is the live case that reaches that surface. (e) The layout gate measures **one** panel at devicePixelRatio 1; `27 §1a`'s second counter panel must enter its fixture with this work, or the ruling ships untested on precisely the case that produced trap 1 | implementing session (renderer density input, second panel in the layout-gate fixture) | W1 |
| DEC-HW-001 | Bring-your-own-hardware: the product adapts to the device, the device is not specified to the customer | **accepted — RATIFIED (premise)**, sub-question (1) ANSWERED, (2) answered by `DEC-HW-003` | restaurant-os.md:47 / 00 §7 / 02-F31 / 02-F40 / 03-F22 / 03-F24 / 27-F11c / 27-F11e / 27-F68 | **RULED (founder, August 2026):** *"the system should adapt to the device it runs on and not assume that everyone will have a proper screen and KDS and all. It would be foolish, we have a bring-your-own-hardware software, not a we-will-give-you-hardware."* A Pakistani restaurant runs RestOS on what it already owns. **The ruling was already the corpus's position and only the CODE dissented:** `restaurant-os.md:47` (top of the authority order) reads *"Hardware tiers: **T1 Counter** (terminal + printers) · **T2 Counter+Pass** · **T3 Full mesh**. Tiers upgradeable; lower tiers degrade gracefully, never feel crippled"*; `02-F31` specifies T1 detection and auto-advance, `03-F22` makes KDS-vs-printer a per-station layer-2 choice, `03-F24` makes ready-signal ownership a role assignment, `27-F11e` makes the pass screen optional, and `02-F40` reasons about *"a T1/T2 kitchen (27-F11e — most deployments)"*. **Measured August 2026: `grep -arn "hardware_tier\|hardwareTier" apps packages services` returns ZERO hits.** The implementation collapsed three specified tiers into the richest one and hardcoded that tier's hardware — an implementation that never read its own spec, the same shape as `DEC-UI-001` one layer up. **Register (August 2026): 8 hard refusals, 6 absence-modelled-as-fault, 11 capacity claims pinned to a panel, 15 genuine physical laws that must NOT be adapted away.** The severe ones, each verified: (a) **`order.line_state_changed` has a Zod schema (`domain/registry.ts`) and a fold consumer (`sync-client/folds/merge.ts`) and no production emitter** — so on a T1 branch, the configuration the corpus calls most deployments, **no order line ever advances past `placed`**. A missing PRODUCER for an event type, which `seams:check` cannot express because a key in an object literal is not an export; the prior instance was `audit.print_acknowledged`. (b) **The size floor is on the wrong axis.** `window-options.ts` declares `minWidth: 1366, minHeight: 768` and Electron *prevents* the resize — measured, 1280×800 on 13.3″ glass lays out with **zero violations and is refused**, while 1366×768 on 10.1″ glass **satisfies the pixel floor and clips two surfaces**. The requirement is physical: chrome is 37.4 mm with the alarm band up and the Cash tab needs 94–96 mm of work area, so **≈131–134 mm of glass height** is the real floor (10.1″ panels have 126–130 and fall 2–6 mm short; 13.3″/14″ have 174–179 and clear it). **130–174 mm is unmeasured, not known-good.** (c) **Absence of a printer is modelled as failure, not configuration.** `unattachedPrinter` manufactures a failed transmit per job, retries exhaust, `03-F5` raises a permanent band, `printing.ts` appends a **permanent** `kot.print_failed` under `01-F1`, `05-F3` alarms the manager and `15-F14` pages vendor support on `kot.print_failed` rates — a printerless branch generates real support load forever. There is **no `printer: none` mode** in specs or code. (d) **The density fallback assumes a 15.6″ PANEL, not merely a density**: on a 10.1″ tablet it yields ~100 PPI against ~224 PPI of real glass, so every `27-F8` target renders at **~45% of its ergonomic size, silently**. (e) `surface-mode.tsx` has two consumers repo-wide and changes three numbers; a 6.5″ phone and a 13.3″ laptop resolve to the **same mode** at 151 violations and zero respectively — **the product has dispatch, not a degraded mode.** **RATIFIED, and implementable without further ruling:** tier becomes a real read value **derived** from the device registry per `02-F31` (following `panel_ppi`'s precedent in `00 §7` — measure first, configure only to correct, because *"a number a technician types is a number a technician mistypes, and the failure is silent"*); `02-F31`'s auto-advance ships exactly as written, **including its prohibition — no `ready` state is ever fabricated**, since `03-F26` makes T1's absence of ready samples the honest reason T1 restaurants get aging timers and never learned ETAs; the floor is restated in millimetres through the measured density. **✅ SUB-QUESTION (1) — `03-F49` re-opened on LEGIBILITY grounds and ANSWERED: NO. FONT B DOES NOT CLEAR; `03-F49` STANDS AS WRITTEN (August 2026).** Why it had to be re-decided rather than inherited: `min-columns.ts` gates on **Font A** specifically to keep that FR true — its own comment concedes 58 mm yields `floor(384÷9) = 42` Font-B columns, **exactly `MIN_COLUMNS.kot`**, so *"a Font-B gate would admit the printer the FR says is excluded"* — while 58 mm is the named Pakistani installed base and `27-F11e` makes the KOT the primary kitchen interface. **The geometry, at 203 dpi (0.1251 mm/dot).** The corpus states font cell **widths only** (`03 §7`: *"Font A = 12, Font B = 9"*) and **no glyph or cap height for either font anywhere** (corpus-wide sweep: zero hits), so the heights are an **IMPORT** from published ESC/POS geometry, marked as such rather than cited: Font A's cap is **1.75 mm** measured (14 dots — `simulate.ts` draws a 5×7 face at 2× inside a 12×24 cell, which it declares an interpretation and disclaims as a legibility claim) to **2.13 mm** published (17 dots); Font B's conventional 9×17 cell puts its cap at **1.13–1.50 mm** (9–12 dots). **The angular answer.** The corpus states **no viewing distance for paper** — `27-F27` is scoped to glass by its own words (*"KDS type"*, *"KDS primaries"*), `27-F11h` says outright *"this document has no design language for thermal paper"*, and `27-F55`'s *"at arm's length"* is unquantified — so **0.45 m is a STATED ASSUMPTION, not a citation** (a chit clipped at a station or laid on a pass while both hands work). At 0.45 m Font A subtends **13.4–16.2 arcmin** and Font B **8.6–11.5**, against ISO 9241-303's **16 minimum** and 20–22 recommended. Font B reaches the bare minimum only within **242–323 mm**, the recommended band only within **194–258 mm**, and `27-F27`'s **30 arcmin** — the factor it prescribes for steam, grease and a moving reader, which are the kitchen's conditions and not glass's properties — only within **129–172 mm**, which is not a reading distance. **The budget has no slack to spend: the KOT is ALREADY at ISO's floor in Font A** (which clears 16 arcmin only out to 376–457 mm), and the item **NAME** — the thing the cook must actually read — renders at `normal`, because `27-F56` allocates the 2× rung to the quantity and the identifier. Font B removes ~30% of a cap height already at the minimum, and **the NO is robust to the imported heights**: even the most generous Font B reading (12 dots, 1.50 mm) misses the recommended band beyond 26 cm. **AND IT IS NOT A ONE-LINE GATE FLIP — THE ENCODER CANNOT EMIT FONT B AT ALL.** `packages/escpos/src/encoder.ts` emits exactly `ESC @`, `GS ! n`, `GS B n`, `ESC d n`, `GS V m` and `GS v 0` — **there is no `ESC M`** — `simulate.ts` carries one 5×7 face and no font state, and `ESC M` is **not in K-2's oracle allowlist**, whose walker fails any un-admitted `ESC` command as an unaccounted byte; so admitting Font B means an encoder change, a simulator font model and a Font B cell height the corpus does not state, all resting on the legibility claim this row refuses. **`cols_font_b` has ZERO production readers** (measured, comment-blind) — a capability field the product declares and never uses — and `packages/testing`'s own oracle says so: *"FONT B IS NEVER EXERCISED … no document below selects Font B (`ESC M 1`)"*. **MEASURED, because a flipped gate over a Font-A encoder is `03-F34`'s banned silent degradation rather than a hypothesis:** with the gate mutated to read `cols_font_b` (42) and the paper left at 384 dots, a realistic two-line ticket (longest name 35 chars, inside the budget `27-F57` sets) **discards 320 dots and overhangs by 95** — the word `Portion` is simply absent from the paper, with no signal to the cook that anything was cut. Under the shipped `example_data` the same mutant discards **zero**, which is exactly why no suite would catch it: `03-F36`'s build-time gate renders the KOT at **42 Font-A columns = 504 dots** and has never rendered it on a 384-dot page. **WHAT WOULD REOPEN IT IS A PHYSICAL TEST, AND THE CORPUS ALREADY SAYS SO** — `27-F61` is the precedent (Urdu on thermal paper is *"UNPROVEN and must be physically tested before it is designed around"*), and §2b subordinates every FR in it to `27-F35`'s ≥85% / ≤5% gate. **Folded into K-8:** print the `kot` `example_data` and a 42-column worst case on the **48 GSM stock actually sold in Pakistan** (`03-F45`), Font A on 80 mm against Font B on a BC-58U, and run `27-F35`'s post-training retest with real kitchen staff **at the distance the ticket is actually read, measured rather than assumed** — that measurement is the missing corpus figure and is worth recording whatever the verdict. Until that sheet exists **no FR may assume Font B is legible on a KOT**. Two riders for the rig: the unresolved `size_2x2`-vs-line-advance overlap (`apps/pos-electron/CLAUDE.md`) is visible on every rendered KOT and only hardware separates its two readings; and 42 Font-B columns is **exactly** 42 with **zero slack**, so any future KOT content change breaks 58 mm silently while 80 mm keeps 14 columns of headroom — a standing argument against admitting it even if the type were legible. **The live symptom is FIXED:** `main/index.ts` defaulted to `printerCapability("TH230")` — 44 Font-A columns, a cutter and raster, for a printer the till has never seen — and because `render()` lays out against the *claimed* record, attaching the corpus's own baseline **BC-58U** (`03-F10`) without setting `RESTOS_KOT_PRINTER` produces exactly the 320-discarded-dot silent truncation measured above, so the TH230 pin was itself a latent `03-F34` violation waiting on the named installed base. The default is now `03 §7`'s own *"defaulting conservatively to 32 for an unknown model"* under the model id `no printer configured`, so an unconfigured till takes `03-F49`'s refusal and `03-F34`'s S1 band naming both column counts | (2) ANSWERED by `DEC-HW-003` below — the model is a capability set and the ladder a derived label; a founder ruling is owed only on the `restaurant-os.md:47` amendment text, which is proposed there and deliberately not applied | W1 |
| DEC-HW-002 | `02-F31`'s settlement half is unreachable: `served` has no legal predecessor at T1 | **accepted — RULED**, protected path, adversarial review owed | 02-F31 / 03-F26 / 01 §4 / 01-F1 / 01-F34 / 26 §7 | **A contradiction between an FR and the kernel state machine, found while building `02-F31`'s producer (August 2026).** `02-F31` requires *settlement → lines `served`* and in the very next clause forbids fabricating `ready`. `packages/domain/src/states.ts` reads `in_prep: ["ready", …EXITS]` and `ready: ["served", "picked_up", …EXITS]` — so **`served` is reachable only through `ready`**, and the two clauses cannot both hold. `03-F26` closes the escape route that the prohibition might have been about *samples* rather than *states*: it says outright *"T1 branches produce no ready-marks (02-F31), so they honestly produce no samples."* Two documents agree the state is never entered. **Every route out fails except one, and the failures are instructive.** Declaring `from_states: ["ready"]` is a legal-looking lie that trips `inconsistent_predecessor`. Filtering by legality and emitting nothing is the **worst** option, because it looks finished with every gate green — this wave's named defect exactly. ⚠ **Corrected August 2026 by the implementing session's M4 mutant:** this row first predicted that an `in_prep → served` edge against the old matrix would be *"recorded `illegal_transition` permanently under `01-F1`"*. It would not — `advanceEdgesFor`'s legality filter refuses the line *before* anything is emitted, so the real outcome is the wired-and-emitting-nothing case above: no ledger polluted, and silent where an `illegal_transition` flag would at least have been a record. **A ruling may state the decision from the specs; it may not state the *runtime consequence* without running it.** And **a tier-conditional legality is not merely inexpressible, it is a law-1 violation**: `26 §7` row 65 makes legality a pure function of one edge's payload, so gating it on the branch's tier would make a projected value depend on the reading device's configuration and convergence would depend on who is looking (`01-F34`). That is the decisive argument — it eliminates the option that would otherwise look safest. **RULED: `LEGAL_NEXT.in_prep` gains `served`.** Not as a concession to T1 but because it is the honest model — **in a restaurant with no pass, a line goes from being cooked to being handed over with no observed moment of readiness**, and the machine as written encodes *"a pass exists to observe readiness"* as universal law. That is the same T3-assumed-universal error `DEC-HW-001` names throughout, reaching the kernel. **Safe on all three standing laws:** `served` is terminal so nothing downstream can reorder (law 1); no clock is read (law 2); no money is touched (law 3). **And it loses no timing fidelity**, because `03-F26` keys sample capture on ready-marks, which T1 still does not produce. Permissive-legality is not mandate: a T2/T3 branch has a pass screen emitting `ready`, and only the T1 producer ever walks the new edge. **`packages/domain` is a protected path (commandment 10) — this ships with an adversarial review in a separate context, and the mutation requirement is that a control differing in exactly one branch attributes the kills** | adversarial review of the `states.ts` amendment (`20 §4.4` leg (1); the human-senior leg is retired — founder ruling, August 2026) | W1 |
| DEC-HW-003 | Is there a tier below T1? — **no. The ladder is a derived LABEL; the capability set is the model** | **proposed — recommendation, founder ruling owed on the `restaurant-os.md:47` amendment** | restaurant-os.md:47 / 00 §7 / 02-F31 / 03-F22 / 03-F24 / 03-F50 / 27-F11e / 15-F10 / 27-F28 / 01-F34 / 26 §7 | **Answers `DEC-HW-001`'s second open sub-question — *"is there a screen-only tier BELOW T1?"* — and the answer is that the question has no good answer because the ladder is the wrong shape to ask it in.** **(a) A screen-only kitchen is SIDEWAYS, not below.** `restaurant-os.md:47` defines T1 as *"terminal + **printers**"* and T2 as *"adds pass screen w/ ready-marking"*. A screen-only branch has T2's glass and none of T1's paper, so on a linear ladder it must sit *below T1* and *at T2* at once. Calling it **T0 asserts a rung the hardware does not form**, and it would be the third time this corpus encoded one deployment's hardware as universal law (`DEC-HW-001` at the app layer, `DEC-HW-002` in the kernel state machine). **(b) The corpus ALREADY derives the tier from a capability set — this is not an argument, it is `02-F31`'s own normative text.** *"detection: the branch device registry contains no pass/KDS/waiter device."* That is a pure function from a set of device classes to a rung, and `apps/pos-electron/src/main/hardware-tier.ts`'s `tierFromRoster` is literally that function, shipped. So the ground truth in the corpus's own detection rule is **already** the capability set, and the tier is **already** its projection. Recommending a capability model therefore overturns nothing in Part I — it names what `02-F31` does. **(c) A branch-wide scalar cannot express a per-station fact, and the code says so in its own comment.** `03-F22` makes screen-vs-paper a **per-station** layer-2 choice, `03-F24` makes ready-signal ownership a role assignment, and `03-F50` lists *"KDS-vs-printer per station"* among what stays layer-2. `hardware-tier.ts`'s `autoAdvancesLines` records the mismatch verbatim — *"The proxy is branch-wide and `03-F22` is per-station"* — and returns the wrong answer for a T2 branch's one printer-only station. A ladder cannot say *"grill has a screen, tandoor has paper"*, which `03-F22` states as supported. **(d) Cardinality is the mechanical reason.** A ladder is linear in rungs; hardware is a **product** — paper-or-screen **per station** × pass screen × waiter handhelds × cash drawer. When the vendor sold the package that product had one degree of freedom and a ladder was exactly right. Under bring-your-own-hardware the exponent is whatever the restaurant already owns, and enumerating those as rungs is the combinatorial blow-up the ladder was invented to avoid. **(e) `DEC-HW-002` already forbids the tier from being the model where it would matter most.** `26 §7` makes edge legality a pure function of one edge's payload, so a tier-conditional rule makes a projected value depend on the reading device's configuration and breaks `01-F34`. A tier may therefore **never** be an input to a fold — which confines it to precisely the jobs a label is good at, and is decisive. **(f) What the ladder is genuinely good for, and keeps.** Selling and quoting; `15-F10`'s onboarding runbook; support scripts; `03-F26`'s honest *"T1 branches produce no ready-marks … so they honestly produce no samples"*; `02-F31`'s coarse auto-advance. An owner asking *"what do I need to buy"* deserves *"T1"*, not a vector. **RECOMMENDATION — keep the key, change what it IS.** The **capability set is normative** (per station: paper / screen / both, `03-F22`; per branch: pass screen, waiter handhelds, cash drawer). **T1/T2/T3 survive as a derived NAME for the common combinations** — derived by `02-F31`'s rule where the registry is reachable, configured as a correction where it is not (unchanged from today, `00 §7` layer 2). **No code may branch on the tier to decide whether a piece of hardware EXISTS.** That last sentence is the checkable test that separates the two models, and it is why this is not aesthetics: with the `03-F22` implementation that lands alongside this row, `printing.ts` asks a per-station route and never the tier, and any future `if (tier === …)` guarding a device's presence is the defect returning. **No `T0` is created**, for three reasons: it asserts a rung; it would make `apps/pass-kds` load-bearing by a NAME rather than by a capability; and every existing `T1` assertion would silently have to decide whether it means *"no pass screen"* or *"has printers"*, which are different facts wearing one label — the exact conflation that produced `DEC-HW-001`'s register. **PROPOSED AMENDMENT TEXT for `restaurant-os.md:47` — the founder's document and the founder's call, written here and deliberately NOT applied:** *"**Hardware capabilities**, per branch and per station: does this kitchen station take **paper**, a **screen**, or both (`03-F22`); does the branch have a **pass screen** (`27-F11e` — optional), **waiter handhelds**, a **cash drawer** (`03-F9`). **T1 / T2 / T3 are names for the common combinations, not requirements** — T1 Counter (terminal + printers) · T2 Counter+Pass (adds pass screen w/ ready-marking) · T3 Full mesh (waiter handhelds, station routing/KDS, manager console). A restaurant brings what it owns; the product adapts, and states the capacity a given panel yields rather than mandating a purchase (`27-F28`). Combinations degrade gracefully, never feel crippled — and a combination we have no name for is **supported**, not refused."* **THE TENSION, STATED RATHER THAN WON:** the ladder is in Part I at the top of the authority order and `00 §7` declares `hardware tier (T1/T2/T3)` as a layer-2 key, so a capability set is the junior document arguing with the senior one. The recommendation is shaped to need no retraction from Part I — the ladder keeps its sentence, its key and its uses, and loses only the claim that a restaurant's hardware must be one of three points on a line. **NOT A BLOCKER:** `03-F22` is an FR and needs no ruling, so the per-station fulfilment route and its configuration-time validation ship under either model | founder ruling on the `restaurant-os.md:47` amendment; whether the `00 §7` layer-2 tier key is retained as an override or retired once `02-F31`'s registry is reachable on a device | W1 |

| DEC-SYNC-012 | The envelope has no shape for a CLOUD-ORIGINATED, ORG-SCOPED event | **accepted** | 01 §4 / 01-F18 / 01-F43..F46 / 01-F9 / 14-F3 | `EventEnvelope` requires `branch_id`, `branch_created_at` and `time_basis` on every event, because `01-F43`..`01-F46` make time branch-consensus and stamped at append by an originating DEVICE. A back-office catalog edit (`14-F3`'s `catalog.changed`) has **neither a branch nor a hub** — it is org-scoped, authored on the cloud plane, and `01-F18` already rules its ordering by `server_received_at` rather than by any branch clock. So the ORDERING half was settled and the ENVELOPE half was not: there was no legal shape for the event `14-F3` requires. Found August 2026 by B-3/B-4, which built the publish path against a `LedgerAppender` PORT and declined to mint an envelope rather than guess. **RULED (August 2026) and promoted into `01-F62`: shape (c) — an org-scoped path that is not the branch ledger at all.** The `01 §4` catalog holds TWO SCOPES, and the test is WHO MAY LEGITIMATELY EMIT — not which doc declares the type. Org-scoped = cloud plane is the only legitimate emitter: `catalog.changed`, `device.registered/revoked`, `user.changed`, `config.changed`. Everything else stays branch-scoped **INCLUDING `audit.*`**, because `audit.login` is emitted by a device at a PIN unlock and `01-F5`'s chain is per-device — so the admin *family* does not split cleanly and the emitter does. An org-scoped event carries `org_id`, no `branch_id`, no branch stamp; its ordering authority is `server_received_at`, which is trustworthy precisely because the cloud plane is the one place a clock is not a threat — the inverse of the device-clock threat model `01-F43` exists for. It never enters a branch stream and no device folds it. `01-F9` already treats org-scope reference data as a SEPARATE path from the branch stream and the catalog artifact itself travels that way (T-C2/T-C3), which is why (c) is the shape the corpus already implied. **Rejected:** (a) a `server` value on `time_basis` with the server's own stamp in `branch_created_at` — it puts a non-branch value in a branch field; (b) an org-scoped envelope variant — a kernel-wide change touching every fold and reader, for events no fold reads | promoted into `01-F62` | W1 |
| DEC-ARCH-001 | Where a shared implementation lives once it acquires a second consumer — `runAuditor` across a service boundary | **accepted — RULED (founder, August 2026); (B) LANDED August 2026**, protected path, adversarial review owed | 18 §2 / 20 §4.2 / 22-F8 / DEC-TEST-003 | **The question, forced by landing `20 §4.2`'s Auditor host.** `runAuditor` lived in `services/sync-gateway/src/auditor.ts`; `services/jobs` schedules it. `18 §2` states the dependency direction as a **MUST** — `apps → packages`, `services → packages`, `packages → packages`, and *"Cross-module calls go through tRPC/events, **never through direct imports across service boundaries**"* — and `services → services` is on neither list. **(A)** shipped first so the Auditor never stopped running: the gateway published a two-entry `exports` map and `services/jobs` imported directly, with the violation named in code rather than hidden. **RULED: (B) — move `auditor.ts` into `packages/auditor`, which both services may import, as a pure move behind an unchanged public barrel.** **The decisive reason is that the handbook already answers this exact situation one section away from the MUST everyone argues about.** `18 §2` on the outbox core: *"One shared implementation (**extracted from `sync-client`'s outbox core when the second consumer is built**); a module specifying a persist-before-attempt queue MUST consume it, not reinvent it."* The handbook's rule for *"an implementation living inside one owner has acquired a second consumer"* is **extract it at that moment**, and the trigger had fired — not speculatively: `20 §4.2` defers *"whether the Auditor also runs device-side … decide after Wave 1 field data"* and `22-F8` requires the same refold inside a restore drill, so a single-host structure for what `20 §4.2` calls *"the single highest-value correctness artifact we build"* is a bet against three dated decisions. `packages/testing` → `packages/escpos`, with `turbo.json` cutting the cycle, is this repo's own precedent for a package whose integration wiring lives in its consumer. **AS LANDED (August 2026):** `auditor.ts` is `packages/auditor/src/auditor.ts`; `redactedDsn` is `@restos/config`'s, imported rather than copied for the reason `03-F40`'s two sensor bit layouts records, while `DATABASE_URL_DEFAULT` stays in the gateway where it has a gateway-specific reason; the gateway's `exports` field is **gone**; the gateway barrel re-exports the Auditor unchanged, so `services/sync-gateway` stayed at **330/330 with zero suites touched** (every acceptance suite reaches `runAuditor` through `auditor-builders.ts`, which destructures it from the public barrel); and the spec PR extending `18 §2`'s enumerated `packages/` tree landed with it. **Three things a similar move must expect:** a `packages → services` type import is a WORSE inversion than the one being removed, so `GatewayDb` became a locally-declared `AuditorDb`; a note describing the old violation has to be rewritten, not left; and a PATH can be a dependency — `services/jobs/src/__acceptance__/auditor-host.test.ts` §H `readFile`s the auditor source **by path** and would otherwise fail with ENOENT instead of its own message, because *"exactly three files import the auditor module"* is not the same claim as *"exactly three files name it"*. **(C), an `/internal/auditor/run` route, is rejected and stays rejected, on three grounds.** `18 §5` reserves *"plain REST endpoints only for third-party webhooks/callbacks"*, so an `/internal` route is neither of `18 §2`'s sanctioned mechanisms; the Auditor selects the whole org's `kernel.events` with **no LIMIT and no cursor** and refolds synchronously, so serving it from the gateway puts an unbounded CPU-bound refold inside the process every till holds a socket to, against `20 §4.2`'s *"never crash a service mid-service"*; and the verifier would inherit the gateway's build, pool and `DATABASE_URL`, **auditing from inside the fault it exists to catch**. `packages/auditor` makes the route trivially buildable later for `22-F8` — do not build it now (`24 §3b`). **⚠ A SECOND BOUNDARY QUESTION IS OPEN AND IS NOT CLOSED BY THIS RULING:** `services/jobs` opens **its own Postgres handle on `kernel.*`** for the org list, which neither (A) nor (B) touches. `18 §4` is a *writer* rule and a read does not breach it — but `services/sync-gateway/src/publish-http.ts` records the founder rejecting a **read** of another service's tables on coupling grounds (*"buys one copy of the menu at the cost of coupling two services at the database — the thing a service boundary exists to prevent"*). The corpus points both ways; left unruled, `services/jobs` becomes the precedent the next session copies. **Also noted:** `24-F14` specifies a dependency-direction CI rail and `grep -arn "dependency-cruiser"` finds it **nowhere outside `specs/`** — (B) is what would let that rail land green | the kernel-handle question above; adversarial review of the `packages/auditor` move (`20 §4.4` leg (1) — the human-senior leg is retired, founder ruling August 2026; `services/sync-gateway` is a protected path) | W1 |

**Rules:** promoting a decision = writing it into the owning doc and flipping status to accepted with the FR reference; a superseded row keeps its ID and points at its successor. **Where the reasoning lives, and how long a row stays.** The reasoning belongs with the ruling that WON: once a decision is promoted, the owning FR is the governing text and this row keeps only what the FR does not carry — the rejected alternatives, the measurements, the open residual. A **superseded or amended** row collapses to ONE line — what was ruled, what overruled it, when, and where the reasoning now lives — and never to a retelling of how the staleness was found; a **live** ruling keeps its reasoning; an **open** question keeps everything, because an open question compressed into a slogan is one nobody can answer. CI (20 §2.1 static checks) will eventually lint FR references in this table against the specs.
