{
  "name": "@restos/device-config",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "echo 'no build yet — scaffold stub'"
  },
  "exports": {
    ".": "./src/index.ts",
    "./aging": "./src/aging.ts",
    "./device-identity": "./src/device-identity.ts"
  },
  "dependencies": {
    "@restos/domain": "workspace:*"
  },
  "//devDependencies": "⚠ `@restos/sync-client` is a TEST-ONLY edge and must stay one. `CLAUDE.md` here says the package depends on `@restos/domain` alone; that claim is about what SHIPS and is still true — `src/dev-staff.ts` types its registry argument structurally (`DevStaffRegistry`) precisely so no runtime edge exists. `src/__acceptance__/dev-staff-seed.test.ts` needs the REAL `createStaffRegistry` (via `openStore`) rather than a hand-rolled fake, because the whole subject of that suite is how the seed interacts with `staff.ts`'s `clearAll`-on-snapshot and its `stale` rule — a fake would be an oracle asserting a hand-copy of the very semantics under test. There is no cycle: `@restos/sync-client` does not depend on this package (it names it in three comments only).",
  "devDependencies": {
    "@restos/sync-client": "workspace:*",
    "vitest": "4.1.10"
  }
}
