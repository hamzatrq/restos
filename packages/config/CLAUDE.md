# @restos/config

**Owning spec: `specs/18-engineering-handbook.md §1/§5` — read it before modifying anything here (AGENTS.md routing).**

- Shared tsconfig base, Biome config, env-validation factory. Zero per-package overrides without a rule change in 18.
- This package is a scaffold stub: no implementation exists until its plans/ task and pre-implementation artifacts (24-F8) do.
