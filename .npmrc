save-exact=true
# Storybook 10 pulls recast transitively, which requires tslib at runtime; pnpm strict
# linking leaves it unresolvable from the store. Hoist just that one package.
public-hoist-pattern[]=tslib
