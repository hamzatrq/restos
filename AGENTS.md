# RestOS — Agent Guide

Restaurant OS for Pakistani restaurants. TypeScript monorepo (planned per `specs/18`), offline-first, event-sourced. **The specs are the contract: never code or edit from memory of a spec — open the owning doc first, per the routing table below.** Full context rules: `specs/23-ai-context.md`.

**Repo state (update this line when it changes):** Wave 0 kernel merged; Wave 1 work is on **`feat/wave-1-ui-foundation`** (Wave 1 substantially complete, not yet pushed). Suites **GREEN**: **2623 tests**, 24/24 turbo tasks (sync-client 582, pos-electron 406, domain 328, escpos 293, gateway 317, ui 247, api 169, backoffice 129, sync-protocol 77, testing 75 — **all ten re-measured together 2026-08-09 in one `pnpm test --force --continue`**, so the total is a measurement rather than a sum of carried-forward numbers; the exit code was read from a `REAL_EXIT=$?` marker written INSIDE the log and from turbo's own `Tasks:` line, never from a reported status — see the laundering trap below); `pnpm verify` exit 0 and `pnpm seams:check` clean. **ALL THREE PROCESSES NOW START.** `services/sync-gateway` was the last one that could not: it had `test` and a `build` stub and **no `dev`/`start`**, so the cloud sync end had never run as a process — the tenth instance of the defect below. It now runs on `tsx` (`pnpm -C services/sync-gateway start`), prints a load-bearing boot line, **needs Docker only to be TESTED and not to be STARTED** (lazy Postgres, `DATABASE_URL` defaulted, a missing database is a named 500 in ~9 ms), and `__acceptance__/startable.test.ts` spawns the DECLARED script and reddens if that stops being true. **BOTH PLANES RUN AND TALK:** `services/api` boots on `tsx` and serves authenticated tRPC (Argon2id login → session → `can()`-gated read, verified over HTTP by hand), `apps/backoffice` reaches it through its own `/api/trpc` rewrite, and an authored menu now travels API → gateway → device read path. **THE WHOLE CHAIN HAS NOW BEEN RUN AS ONE SYSTEM AND THE PRODUCT CLAIM HOLDS (August 2026):** an owner logged into the back office in a real browser, priced an item, published it, and **a real Electron till rang it at the price typed** (Rs 450 + Rs 320 = Rs 770), on both delivery paths — snapshot on `hello_ack` mismatch, and a **live delta** on `catalog_notice` that moved a connected till v1→v2 with no restart. **The runbook is `plans/wave-1/running-the-stack.md`** — four processes, one Postgres, from a clean checkout; it is the only document that gets you there and it names what is still seeded (device identity, staff roster, the one env-declared owner, in-memory staged edits) and what is owed. **THE WHOLE CHAIN WAS RE-RUN FROM A CLEAN CHECKOUT AND STILL HOLDS (2026-08-08, second independent run):** four processes, a 20-entry menu authored one entry at a time in a real browser, a till **connected and idle throughout** riding the `catalog_notice` path from **v0 to v20 with no restart**, and four tiles ringing **Rs 450 + 320 + 60 + 180 = Rs 1,010** — the prices typed in the back office. **DEVICE PROVISIONING IS NO LONGER SQL: `pnpm -C services/sync-gateway provision-device --org … --branch … --device … --class …`** registers the device and mints its `01-F47` token in one declared command (that run used it, not the hand-written `INSERT`). Both halves of admission had been correct, tested and unreachable — `registerDevice` was marked owed and `issueDeviceToken`'s only production caller was the RENEWAL path, so the service could renew a credential it had no way to issue: the **eleventh** instance of the defect below. It also removed a live security defect the runbook itself instructed: §6b's `on conflict … do update set revoked_at = null` **un-revoked a revoked device**, and the command refuses that in both modes. **What it does NOT close:** `01-F25`'s back-office pairing code (an owner still needs shell access on the service host — deliberately not invented, the corpus specifies it in one clause). **THE REVOCATION HALF IS NOW CLOSED TOO (August 2026) — `pnpm -C services/sync-gateway revoke-device --org … --device …`.** `revokeDevice` had **zero shipping callers** for the window between the two commands, so a till could be *admitted* by a declared command and *revoked* only with hand-written SQL — the eleventh-and-a-half instance, and the security half. **`seams:check` could not see it and its clean run said so out loud**: the export was never dead, `revokeDevice` is called by **nine** acceptance suites (re-counted 2026-08-09 as files containing `revokeDevice(`; this said five); Rule A asks whether shipping code reaches an export and Rule B whether an optional seam is supplied, and neither asks *"can a human invoke this"* — the rail's own `@unreached-owed` marker on it was carrying the debt, which is the register working, not the walk. Landing the caller took the owed register 35 → 34 and dropped `registry.ts` off the list. **What the spec says a revoked device experiences was READ, not designed** (`01-F48`: eviction *"within 30 s … rather than only at its next voluntary contact"*, the cloud *"drops live sessions"*, revocation blocks *"reads as well as writes"*; `01-F42`: a `purge_command` on next contact) — all three enforcement points already shipped, and only the act of setting `revoked_at` was missing. **`14-F13` CLOSED (August 2026)** — the shell command was always the stopgap and the back-office list is the spec's answer: an owner revokes from `14-F12`'s device list and `device.revoked` carries the **actor** a shell could never supply. `14-F30` is the new permission action, **owner-only**, and it lives in **doc 14 rather than Appendix A** on the corpus's own precedent — **7 of the 22** existing actions have no appendix row, so an FR-decided action does not extend the seed appendix. (**The denominator was 21 here for a day** — `PERMISSION_ACTIONS` held 22 before `device.manage`, 23 after; Appendix A is 15 rows mapping 1:1 onto actions. Numerator right, denominator off by one, and the ratio rests on a stated reading — Appendix A's *Day open / close, cash count* row decides `day.open_close` only and not `cash.count`, which `permissions.ts` already ships; read the other way it is 6 of 22 and the precedent holds either way. **The sharper finding is that this argument was never in the CORPUS at all** — it lived in a commit message and in this file, so the spec carried a placement with no recorded reason. It is now in `14-F30` with the reading stated, which is what makes a number checkable rather than asserted.) Two interpretations are PINNED as such rather than transcribed: owner-only (doc 14 §1 reads like a licence to widen, but §9's first open question is whether managers get a scoped back-office slice at all, so widening would answer an open question by accident — and widening later is additive, while the wrong guess the other way stops a till mid-service), and one action rather than two (four identical cells differ in nothing an implementation can observe; a tripwire stops the split happening silently). **The lockout is proven on a SESSION, not a column**: a till holding an open socket got `purge_command` and the socket closed on the browser click, its reconnect got `purge_command` and no `hello_ack` (`01-F42`), and a bystander device kept its `hello_ack` as the control. The command deliberately emits **no event** — a shell on the service host has no authenticated user, `OrgEvent.actor_user_id` would be `null` permanently in an append-only store, and `PERMISSION_ACTIONS` declares no device action for commandment 8 to authorize against — so **a revocation today leaves no ledger record and no attribution.** Un-revocation is not offered and the corpus is **silent** on it rather than permissive (no FR, no `DECISIONS.md` row); `01-N5`'s replacement path is a fresh `device_id`. Device-side persistence of `01-F47`'s silent renewal is also owed — `apps/pos-electron` re-reads `RESTOS_DEVICE_TOKEN` from env every launch and stores nothing, which bites at TTL and not before. **The migrate entry point is CLOSED — `pnpm -C services/sync-gateway migrate` is declared, idempotent, verified against a real Postgres, and boot now REPORTS the schema state on a fourth line instead of 500ing later and elsewhere.** **`catalog.enabled` is CLOSED (August 2026):** `01-F60`'s enabled `(branch, channel)` set was declared twice — `ENABLED_*` on `services/api`, `NEXT_PUBLIC_ENABLED_*` in `apps/backoffice` — so the axes `14-F29`'s grid drew and the axes the writer checks could disagree, silently. There is now ONE declaration: the procedure returns `ctx.catalog.enabled`, *the same value* `assertSavable` refuses against, the back office draws from it with **no fallback** (a grid on a guess is how the drift returns), and `lib/env.ts` is deleted. `02-F42`'s closed-channel check moved with the authority into the API's boot env — deleting the file without moving it would have silently deleted the check, which is the shape the rails cannot see. **The seam mutant is the number to remember: restoring the client-side fallback fails 0 of the 95 pre-existing `backoffice` tests** while `pnpm verify` and `seams:check` stay clean. **Half of `BOOTSTRAP_ORG_ID`'s trap survives** — the API and the back office agreeing is a different claim from either agreeing with the DEVICE's `branch_id`, and nothing checks the second. **The run cost two live defects, and NEITHER was visible to any suite — this is the strongest evidence yet for the recurring defect below.** (1) `packages/sync-client`'s `catalog-fetch.ts` `toEntry` **dropped `prices` and `station`**, so every synced tile read `no price set` and every station fell back to `DEFAULT_STATION`: the gateway served them, the wire schema carried them, the device store declared and read them, and the reshape between did not copy them — **it failed 0 of the 579 pre-existing `sync-client` tests**, because `catalog-pricing.test.ts` calls `store.catalog.apply()` directly and never crosses that seam while `catalog-fetch.test.ts` did not contain the word *price*. (2) `notifyCatalogVersion` had **zero production callers**, so `/internal/catalog/publish` never told live sessions and *Apply now* reached a connected till only on its next reconnect — under a screen promising "every till in the organisation changes as soon as this saves" — **failing 0 of the 280 pre-existing gateway tests**. Both fixed, both PROTECTED paths, **both want senior review**. **A third finding is a warning about the fix itself:** the first draft of the notice seam test mounted its own wiring, and the mutation matrix showed it **survived** the mutant that mattered (`server.ts` wiring a no-op) — the wave's defect reproduced *inside* the fix for it, found only by mutating and not by reading. **`BOOTSTRAP_ORG_ID` is a join key with three ends and no error message:** it must equal `apps/pos-electron`'s `DEV_IDENTITY.org_id` and `ENABLED_BRANCHES` must hold its `branch_id`, or all four processes report success and no till ever sees a menu — `apps/backoffice/CLAUDE.md`'s two-process example uses values that produce exactly that silent failure when copied into a four-process run. **THE COUNTER RUNS END TO END BEHIND REAL AUTHENTICATION:** identify on the staff grid → unlock against Argon2id (`01-F61`'s durable per-(device,user) lockout) → open the day with a float → ring priced lines → send to kitchen, which reaches a **durable** spooler → settle by method → close the shift → read your own reconciliation on `Me`. **`apps/pos-electron` runs** — `pnpm rebuild:native` once, then `RESTOS_DEV_PIN=<digits> pnpm start`. **The gateway suite requires local Docker** (Testcontainers) and by design **fails LOUDLY rather than skipping** (`T-01-07`) — with Docker down you get `Tasks: 23 successful, 24 total` and a red exit that is an **environment prerequisite, not a regression**. **⚠ TWO MEASUREMENT TRAPS, both cost real time this round.** (1) **`pnpm test` HANGS FOREVER in a sandboxed shell** — vitest stays alive at 0% CPU inside `globalSetup` and **zero containers are ever created**, because the sandbox blocks the Docker socket. It is not slowness and no timeout will save you: run it unsandboxed, or run `pnpm -C services/sync-gateway test` separately. A 10-minute "timeout" here was misread as a slow suite for an hour. (2) **`cmd > log; echo "exit=$?"` LAUNDERS A KILLED RUN** — the trailing `echo` succeeds, so the harness reports the whole command as exit 0 even when the real command was SIGKILLed. Capture the exit code of the command itself, and never read a wrapper's success as the suite's. **⚠ THIS TRAP IS ALSO LIVE IN THE BACKGROUND-TASK NOTIFICATION PATH, which is worse because nothing looks like a wrapper.** Measured 2026-08-07: an agent's backgrounded suite runs were each reported by the harness as *"completed (exit code 0)"* — including three mutant runs that were really **exit 1**. Only a `REAL_EXIT=$?` marker written **inside the log** told the truth. So the rule is not "avoid the `echo` idiom", it is: **a reported exit code is not evidence; read the suite's own summary line, or write your own marker and read that.** Every green claimed in this file from that date was taken from turbo's `Tasks: N successful` line rather than from a reported status, for exactly this reason. **Run `pnpm test --force --continue`** — `--force` because a cached turbo run has produced a false green here, and `--continue` because **turbo KILLS a failing task's siblings**, so without it seven green suites are indistinguishable from seven killed ones (that hid 3 real `ui` failures for an hour). **Do not trust ONE green run:** four subprocess-spawning oracle suites have flaked on a 5 s default across two packages (`biome` via `spawnSync`, `tsc` via `execFileSync` — search the PROPERTY, not the mechanism), and a killed suite leaves `.oracle-typecheck/` in the tree which then reds `pnpm verify` on generated fixtures. **THE COUNTER LOOP RUNS END TO END:** `C4` start an order (no default order type, founder ruling) → `C5` tap a tile for a priced line → `C9` send to kitchen → settle (`02-F12/F13`). **The `24 §3` test-authorship split is now REAL, not aspirational** — `T-2`, `T-3` and `K-1` were each authored by a separate session from spec text, committed RED, then implemented against; briefs live in `plans/wave-1/t2-t3-test-brief.md`, `plans/wave-1/kot-test-brief.md` and `plans/wave-1/service-surface-test-brief.md`, and their **⚠ Do NOT read** sections are load-bearing — **the session that wrote a plan is disqualified as its own test author.** **Pricing is per (branch, channel)** (`01-F60`, founder ruling: commission drives it) with **no fallback** — completeness is enforced at the writer, and the editor prefills (`14-F29`). `02-F42` closed `channel` (it is a PRICE KEY now); `03-F50` put `station` on the catalog entry, inheriting down the `01-F21` chain. **Wave 1's foundation:** `specs/27` (**77** FRs incl. §2b thermal paper — this line said 67, counted before `27-F63..F68` and the lettered `27-F2a`/`27-F11a..h` rows were written; measured as definition lines `^- 27-F…`, highest id `27-F68`), `plans/wave-1/role-task-inventories.md`, `plans/wave-1/screen-map.md`, `specs/03-F30..F50`. **`screen-map.md`'s tab-rail task IDs were WRONG as authored** (4 of 5 rows; verified against `a235a96` that the inventory was never renumbered) and were corrected 2026-07-30 — a session routed by the old table built the wrong screen. `C1` (unlock) is deliberately not a tab and had no home anywhere in the map; `service-surface.md` S-0c owns it. **`packages/ui`:** **17** components, theme-aware, with a happy-dom suite (`.dom.test.tsx`) — the count is the line count of `src/components/index.ts`, which is the convention the "15" and "16" before it used; `CatalogHealth` landed after the last update and nobody moved the number. (`PanelRoot` is an 18th exported component but lives in `src/physical.tsx`, outside `components/`, so it has never been in this count.) **`apps/pos-electron` RUNS** — `pnpm rebuild:native` once, then `pnpm start`. **⚠ READ `plans/wave-1/oracle-round-2-findings.md` §C before writing any test here** — its three test-authorship failure patterns have each recurred *inside the work that was fixing them*, twice more this round (a tripwire that stayed vacuous after its blocker cleared; a no-degradation guard that banned a field it required). **THE ROUND-3 LAW — THE GUARD THAT WAS NEVER POINTED AT THE DANGEROUS CASE.** Five acceptance suites were authored in parallel by five agents each explicitly warned with §C, and **all five** were returned BROKEN or WEAK by adversarial review — every one for the *same* defect wearing a different costume: the mechanism was built correctly and simply never aimed at the case that matters. `S-2` had Proxy-poisoned envelopes, order-reversing relabel and injection nets — and a `min(envelope.id)` tiebreak **on a money field** still passed all 38 tests, because the harness never ran over the divergent-payload fixture. `K-4` varied `spec` and `profile` across ~90 renders and **never varied `data`**, so an implementation ignoring `data` entirely passed. `K-3` declared the `Transport` interface it existed to deliver, then asserted against a hand-copy — both oracle symbols were dead exports. `S-1` had a test titled *"derived from branch time"* citing `01-F45` that passed against a schema emptied to `{}`. `F60`'s amendment test published a *fully priced* entry, so it could not distinguish "refused for the right reason" from any refusal. **Reading a suite does not detect this — only MUTATION does.** The standing requirement is now: build a plausible implementation out-of-tree, take the suite green, then break the specific thing each assertion claims to own and confirm *that* assertion fails. Report the numbers; a claim that a test bites is not evidence that it does. Two corollaries with teeth: **use a CONTROL** (an implementation differing in exactly one branch) or your kill count proves nothing about attribution; and **a test that stays RED under a CORRECT implementation is as damaging as a vacuous one** — it blocks the implementer indefinitely, and this round produced three (`S-1`'s Zod path, `K-4`'s `₨` sweep against K-2's `isPrintableLatin`, `F60`'s 38 call sites). **Owed on Wave 1, in priority order:** (0) ~~`01-F60`'s two July rulings are spec-closed and code-owed~~ **— CLOSED (August 2026, `wf_98598fbd-a3b`).** `modifier` is now in `SELLABLE_KINDS`, the enabled set is a required input, the superseded doc comment is rewritten, and the green test that defended the overruled rule (`catalog-pricing.test.ts:394` + pinned interpretation 2) is retired. Reviewer verdict **SHIP**, gateway 254/254 green on Docker. *Kept as a worked example, because the failure took ~3 weeks to surface and its shape recurs:* a test author flagged the hole **as a finding**, the founder **ruled on that finding**, and nobody carried the ruling back into the suite — so a GREEN test went on defending an overruled rule, and would have failed the correct implementation. **When a ruling lands, grep the suites that encode the old rule the same day.** (1) **KOT printing** — `plans/wave-1/kot-printing.md` APPROVED. ⚠ **This line said *"K-1 and K-2 landed (escpos 33→90 tests), K-3/K-4/K-5 need test sessions"* long after all three had test sessions AND implementations.** Measured 2026-08-09: **K-1 through K-7 have all landed** — `packages/escpos/src/index.ts`'s own header says so, the oracle suites exist and carry their *"authored from spec text ONLY"* headers (`transport.test.ts` K-3, `render.test.ts` K-4, `kot-document.test.ts` K-5, `spooler*.test.ts` K-6), and escpos is **293 tests**, not 90. `packages/escpos/CLAUDE.md` has been correct about this throughout — **this file was the stale copy, which is the reverse of the usual direction and worth noting: the package guide is not always the one that rots.** **The one thing still owed is K-8, the physical pass** — **no printer yet**, so no test may imply otherwise. **You can now LOOK at a document without hardware and it changes nothing about K-8:** `RESTOS_PRINT_TO_FILE=<dir> pnpm start` swaps `unattachedPrinter` for a transport that writes each document to a PDF (one file per document, one page per **cut** — `03-F42`). The byte→page walk moved out of `packages/testing`'s virtual printer into `packages/escpos/src/simulate.ts` so there is **ONE interpretation** and not two (two walks diverge, and then a document looks right in the snapshot suite and wrong in the app — `03-F40`'s two sensor bit layouts is the corpus's own instance); the virtual printer is now a thin wrapper and `packages/testing` took a dependency on `packages/escpos`, whose **package-graph cycle `turbo.json` cuts at `@restos/testing#build`**. With the variable unset the device still ships `unattachedPrinter` and every confirm still raises `03-F5`'s band — **that band is the honest signal and the simulator must never become the default.** **It is not evidence:** it renders what our own encoder thinks the bytes mean, `27-F35`'s ≥85% comprehension gate on real staff is untouched, and paper never runs out in it so `03-F41`'s hold is unreachable through it. **Its first document already produced a finding:** the KOT's quantity column is `size_2x2` (48 dots) while `simulate()` advances a line feed by the size in effect at the `LF` (24), so quantities overlap — either a real head expands the line to its tallest glyph and the simulator is wrong, or it does not and the layout is; nothing in the corpus rules and only hardware separates them. Recorded in `apps/pos-electron/CLAUDE.md`; **a finding for the K-3/K-5 test owners and K-8's rig, not a fix.** (2) **the service surface** — shifts, cash, day open/close: `plans/wave-1/service-surface.md` **APPROVED**, brief at `plans/wave-1/service-surface-test-brief.md`. ⚠ **This line said *"S-1 (7 payload schemas + the shift key on `payment.recorded`) and S-2 (the `shift_cash` fold) go first and need test sessions"* after the whole ladder had shipped** — and it sat two hundred words below this same paragraph's *"THE COUNTER RUNS END TO END … open the day with a float … settle by method → close the shift → read your own reconciliation on `Me`"*, which cannot be true of a surface whose first two tasks are unstarted. **A doc that contradicts itself within one paragraph is the cheapest kind of staleness to catch and the easiest to read past.** Measured 2026-08-09, **S-1 through S-7 are all in code with their own suites**: S-1 `packages/domain/src/registry.ts` + `__acceptance__/service-surface-schemas.test.ts`; S-2 `packages/sync-client/src/folds/shift-cash.ts` + `shift-cash-fold.test.ts` and `shift-cash-invariance.test.ts`; S-0b/S-0c `pin-session.test.ts` + `unlock-gate.dom.test.tsx`; S-3/S-4 `cash-tab.dom.test.tsx`; S-5 `me-tab.dom.test.tsx`; S-6 `unbound-settlement.dom.test.tsx`; and **S-7 — which the plan still lists as *deferred, blocked on the KOT ladder* — landed as `packages/escpos/src/cash-documents.ts` + `cash-slip-printing.test.ts`.** The plan's task table is the ORIGINAL scope and was never a status board; do not read it as one. **Identity RULED (founder): A-narrow** — PIN session + minimal staff registry (`01-F26`/`F27`/`F28`), nothing else from doc 11. **It is not unprotected work**: 4 of its 9 live tasks are protected. (3) **the back office** — `plans/wave-1/backoffice-catalog.md`. Its two blockers are both **smaller than this line used to claim**: user authentication is **RULED** (`dac8747` — email + password, our own implementation, Argon2id, reusing `01-F26`'s hashing story), and the `domain` permission matrix — `packages/domain/src/permissions.ts` ships `ROLES`, `PERMISSION_ACTIONS`, `can()`, `canPayOut` and `reportScope` (89 tests, 28/28 mutants killed, a 55-cell Appendix A sweep) — **is now REACHED ON BOTH PLANES, and this line said the opposite for a fortnight after it stopped being true.** It read *"zero production callers for all five"* and *"Commandment 8 is not enforced anywhere in this product"*. **The correction was right and its EVIDENCE was not, so here is the re-measurement (2026-08-09, symbol-precise, comments stripped).** The line claimed *"nine shipped files import it — `apps/pos-electron/src/main/{authorize,gateway,index,printing}.ts` … and `services/api/src/{router,trpc,users,catalog,server}.ts`"*; of those nine, **`gateway.ts`, `index.ts` and `printing.ts` name the matrix only in COMMENTS, `users.ts` does not mention it at all, and `services/api/src/catalog.ts` exists but authorizes nothing — the gate is in `catalog-router.ts`.** **The generalisable error, which is why this is written up rather than quietly fixed: the count came from grepping for files that MENTION a symbol and calling them importers.** A mention is not an import, exactly as `seams:check`'s Rule A already insists that *a barrel re-export is not a use* — the same distinction, one tool over, and the session that made it had read that rule the same hour. It is the measurement mistake behind the exit-code trap too: **a proxy for the evidence, accepted as the evidence.** When a claim rests on a grep, strip comments and match the import, or say plainly that you counted mentions. What actually reaches it: **two** files import a matrix symbol from `@restos/domain` — `apps/pos-electron/src/main/authorize.ts` (`can`, `canPayOut`, `reportScope`, `ROLES`) and `services/api/src/trpc.ts` (`can`) — and the enforcement chain around them is **six** files, adding `services/api/src/{router,catalog-router}.ts` (which build procedures with `authorized(...)`), `services/api/src/server.ts` (which calls `assertEveryProcedureIsGated` at boot) and `apps/pos-electron/src/main/index.ts` (which wires `authorizeWrites`/`authorizeReads`). **The substance is unchanged and both planes still enforce** — but a count inflated by comment hits is how a reader is sent to `users.ts` to find a gate that was never there, and *"grep for the production caller"* is this file's own closing evidence for the wave's named defect, so the grep has to be symbol-precise and comment-blind or it proves the wrong thing in either direction. `main/authorize.ts` runs every renderer-originated append through `can()`/`canPayOut` before the ledger is touched and narrows `cashState` through `reportScope` (`02-F23`, measured narrowing: as Hina the seam serves her branch's shift, as Ayesha `[]`); `services/api` builds **every** procedure with `authorized(<action>)` and refuses at boot to host an ungated one. **Keep the correction, not just the fact:** this is the same shape as `catalog-pricing.test.ts:394` defending an overruled rule and as the back office apologising for a `14-F3` gap it had already closed — and it is the most dangerous instance yet, because a security claim that understates enforcement invites a session to "add" a check that already exists, or to route around one it believes is absent. **And the same trap caught THIS correction as it was written:** the sentence that stood here said *"`escalate` remains genuinely unbuilt — `02-F20` has no UI"*, copied in good faith from a package guide that had gone stale, in the very edit that was fixing a stale claim beside it. `02-F20`'s **local manager-PIN path is BUILT and reachable** — `main/index.ts` wires `CHANNELS.escalationFor` (a display-only read that authorizes nothing) and `CHANNELS.escalate` (the approval, behind Argon2id), `Counter.tsx` renders `ManagerApproval`, and `__acceptance__/escalation.test.ts` + `manager-approval.dom.test.tsx` cover it. It uses a **second** `createPinSession` on purpose: `unlock()` MOVES the session, so approving with the shared one would sign the cashier out and `02-F41` would attribute her next twenty orders to whoever authorised one paid-out, permanently (`01-F1`). `02-F20` wants two identities — actor unchanged, approver recorded. **The lesson is the one this file keeps re-learning and a reader should distrust accordingly: a stale claim propagates fastest through the person fixing a different stale claim in the same paragraph.** What IS still owed: `02-F20`'s REMOTE path (doc 05 approval), and `01-F47` device admission remains separate from authorizing a *person*. (4) the rest of the service surface — ⚠ this read *"aging timers, alarms, approvals"* and two of the three have shipped: **alarms** are live (`03-F5`'s band, `AlarmBand` through `AppShell`, `print-alarm.dom.test.tsx`) and **local approvals** are live (`ManagerApproval`, see (3)). What is actually left is **aging timers** — `AgeBadge`/`ageLevel` carry `@unreached-owed` markers naming `03-F47` and waiting on the pass/KDS screen, `apps/pass-kds` being a one-file stub — and `02-F20`'s REMOTE approval path. **Open questions, all recorded in their plans:** `DEFAULT_STATION`'s value is pinned not specified; `03 §7`'s derivation cannot reproduce its own 44/42/48 at 576 dots; `02-F1` writes "WhatsApp" where `02-F42` writes `whatsapp`; **tips** — `DEC-MONEY-004` is RATIFIED at FULL tips and forbids a tip field until `tip.pooled`/`tip.paid_out` enter the `01 §4` catalog, but a tip is drawer cash outside `billed_total`, so the `shift_cash` fold is being built directly in front of a ratified change to its own arithmetic. *(Closed by `dac8747` and removed from this list: `modifier` is now SELLABLE and the enabled set is now REQUIRED — both are spec-closed and **code-owed**, see (0) above.)* **Remaining Wave 0 — and this line was UNDERSTATED until 2026-08-09:** H-01 harness rungs + physical wall-clock (D3), **plus the LAN-first sync mesh, which `restaurant-os.md`'s Wave-0 definition names outright** (*"kernel (ledger, catalog, customer file), LAN-first sync mesh, printing layer, auth/roles"*). The mesh is BUILT and **hosted by nothing**: `mesh-session.ts` carries `@unreached-owed NO HOST RUNS THE LAN MESH YET`, `hub-election.ts` and `transport-ws.ts` say the same, `main/sync.ts` reports `lan` and `hub` **down**, and the only construction of a mesh session anywhere is a spike file under `services/sync-gateway/src/__acceptance__/spike/`. By `00 §1`'s own test — a module's wave is when its first production slice ships to a dev-pilot — that is not shipped. **The consequence is the product's shape, not a checkbox: RestOS is T1-only today**, one till per branch, so `01-F12/F13/F14/F15`, `02-F11` and `01-F43`'s hub-as-clock-authority are all unexercised in production. Found while reconciling Wave 1's scope against `restaurant-os.md`, which is the same root cause: **a wave was tracked from derived documents instead of re-derived from the authority.** `KERNEL.md` §"Not yet done at all" agrees. ⚠ **`pnpm verify` = docs-lint + typecheck + lint was WRONG and this file names the missing three itself, twice, one screen up.** Measured from `package.json` 2026-08-09 it is **docs-lint → typecheck → lint → `tokens:check` → `seams:check` → `layout:check`**, six steps in that order, and the last two are the two CI rails this document spends whole paragraphs on. A session that trusted the short list would have believed it could break a seam or push a control off the screen and still be green. `pnpm test` = the suites (Docker required).

**⚠ THE RECURRING DEFECT OF THIS WAVE: A CORRECT SUBSYSTEM WITH NO SEAM TO THE PRODUCT.** **TWELVE instances and counting** — the five worked below are the first five and the ones with mutation numbers; the header said "FIVE" while the same file counted a *tenth* (`services/sync-gateway` with no `dev`/`start`), an *eleventh* (`registerDevice`/`issueDeviceToken` unreachable), an *eleventh-and-a-half* (`revokeDevice` with zero shipping callers) and `apps/pos-electron/CLAUDE.md` a *twelfth* (`Uplink.catalogRefusal` produced and consumed by nobody). **Read the five as worked examples, not as a census** — the census is twelve, which is the number that should govern how much you distrust a green suite here. Every one with all gates green — `pnpm verify` exit 0, suites passing, review clean — because tests exercise a module **directly** and nothing asserts the *application* reaches it. (1) `createPinSession` had **zero production callers** while the shipped app compared `pin === "1234"`; Argon2id, the lockout and its persistence were real, tested and unreachable. (2) The durable lockout counter engaged only if a host passed `store.pinAttempts`, and none did. (3) The Argon2id cost floor was exported precisely so a test could assert it, and nothing did — `m=8,t=1,p=1` passed in 34 ms. (4) `audit.login`'s sink was wired and tested in `sync-client` while `main/index.ts` passed `audit: () => {}`. (5) K-6 built a durable `SpoolerJobStore` and **mutation-proved** a `Map`-backed spooler fails 8 durability tests — then K-7 wired the spooler and **passed no store**, so a power cut lost every queued kitchen ticket, the exact case `03-F4` names. *(1)–(5) are closed.* The pattern is not a testing failure; the tests are good. It is an **integration-coverage** failure, and reading a diff never finds it.

**IT IS NOW A CI RAIL: `pnpm seams:check`, inside `pnpm verify`.** Two rules, because the defect has two shapes and only one is a dead export. **Rule A — unreached export:** a value export no shipping code reaches, by a symbol-precise walk rooted at `apps/*/src` + `services/*/src` minus tests/stories; **a barrel re-export is not a use**, which is the hole `createPinSession` fell through. **Rule B — unsupplied seam:** an *optional* member of an options bag on a factory shipping code already calls, that no call site ever passes — instances 2 and 5, half the class by count, and `knip --production` cannot express it (`createSpooler` is imported, called, and its durable queue still dead). **⚠ RULE B ONLY WALKS FACTORIES DECLARED UNDER `packages/` — measured 2026-08-10, and it is a third blind spot this paragraph did not name.** Its loop opens `if (groupOf(mod.file) !== "packages") continue;` (`scripts/check-seams.mjs`), so an optional member of an options bag on an **app-level** factory is not a Rule-B candidate at all. Demonstrated on `03-F51`'s routing seam: `createKotPrinter` lives in `apps/pos-electron/src/main/printing.ts`, and `pnpm seams:check` is **exit 0 and CLEAN** both with its `routesToPaper` argument deleted from `main/index.ts` and with it stubbed to `() => true` — reporting the same `5 optional seams` in each case, because the member was never counted. The narrowing is deliberate and probably right (an app is where seams are *supplied*, not declared), but the consequence is not: `main/printing.ts` alone declares three factories whose optional members no rail can see, and `CashPrinterDeps.append`'s own comment currently claims Rule B protects it. **Any optional seam on an app-declared factory needs a hand-written assertion on the host's construction; do not let a clean run stand in for one.** Carries `24-F14` empty-match protection: rename `apps/` and it hard-fails rather than passing vacuously. Exceptions are **in-code markers carrying a reason**, and a marker on something now reached FAILS — so the register cannot rot. Current state, **read off a clean run 2026-08-09 rather than carried forward**: **29 `@unreached-by-design`** (test-support, design-time math, drizzle-kit schema, deploy steps) and **34 `@unreached-owed` across 19 files** — largest debtors `packages/domain/src/money.ts` and `packages/sync-client/src/mesh-session.ts` at 6 each. It also reports 159 production modules scanned (86 shipping), 375 value exports and 5 optional seams. ⚠ **This line said "26 … and 44 across 24 files" while the paragraph above it said the register stood at 34** — two numbers for one register, in one document, and the rail prints the true one on **every** clean run. **Do not hand-copy a number a command prints for you**; that is `K-3`'s dead-oracle defect wearing a governance doc. An honest measure of how far the kernel runs ahead of its apps. **TWO THINGS IT CANNOT CATCH, both measured — do not read a clean run as total coverage.** (i) *Instance 3's shape*: a constant exported so a test **could** assert it, and none did. No reachability walk sees a missing assertion. (ii) **A PORT SUPPLIED WITH A STUB.** Measured on the publish adapter: replace the real gateway publisher with the in-memory stub and `pnpm verify` is exit 0, `seams:check` is **clean**, 111 of 116 API tests pass — and the product ships no menu to any till. Rule B asks whether an optional member is *supplied*, never whether what was supplied is *real*, and a stub is a supply. **Nor can it see a missing PRODUCER for an event type** — a key in an object literal is not an export, which is how `audit.print_acknowledged` sat in the registry with nothing emitting it. (*That instance is CLOSED — `apps/pos-electron/src/main/printing.ts` emits it and `__acceptance__/print-ack-audit.test.ts` is the hand-written assertion. The blind spot is unchanged; only the example has been paid off, and it is kept because the next missing producer will look exactly like it did.*) For all three, the assertion has to be written by hand; the rail narrows the surface, it does not close it.

**⚠ `plans/wave-1/` IS NOT WAVE 1's SCOPE, AND HAS BEEN READ AS IT. THE FULL RECONCILIATION IS `plans/wave-1/wave-1-scope-reconciliation.md` — READ IT BEFORE TREATING THE PLANS AS A CHECKLIST.** The authority order puts **`restaurant-os.md` Part I first for waves**; `plans/` is not in that list at all. §8 defines Wave 1 as *"ops fabric T1/T2, payments/shifts, aging timers, availability, manager alarms+approvals, nightly owner summary, **plus POS quick-entry for phone and foodpanda orders**"*, and `00 §1` rows **05 = 1 core, 08 = 1 manual, 12 = 1 basic, 13 = 4 (foundations from 1), 15 = 1+** — where a module's wave is *"when its first production slice ships to a dev-pilot restaurant"* (`00:43`), not when its package exists. `plans/wave-1/` covers six areas — counter loop, KOT printing, service surface, back-office catalog, identity, design language — and **four Wave-1 modules have no plan at all: 05, 08, 12, 13**, each a two-line scaffold stub (`apps/manager`, `services/foodpanda`, `apps/owner`, `services/intelligence`). **Of §8's seven named items, three are unbuilt and two are half-built:** T2/the pass screen (`apps/pass-kds` is a stub), aging timers (`AgeBadge`/`ageLevel` carry `@unreached-owed` naming `03-F47`, blocked on that same stub), the nightly owner summary, availability (kernel fold + `01-F57`'s convergent supersedes-link are **built**; `02-F7`'s POS toggle does not exist, so nothing can 86 an item), and manager alarms+approvals (local `02-F20` PIN shipped; the console's `05-F1..F4` alarms and the remote path did not). **Item 7 — POS quick-entry for phone and foodpanda, the one item §8 sets off with "plus" and gives a reason for — is entirely unbuilt**: `Counter.tsx:119` hardcodes `COUNTER_CHANNEL`, and `02-F28`/`02-F30` have no surface, though `foodpanda` *is* a priced channel. **The delta is bigger than the four-module count**, because a per-module reading misses items sitting inside modules that DO have plans, and an item-by-item reading misses the modules — you need both passes. **The plans are not wrong about what they cover** and `kot-printing.md` never claimed the pass screen; the error was reading six correct area plans as a covering set. **This is the same disease as the owed lists with the sign flipped** — those claimed gaps that had already closed (six times in two days, `05-F8` this round), this understated scope; both because **a derived document was mistaken for the source and nobody re-derived it**. ⚠ **Do NOT re-plan the four modules** — sequence is a founder call, and the reconciliation deliberately separates *unplanned* from *plausibly deferred* from *cannot tell* rather than filing everything as owed (08's Wave-1 slice is mostly a doc-02 screen; 13's is one block inside 12's summary; 15's may already be satisfied by the gateway CLI). **One item is Wave 0, not Wave 1, and is recorded there rather than swept in:** `restaurant-os.md:119` puts the **LAN-first sync mesh** in Wave 0, and `packages/sync-client/src/mesh-session.ts:17` says `@unreached-owed NO HOST RUNS THE LAN MESH YET` — so "Remaining Wave 0: H-01 rungs + physical wall-clock" above is understated by `00 §1`'s own shipping test.

**⚠ THE SECOND RECURRING DEFECT OF THIS WAVE: A CORRECT COMPONENT THAT IS NOT ON THE SCREEN — AND ITS RAIL, `pnpm layout:check`, ALSO INSIDE `pnpm verify`.** **Seven** layout defects have now been found by *launching the app and looking*, and **zero** by the suites: a 1418 px band in a 1392 px window (nothing ever set `box-sizing`); the counter's work surface at 919 px inside 600 px, silently clipping `TAKE CASH` so a cashier **could not settle an order**; a `BrowserWindow` built with `width`/`height`, which describe the *frame*, so the renderer got 736 px where `27 §1a` promises 768; the keypad surfaces under `03-F5`'s band, where `Counted Rs 0` went **entirely off-screen** on Cash (`apps/pos-electron/CLAUDE.md`'s "fourth defect"); `ManagerApproval` laying out 1162 px in a 632 px box so `Approve` / `Not them?` / `Cancel` sat below the viewport in **both** device states (its "fifth"); an accept tile 76 CSS px tall in a row costed at 45; and a pager not subtracted from its own box, clipping the row beneath it. ⚠ *This list said "Five" and omitted precisely the two that the package guide numbers **fourth** and **fifth** — the two with a founder ruling (`DEC-UI-001`) and a `27-F4` breaking change behind them. Two documents were numbering the same sequence differently, which is how a count goes stale without anyone editing it.* Every one was invisible for the same reason: **the `.dom.test.tsx` suites run under happy-dom, which performs no layout at all** — every `getBoundingClientRect` is zeroes — so they can express *"the button is in the document"* and never *"the button is on the screen"*. The gate opens a real `BrowserWindow` from the app's **real** `COUNTER_WINDOW_OPTIONS` (it *imports* them — a gate measuring its own copy of `1366×768` proves only that its copy is right), mounts the shipped renderer, and measures in Blink: every clipping box against its content, every control against the viewport and `elementFromPoint`. It reads the tab list **from the DOM**, which is why it began measuring the Orders tab the day another agent added it, with no coordination. **Playwright was on `18 §14`'s allowlist and was still the wrong tool** — in a headless browser you *set* the viewport to 1366×768, so it is structurally blind to the defect where the app does not get 1366×768. **What it cannot catch, measured not guessed:** it only sees states its **fixture** produces — the alarm-band defects were invisible until the fixture raised a band, and `ManagerApproval`'s dead controls until `escalationFor` returned an offer, so **the fixture is the real coverage boundary, not the assertions**; it judges nothing about legibility, contrast or typography (a control can be reachable and unreadable); `27-F4`'s positional contract is invisible to it (reorder every control and it stays green); and it measures **one DPI, one platform** — the actual Windows till, and `27 §1a`'s ~224-PPI tablet and ~405-PPI phone, are not measured. **It now sweeps BOTH of `27 §1a`'s counter panels** (`DEC-UI-001` (e), August 2026) and carries **one check that is not about fitting** — `27-F8`'s keypad target measured in **millimetres of glass** — because after `27-F68` a 126 CSS px key fits a 1920×1080 panel perfectly while being 22.7 mm instead of 20. That row is not decoration: a mutant pinning one panel's pixel answer (`27-F68` (a)'s named trap) reddens **only** on the second panel and leaves all 613 tests green. **A `withinViewport` failure means CLIPPED, not UNTOUCHABLE**, and conflating those two cost a session: the verdict text once concluded "this control cannot be touched" for a key overhanging by 31 px of 126 whose centre hit-tests fine, that wording propagated into a `CLAUDE.md` and then into a task brief, and an agent was sent to fix a blocker that did not exist. It disproved the premise instead — correctly.

**Two properties, and you need BOTH — measured, not argued.** Removing the store from the shipped construction failed **1 of 160** tests (the seam assertion, alone). Leaving the store in place but making its `put` a no-op failed 4 — **and the seam assertion stayed green.** So "a store was passed" and "the store durably works" are different claims: a seam test alone blesses a decorative object, and a durability suite alone blesses a subsystem nothing calls. What finds it: **mutate the SEAM, not the logic** — delete the call site and see whether anything reddens. If nothing does, the subsystem is decorative. **When you finish a subsystem, grep for its production caller before you call it done** — and prefer the grep over the suite as your closing evidence, because for this defect the suite passing proves nothing.

**⚠ AND THAT GREP CAN LIE TO YOU UNDER A `C` LOCALE — the one tool this rule rests on.** BSD/GNU `grep` decides a file is *binary* from its bytes **in the current locale**, and under `LC_ALL=C`/`POSIX` any UTF-8 byte qualifies. This repo's source is full of them — `—`, `·`, `₨`, `⚠` in comments and strings — so a shipped file gets `Binary file <path> matches` **instead of its matching lines**. Reproduced 2026-08-08 on `apps/backoffice/src/components/catalog-screen.tsx`: `LC_ALL=C grep -rn PendingEdits apps/backoffice/src` prints 7 lines, `grep -arn` prints 8, and the missing one **is a real production call site**. An agent hunting a caller this way was seconds from reporting a dead export that is very much alive. It does NOT bite an interactive shell with `LANG=en_US.UTF-8`, which is exactly why it survives review — it bites scripts, subagent shells and CI steps that inherit a C locale, and the failure mode is a *quieter* answer rather than an error. **Use `grep -a`** (or keep a UTF-8 locale) whenever a grep is your evidence, and treat a `Binary file … matches` line as a hit you have not yet read, never as noise.

**⚠ TWO MERGE-TIME TRAPS WHEN A WORKTREE BRANCH LANDS.** (1) **A branch that adds a WORKSPACE DEPENDENCY needs `pnpm install` in the main repo after the merge.** The agent ran it in its worktree, so its numbers were real there; in the main tree the module does not resolve, one test FILE fails to load, and the count silently reads 121 where the branch reported 152 — with a `Cannot find module` that looks like a broken merge rather than an unlinked workspace. (2) **A single RED run is exactly as untrustworthy as a single green one.** `domain` read 4-failed under the parallel turbo run and 306/306 run alone; re-running the full suite gave 24/24. That is the documented subprocess-oracle flake, and it nearly got reported as a K-7 regression that did not exist. Re-run before believing a red, the same way you re-run before believing a green.

**⚠ A NEW GIT WORKTREE IS CREATED FROM A STALE BASE — CHECK IT BEFORE YOU WRITE A LINE.** Parallel agents each get their own worktree (isolation is what stops one capturing another's half-written files), but **both worktrees cut this session started behind HEAD — one by 14 commits, one by 96** — far enough back that the packages they were sent to implement barely existed and their own oracles had not been written. Both agents noticed and fast-forwarded; that was two independent strokes of diligence, not a property of the system. **First command in any worktree: `git log --oneline -1` and compare against the target branch; fast-forward if behind.** Implementing against a stale base produces a green suite that means nothing, because the tests you were sent to satisfy are not there. Setup cost is small — `pnpm install --prefer-offline` is ~4 s off pnpm's global store, and native addons work without a rebuild.

**⚠ NEVER `git add -A` WHILE AGENTS ARE RUNNING — STAGE EXPLICIT PATHS.** An orchestrator running parallel agents shares one working tree with all of them, so a blanket stage captures whatever every other agent happens to have half-written. It has already happened here: `8c03e4d`, whose message is entirely about the unlock oracle, also carries 132 lines of a *concurrent* session's `packages/escpos/src/__acceptance__/spooler-oracle-surface.ts` — **a protected path, committed under a message that does not mention it**, splitting that agent's work across a commit it did not make and an uncommitted remainder. Nothing was lost (the content was byte-identical), and it was caught only because the affected agent read the log and said so. `git add <explicit paths>`, and if a stray path appears in `git status` that your task did not touch, leave it: it belongs to someone still working. Corrections follow commandment 1's spirit — a new linked record naming the earlier commit, never a rewrite.

**⚠ MUTATION TESTING OF A SECURITY PARAMETER HAPPENS OUT-OF-TREE. NEVER IN-TREE.** The round-3 law makes mutation mandatory, and for a *security constant* the obvious way to run it — weaken the value, run the suite, revert — is unsafe here for a reason this repo has already demonstrated: **agents get killed mid-task** (three times in one session, at 1.3–1.7 h each), and one killed between "weaken" and "revert" leaves live weakened crypto in the working tree with every test green. A real instance: `PIN_ARGON2ID_PARAMS` was flagged at `m=8,t=1,p=1` by an automated scan while an agent was proving the `01-F61` cost floor actually bites; the tree was correct minutes later, and nothing but that scan's timing would have caught the other outcome. Copy the package to a scratchpad and mutate the copy — the same discipline every oracle author in this wave already used for implementation mutants. The rule is narrow on purpose: in-tree mutate-and-revert stays fine for ordinary logic, where a stranded mutation reds a test instead of silently downgrading a credential.

**Three standing laws every session must know (all now RATIFIED and shipped — the earlier "not yet implemented" warnings are retired):**
1. **The ordering design is per-fold merge semantics (`26`, `DEC-PERF-001`).** `01-F34`: folds declare explicit merge rules and read **no ordering metadata** — no `global_seq`, no `lamport_seq`, **no device clock**, no envelope-id comparison that reaches a projected VALUE. Tested by bijective id-relabel + clock-injection invariance; plain convergence testing is insufficient (a min-id tiebreak passes it while smuggling wall clock in through the UUIDv7 prefix). `global_seq` = delivery cursor + compaction watermark only. `causal_seq` remains refuted; `lamport_seq` keeps its gap-free transport/audit role. **This is the law most often broken by accident** — twice in the post-review round, each time by a projected value quietly depending on id sort order or on the reading device's state.
2. **Time is branch-consensus and stamped at APPEND (`01-F43..F46`, `DEC-TIME-001`).** Durations need a *consistent* clock, not a correct one. `branch_created_at` + `time_basis` travel INSIDE the event, because a fold applying its own offset would break law 1 silently. `device_created_at` is an untrusted forensic hint with exactly one sanctioned reader (`01-N2` skew detection). Branch time is continuous across hub re-election — the hub serves the clock, it does not define it. Business day = Asia/Karachi, 05:00 cutover (layer-2 configurable).
3. **Money is integers-in-a-double, and the double is the hazard.** Integer paisas only; rates as integer basis points; `splitPaisa`/`applyRateBps` for division and rates (`DEC-MONEY-005`). Folds accumulate in **BigInt** — float `+` is non-associative near 2^53, so a running double total lets delivery order decide a money outcome (a live law-1 break, not a theoretical range concern). A total that cannot be represented exactly contributes **zero** and raises `money_overflow`; never truncate, never throw on the ingest path (`01-F17`). The GritQL ban covers bare identifiers **and** money-named member expressions incl. `as` casts — comparisons and plain assignment stay legal on purpose.

## Commandments (always binding; each is also machine-enforced — violating code fails CI regardless of what you read)

1. **Append-only ledger.** Never mutate or delete history; corrections are new linked events (`01`).
2. **Never invent events, states, or policy.** Event types live in the `01 §4` catalog; order states in `01 §4` only. If the spec doesn't cover your case: STOP → check `specs/DECISIONS.md` → propose a spec change. Do not fill gaps with plausible behavior. **Any policy you assert must cite an FR ID that resolves** (`grep -rn "02-F9" specs/`) — an ID that greps to nothing means you invented it.
3. **Money = integer paisas; quantities = integer mg/ml/units.** Branded types from `domain`; floats in ledgers never (`00 §6`).
4. **Offline-first.** No in-branch feature may require WAN; confirmed = locally persisted before UI ack (`00 §5.1–5.2`). A sale is never blocked — not by inventory math, sync, or approval timeouts (`01-F17`, `05-F8`).
5. **Two-plane law.** Operational screens: `sync-client` reads/writes only. Cloud screens: tRPC + TanStack Query only. Never mixed silently (`18 §6`).
6. **UI = closed vocabulary.** `packages/ui` semantic components only; no raw primitives, no Tailwind arbitrary values in app code (`21 §2`).
7. **English-only UI; user content is Unicode** and renders/prints faithfully (`00 §5.6`).
8. **Server-side authorization always** via the `domain` permission matrix; client role claims are never trusted (`18 §5`).
9. **Spec change before behavior change.** Behavior-carrying code changes cite their FR ID (e.g. `02-F9`) in tests/commits; no matching FR = write the spec PR first (`20 §4.1`).
10. **Protected paths need senior review:** `domain`, `sync-client`, `sync-protocol`, `escpos`, tax, auth (`20 §4.4`).

## Routing — read the owning spec before touching its area

All docs in `specs/` (`NN-name.md`). `restaurant-os.md` = product vision + seed appendices (read for "why"/scope questions).

| Area | Owning spec | Usually also |
|---|---|---|
| Kernel: events, sync, money contract, auth, catalog | `01` | `19` (why custom), `20 §2.4` |
| POS / counter app | `02` | `01 §4`, `21` |
| Printing, pass screen, KDS, timing/ETA | `03` | `21`, `27` |
| Printed-document model (`DocumentSpec`/`DocumentProfile`, KOT + receipt layout) | `03` owns the renderer; `14` owns only the editing surface | `27`, `16` |
| Waiter app · Manager console | `04` · `05` | `21` |
| Storefront (QR/pickup/delivery, confirm policy) | `06` | `02-F9`, `07` |
| WhatsApp (doors, templates, voice, language policy) | `07` | `06`, `13` (analyst) |
| Foodpanda / aggregators | `08` | `02-F30` |
| Riders, dispatch, COD | `09` | `02`, `05` |
| Inventory, purchasing, counts, prep, forecasting | `10` | `01` money |
| Staff: attendance, advances, memory | `11` | — |
| Owner app · Intelligence/analyst/alerts | `12` · `13` | each other |
| Back office (layer-2 config) · Platform admin (layer-1) | `14` · `15` | `00 §7` |
| Tax / fiscalization | `16` | `01` money |
| Marketing & loyalty | `17` | `07` |
| Stack, packages, monorepo layout, code rules | `18` | — |
| Testing, environments, Auditor, release gates | `20` | — |
| Any UI/UX work (budgets, role laws, components) | `21` | `27` |
| Visual language: colour, type, numerals, icons, tokens | `27` | `21` |
| Backup/DR, retention, erasure, export | `22` | — |
| Fold performance, incremental maintenance, retroactive reordering | `25` | `01`, `19` |
| Fold merge semantics, convergence without a total order (**live design**) | `26` | `25`, `01` |
| Cross-cutting open/undecided questions | `DECISIONS.md` | — |
| This file's governance, agent context rules | `23` | — |
| Any build task: what "done" means, loop protocol, DoD, verify commands | `24` | `20` |

Cross-cutting laws (offline, performance, security, language, config layers): `00 §5–§7`. Doc conflicts: authority order in `00` header. Find an FR by grepping its ID (`grep -rn "02-F9" specs/`); unsure which doc owns a topic → search, don't guess.

## Working rules

- One module per session; load only the routed docs (a task needs ~2–4 specs, not the corpus).
- Build tasks follow the `24 §3` loop: approved plan → acceptance tests exist first (written by a different session; read-only to you) → implement → the named check passes → evidence (captured command output) in your final message. **"Done" is the check passing — never your own judgment.**
- Craft rules (`24 §3b`): **surface assumptions in the plan** (ambiguous task → state interpretations, name the simpler alternative — don't silently pick); **minimum code that closes the FR** (no speculative features, flexibility, or error handling for implausible cases); **surgical diffs** (touch only the task's files; never "improve" adjacent code — cleanup is scheduled consolidation work, not a drive-by).
- Editing a spec: follow the template in `00 §8`; new FRs continue the doc's numbering; never renumber or delete existing IDs.
- New event types / states / config keys: spec PR to the owning doc **and** `01 §4` / `00 §7` first, code second.
- Anything here that seems to conflict with a spec: the spec wins — and flag the drift so this file gets fixed.
